var __defProp = Object.defineProperty;
var __typeError = (msg) => {
  throw TypeError(msg);
};
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __publicField = (obj, key, value) => __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);
var __accessCheck = (obj, member, msg) => member.has(obj) || __typeError("Cannot " + msg);
var __privateGet = (obj, member, getter) => (__accessCheck(obj, member, "read from private field"), getter ? getter.call(obj) : member.get(obj));
var __privateAdd = (obj, member, value) => member.has(obj) ? __typeError("Cannot add the same private member more than once") : member instanceof WeakSet ? member.add(obj) : member.set(obj, value);
var __privateSet = (obj, member, value, setter) => (__accessCheck(obj, member, "write to private field"), setter ? setter.call(obj, value) : member.set(obj, value), value);
var __privateMethod = (obj, member, method) => (__accessCheck(obj, member, "access private method"), method);

// node_modules/playhtml/dist/leafEditor.es.js
function o(r) {
  return r === null ? true : typeof r == "number" ? Number.isFinite(r) : typeof r == "string" || typeof r == "boolean";
}
function l(r) {
  return JSON.stringify(r);
}
function c(r) {
  const e = r.trim();
  if (!e)
    return {
      ok: false,
      error: "Enter a JSON string, number, boolean, or null."
    };
  let t;
  try {
    t = JSON.parse(e);
  } catch {
    return {
      ok: false,
      error: "Enter a valid JSON string, number, boolean, or null."
    };
  }
  return o(t) ? { ok: true, value: t } : {
    ok: false,
    error: "Only primitive values can be edited inline."
  };
}
function y(r, e, t) {
  if (e.length === 0)
    return {
      ok: false,
      error: "Choose a value inside the state tree."
    };
  const n = f(r, e);
  return n.exists ? o(n.value) ? {
    ok: true,
    data: a(r, e, t)
  } : {
    ok: false,
    error: "Only primitive values can be edited inline."
  } : { ok: false, error: "State path does not exist." };
}
function f(r, e) {
  let t = r;
  for (const n of e) {
    if (!s(t, n))
      return { exists: false };
    t = t[n];
  }
  return { exists: true, value: t };
}
function s(r, e) {
  return Array.isArray(r) ? typeof e == "number" && Number.isInteger(e) && e >= 0 && e < r.length : r === null || typeof r != "object" ? false : Object.prototype.hasOwnProperty.call(r, String(e));
}
function a(r, e, t) {
  const [n, ...u] = e;
  if (n === void 0) return t;
  const i = Array.isArray(r) ? [...r] : { ...r };
  return i[n] = a(
    r[n],
    u,
    t
  ), i;
}

// node_modules/playhtml/dist/index-DlJfxvdB.js
var go = 1;
var yo = 2;
var nr = 4;
var sr = 8;
var un = 32;
var De = 64;
var ie = 128;
var Ms = 31;
var Tr = 63;
var ot = 127;
var Yu = 2147483647;
var pe = Math.floor;
var os = Math.abs;
var ri = (t, e) => t < e ? t : e;
var vt = (t, e) => t > e ? t : e;
var Gu = Math.pow;
var qc = (t) => t !== 0 ? t < 0 : 1 / t < 0;
var ps = Number.MAX_SAFE_INTEGER;
var mo = Number.MIN_SAFE_INTEGER;
var qu = Number.isInteger || ((t) => typeof t == "number" && isFinite(t) && pe(t) === t);
var Ve = () => /* @__PURE__ */ new Set();
var rr = (t) => t[t.length - 1];
var Ju = (t, e) => {
  for (let n = 0; n < e.length; n++)
    t.push(e[n]);
};
var Le = Array.from;
var ii = (t, e) => {
  for (let n = 0; n < t.length; n++)
    if (!e(t[n], n, t))
      return false;
  return true;
};
var Jc = (t, e) => {
  for (let n = 0; n < t.length; n++)
    if (e(t[n], n, t))
      return true;
  return false;
};
var Xu = (t, e) => {
  const n = new Array(t);
  for (let s2 = 0; s2 < t; s2++)
    n[s2] = e(s2, n);
  return n;
};
var $s = Array.isArray;
var Xc = String.fromCharCode;
var Zu = (t) => t.toLowerCase();
var Qu = /^\s*/g;
var eh = (t) => t.replace(Qu, "");
var th = /([A-Z])/g;
var wo = (t, e) => eh(t.replace(th, (n) => `${e}${Zu(n)}`));
var nh = (t) => {
  const e = unescape(encodeURIComponent(t)), n = e.length, s2 = new Uint8Array(n);
  for (let r = 0; r < n; r++)
    s2[r] = /** @type {number} */
    e.codePointAt(r);
  return s2;
};
var hn = (
  /** @type {TextEncoder} */
  typeof TextEncoder < "u" ? new TextEncoder() : null
);
var sh = (t) => hn.encode(t);
var rh = hn ? sh : nh;
var tn = typeof TextDecoder > "u" ? null : new TextDecoder("utf-8", { fatal: true, ignoreBOM: true });
tn && tn.decode(new Uint8Array()).length === 1 && (tn = null);
var ih = (t, e) => Xu(e, () => t).join("");
var Ce = (t) => new Error(t);
var de = () => {
  throw Ce("Method unimplemented");
};
var ce = () => {
  throw Ce("Unexpected case");
};
var Mn = class {
  constructor() {
    this.cpos = 0, this.cbuf = new Uint8Array(100), this.bufs = [];
  }
};
var Y = () => new Mn();
var oi = (t) => {
  let e = t.cpos;
  for (let n = 0; n < t.bufs.length; n++)
    e += t.bufs[n].length;
  return e;
};
var T = (t) => {
  const e = new Uint8Array(oi(t));
  let n = 0;
  for (let s2 = 0; s2 < t.bufs.length; s2++) {
    const r = t.bufs[s2];
    e.set(r, n), n += r.length;
  }
  return e.set(new Uint8Array(t.cbuf.buffer, 0, t.cpos), n), e;
};
var oh = (t, e) => {
  const n = t.cbuf.length;
  n - t.cpos < e && (t.bufs.push(new Uint8Array(t.cbuf.buffer, 0, t.cpos)), t.cbuf = new Uint8Array(vt(n, e) * 2), t.cpos = 0);
};
var U = (t, e) => {
  const n = t.cbuf.length;
  t.cpos === n && (t.bufs.push(t.cbuf), t.cbuf = new Uint8Array(n * 2), t.cpos = 0), t.cbuf[t.cpos++] = e;
};
var Lr = U;
var b = (t, e) => {
  for (; e > ot; )
    U(t, ie | ot & e), e = pe(e / 128);
  U(t, ot & e);
};
var ci = (t, e) => {
  const n = qc(e);
  for (n && (e = -e), U(t, (e > Tr ? ie : 0) | (n ? De : 0) | Tr & e), e = pe(e / 64); e > 0; )
    U(t, (e > ot ? ie : 0) | ot & e), e = pe(e / 128);
};
var Ir = new Uint8Array(3e4);
var ch = Ir.length / 3;
var ah = (t, e) => {
  if (e.length < ch) {
    const n = hn.encodeInto(e, Ir).written || 0;
    b(t, n);
    for (let s2 = 0; s2 < n; s2++)
      U(t, Ir[s2]);
  } else
    I(t, rh(e));
};
var lh = (t, e) => {
  const n = unescape(encodeURIComponent(e)), s2 = n.length;
  b(t, s2);
  for (let r = 0; r < s2; r++)
    U(
      t,
      /** @type {number} */
      n.codePointAt(r)
    );
};
var ct = hn && /** @type {any} */
hn.encodeInto ? ah : lh;
var Os = (t, e) => {
  const n = t.cbuf.length, s2 = t.cpos, r = ri(n - s2, e.length), i = e.length - r;
  t.cbuf.set(e.subarray(0, r), s2), t.cpos += r, i > 0 && (t.bufs.push(t.cbuf), t.cbuf = new Uint8Array(vt(n * 2, i)), t.cbuf.set(e.subarray(r)), t.cpos = i);
};
var I = (t, e) => {
  b(t, e.byteLength), Os(t, e);
};
var ai = (t, e) => {
  oh(t, e);
  const n = new DataView(t.cbuf.buffer, t.cpos, e);
  return t.cpos += e, n;
};
var uh = (t, e) => ai(t, 4).setFloat32(0, e, false);
var hh = (t, e) => ai(t, 8).setFloat64(0, e, false);
var dh = (t, e) => (
  /** @type {any} */
  ai(t, 8).setBigInt64(0, e, false)
);
var bo = new DataView(new ArrayBuffer(4));
var fh = (t) => (bo.setFloat32(0, t), bo.getFloat32(0) === t);
var dn = (t, e) => {
  switch (typeof e) {
    case "string":
      U(t, 119), ct(t, e);
      break;
    case "number":
      qu(e) && os(e) <= Yu ? (U(t, 125), ci(t, e)) : fh(e) ? (U(t, 124), uh(t, e)) : (U(t, 123), hh(t, e));
      break;
    case "bigint":
      U(t, 122), dh(t, e);
      break;
    case "object":
      if (e === null)
        U(t, 126);
      else if ($s(e)) {
        U(t, 117), b(t, e.length);
        for (let n = 0; n < e.length; n++)
          dn(t, e[n]);
      } else if (e instanceof Uint8Array)
        U(t, 116), I(t, e);
      else {
        U(t, 118);
        const n = Object.keys(e);
        b(t, n.length);
        for (let s2 = 0; s2 < n.length; s2++) {
          const r = n[s2];
          ct(t, r), dn(t, e[r]);
        }
      }
      break;
    case "boolean":
      U(t, e ? 120 : 121);
      break;
    default:
      U(t, 127);
  }
};
var vo = class extends Mn {
  /**
   * @param {function(Encoder, T):void} writer
   */
  constructor(e) {
    super(), this.w = e, this.s = null, this.count = 0;
  }
  /**
   * @param {T} v
   */
  write(e) {
    this.s === e ? this.count++ : (this.count > 0 && b(this, this.count - 1), this.count = 1, this.w(this, e), this.s = e);
  }
};
var Co = (t) => {
  t.count > 0 && (ci(t.encoder, t.count === 1 ? t.s : -t.s), t.count > 1 && b(t.encoder, t.count - 2));
};
var cs = class {
  constructor() {
    this.encoder = new Mn(), this.s = 0, this.count = 0;
  }
  /**
   * @param {number} v
   */
  write(e) {
    this.s === e ? this.count++ : (Co(this), this.count = 1, this.s = e);
  }
  /**
   * Flush the encoded state and transform this to a Uint8Array.
   *
   * Note that this should only be called once.
   */
  toUint8Array() {
    return Co(this), T(this.encoder);
  }
};
var So = (t) => {
  if (t.count > 0) {
    const e = t.diff * 2 + (t.count === 1 ? 0 : 1);
    ci(t.encoder, e), t.count > 1 && b(t.encoder, t.count - 2);
  }
};
var ir = class {
  constructor() {
    this.encoder = new Mn(), this.s = 0, this.count = 0, this.diff = 0;
  }
  /**
   * @param {number} v
   */
  write(e) {
    this.diff === e - this.s ? (this.s = e, this.count++) : (So(this), this.count = 1, this.diff = e - this.s, this.s = e);
  }
  /**
   * Flush the encoded state and transform this to a Uint8Array.
   *
   * Note that this should only be called once.
   */
  toUint8Array() {
    return So(this), T(this.encoder);
  }
};
var ph = class {
  constructor() {
    this.sarr = [], this.s = "", this.lensE = new cs();
  }
  /**
   * @param {string} string
   */
  write(e) {
    this.s += e, this.s.length > 19 && (this.sarr.push(this.s), this.s = ""), this.lensE.write(e.length);
  }
  toUint8Array() {
    const e = new Mn();
    return this.sarr.push(this.s), this.s = "", ct(e, this.sarr.join("")), Os(e, this.lensE.toUint8Array()), T(e);
  }
};
var Zc = Ce("Unexpected end of array");
var Qc = Ce("Integer out of Range");
var Ps = class {
  /**
   * @param {Uint8Array<Buf>} uint8Array Binary data to decode
   */
  constructor(e) {
    this.arr = e, this.pos = 0;
  }
};
var qe = (t) => new Ps(t);
var gh = (t) => t.pos !== t.arr.length;
var yh = (t, e) => {
  const n = new Uint8Array(t.arr.buffer, t.pos + t.arr.byteOffset, e);
  return t.pos += e, n;
};
var W = (t) => yh(t, S(t));
var Mt = (t) => t.arr[t.pos++];
var S = (t) => {
  let e = 0, n = 1;
  const s2 = t.arr.length;
  for (; t.pos < s2; ) {
    const r = t.arr[t.pos++];
    if (e = e + (r & ot) * n, n *= 128, r < ie)
      return e;
    if (e > ps)
      throw Qc;
  }
  throw Zc;
};
var li = (t) => {
  let e = t.arr[t.pos++], n = e & Tr, s2 = 64;
  const r = (e & De) > 0 ? -1 : 1;
  if ((e & ie) === 0)
    return r * n;
  const i = t.arr.length;
  for (; t.pos < i; ) {
    if (e = t.arr[t.pos++], n = n + (e & ot) * s2, s2 *= 128, e < ie)
      return r * n;
    if (n > ps)
      throw Qc;
  }
  throw Zc;
};
var mh = (t) => {
  let e = S(t);
  if (e === 0)
    return "";
  {
    let n = String.fromCodePoint(Mt(t));
    if (--e < 100)
      for (; e--; )
        n += String.fromCodePoint(Mt(t));
    else
      for (; e > 0; ) {
        const s2 = e < 1e4 ? e : 1e4, r = t.arr.subarray(t.pos, t.pos + s2);
        t.pos += s2, n += String.fromCodePoint.apply(
          null,
          /** @type {any} */
          r
        ), e -= s2;
      }
    return decodeURIComponent(escape(n));
  }
};
var wh = (t) => (
  /** @type any */
  tn.decode(W(t))
);
var He = tn ? wh : mh;
var ui = (t, e) => {
  const n = new DataView(t.arr.buffer, t.arr.byteOffset + t.pos, e);
  return t.pos += e, n;
};
var bh = (t) => ui(t, 4).getFloat32(0, false);
var vh = (t) => ui(t, 8).getFloat64(0, false);
var Ch = (t) => (
  /** @type {any} */
  ui(t, 8).getBigInt64(0, false)
);
var Sh = [
  (t) => {
  },
  // CASE 127: undefined
  (t) => null,
  // CASE 126: null
  li,
  // CASE 125: integer
  bh,
  // CASE 124: float32
  vh,
  // CASE 123: float64
  Ch,
  // CASE 122: bigint
  (t) => false,
  // CASE 121: boolean (false)
  (t) => true,
  // CASE 120: boolean (true)
  He,
  // CASE 119: string
  (t) => {
    const e = S(t), n = {};
    for (let s2 = 0; s2 < e; s2++) {
      const r = He(t);
      n[r] = fn(t);
    }
    return n;
  },
  (t) => {
    const e = S(t), n = [];
    for (let s2 = 0; s2 < e; s2++)
      n.push(fn(t));
    return n;
  },
  W
  // CASE 116: Uint8Array
];
var fn = (t) => Sh[127 - Mt(t)](t);
var Eo = class extends Ps {
  /**
   * @param {Uint8Array} uint8Array
   * @param {function(Decoder):T} reader
   */
  constructor(e, n) {
    super(e), this.reader = n, this.s = null, this.count = 0;
  }
  read() {
    return this.count === 0 && (this.s = this.reader(this), gh(this) ? this.count = S(this) + 1 : this.count = -1), this.count--, /** @type {T} */
    this.s;
  }
};
var as = class extends Ps {
  /**
   * @param {Uint8Array} uint8Array
   */
  constructor(e) {
    super(e), this.s = 0, this.count = 0;
  }
  read() {
    if (this.count === 0) {
      this.s = li(this);
      const e = qc(this.s);
      this.count = 1, e && (this.s = -this.s, this.count = S(this) + 2);
    }
    return this.count--, /** @type {number} */
    this.s;
  }
};
var or = class extends Ps {
  /**
   * @param {Uint8Array} uint8Array
   */
  constructor(e) {
    super(e), this.s = 0, this.count = 0, this.diff = 0;
  }
  /**
   * @return {number}
   */
  read() {
    if (this.count === 0) {
      const e = li(this), n = e & 1;
      this.diff = pe(e / 2), this.count = 1, n && (this.count = S(this) + 2);
    }
    return this.s += this.diff, this.count--, this.s;
  }
};
var Eh = class {
  /**
   * @param {Uint8Array} uint8Array
   */
  constructor(e) {
    this.decoder = new as(e), this.str = He(this.decoder), this.spos = 0;
  }
  /**
   * @return {string}
   */
  read() {
    const e = this.spos + this.decoder.read(), n = this.str.slice(this.spos, e);
    return this.spos = e, n;
  }
};
var $t = Date.now;
var te = () => /* @__PURE__ */ new Map();
var Mr = (t) => {
  const e = te();
  return t.forEach((n, s2) => {
    e.set(s2, n);
  }), e;
};
var ke = (t, e, n) => {
  let s2 = t.get(e);
  return s2 === void 0 && t.set(e, s2 = n()), s2;
};
var _h = (t, e) => {
  const n = [];
  for (const [s2, r] of t)
    n.push(e(r, s2));
  return n;
};
var Ah = (t, e) => {
  for (const [n, s2] of t)
    if (e(s2, n))
      return true;
  return false;
};
var kh = class {
  constructor() {
    this._observers = te();
  }
  /**
   * @template {keyof EVENTS & string} NAME
   * @param {NAME} name
   * @param {EVENTS[NAME]} f
   */
  on(e, n) {
    return ke(
      this._observers,
      /** @type {string} */
      e,
      Ve
    ).add(n), n;
  }
  /**
   * @template {keyof EVENTS & string} NAME
   * @param {NAME} name
   * @param {EVENTS[NAME]} f
   */
  once(e, n) {
    const s2 = (...r) => {
      this.off(
        e,
        /** @type {any} */
        s2
      ), n(...r);
    };
    this.on(
      e,
      /** @type {any} */
      s2
    );
  }
  /**
   * @template {keyof EVENTS & string} NAME
   * @param {NAME} name
   * @param {EVENTS[NAME]} f
   */
  off(e, n) {
    const s2 = this._observers.get(e);
    s2 !== void 0 && (s2.delete(n), s2.size === 0 && this._observers.delete(e));
  }
  /**
   * Emit a named event. All registered event listeners that listen to the
   * specified name will receive the event.
   *
   * @todo This should catch exceptions
   *
   * @template {keyof EVENTS & string} NAME
   * @param {NAME} name The event name.
   * @param {Parameters<EVENTS[NAME]>} args The arguments that are applied to the event listener.
   */
  emit(e, n) {
    return Le((this._observers.get(e) || te()).values()).forEach((s2) => s2(...n));
  }
  destroy() {
    this._observers = te();
  }
};
var ea = class {
  constructor() {
    this._observers = te();
  }
  /**
   * @param {N} name
   * @param {function} f
   */
  on(e, n) {
    ke(this._observers, e, Ve).add(n);
  }
  /**
   * @param {N} name
   * @param {function} f
   */
  once(e, n) {
    const s2 = (...r) => {
      this.off(e, s2), n(...r);
    };
    this.on(e, s2);
  }
  /**
   * @param {N} name
   * @param {function} f
   */
  off(e, n) {
    const s2 = this._observers.get(e);
    s2 !== void 0 && (s2.delete(n), s2.size === 0 && this._observers.delete(e));
  }
  /**
   * Emit a named event. All registered event listeners that listen to the
   * specified name will receive the event.
   *
   * @todo This should catch exceptions
   *
   * @param {N} name The event name.
   * @param {Array<any>} args The arguments that are applied to the event listener.
   */
  emit(e, n) {
    return Le((this._observers.get(e) || te()).values()).forEach((s2) => s2(...n));
  }
  destroy() {
    this._observers = te();
  }
};
var pn = Symbol("Equality");
var ta = (t, e) => {
  var _a4;
  return t === e || !!((_a4 = t == null ? void 0 : t[pn]) == null ? void 0 : _a4.call(t, e)) || false;
};
var xh = (t) => typeof t == "object";
var Dh = Object.assign;
var Th = Object.keys;
var Lh = (t, e) => {
  for (const n in t)
    e(t[n], n);
};
var Ih = (t, e) => {
  const n = [];
  for (const s2 in t)
    n.push(e(t[s2], s2));
  return n;
};
var gs = (t) => Th(t).length;
var Mh = (t) => {
  for (const e in t)
    return false;
  return true;
};
var $n = (t, e) => {
  for (const n in t)
    if (!e(t[n], n))
      return false;
  return true;
};
var hi = (t, e) => Object.prototype.hasOwnProperty.call(t, e);
var $h = (t, e) => t === e || gs(t) === gs(e) && $n(t, (n, s2) => (n !== void 0 || hi(e, s2)) && ta(e[s2], n));
var di = (t, e, n = 0) => {
  try {
    for (; n < t.length; n++)
      t[n](...e);
  } finally {
    n < t.length && di(t, e, n + 1);
  }
};
var Oh = (t) => t;
var xt = (t, e) => {
  if (t === e)
    return true;
  if (t == null || e == null || t.constructor !== e.constructor && (t.constructor || Object) !== (e.constructor || Object))
    return false;
  if (t[pn] != null)
    return t[pn](e);
  switch (t.constructor) {
    case ArrayBuffer:
      t = new Uint8Array(t), e = new Uint8Array(e);
    case Uint8Array: {
      if (t.byteLength !== e.byteLength)
        return false;
      for (let n = 0; n < t.length; n++)
        if (t[n] !== e[n])
          return false;
      break;
    }
    case Set: {
      if (t.size !== e.size)
        return false;
      for (const n of t)
        if (!e.has(n))
          return false;
      break;
    }
    case Map: {
      if (t.size !== e.size)
        return false;
      for (const n of t.keys())
        if (!e.has(n) || !xt(t.get(n), e.get(n)))
          return false;
      break;
    }
    case void 0:
    case Object:
      if (gs(t) !== gs(e))
        return false;
      for (const n in t)
        if (!hi(t, n) || !xt(t[n], e[n]))
          return false;
      break;
    case Array:
      if (t.length !== e.length)
        return false;
      for (let n = 0; n < t.length; n++)
        if (!xt(t[n], e[n]))
          return false;
      break;
    default:
      return false;
  }
  return true;
};
var Ph = (t, e) => e.includes(t);
var Rh = crypto.getRandomValues.bind(crypto);
var na = () => Rh(new Uint32Array(1))[0];
var Nh = "10000000-1000-4000-8000" + -1e11;
var Uh = () => Nh.replace(
  /[018]/g,
  /** @param {number} c */
  (t) => (t ^ na() & 15 >> t / 4).toString(16)
);
var _o = (t) => (
  /** @type {Promise<T>} */
  new Promise(t)
);
Promise.all.bind(Promise);
var Ao = (t) => t === void 0 ? null : t;
var Fh = class {
  constructor() {
    this.map = /* @__PURE__ */ new Map();
  }
  /**
   * @param {string} key
   * @param {any} newValue
   */
  setItem(e, n) {
    this.map.set(e, n);
  }
  /**
   * @param {string} key
   */
  getItem(e) {
    return this.map.get(e);
  }
};
var sa = new Fh();
var fi = true;
try {
  typeof localStorage < "u" && localStorage && (sa = localStorage, fi = false);
} catch {
}
var ra = sa;
var jh = (t) => fi || addEventListener(
  "storage",
  /** @type {any} */
  t
);
var Hh = (t) => fi || removeEventListener(
  "storage",
  /** @type {any} */
  t
);
var Ot = typeof process < "u" && process.release && /node|io\.js/.test(process.release.name) && Object.prototype.toString.call(typeof process < "u" ? process : 0) === "[object process]";
var ia = typeof window < "u" && typeof document < "u" && !Ot;
var we;
var zh = () => {
  if (we === void 0)
    if (Ot) {
      we = te();
      const t = process.argv;
      let e = null;
      for (let n = 0; n < t.length; n++) {
        const s2 = t[n];
        s2[0] === "-" ? (e !== null && we.set(e, ""), e = s2) : e !== null && (we.set(e, s2), e = null);
      }
      e !== null && we.set(e, "");
    } else typeof location == "object" ? (we = te(), (location.search || "?").slice(1).split("&").forEach((t) => {
      if (t.length !== 0) {
        const [e, n] = t.split("=");
        we.set(`--${wo(e, "-")}`, n), we.set(`-${wo(e, "-")}`, n);
      }
    })) : we = te();
  return we;
};
var $r = (t) => zh().has(t);
var Or = (t) => Ao(Ot ? process.env[t.toUpperCase().replaceAll("-", "_")] : ra.getItem(t));
var oa = (t) => $r("--" + t) || Or(t) !== null;
var Bh = oa("production");
var Vh = Ot && Ph(process.env.FORCE_COLOR, ["true", "1", "2"]);
var Kh = Vh || !$r("--no-colors") && // @todo deprecate --no-colors
!oa("no-color") && (!Ot || process.stdout.isTTY) && (!Ot || $r("--color") || Or("COLORTERM") !== null || (Or("TERM") || "").includes("color"));
var ca = (t) => new Uint8Array(t);
var Wh = (t, e, n) => new Uint8Array(t, e, n);
var Yh = (t) => new Uint8Array(t);
var Gh = (t) => {
  let e = "";
  for (let n = 0; n < t.byteLength; n++)
    e += Xc(t[n]);
  return btoa(e);
};
var qh = (t) => Buffer.from(t.buffer, t.byteOffset, t.byteLength).toString("base64");
var Jh = (t) => {
  const e = atob(t), n = ca(e.length);
  for (let s2 = 0; s2 < e.length; s2++)
    n[s2] = e.charCodeAt(s2);
  return n;
};
var Xh = (t) => {
  const e = Buffer.from(t, "base64");
  return Wh(e.buffer, e.byteOffset, e.byteLength);
};
var Zh = ia ? Gh : qh;
var Qh = ia ? Jh : Xh;
var ed = (t) => {
  const e = ca(t.byteLength);
  return e.set(t), e;
};
var td = class {
  /**
   * @param {L} left
   * @param {R} right
   */
  constructor(e, n) {
    this.left = e, this.right = n;
  }
};
var xe = (t, e) => new td(t, e);
var ko = (t) => t.next() >= 0.5;
var cr = (t, e, n) => pe(t.next() * (n + 1 - e) + e);
var aa = (t, e, n) => pe(t.next() * (n + 1 - e) + e);
var pi = (t, e, n) => aa(t, e, n);
var nd = (t) => Xc(pi(t, 97, 122));
var sd = (t, e = 0, n = 20) => {
  const s2 = pi(t, e, n);
  let r = "";
  for (let i = 0; i < s2; i++)
    r += nd(t);
  return r;
};
var ar = (t, e) => e[pi(t, 0, e.length - 1)];
var rd = Symbol("0schema");
var id = class {
  constructor() {
    this._rerrs = [];
  }
  /**
   * @param {string?} path
   * @param {string} expected
   * @param {string} has
   * @param {string?} message
   */
  extend(e, n, s2, r = null) {
    this._rerrs.push({ path: e, expected: n, has: s2, message: r });
  }
  toString() {
    const e = [];
    for (let n = this._rerrs.length - 1; n > 0; n--) {
      const s2 = this._rerrs[n];
      e.push(ih(" ", (this._rerrs.length - n) * 2) + `${s2.path != null ? `[${s2.path}] ` : ""}${s2.has} doesn't match ${s2.expected}. ${s2.message}`);
    }
    return e.join(`
`);
  }
};
var Pr = (t, e) => t === e ? true : t == null || e == null || t.constructor !== e.constructor ? false : t[pn] ? ta(t, e) : $s(t) ? ii(
  t,
  (n) => Jc(e, (s2) => Pr(n, s2))
) : xh(t) ? $n(
  t,
  (n, s2) => Pr(n, e[s2])
) : false;
var ee = class {
  /**
   * @param {Schema<any>} other
   */
  extends(e) {
    let [n, s2] = [
      /** @type {any} */
      this.shape,
      /** @type {any} */
      e.shape
    ];
    return (
      /** @type {typeof Schema<any>} */
      this.constructor._dilutes && ([s2, n] = [n, s2]), Pr(n, s2)
    );
  }
  /**
   * Overwrite this when necessary. By default, we only check the `shape` property which every shape
   * should have.
   * @param {Schema<any>} other
   */
  equals(e) {
    return this.constructor === e.constructor && xt(this.shape, e.shape);
  }
  [rd]() {
    return true;
  }
  /**
   * @param {object} other
   */
  [pn](e) {
    return this.equals(
      /** @type {any} */
      e
    );
  }
  /**
   * Use `schema.validate(obj)` with a typed parameter that is already of typed to be an instance of
   * Schema. Validate will check the structure of the parameter and return true iff the instance
   * really is an instance of Schema.
   *
   * @param {T} o
   * @return {boolean}
   */
  validate(e) {
    return this.check(e);
  }
  /* c8 ignore start */
  /**
   * Similar to validate, but this method accepts untyped parameters.
   *
   * @param {any} _o
   * @param {ValidationError} [_err]
   * @return {_o is T}
   */
  check(e, n) {
    de();
  }
  /* c8 ignore stop */
  /**
   * @type {Schema<T?>}
   */
  get nullable() {
    return Ht(this, js);
  }
  /**
   * @type {$Optional<Schema<T>>}
   */
  get optional() {
    return new ha(
      /** @type {Schema<T>} */
      this
    );
  }
  /**
   * Cast a variable to a specific type. Returns the casted value, or throws an exception otherwise.
   * Use this if you know that the type is of a specific type and you just want to convince the type
   * system.
   *
   * **Do not rely on these error messages!**
   * Performs an assertion check only if not in a production environment.
   *
   * @template OO
   * @param {OO} o
   * @return {Extract<OO, T> extends never ? T : (OO extends Array<never> ? T : Extract<OO,T>)}
   */
  cast(e) {
    return xo(e, this), /** @type {any} */
    e;
  }
  /**
   * EXPECTO PATRONUM!! 🪄
   * This function protects against type errors. Though it may not work in the real world.
   *
   * "After all this time?"
   * "Always." - Snape, talking about type safety
   *
   * Ensures that a variable is a a specific type. Returns the value, or throws an exception if the assertion check failed.
   * Use this if you know that the type is of a specific type and you just want to convince the type
   * system.
   *
   * Can be useful when defining lambdas: `s.lambda(s.$number, s.$void).expect((n) => n + 1)`
   *
   * **Do not rely on these error messages!**
   * Performs an assertion check if not in a production environment.
   *
   * @param {T} o
   * @return {o extends T ? T : never}
   */
  expect(e) {
    return xo(e, this), e;
  }
};
// this.shape must not be defined on Schema. Otherwise typecheck on metatypes (e.g. $$object) won't work as expected anymore
/**
 * If true, the more things are added to the shape the more objects this schema will accept (e.g.
 * union). By default, the more objects are added, the the fewer objects this schema will accept.
 * @protected
 */
__publicField(ee, "_dilutes", false);
var gi = class extends ee {
  /**
   * @param {C} c
   * @param {((o:Instance<C>)=>boolean)|null} check
   */
  constructor(e, n) {
    super(), this.shape = e, this._c = n;
  }
  /**
   * @param {any} o
   * @param {ValidationError} [err]
   * @return {o is C extends ((...args:any[]) => infer T) ? T : (C extends (new (...args:any[]) => any) ? InstanceType<C> : never)} o
   */
  check(e, n = void 0) {
    const s2 = (e == null ? void 0 : e.constructor) === this.shape && (this._c == null || this._c(e));
    return !s2 && (n == null ? void 0 : n.extend(null, this.shape.name, e == null ? void 0 : e.constructor.name, (e == null ? void 0 : e.constructor) !== this.shape ? "Constructor match failed" : "Check failed")), s2;
  }
};
var $ = (t, e = null) => new gi(t, e);
$(gi);
var yi = class extends ee {
  /**
   * @param {(o:any) => boolean} check
   */
  constructor(e) {
    super(), this.shape = e;
  }
  /**
   * @param {any} o
   * @param {ValidationError} err
   * @return {o is any}
   */
  check(e, n) {
    const s2 = this.shape(e);
    return !s2 && (n == null ? void 0 : n.extend(null, "custom prop", e == null ? void 0 : e.constructor.name, "failed to check custom prop")), s2;
  }
};
var B = (t) => new yi(t);
$(yi);
var Rs = class extends ee {
  /**
   * @param {Array<T>} literals
   */
  constructor(e) {
    super(), this.shape = e;
  }
  /**
   *
   * @param {any} o
   * @param {ValidationError} [err]
   * @return {o is T}
   */
  check(e, n) {
    const s2 = this.shape.some((r) => r === e);
    return !s2 && (n == null ? void 0 : n.extend(null, this.shape.join(" | "), e.toString())), s2;
  }
};
var Ns = (...t) => new Rs(t);
var la = $(Rs);
var od = (
  /** @type {any} */
  RegExp.escape || /** @type {(str:string) => string} */
  ((t) => t.replace(/[().|&,$^[\]]/g, (e) => "\\" + e))
);
var ua = (t) => {
  if (Pt.check(t))
    return [od(t)];
  if (la.check(t))
    return (
      /** @type {Array<string|number>} */
      t.shape.map((e) => e + "")
    );
  if (va.check(t))
    return ["[+-]?\\d+.?\\d*"];
  if (Ca.check(t))
    return [".*"];
  if (ys.check(t))
    return t.shape.map(ua).flat(1);
  ce();
};
var cd = class extends ee {
  /**
   * @param {T} shape
   */
  constructor(e) {
    super(), this.shape = e, this._r = new RegExp("^" + e.map(ua).map((n) => `(${n.join("|")})`).join("") + "$");
  }
  /**
   * @param {any} o
   * @param {ValidationError} [err]
   * @return {o is CastStringTemplateArgsToTemplate<T>}
   */
  check(e, n) {
    const s2 = this._r.exec(e) != null;
    return !s2 && (n == null ? void 0 : n.extend(null, this._r.toString(), e.toString(), "String doesn't match string template.")), s2;
  }
};
$(cd);
var ad = Symbol("optional");
var ha = class extends ee {
  /**
   * @param {S} shape
   */
  constructor(e) {
    super(), this.shape = e;
  }
  /**
   * @param {any} o
   * @param {ValidationError} [err]
   * @return {o is (Unwrap<S>|undefined)}
   */
  check(e, n) {
    const s2 = e === void 0 || this.shape.check(e);
    return !s2 && (n == null ? void 0 : n.extend(null, "undefined (optional)", "()")), s2;
  }
  get [ad]() {
    return true;
  }
};
var ld = $(ha);
var ud = class extends ee {
  /**
   * @param {any} _o
   * @param {ValidationError} [err]
   * @return {_o is never}
   */
  check(e, n) {
    return n == null ? void 0 : n.extend(null, "never", typeof e), false;
  }
};
$(ud);
var _Us = class _Us extends ee {
  /**
   * @param {S} shape
   * @param {boolean} partial
   */
  constructor(e, n = false) {
    super(), this.shape = e, this._isPartial = n;
  }
  /**
   * @type {Schema<Partial<$ObjectToType<S>>>}
   */
  get partial() {
    return new _Us(this.shape, true);
  }
  /**
   * @param {any} o
   * @param {ValidationError} err
   * @return {o is $ObjectToType<S>}
   */
  check(e, n) {
    return e == null ? (n == null ? void 0 : n.extend(null, "object", "null"), false) : $n(this.shape, (s2, r) => {
      const i = this._isPartial && !hi(e, r) || s2.check(e[r], n);
      return !i && (n == null ? void 0 : n.extend(r.toString(), s2.toString(), typeof e[r], "Object property does not match")), i;
    });
  }
};
__publicField(_Us, "_dilutes", true);
var Us = _Us;
var hd = (t) => (
  /** @type {any} */
  new Us(t)
);
var dd = $(Us);
var fd = B((t) => t != null && (t.constructor === Object || t.constructor == null));
var da = class extends ee {
  /**
   * @param {Keys} keys
   * @param {Values} values
   */
  constructor(e, n) {
    super(), this.shape = {
      keys: e,
      values: n
    };
  }
  /**
   * @param {any} o
   * @param {ValidationError} err
   * @return {o is { [key in Unwrap<Keys>]: Unwrap<Values> }}
   */
  check(e, n) {
    return e != null && $n(e, (s2, r) => {
      const i = this.shape.keys.check(r, n);
      return !i && (n == null ? void 0 : n.extend(r + "", "Record", typeof e, i ? "Key doesn't match schema" : "Value doesn't match value")), i && this.shape.values.check(s2, n);
    });
  }
};
var fa = (t, e) => new da(t, e);
var pd = $(da);
var pa = class extends ee {
  /**
   * @param {S} shape
   */
  constructor(e) {
    super(), this.shape = e;
  }
  /**
   * @param {any} o
   * @param {ValidationError} err
   * @return {o is { [K in keyof S]: S[K] extends Schema<infer Type> ? Type : never }}
   */
  check(e, n) {
    return e != null && $n(this.shape, (s2, r) => {
      const i = (
        /** @type {Schema<any>} */
        s2.check(e[r], n)
      );
      return !i && (n == null ? void 0 : n.extend(r.toString(), "Tuple", typeof s2)), i;
    });
  }
};
var gd = (...t) => new pa(t);
$(pa);
var ga = class extends ee {
  /**
   * @param {Array<S>} v
   */
  constructor(e) {
    super(), this.shape = e.length === 1 ? e[0] : new mi(e);
  }
  /**
   * @param {any} o
   * @param {ValidationError} [err]
   * @return {o is Array<S extends Schema<infer T> ? T : never>} o
   */
  check(e, n) {
    const s2 = $s(e) && ii(e, (r) => this.shape.check(r));
    return !s2 && (n == null ? void 0 : n.extend(null, "Array", "")), s2;
  }
};
var ya = (...t) => new ga(t);
var yd = $(ga);
var md = B((t) => $s(t));
var ma = class extends ee {
  /**
   * @param {new (...args:any) => T} constructor
   * @param {((o:T) => boolean)|null} check
   */
  constructor(e, n) {
    super(), this.shape = e, this._c = n;
  }
  /**
   * @param {any} o
   * @param {ValidationError} err
   * @return {o is T}
   */
  check(e, n) {
    const s2 = e instanceof this.shape && (this._c == null || this._c(e));
    return !s2 && (n == null ? void 0 : n.extend(null, this.shape.name, e == null ? void 0 : e.constructor.name)), s2;
  }
};
var wd = (t, e = null) => new ma(t, e);
$(ma);
var bd = wd(ee);
var vd = class extends ee {
  /**
   * @param {Args} args
   */
  constructor(e) {
    super(), this.len = e.length - 1, this.args = gd(...e.slice(-1)), this.res = e[this.len];
  }
  /**
   * @param {any} f
   * @param {ValidationError} err
   * @return {f is _LArgsToLambdaDef<Args>}
   */
  check(e, n) {
    const s2 = e.constructor === Function && e.length <= this.len;
    return !s2 && (n == null ? void 0 : n.extend(null, "function", typeof e)), s2;
  }
};
var Cd = $(vd);
var Sd = B((t) => typeof t == "function");
var Ed = class extends ee {
  /**
   * @param {T} v
   */
  constructor(e) {
    super(), this.shape = e;
  }
  /**
   * @param {any} o
   * @param {ValidationError} [err]
   * @return {o is Intersect<UnwrapArray<T>>}
   */
  check(e, n) {
    const s2 = ii(this.shape, (r) => r.check(e, n));
    return !s2 && (n == null ? void 0 : n.extend(null, "Intersectinon", typeof e)), s2;
  }
};
$(Ed, (t) => t.shape.length > 0);
var mi = class extends ee {
  /**
   * @param {Array<Schema<S>>} v
   */
  constructor(e) {
    super(), this.shape = e;
  }
  /**
   * @param {any} o
   * @param {ValidationError} [err]
   * @return {o is S}
   */
  check(e, n) {
    const s2 = Jc(this.shape, (r) => r.check(e, n));
    return n == null ? void 0 : n.extend(null, "Union", typeof e), s2;
  }
};
__publicField(mi, "_dilutes", true);
var Ht = (...t) => t.findIndex((e) => ys.check(e)) >= 0 ? Ht(...t.map((e) => gn(e)).map((e) => ys.check(e) ? e.shape : [e]).flat(1)) : t.length === 1 ? t[0] : new mi(t);
var ys = (
  /** @type {Schema<$Union<any>>} */
  $(mi)
);
var wa = () => true;
var ms = B(wa);
var _d = (
  /** @type {Schema<Schema<any>>} */
  $(yi, (t) => t.shape === wa)
);
var wi = B((t) => typeof t == "bigint");
var Ad = (
  /** @type {Schema<Schema<BigInt>>} */
  B((t) => t === wi)
);
var ba = B((t) => typeof t == "symbol");
B((t) => t === ba);
var Dt = B((t) => typeof t == "number");
var va = (
  /** @type {Schema<Schema<number>>} */
  B((t) => t === Dt)
);
var Pt = B((t) => typeof t == "string");
var Ca = (
  /** @type {Schema<Schema<string>>} */
  B((t) => t === Pt)
);
var Fs = B((t) => typeof t == "boolean");
var kd = (
  /** @type {Schema<Schema<Boolean>>} */
  B((t) => t === Fs)
);
var Sa = Ns(void 0);
$(Rs, (t) => t.shape.length === 1 && t.shape[0] === void 0);
Ns(void 0);
var js = Ns(null);
var xd = (
  /** @type {Schema<Schema<null>>} */
  $(Rs, (t) => t.shape.length === 1 && t.shape[0] === null)
);
$(Uint8Array);
$(gi, (t) => t.shape === Uint8Array);
var Dd = Ht(Dt, Pt, js, Sa, wi, Fs, ba);
(() => {
  const t = (
    /** @type {$Array<$any>} */
    ya(ms)
  ), e = (
    /** @type {$Record<$string,$any>} */
    fa(Pt, ms)
  ), n = Ht(Dt, Pt, js, Fs, t, e);
  return t.shape = n, e.shape.values = n, n;
})();
var gn = (t) => {
  if (bd.check(t))
    return (
      /** @type {any} */
      t
    );
  if (fd.check(t)) {
    const e = {};
    for (const n in t)
      e[n] = gn(t[n]);
    return (
      /** @type {any} */
      hd(e)
    );
  } else {
    if (md.check(t))
      return (
        /** @type {any} */
        Ht(...t.map(gn))
      );
    if (Dd.check(t))
      return (
        /** @type {any} */
        Ns(t)
      );
    if (Sd.check(t))
      return (
        /** @type {any} */
        $(
          /** @type {any} */
          t
        )
      );
  }
  ce();
};
var xo = Bh ? () => {
} : (t, e) => {
  const n = new id();
  if (!e.check(t, n))
    throw Ce(`Expected value to be of type ${e.constructor.name}.
${n.toString()}`);
};
var Td = class {
  /**
   * @param {Schema<State>} [$state]
   */
  constructor(e) {
    this.patterns = [], this.$state = e;
  }
  /**
   * @template P
   * @template R
   * @param {P} pattern
   * @param {(o:NoInfer<Unwrap<ReadSchema<P>>>,s:State)=>R} handler
   * @return {PatternMatcher<State,Patterns|Pattern<Unwrap<ReadSchema<P>>,R>>}
   */
  if(e, n) {
    return this.patterns.push({ if: gn(e), h: n }), this;
  }
  /**
   * @template R
   * @param {(o:any,s:State)=>R} h
   */
  else(e) {
    return this.if(ms, e);
  }
  /**
   * @return {State extends undefined
   *   ? <In extends Unwrap<Patterns['if']>>(o:In,state?:undefined)=>PatternMatchResult<Patterns,In>
   *   : <In extends Unwrap<Patterns['if']>>(o:In,state:State)=>PatternMatchResult<Patterns,In>}
   */
  done() {
    return (
      /** @type {any} */
      (e, n) => {
        for (let s2 = 0; s2 < this.patterns.length; s2++) {
          const r = this.patterns[s2];
          if (r.if.check(e))
            return r.h(e, n);
        }
        throw Ce("Unhandled pattern");
      }
    );
  }
};
var Ld = (t) => new Td(
  /** @type {any} */
  t
);
var Ea = (
  /** @type {any} */
  Ld(
    /** @type {Schema<prng.PRNG>} */
    ms
  ).if(va, (t, e) => cr(e, mo, ps)).if(Ca, (t, e) => sd(e)).if(kd, (t, e) => ko(e)).if(Ad, (t, e) => BigInt(cr(e, mo, ps))).if(ys, (t, e) => St(e, ar(e, t.shape))).if(dd, (t, e) => {
    const n = {};
    for (const s2 in t.shape) {
      let r = t.shape[s2];
      if (ld.check(r)) {
        if (ko(e))
          continue;
        r = r.shape;
      }
      n[s2] = Ea(r, e);
    }
    return n;
  }).if(yd, (t, e) => {
    const n = [], s2 = aa(e, 0, 42);
    for (let r = 0; r < s2; r++)
      n.push(St(e, t.shape));
    return n;
  }).if(la, (t, e) => ar(e, t.shape)).if(xd, (t, e) => null).if(Cd, (t, e) => {
    const n = St(e, t.res);
    return () => n;
  }).if(_d, (t, e) => St(e, ar(e, [
    Dt,
    Pt,
    js,
    Sa,
    wi,
    Fs,
    ya(Dt),
    fa(Ht("a", "b", "c"), Dt)
  ]))).if(pd, (t, e) => {
    const n = {}, s2 = cr(e, 0, 3);
    for (let r = 0; r < s2; r++) {
      const i = St(e, t.shape.keys), o2 = St(e, t.shape.values);
      n[i] = o2;
    }
    return n;
  }).done()
);
var St = (t, e) => (
  /** @type {any} */
  Ea(gn(e), t)
);
var Hs = (
  /** @type {Document} */
  typeof document < "u" ? document : {}
);
B((t) => t.nodeType === Pd);
typeof DOMParser < "u" && new DOMParser();
B((t) => t.nodeType === Md);
B((t) => t.nodeType === $d);
var Id = (t) => _h(t, (e, n) => `${n}:${e};`).join("");
var Md = Hs.ELEMENT_NODE;
var $d = Hs.TEXT_NODE;
var Od = Hs.DOCUMENT_NODE;
var Pd = Hs.DOCUMENT_FRAGMENT_NODE;
B((t) => t.nodeType === Od);
var $e = Symbol;
var _a = $e();
var Aa = $e();
var Rd = $e();
var Nd = $e();
var Ud = $e();
var ka = $e();
var Fd = $e();
var xa = $e();
var jd = $e();
var Hd = (t) => {
  var _a4;
  t.length === 1 && ((_a4 = t[0]) == null ? void 0 : _a4.constructor) === Function && (t = /** @type {Array<string|Symbol|Object|number>} */
  /** @type {[function]} */
  t[0]());
  const e = [], n = [];
  let s2 = 0;
  for (; s2 < t.length; s2++) {
    const r = t[s2];
    if (r === void 0)
      break;
    if (r.constructor === String || r.constructor === Number)
      e.push(r);
    else if (r.constructor === Object)
      break;
  }
  for (s2 > 0 && n.push(e.join("")); s2 < t.length; s2++) {
    const r = t[s2];
    r instanceof Symbol || n.push(r);
  }
  return n;
};
var zd = {
  [_a]: xe("font-weight", "bold"),
  [Aa]: xe("font-weight", "normal"),
  [Rd]: xe("color", "blue"),
  [Ud]: xe("color", "green"),
  [Nd]: xe("color", "grey"),
  [ka]: xe("color", "red"),
  [Fd]: xe("color", "purple"),
  [xa]: xe("color", "orange"),
  // not well supported in chrome when debugging node with inspector - TODO: deprecate
  [jd]: xe("color", "black")
};
var Bd = (t) => {
  var _a4;
  t.length === 1 && ((_a4 = t[0]) == null ? void 0 : _a4.constructor) === Function && (t = /** @type {Array<string|Symbol|Object|number>} */
  /** @type {[function]} */
  t[0]());
  const e = [], n = [], s2 = te();
  let r = [], i = 0;
  for (; i < t.length; i++) {
    const o2 = t[i], c2 = zd[o2];
    if (c2 !== void 0)
      s2.set(c2.left, c2.right);
    else {
      if (o2 === void 0)
        break;
      if (o2.constructor === String || o2.constructor === Number) {
        const a2 = Id(s2);
        i > 0 || a2.length > 0 ? (e.push("%c" + o2), n.push(a2)) : e.push(o2);
      } else
        break;
    }
  }
  for (i > 0 && (r = n, r.unshift(e.join(""))); i < t.length; i++) {
    const o2 = t[i];
    o2 instanceof Symbol || r.push(o2);
  }
  return r;
};
var Vd = Kh ? Bd : Hd;
var Kd = (...t) => {
  console.log(...Vd(t)), Wd.forEach((e) => e.print(t));
};
var Wd = Ve();
var Da = (t) => ({
  /**
   * @return {IterableIterator<T>}
   */
  [Symbol.iterator]() {
    return this;
  },
  // @ts-ignore
  next: t
});
var Yd = (t, e) => Da(() => {
  let n;
  do
    n = t.next();
  while (!n.done && !e(n.value));
  return n;
});
var lr = (t, e) => Da(() => {
  const { done: n, value: s2 } = t.next();
  return { done: n, value: n ? void 0 : e(s2) };
});
var bi = class {
  /**
   * @param {number} clock
   * @param {number} len
   */
  constructor(e, n) {
    this.clock = e, this.len = n;
  }
};
var On = class {
  constructor() {
    this.clients = /* @__PURE__ */ new Map();
  }
};
var Ta = (t, e, n) => e.clients.forEach((s2, r) => {
  const i = (
    /** @type {Array<GC|Item>} */
    t.doc.store.clients.get(r)
  );
  for (let o2 = 0; o2 < s2.length; o2++) {
    const c2 = s2[o2];
    za(t, i, c2.clock, c2.len, n);
  }
});
var Gd = (t, e) => {
  let n = 0, s2 = t.length - 1;
  for (; n <= s2; ) {
    const r = pe((n + s2) / 2), i = t[r], o2 = i.clock;
    if (o2 <= e) {
      if (e < o2 + i.len)
        return r;
      n = r + 1;
    } else
      s2 = r - 1;
  }
  return null;
};
var La = (t, e) => {
  const n = t.clients.get(e.client);
  return n !== void 0 && Gd(n, e.clock) !== null;
};
var vi = (t) => {
  t.clients.forEach((e) => {
    e.sort((r, i) => r.clock - i.clock);
    let n, s2;
    for (n = 1, s2 = 1; n < e.length; n++) {
      const r = e[s2 - 1], i = e[n];
      r.clock + r.len >= i.clock ? r.len = vt(r.len, i.clock + i.len - r.clock) : (s2 < n && (e[s2] = i), s2++);
    }
    e.length = s2;
  });
};
var qd = (t) => {
  const e = new On();
  for (let n = 0; n < t.length; n++)
    t[n].clients.forEach((s2, r) => {
      if (!e.clients.has(r)) {
        const i = s2.slice();
        for (let o2 = n + 1; o2 < t.length; o2++)
          Ju(i, t[o2].clients.get(r) || []);
        e.clients.set(r, i);
      }
    });
  return vi(e), e;
};
var ws = (t, e, n, s2) => {
  ke(t.clients, e, () => (
    /** @type {Array<DeleteItem>} */
    []
  )).push(new bi(n, s2));
};
var Jd = () => new On();
var Xd = (t) => {
  const e = Jd();
  return t.clients.forEach((n, s2) => {
    const r = [];
    for (let i = 0; i < n.length; i++) {
      const o2 = n[i];
      if (o2.deleted) {
        const c2 = o2.id.clock;
        let a2 = o2.length;
        if (i + 1 < n.length)
          for (let l2 = n[i + 1]; i + 1 < n.length && l2.deleted; l2 = n[++i + 1])
            a2 += l2.length;
        r.push(new bi(c2, a2));
      }
    }
    r.length > 0 && e.clients.set(s2, r);
  }), e;
};
var zt = (t, e) => {
  b(t.restEncoder, e.clients.size), Le(e.clients.entries()).sort((n, s2) => s2[0] - n[0]).forEach(([n, s2]) => {
    t.resetDsCurVal(), b(t.restEncoder, n);
    const r = s2.length;
    b(t.restEncoder, r);
    for (let i = 0; i < r; i++) {
      const o2 = s2[i];
      t.writeDsClock(o2.clock), t.writeDsLen(o2.len);
    }
  });
};
var Ci = (t) => {
  const e = new On(), n = S(t.restDecoder);
  for (let s2 = 0; s2 < n; s2++) {
    t.resetDsCurVal();
    const r = S(t.restDecoder), i = S(t.restDecoder);
    if (i > 0) {
      const o2 = ke(e.clients, r, () => (
        /** @type {Array<DeleteItem>} */
        []
      ));
      for (let c2 = 0; c2 < i; c2++)
        o2.push(new bi(t.readDsClock(), t.readDsLen()));
    }
  }
  return e;
};
var Do = (t, e, n) => {
  const s2 = new On(), r = S(t.restDecoder);
  for (let i = 0; i < r; i++) {
    t.resetDsCurVal();
    const o2 = S(t.restDecoder), c2 = S(t.restDecoder), a2 = n.clients.get(o2) || [], l2 = j(n, o2);
    for (let u = 0; u < c2; u++) {
      const h = t.readDsClock(), d = h + t.readDsLen();
      if (h < l2) {
        l2 < d && ws(s2, o2, l2, d - l2);
        let f2 = ge(a2, h), p = a2[f2];
        for (!p.deleted && p.id.clock < h && (a2.splice(f2 + 1, 0, Es(e, p, h - p.id.clock)), f2++); f2 < a2.length && (p = a2[f2++], p.id.clock < d); )
          p.deleted || (d < p.id.clock + p.length && a2.splice(f2, 0, Es(e, p, d - p.id.clock)), p.delete(e));
      } else
        ws(s2, o2, h, d - h);
    }
  }
  if (s2.clients.size > 0) {
    const i = new ht();
    return b(i.restEncoder, 0), zt(i, s2), i.toUint8Array();
  }
  return null;
};
var Ia = na;
var me = class _me extends kh {
  /**
   * @param {DocOpts} opts configuration
   */
  constructor({ guid: e = Uh(), collectionid: n = null, gc: s2 = true, gcFilter: r = () => true, meta: i = null, autoLoad: o2 = false, shouldLoad: c2 = true } = {}) {
    super(), this.gc = s2, this.gcFilter = r, this.clientID = Ia(), this.guid = e, this.collectionid = n, this.share = /* @__PURE__ */ new Map(), this.store = new ja(), this._transaction = null, this._transactionCleanups = [], this.subdocs = /* @__PURE__ */ new Set(), this._item = null, this.shouldLoad = c2, this.autoLoad = o2, this.meta = i, this.isLoaded = false, this.isSynced = false, this.whenLoaded = _o((l2) => {
      this.on("load", () => {
        this.isLoaded = true, l2(this);
      });
    });
    const a2 = () => _o((l2) => {
      const u = (h) => {
        (h === void 0 || h === true) && (this.off("sync", u), l2());
      };
      this.on("sync", u);
    });
    this.on("sync", (l2) => {
      l2 === false && this.isSynced && (this.whenSynced = a2()), this.isSynced = l2 === void 0 || l2 === true, this.isSynced && !this.isLoaded && this.emit("load", [this]);
    }), this.whenSynced = a2();
  }
  /**
   * Notify the parent document that you request to load data into this subdocument (if it is a subdocument).
   *
   * `load()` might be used in the future to request any provider to load the most current data.
   *
   * It is safe to call `load()` multiple times.
   */
  load() {
    const e = this._item;
    e !== null && !this.shouldLoad && k(
      /** @type {any} */
      e.parent.doc,
      (n) => {
        n.subdocsLoaded.add(this);
      },
      null,
      true
    ), this.shouldLoad = true;
  }
  getSubdocs() {
    return this.subdocs;
  }
  getSubdocGuids() {
    return new Set(Le(this.subdocs).map((e) => e.guid));
  }
  /**
   * Changes that happen inside of a transaction are bundled. This means that
   * the observer fires _after_ the transaction is finished and that all changes
   * that happened inside of the transaction are sent as one message to the
   * other peers.
   *
   * @template T
   * @param {function(Transaction):T} f The function that should be executed as a transaction
   * @param {any} [origin] Origin of who started the transaction. Will be stored on transaction.origin
   * @return T
   *
   * @public
   */
  transact(e, n = null) {
    return k(this, e, n);
  }
  /**
   * Define a shared data type.
   *
   * Multiple calls of `ydoc.get(name, TypeConstructor)` yield the same result
   * and do not overwrite each other. I.e.
   * `ydoc.get(name, Y.Array) === ydoc.get(name, Y.Array)`
   *
   * After this method is called, the type is also available on `ydoc.share.get(name)`.
   *
   * *Best Practices:*
   * Define all types right after the Y.Doc instance is created and store them in a separate object.
   * Also use the typed methods `getText(name)`, `getArray(name)`, ..
   *
   * @template {typeof AbstractType<any>} Type
   * @example
   *   const ydoc = new Y.Doc(..)
   *   const appState = {
   *     document: ydoc.getText('document')
   *     comments: ydoc.getArray('comments')
   *   }
   *
   * @param {string} name
   * @param {Type} TypeConstructor The constructor of the type definition. E.g. Y.Text, Y.Array, Y.Map, ...
   * @return {InstanceType<Type>} The created type. Constructed with TypeConstructor
   *
   * @public
   */
  get(e, n = (
    /** @type {any} */
    L
  )) {
    const s2 = ke(this.share, e, () => {
      const i = new n();
      return i._integrate(this, null), i;
    }), r = s2.constructor;
    if (n !== L && r !== n)
      if (r === L) {
        const i = new n();
        i._map = s2._map, s2._map.forEach(
          /** @param {Item?} n */
          (o2) => {
            for (; o2 !== null; o2 = o2.left)
              o2.parent = i;
          }
        ), i._start = s2._start;
        for (let o2 = i._start; o2 !== null; o2 = o2.right)
          o2.parent = i;
        return i._length = s2._length, this.share.set(e, i), i._integrate(this, null), /** @type {InstanceType<Type>} */
        i;
      } else
        throw new Error(`Type with the name ${e} has already been defined with a different constructor`);
    return (
      /** @type {InstanceType<Type>} */
      s2
    );
  }
  /**
   * @template T
   * @param {string} [name]
   * @return {YArray<T>}
   *
   * @public
   */
  getArray(e = "") {
    return (
      /** @type {YArray<T>} */
      this.get(e, be)
    );
  }
  /**
   * @param {string} [name]
   * @return {YText}
   *
   * @public
   */
  getText(e = "") {
    return this.get(e, Ie);
  }
  /**
   * @template T
   * @param {string} [name]
   * @return {YMap<T>}
   *
   * @public
   */
  getMap(e = "") {
    return (
      /** @type {YMap<T>} */
      this.get(e, Se)
    );
  }
  /**
   * @param {string} [name]
   * @return {YXmlElement}
   *
   * @public
   */
  getXmlElement(e = "") {
    return (
      /** @type {YXmlElement<{[key:string]:string}>} */
      this.get(e, Me)
    );
  }
  /**
   * @param {string} [name]
   * @return {YXmlFragment}
   *
   * @public
   */
  getXmlFragment(e = "") {
    return this.get(e, Ee);
  }
  /**
   * Converts the entire document into a js object, recursively traversing each yjs type
   * Doesn't log types that have not been defined (using ydoc.getType(..)).
   *
   * @deprecated Do not use this method and rather call toJSON directly on the shared types.
   *
   * @return {Object<string, any>}
   */
  toJSON() {
    const e = {};
    return this.share.forEach((n, s2) => {
      e[s2] = n.toJSON();
    }), e;
  }
  /**
   * Emit `destroy` event and unregister all event handlers.
   */
  destroy() {
    Le(this.subdocs).forEach((n) => n.destroy());
    const e = this._item;
    if (e !== null) {
      this._item = null;
      const n = (
        /** @type {ContentDoc} */
        e.content
      );
      n.doc = new _me({ guid: this.guid, ...n.opts, shouldLoad: false }), n.doc._item = e, k(
        /** @type {any} */
        e.parent.doc,
        (s2) => {
          const r = n.doc;
          e.deleted || s2.subdocsAdded.add(r), s2.subdocsRemoved.add(this);
        },
        null,
        true
      );
    }
    this.emit("destroyed", [true]), this.emit("destroy", [this]), super.destroy();
  }
};
var Ma = class {
  /**
   * @param {decoding.Decoder} decoder
   */
  constructor(e) {
    this.restDecoder = e;
  }
  resetDsCurVal() {
  }
  /**
   * @return {number}
   */
  readDsClock() {
    return S(this.restDecoder);
  }
  /**
   * @return {number}
   */
  readDsLen() {
    return S(this.restDecoder);
  }
};
var $a = class extends Ma {
  /**
   * @return {ID}
   */
  readLeftID() {
    return A(S(this.restDecoder), S(this.restDecoder));
  }
  /**
   * @return {ID}
   */
  readRightID() {
    return A(S(this.restDecoder), S(this.restDecoder));
  }
  /**
   * Read the next client id.
   * Use this in favor of readID whenever possible to reduce the number of objects created.
   */
  readClient() {
    return S(this.restDecoder);
  }
  /**
   * @return {number} info An unsigned 8-bit integer
   */
  readInfo() {
    return Mt(this.restDecoder);
  }
  /**
   * @return {string}
   */
  readString() {
    return He(this.restDecoder);
  }
  /**
   * @return {boolean} isKey
   */
  readParentInfo() {
    return S(this.restDecoder) === 1;
  }
  /**
   * @return {number} info An unsigned 8-bit integer
   */
  readTypeRef() {
    return S(this.restDecoder);
  }
  /**
   * Write len of a struct - well suited for Opt RLE encoder.
   *
   * @return {number} len
   */
  readLen() {
    return S(this.restDecoder);
  }
  /**
   * @return {any}
   */
  readAny() {
    return fn(this.restDecoder);
  }
  /**
   * @return {Uint8Array}
   */
  readBuf() {
    return ed(W(this.restDecoder));
  }
  /**
   * Legacy implementation uses JSON parse. We use any-decoding in v2.
   *
   * @return {any}
   */
  readJSON() {
    return JSON.parse(He(this.restDecoder));
  }
  /**
   * @return {string}
   */
  readKey() {
    return He(this.restDecoder);
  }
};
var Zd = class {
  /**
   * @param {decoding.Decoder} decoder
   */
  constructor(e) {
    this.dsCurrVal = 0, this.restDecoder = e;
  }
  resetDsCurVal() {
    this.dsCurrVal = 0;
  }
  /**
   * @return {number}
   */
  readDsClock() {
    return this.dsCurrVal += S(this.restDecoder), this.dsCurrVal;
  }
  /**
   * @return {number}
   */
  readDsLen() {
    const e = S(this.restDecoder) + 1;
    return this.dsCurrVal += e, e;
  }
};
var Rt = class extends Zd {
  /**
   * @param {decoding.Decoder} decoder
   */
  constructor(e) {
    super(e), this.keys = [], S(e), this.keyClockDecoder = new or(W(e)), this.clientDecoder = new as(W(e)), this.leftClockDecoder = new or(W(e)), this.rightClockDecoder = new or(W(e)), this.infoDecoder = new Eo(W(e), Mt), this.stringDecoder = new Eh(W(e)), this.parentInfoDecoder = new Eo(W(e), Mt), this.typeRefDecoder = new as(W(e)), this.lenDecoder = new as(W(e));
  }
  /**
   * @return {ID}
   */
  readLeftID() {
    return new Tt(this.clientDecoder.read(), this.leftClockDecoder.read());
  }
  /**
   * @return {ID}
   */
  readRightID() {
    return new Tt(this.clientDecoder.read(), this.rightClockDecoder.read());
  }
  /**
   * Read the next client id.
   * Use this in favor of readID whenever possible to reduce the number of objects created.
   */
  readClient() {
    return this.clientDecoder.read();
  }
  /**
   * @return {number} info An unsigned 8-bit integer
   */
  readInfo() {
    return (
      /** @type {number} */
      this.infoDecoder.read()
    );
  }
  /**
   * @return {string}
   */
  readString() {
    return this.stringDecoder.read();
  }
  /**
   * @return {boolean}
   */
  readParentInfo() {
    return this.parentInfoDecoder.read() === 1;
  }
  /**
   * @return {number} An unsigned 8-bit integer
   */
  readTypeRef() {
    return this.typeRefDecoder.read();
  }
  /**
   * Write len of a struct - well suited for Opt RLE encoder.
   *
   * @return {number}
   */
  readLen() {
    return this.lenDecoder.read();
  }
  /**
   * @return {any}
   */
  readAny() {
    return fn(this.restDecoder);
  }
  /**
   * @return {Uint8Array}
   */
  readBuf() {
    return W(this.restDecoder);
  }
  /**
   * This is mainly here for legacy purposes.
   *
   * Initial we incoded objects using JSON. Now we use the much faster lib0/any-encoder. This method mainly exists for legacy purposes for the v1 encoder.
   *
   * @return {any}
   */
  readJSON() {
    return fn(this.restDecoder);
  }
  /**
   * @return {string}
   */
  readKey() {
    const e = this.keyClockDecoder.read();
    if (e < this.keys.length)
      return this.keys[e];
    {
      const n = this.stringDecoder.read();
      return this.keys.push(n), n;
    }
  }
};
var Oa = class {
  constructor() {
    this.restEncoder = Y();
  }
  toUint8Array() {
    return T(this.restEncoder);
  }
  resetDsCurVal() {
  }
  /**
   * @param {number} clock
   */
  writeDsClock(e) {
    b(this.restEncoder, e);
  }
  /**
   * @param {number} len
   */
  writeDsLen(e) {
    b(this.restEncoder, e);
  }
};
var Pn = class extends Oa {
  /**
   * @param {ID} id
   */
  writeLeftID(e) {
    b(this.restEncoder, e.client), b(this.restEncoder, e.clock);
  }
  /**
   * @param {ID} id
   */
  writeRightID(e) {
    b(this.restEncoder, e.client), b(this.restEncoder, e.clock);
  }
  /**
   * Use writeClient and writeClock instead of writeID if possible.
   * @param {number} client
   */
  writeClient(e) {
    b(this.restEncoder, e);
  }
  /**
   * @param {number} info An unsigned 8-bit integer
   */
  writeInfo(e) {
    Lr(this.restEncoder, e);
  }
  /**
   * @param {string} s
   */
  writeString(e) {
    ct(this.restEncoder, e);
  }
  /**
   * @param {boolean} isYKey
   */
  writeParentInfo(e) {
    b(this.restEncoder, e ? 1 : 0);
  }
  /**
   * @param {number} info An unsigned 8-bit integer
   */
  writeTypeRef(e) {
    b(this.restEncoder, e);
  }
  /**
   * Write len of a struct - well suited for Opt RLE encoder.
   *
   * @param {number} len
   */
  writeLen(e) {
    b(this.restEncoder, e);
  }
  /**
   * @param {any} any
   */
  writeAny(e) {
    dn(this.restEncoder, e);
  }
  /**
   * @param {Uint8Array} buf
   */
  writeBuf(e) {
    I(this.restEncoder, e);
  }
  /**
   * @param {any} embed
   */
  writeJSON(e) {
    ct(this.restEncoder, JSON.stringify(e));
  }
  /**
   * @param {string} key
   */
  writeKey(e) {
    ct(this.restEncoder, e);
  }
};
var Pa = class {
  constructor() {
    this.restEncoder = Y(), this.dsCurrVal = 0;
  }
  toUint8Array() {
    return T(this.restEncoder);
  }
  resetDsCurVal() {
    this.dsCurrVal = 0;
  }
  /**
   * @param {number} clock
   */
  writeDsClock(e) {
    const n = e - this.dsCurrVal;
    this.dsCurrVal = e, b(this.restEncoder, n);
  }
  /**
   * @param {number} len
   */
  writeDsLen(e) {
    e === 0 && ce(), b(this.restEncoder, e - 1), this.dsCurrVal += e;
  }
};
var ht = class extends Pa {
  constructor() {
    super(), this.keyMap = /* @__PURE__ */ new Map(), this.keyClock = 0, this.keyClockEncoder = new ir(), this.clientEncoder = new cs(), this.leftClockEncoder = new ir(), this.rightClockEncoder = new ir(), this.infoEncoder = new vo(Lr), this.stringEncoder = new ph(), this.parentInfoEncoder = new vo(Lr), this.typeRefEncoder = new cs(), this.lenEncoder = new cs();
  }
  toUint8Array() {
    const e = Y();
    return b(e, 0), I(e, this.keyClockEncoder.toUint8Array()), I(e, this.clientEncoder.toUint8Array()), I(e, this.leftClockEncoder.toUint8Array()), I(e, this.rightClockEncoder.toUint8Array()), I(e, T(this.infoEncoder)), I(e, this.stringEncoder.toUint8Array()), I(e, T(this.parentInfoEncoder)), I(e, this.typeRefEncoder.toUint8Array()), I(e, this.lenEncoder.toUint8Array()), Os(e, T(this.restEncoder)), T(e);
  }
  /**
   * @param {ID} id
   */
  writeLeftID(e) {
    this.clientEncoder.write(e.client), this.leftClockEncoder.write(e.clock);
  }
  /**
   * @param {ID} id
   */
  writeRightID(e) {
    this.clientEncoder.write(e.client), this.rightClockEncoder.write(e.clock);
  }
  /**
   * @param {number} client
   */
  writeClient(e) {
    this.clientEncoder.write(e);
  }
  /**
   * @param {number} info An unsigned 8-bit integer
   */
  writeInfo(e) {
    this.infoEncoder.write(e);
  }
  /**
   * @param {string} s
   */
  writeString(e) {
    this.stringEncoder.write(e);
  }
  /**
   * @param {boolean} isYKey
   */
  writeParentInfo(e) {
    this.parentInfoEncoder.write(e ? 1 : 0);
  }
  /**
   * @param {number} info An unsigned 8-bit integer
   */
  writeTypeRef(e) {
    this.typeRefEncoder.write(e);
  }
  /**
   * Write len of a struct - well suited for Opt RLE encoder.
   *
   * @param {number} len
   */
  writeLen(e) {
    this.lenEncoder.write(e);
  }
  /**
   * @param {any} any
   */
  writeAny(e) {
    dn(this.restEncoder, e);
  }
  /**
   * @param {Uint8Array} buf
   */
  writeBuf(e) {
    I(this.restEncoder, e);
  }
  /**
   * This is mainly here for legacy purposes.
   *
   * Initial we incoded objects using JSON. Now we use the much faster lib0/any-encoder. This method mainly exists for legacy purposes for the v1 encoder.
   *
   * @param {any} embed
   */
  writeJSON(e) {
    dn(this.restEncoder, e);
  }
  /**
   * Property keys are often reused. For example, in y-prosemirror the key `bold` might
   * occur very often. For a 3d application, the key `position` might occur very often.
   *
   * We cache these keys in a Map and refer to them via a unique number.
   *
   * @param {string} key
   */
  writeKey(e) {
    const n = this.keyMap.get(e);
    n === void 0 ? (this.keyClockEncoder.write(this.keyClock++), this.stringEncoder.write(e)) : this.keyClockEncoder.write(n);
  }
};
var Qd = (t, e, n, s2) => {
  s2 = vt(s2, e[0].id.clock);
  const r = ge(e, s2);
  b(t.restEncoder, e.length - r), t.writeClient(n), b(t.restEncoder, s2);
  const i = e[r];
  i.write(t, s2 - i.id.clock);
  for (let o2 = r + 1; o2 < e.length; o2++)
    e[o2].write(t, 0);
};
var Si = (t, e, n) => {
  const s2 = /* @__PURE__ */ new Map();
  n.forEach((r, i) => {
    j(e, i) > r && s2.set(i, r);
  }), zs(e).forEach((r, i) => {
    n.has(i) || s2.set(i, 0);
  }), b(t.restEncoder, s2.size), Le(s2.entries()).sort((r, i) => i[0] - r[0]).forEach(([r, i]) => {
    Qd(
      t,
      /** @type {Array<GC|Item>} */
      e.clients.get(r),
      r,
      i
    );
  });
};
var ef = (t, e) => {
  const n = te(), s2 = S(t.restDecoder);
  for (let r = 0; r < s2; r++) {
    const i = S(t.restDecoder), o2 = new Array(i), c2 = t.readClient();
    let a2 = S(t.restDecoder);
    n.set(c2, { i: 0, refs: o2 });
    for (let l2 = 0; l2 < i; l2++) {
      const u = t.readInfo();
      switch (Ms & u) {
        case 0: {
          const h = t.readLen();
          o2[l2] = new re(A(c2, a2), h), a2 += h;
          break;
        }
        case 10: {
          const h = S(t.restDecoder);
          o2[l2] = new oe(A(c2, a2), h), a2 += h;
          break;
        }
        default: {
          const h = (u & (De | ie)) === 0, d = new P(
            A(c2, a2),
            null,
            // left
            (u & ie) === ie ? t.readLeftID() : null,
            // origin
            null,
            // right
            (u & De) === De ? t.readRightID() : null,
            // right origin
            h ? t.readParentInfo() ? e.get(t.readString()) : t.readLeftID() : null,
            // parent
            h && (u & un) === un ? t.readString() : null,
            // parentSub
            cl(t, u)
            // item content
          );
          o2[l2] = d, a2 += d.length;
        }
      }
    }
  }
  return n;
};
var tf = (t, e, n) => {
  const s2 = [];
  let r = Le(n.keys()).sort((f2, p) => f2 - p);
  if (r.length === 0)
    return null;
  const i = () => {
    if (r.length === 0)
      return null;
    let f2 = (
      /** @type {{i:number,refs:Array<GC|Item>}} */
      n.get(r[r.length - 1])
    );
    for (; f2.refs.length === f2.i; )
      if (r.pop(), r.length > 0)
        f2 = /** @type {{i:number,refs:Array<GC|Item>}} */
        n.get(r[r.length - 1]);
      else
        return null;
    return f2;
  };
  let o2 = i();
  if (o2 === null)
    return null;
  const c2 = new ja(), a2 = /* @__PURE__ */ new Map(), l2 = (f2, p) => {
    const g = a2.get(f2);
    (g == null || g > p) && a2.set(f2, p);
  };
  let u = (
    /** @type {any} */
    o2.refs[
      /** @type {any} */
      o2.i++
    ]
  );
  const h = /* @__PURE__ */ new Map(), d = () => {
    for (const f2 of s2) {
      const p = f2.id.client, g = n.get(p);
      g ? (g.i--, c2.clients.set(p, g.refs.slice(g.i)), n.delete(p), g.i = 0, g.refs = []) : c2.clients.set(p, [f2]), r = r.filter((m) => m !== p);
    }
    s2.length = 0;
  };
  for (; ; ) {
    if (u.constructor !== oe) {
      const p = ke(h, u.id.client, () => j(e, u.id.client)) - u.id.clock;
      if (p < 0)
        s2.push(u), l2(u.id.client, u.id.clock - 1), d();
      else {
        const g = u.getMissing(t, e);
        if (g !== null) {
          s2.push(u);
          const m = n.get(
            /** @type {number} */
            g
          ) || { refs: [], i: 0 };
          if (m.refs.length === m.i)
            l2(
              /** @type {number} */
              g,
              j(e, g)
            ), d();
          else {
            u = m.refs[m.i++];
            continue;
          }
        } else (p === 0 || p < u.length) && (u.integrate(t, p), h.set(u.id.client, u.id.clock + u.length));
      }
    }
    if (s2.length > 0)
      u = /** @type {GC|Item} */
      s2.pop();
    else if (o2 !== null && o2.i < o2.refs.length)
      u = /** @type {GC|Item} */
      o2.refs[o2.i++];
    else {
      if (o2 = i(), o2 === null)
        break;
      u = /** @type {GC|Item} */
      o2.refs[o2.i++];
    }
  }
  if (c2.clients.size > 0) {
    const f2 = new ht();
    return Si(f2, c2, /* @__PURE__ */ new Map()), b(f2.restEncoder, 0), { missing: a2, update: f2.toUint8Array() };
  }
  return null;
};
var nf = (t, e) => Si(t, e.doc.store, e.beforeState);
var sf = (t, e, n, s2 = new Rt(t)) => k(e, (r) => {
  r.local = false;
  let i = false;
  const o2 = r.doc, c2 = o2.store, a2 = ef(s2, o2), l2 = tf(r, c2, a2), u = c2.pendingStructs;
  if (u) {
    for (const [d, f2] of u.missing)
      if (f2 < j(c2, d)) {
        i = true;
        break;
      }
    if (l2) {
      for (const [d, f2] of l2.missing) {
        const p = u.missing.get(d);
        (p == null || p > f2) && u.missing.set(d, f2);
      }
      u.update = bs([u.update, l2.update]);
    }
  } else
    c2.pendingStructs = l2;
  const h = Do(s2, r, c2);
  if (c2.pendingDs) {
    const d = new Rt(qe(c2.pendingDs));
    S(d.restDecoder);
    const f2 = Do(d, r, c2);
    h && f2 ? c2.pendingDs = bs([h, f2]) : c2.pendingDs = h || f2;
  } else
    c2.pendingDs = h;
  if (i) {
    const d = (
      /** @type {{update: Uint8Array}} */
      c2.pendingStructs.update
    );
    c2.pendingStructs = null, Ra(r.doc, d);
  }
}, n, false);
var Ra = (t, e, n, s2 = Rt) => {
  const r = qe(e);
  sf(r, t, n, new s2(r));
};
var rf = (t, e, n) => Ra(t, e, n, $a);
var of = (t, e, n = /* @__PURE__ */ new Map()) => {
  Si(t, e.store, n), zt(t, Xd(e.store));
};
var cf = (t, e = new Uint8Array([0]), n = new ht()) => {
  const s2 = Na(e);
  of(n, t, s2);
  const r = [n.toUint8Array()];
  if (t.store.pendingDs && r.push(t.store.pendingDs), t.store.pendingStructs && r.push(Ef(t.store.pendingStructs.update, e)), r.length > 1) {
    if (n.constructor === Pn)
      return Cf(r.map((i, o2) => o2 === 0 ? i : Af(i)));
    if (n.constructor === ht)
      return bs(r);
  }
  return r[0];
};
var af = (t, e) => cf(t, e, new Pn());
var lf = (t) => {
  const e = /* @__PURE__ */ new Map(), n = S(t.restDecoder);
  for (let s2 = 0; s2 < n; s2++) {
    const r = S(t.restDecoder), i = S(t.restDecoder);
    e.set(r, i);
  }
  return e;
};
var Na = (t) => lf(new Ma(qe(t)));
var Ua = (t, e) => (b(t.restEncoder, e.size), Le(e.entries()).sort((n, s2) => s2[0] - n[0]).forEach(([n, s2]) => {
  b(t.restEncoder, n), b(t.restEncoder, s2);
}), t);
var uf = (t, e) => Ua(t, zs(e.store));
var hf = (t, e = new Pa()) => (t instanceof Map ? Ua(e, t) : uf(e, t), e.toUint8Array());
var df = (t) => hf(t, new Oa());
var ff = class {
  constructor() {
    this.l = [];
  }
};
var To = () => new ff();
var Lo = (t, e) => t.l.push(e);
var Io = (t, e) => {
  const n = t.l, s2 = n.length;
  t.l = n.filter((r) => e !== r), s2 === t.l.length && console.error("[yjs] Tried to remove event handler that doesn't exist.");
};
var Fa = (t, e, n) => di(t.l, [e, n]);
var Tt = class {
  /**
   * @param {number} client client id
   * @param {number} clock unique per client id, continuous number
   */
  constructor(e, n) {
    this.client = e, this.clock = n;
  }
};
var Wn = (t, e) => t === e || t !== null && e !== null && t.client === e.client && t.clock === e.clock;
var A = (t, e) => new Tt(t, e);
var pf = (t) => {
  for (const [e, n] of t.doc.share.entries())
    if (n === t)
      return e;
  throw ce();
};
var _t = (t, e) => e === void 0 ? !t.deleted : e.sv.has(t.id.client) && (e.sv.get(t.id.client) || 0) > t.id.clock && !La(e.ds, t.id);
var Rr = (t, e) => {
  const n = ke(t.meta, Rr, Ve), s2 = t.doc.store;
  n.has(e) || (e.sv.forEach((r, i) => {
    r < j(s2, i) && Ke(t, A(i, r));
  }), Ta(t, e.ds, (r) => {
  }), n.add(e));
};
var ja = class {
  constructor() {
    this.clients = /* @__PURE__ */ new Map(), this.pendingStructs = null, this.pendingDs = null;
  }
};
var zs = (t) => {
  const e = /* @__PURE__ */ new Map();
  return t.clients.forEach((n, s2) => {
    const r = n[n.length - 1];
    e.set(s2, r.id.clock + r.length);
  }), e;
};
var j = (t, e) => {
  const n = t.clients.get(e);
  if (n === void 0)
    return 0;
  const s2 = n[n.length - 1];
  return s2.id.clock + s2.length;
};
var Ha = (t, e) => {
  let n = t.clients.get(e.id.client);
  if (n === void 0)
    n = [], t.clients.set(e.id.client, n);
  else {
    const s2 = n[n.length - 1];
    if (s2.id.clock + s2.length !== e.id.clock)
      throw ce();
  }
  n.push(e);
};
var ge = (t, e) => {
  let n = 0, s2 = t.length - 1, r = t[s2], i = r.id.clock;
  if (i === e)
    return s2;
  let o2 = pe(e / (i + r.length - 1) * s2);
  for (; n <= s2; ) {
    if (r = t[o2], i = r.id.clock, i <= e) {
      if (e < i + r.length)
        return o2;
      n = o2 + 1;
    } else
      s2 = o2 - 1;
    o2 = pe((n + s2) / 2);
  }
  throw ce();
};
var gf = (t, e) => {
  const n = t.clients.get(e.client);
  return n[ge(n, e.clock)];
};
var ur = (
  /** @type {function(StructStore,ID):Item} */
  gf
);
var Nr = (t, e, n) => {
  const s2 = ge(e, n), r = e[s2];
  return r.id.clock < n && r instanceof P ? (e.splice(s2 + 1, 0, Es(t, r, n - r.id.clock)), s2 + 1) : s2;
};
var Ke = (t, e) => {
  const n = (
    /** @type {Array<Item>} */
    t.doc.store.clients.get(e.client)
  );
  return n[Nr(t, n, e.clock)];
};
var Mo = (t, e, n) => {
  const s2 = e.clients.get(n.client), r = ge(s2, n.clock), i = s2[r];
  return n.clock !== i.id.clock + i.length - 1 && i.constructor !== re && s2.splice(r + 1, 0, Es(t, i, n.clock - i.id.clock + 1)), i;
};
var yf = (t, e, n) => {
  const s2 = (
    /** @type {Array<GC|Item>} */
    t.clients.get(e.id.client)
  );
  s2[ge(s2, e.id.clock)] = n;
};
var za = (t, e, n, s2, r) => {
  if (s2 === 0)
    return;
  const i = n + s2;
  let o2 = Nr(t, e, n), c2;
  do
    c2 = e[o2++], i < c2.id.clock + c2.length && Nr(t, e, i), r(c2);
  while (o2 < e.length && e[o2].id.clock < i);
};
var mf = class {
  /**
   * @param {Doc} doc
   * @param {any} origin
   * @param {boolean} local
   */
  constructor(e, n, s2) {
    this.doc = e, this.deleteSet = new On(), this.beforeState = zs(e.store), this.afterState = /* @__PURE__ */ new Map(), this.changed = /* @__PURE__ */ new Map(), this.changedParentTypes = /* @__PURE__ */ new Map(), this._mergeStructs = [], this.origin = n, this.meta = /* @__PURE__ */ new Map(), this.local = s2, this.subdocsAdded = /* @__PURE__ */ new Set(), this.subdocsRemoved = /* @__PURE__ */ new Set(), this.subdocsLoaded = /* @__PURE__ */ new Set(), this._needFormattingCleanup = false;
  }
};
var $o = (t, e) => e.deleteSet.clients.size === 0 && !Ah(e.afterState, (n, s2) => e.beforeState.get(s2) !== n) ? false : (vi(e.deleteSet), nf(t, e), zt(t, e.deleteSet), true);
var Oo = (t, e, n) => {
  const s2 = e._item;
  (s2 === null || s2.id.clock < (t.beforeState.get(s2.id.client) || 0) && !s2.deleted) && ke(t.changed, e, Ve).add(n);
};
var ls = (t, e) => {
  let n = t[e], s2 = t[e - 1], r = e;
  for (; r > 0; n = s2, s2 = t[--r - 1]) {
    if (s2.deleted === n.deleted && s2.constructor === n.constructor && s2.mergeWith(n)) {
      n instanceof P && n.parentSub !== null && /** @type {AbstractType<any>} */
      n.parent._map.get(n.parentSub) === n && n.parent._map.set(
        n.parentSub,
        /** @type {Item} */
        s2
      );
      continue;
    }
    break;
  }
  const i = e - r;
  return i && t.splice(e + 1 - i, i), i;
};
var wf = (t, e, n) => {
  for (const [s2, r] of t.clients.entries()) {
    const i = (
      /** @type {Array<GC|Item>} */
      e.clients.get(s2)
    );
    for (let o2 = r.length - 1; o2 >= 0; o2--) {
      const c2 = r[o2], a2 = c2.clock + c2.len;
      for (let l2 = ge(i, c2.clock), u = i[l2]; l2 < i.length && u.id.clock < a2; u = i[++l2]) {
        const h = i[l2];
        if (c2.clock + c2.len <= h.id.clock)
          break;
        h instanceof P && h.deleted && !h.keep && n(h) && h.gc(e, false);
      }
    }
  }
};
var bf = (t, e) => {
  t.clients.forEach((n, s2) => {
    const r = (
      /** @type {Array<GC|Item>} */
      e.clients.get(s2)
    );
    for (let i = n.length - 1; i >= 0; i--) {
      const o2 = n[i], c2 = ri(r.length - 1, 1 + ge(r, o2.clock + o2.len - 1));
      for (let a2 = c2, l2 = r[a2]; a2 > 0 && l2.id.clock >= o2.clock; l2 = r[a2])
        a2 -= 1 + ls(r, a2);
    }
  });
};
var Ba = (t, e) => {
  if (e < t.length) {
    const n = t[e], s2 = n.doc, r = s2.store, i = n.deleteSet, o2 = n._mergeStructs;
    try {
      vi(i), n.afterState = zs(n.doc.store), s2.emit("beforeObserverCalls", [n, s2]);
      const c2 = [];
      n.changed.forEach(
        (a2, l2) => c2.push(() => {
          (l2._item === null || !l2._item.deleted) && l2._callObserver(n, a2);
        })
      ), c2.push(() => {
        n.changedParentTypes.forEach((a2, l2) => {
          l2._dEH.l.length > 0 && (l2._item === null || !l2._item.deleted) && (a2 = a2.filter(
            (u) => u.target._item === null || !u.target._item.deleted
          ), a2.forEach((u) => {
            u.currentTarget = l2, u._path = null;
          }), a2.sort((u, h) => u.path.length - h.path.length), Fa(l2._dEH, a2, n));
        });
      }), c2.push(() => s2.emit("afterTransaction", [n, s2])), di(c2, []), n._needFormattingCleanup && Ff(n);
    } finally {
      s2.gc && wf(i, r, s2.gcFilter), bf(i, r), n.afterState.forEach((u, h) => {
        const d = n.beforeState.get(h) || 0;
        if (d !== u) {
          const f2 = (
            /** @type {Array<GC|Item>} */
            r.clients.get(h)
          ), p = vt(ge(f2, d), 1);
          for (let g = f2.length - 1; g >= p; )
            g -= 1 + ls(f2, g);
        }
      });
      for (let u = o2.length - 1; u >= 0; u--) {
        const { client: h, clock: d } = o2[u].id, f2 = (
          /** @type {Array<GC|Item>} */
          r.clients.get(h)
        ), p = ge(f2, d);
        p + 1 < f2.length && ls(f2, p + 1) > 1 || p > 0 && ls(f2, p);
      }
      if (!n.local && n.afterState.get(s2.clientID) !== n.beforeState.get(s2.clientID) && (Kd(xa, _a, "[yjs] ", Aa, ka, "Changed the client-id because another client seems to be using it."), s2.clientID = Ia()), s2.emit("afterTransactionCleanup", [n, s2]), s2._observers.has("update")) {
        const u = new Pn();
        $o(u, n) && s2.emit("update", [u.toUint8Array(), n.origin, s2, n]);
      }
      if (s2._observers.has("updateV2")) {
        const u = new ht();
        $o(u, n) && s2.emit("updateV2", [u.toUint8Array(), n.origin, s2, n]);
      }
      const { subdocsAdded: c2, subdocsLoaded: a2, subdocsRemoved: l2 } = n;
      (c2.size > 0 || l2.size > 0 || a2.size > 0) && (c2.forEach((u) => {
        u.clientID = s2.clientID, u.collectionid == null && (u.collectionid = s2.collectionid), s2.subdocs.add(u);
      }), l2.forEach((u) => s2.subdocs.delete(u)), s2.emit("subdocs", [{ loaded: a2, added: c2, removed: l2 }, s2, n]), l2.forEach((u) => u.destroy())), t.length <= e + 1 ? (s2._transactionCleanups = [], s2.emit("afterAllTransactions", [s2, t])) : Ba(t, e + 1);
    }
  }
};
var k = (t, e, n = null, s2 = true) => {
  const r = t._transactionCleanups;
  let i = false, o2 = null;
  t._transaction === null && (i = true, t._transaction = new mf(t, n, s2), r.push(t._transaction), r.length === 1 && t.emit("beforeAllTransactions", [t]), t.emit("beforeTransaction", [t._transaction, t]));
  try {
    o2 = e(t._transaction);
  } finally {
    if (i) {
      const c2 = t._transaction === r[0];
      t._transaction = null, c2 && Ba(r, 0);
    }
  }
  return o2;
};
function* vf(t) {
  const e = S(t.restDecoder);
  for (let n = 0; n < e; n++) {
    const s2 = S(t.restDecoder), r = t.readClient();
    let i = S(t.restDecoder);
    for (let o2 = 0; o2 < s2; o2++) {
      const c2 = t.readInfo();
      if (c2 === 10) {
        const a2 = S(t.restDecoder);
        yield new oe(A(r, i), a2), i += a2;
      } else if ((Ms & c2) !== 0) {
        const a2 = (c2 & (De | ie)) === 0, l2 = new P(
          A(r, i),
          null,
          // left
          (c2 & ie) === ie ? t.readLeftID() : null,
          // origin
          null,
          // right
          (c2 & De) === De ? t.readRightID() : null,
          // right origin
          // @ts-ignore Force writing a string here.
          a2 ? t.readParentInfo() ? t.readString() : t.readLeftID() : null,
          // parent
          a2 && (c2 & un) === un ? t.readString() : null,
          // parentSub
          cl(t, c2)
          // item content
        );
        yield l2, i += l2.length;
      } else {
        const a2 = t.readLen();
        yield new re(A(r, i), a2), i += a2;
      }
    }
  }
}
var Ei = class {
  /**
   * @param {UpdateDecoderV1 | UpdateDecoderV2} decoder
   * @param {boolean} filterSkips
   */
  constructor(e, n) {
    this.gen = vf(e), this.curr = null, this.done = false, this.filterSkips = n, this.next();
  }
  /**
   * @return {Item | GC | Skip |null}
   */
  next() {
    do
      this.curr = this.gen.next().value || null;
    while (this.filterSkips && this.curr !== null && this.curr.constructor === oe);
    return this.curr;
  }
};
var _i = class {
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   */
  constructor(e) {
    this.currClient = 0, this.startClock = 0, this.written = 0, this.encoder = e, this.clientStructs = [];
  }
};
var Cf = (t) => bs(t, $a, Pn);
var Sf = (t, e) => {
  if (t.constructor === re) {
    const { client: n, clock: s2 } = t.id;
    return new re(A(n, s2 + e), t.length - e);
  } else if (t.constructor === oe) {
    const { client: n, clock: s2 } = t.id;
    return new oe(A(n, s2 + e), t.length - e);
  } else {
    const n = (
      /** @type {Item} */
      t
    ), { client: s2, clock: r } = n.id;
    return new P(
      A(s2, r + e),
      null,
      A(s2, r + e - 1),
      null,
      n.rightOrigin,
      n.parent,
      n.parentSub,
      n.content.splice(e)
    );
  }
};
var bs = (t, e = Rt, n = ht) => {
  if (t.length === 1)
    return t[0];
  const s2 = t.map((u) => new e(qe(u)));
  let r = s2.map((u) => new Ei(u, true)), i = null;
  const o2 = new n(), c2 = new _i(o2);
  for (; r = r.filter((d) => d.curr !== null), r.sort(
    /** @type {function(any,any):number} */
    (d, f2) => {
      if (d.curr.id.client === f2.curr.id.client) {
        const p = d.curr.id.clock - f2.curr.id.clock;
        return p === 0 ? d.curr.constructor === f2.curr.constructor ? 0 : d.curr.constructor === oe ? 1 : -1 : p;
      } else
        return f2.curr.id.client - d.curr.id.client;
    }
  ), r.length !== 0; ) {
    const u = r[0], h = (
      /** @type {Item | GC} */
      u.curr.id.client
    );
    if (i !== null) {
      let d = (
        /** @type {Item | GC | null} */
        u.curr
      ), f2 = false;
      for (; d !== null && d.id.clock + d.length <= i.struct.id.clock + i.struct.length && d.id.client >= i.struct.id.client; )
        d = u.next(), f2 = true;
      if (d === null || // current decoder is empty
      d.id.client !== h || // check whether there is another decoder that has has updates from `firstClient`
      f2 && d.id.clock > i.struct.id.clock + i.struct.length)
        continue;
      if (h !== i.struct.id.client)
        Ne(c2, i.struct, i.offset), i = { struct: d, offset: 0 }, u.next();
      else if (i.struct.id.clock + i.struct.length < d.id.clock)
        if (i.struct.constructor === oe)
          i.struct.length = d.id.clock + d.length - i.struct.id.clock;
        else {
          Ne(c2, i.struct, i.offset);
          const p = d.id.clock - i.struct.id.clock - i.struct.length;
          i = { struct: new oe(A(h, i.struct.id.clock + i.struct.length), p), offset: 0 };
        }
      else {
        const p = i.struct.id.clock + i.struct.length - d.id.clock;
        p > 0 && (i.struct.constructor === oe ? i.struct.length -= p : d = Sf(d, p)), i.struct.mergeWith(
          /** @type {any} */
          d
        ) || (Ne(c2, i.struct, i.offset), i = { struct: d, offset: 0 }, u.next());
      }
    } else
      i = { struct: (
        /** @type {Item | GC} */
        u.curr
      ), offset: 0 }, u.next();
    for (let d = u.curr; d !== null && d.id.client === h && d.id.clock === i.struct.id.clock + i.struct.length && d.constructor !== oe; d = u.next())
      Ne(c2, i.struct, i.offset), i = { struct: d, offset: 0 };
  }
  i !== null && (Ne(c2, i.struct, i.offset), i = null), Ai(c2);
  const a2 = s2.map((u) => Ci(u)), l2 = qd(a2);
  return zt(o2, l2), o2.toUint8Array();
};
var Ef = (t, e, n = Rt, s2 = ht) => {
  const r = Na(e), i = new s2(), o2 = new _i(i), c2 = new n(qe(t)), a2 = new Ei(c2, false);
  for (; a2.curr; ) {
    const u = a2.curr, h = u.id.client, d = r.get(h) || 0;
    if (a2.curr.constructor === oe) {
      a2.next();
      continue;
    }
    if (u.id.clock + u.length > d)
      for (Ne(o2, u, vt(d - u.id.clock, 0)), a2.next(); a2.curr && a2.curr.id.client === h; )
        Ne(o2, a2.curr, 0), a2.next();
    else
      for (; a2.curr && a2.curr.id.client === h && a2.curr.id.clock + a2.curr.length <= d; )
        a2.next();
  }
  Ai(o2);
  const l2 = Ci(c2);
  return zt(i, l2), i.toUint8Array();
};
var Va = (t) => {
  t.written > 0 && (t.clientStructs.push({ written: t.written, restEncoder: T(t.encoder.restEncoder) }), t.encoder.restEncoder = Y(), t.written = 0);
};
var Ne = (t, e, n) => {
  t.written > 0 && t.currClient !== e.id.client && Va(t), t.written === 0 && (t.currClient = e.id.client, t.encoder.writeClient(e.id.client), b(t.encoder.restEncoder, e.id.clock + n)), e.write(t.encoder, n), t.written++;
};
var Ai = (t) => {
  Va(t);
  const e = t.encoder.restEncoder;
  b(e, t.clientStructs.length);
  for (let n = 0; n < t.clientStructs.length; n++) {
    const s2 = t.clientStructs[n];
    b(e, s2.written), Os(e, s2.restEncoder);
  }
};
var _f = (t, e, n, s2) => {
  const r = new n(qe(t)), i = new Ei(r, false), o2 = new s2(), c2 = new _i(o2);
  for (let l2 = i.curr; l2 !== null; l2 = i.next())
    Ne(c2, e(l2), 0);
  Ai(c2);
  const a2 = Ci(r);
  return zt(o2, a2), o2.toUint8Array();
};
var Af = (t) => _f(t, Oh, Rt, Pn);
var Po = "You must not compute changes after the event-handler fired.";
var Bs = class {
  /**
   * @param {T} target The changed type.
   * @param {Transaction} transaction
   */
  constructor(e, n) {
    this.target = e, this.currentTarget = e, this.transaction = n, this._changes = null, this._keys = null, this._delta = null, this._path = null;
  }
  /**
   * Computes the path from `y` to the changed type.
   *
   * @todo v14 should standardize on path: Array<{parent, index}> because that is easier to work with.
   *
   * The following property holds:
   * @example
   *   let type = y
   *   event.path.forEach(dir => {
   *     type = type.get(dir)
   *   })
   *   type === event.target // => true
   */
  get path() {
    return this._path || (this._path = kf(this.currentTarget, this.target));
  }
  /**
   * Check if a struct is deleted by this event.
   *
   * In contrast to change.deleted, this method also returns true if the struct was added and then deleted.
   *
   * @param {AbstractStruct} struct
   * @return {boolean}
   */
  deletes(e) {
    return La(this.transaction.deleteSet, e.id);
  }
  /**
   * @type {Map<string, { action: 'add' | 'update' | 'delete', oldValue: any, newValue: any }>}
   */
  get keys() {
    if (this._keys === null) {
      if (this.transaction.doc._transactionCleanups.length === 0)
        throw Ce(Po);
      const e = /* @__PURE__ */ new Map(), n = this.target;
      this.transaction.changed.get(n).forEach((r) => {
        if (r !== null) {
          const i = (
            /** @type {Item} */
            n._map.get(r)
          );
          let o2, c2;
          if (this.adds(i)) {
            let a2 = i.left;
            for (; a2 !== null && this.adds(a2); )
              a2 = a2.left;
            if (this.deletes(i))
              if (a2 !== null && this.deletes(a2))
                o2 = "delete", c2 = rr(a2.content.getContent());
              else
                return;
            else
              a2 !== null && this.deletes(a2) ? (o2 = "update", c2 = rr(a2.content.getContent())) : (o2 = "add", c2 = void 0);
          } else if (this.deletes(i))
            o2 = "delete", c2 = rr(
              /** @type {Item} */
              i.content.getContent()
            );
          else
            return;
          e.set(r, { action: o2, oldValue: c2 });
        }
      }), this._keys = e;
    }
    return this._keys;
  }
  /**
   * This is a computed property. Note that this can only be safely computed during the
   * event call. Computing this property after other changes happened might result in
   * unexpected behavior (incorrect computation of deltas). A safe way to collect changes
   * is to store the `changes` or the `delta` object. Avoid storing the `transaction` object.
   *
   * @type {Array<{insert?: string | Array<any> | object | AbstractType<any>, retain?: number, delete?: number, attributes?: Object<string, any>}>}
   */
  get delta() {
    return this.changes.delta;
  }
  /**
   * Check if a struct is added by this event.
   *
   * In contrast to change.deleted, this method also returns true if the struct was added and then deleted.
   *
   * @param {AbstractStruct} struct
   * @return {boolean}
   */
  adds(e) {
    return e.id.clock >= (this.transaction.beforeState.get(e.id.client) || 0);
  }
  /**
   * This is a computed property. Note that this can only be safely computed during the
   * event call. Computing this property after other changes happened might result in
   * unexpected behavior (incorrect computation of deltas). A safe way to collect changes
   * is to store the `changes` or the `delta` object. Avoid storing the `transaction` object.
   *
   * @type {{added:Set<Item>,deleted:Set<Item>,keys:Map<string,{action:'add'|'update'|'delete',oldValue:any}>,delta:Array<{insert?:Array<any>|string, delete?:number, retain?:number}>}}
   */
  get changes() {
    let e = this._changes;
    if (e === null) {
      if (this.transaction.doc._transactionCleanups.length === 0)
        throw Ce(Po);
      const n = this.target, s2 = Ve(), r = Ve(), i = [];
      if (e = {
        added: s2,
        deleted: r,
        delta: i,
        keys: this.keys
      }, /** @type Set<string|null> */
      this.transaction.changed.get(n).has(null)) {
        let c2 = null;
        const a2 = () => {
          c2 && i.push(c2);
        };
        for (let l2 = n._start; l2 !== null; l2 = l2.right)
          l2.deleted ? this.deletes(l2) && !this.adds(l2) && ((c2 === null || c2.delete === void 0) && (a2(), c2 = { delete: 0 }), c2.delete += l2.length, r.add(l2)) : this.adds(l2) ? ((c2 === null || c2.insert === void 0) && (a2(), c2 = { insert: [] }), c2.insert = c2.insert.concat(l2.content.getContent()), s2.add(l2)) : ((c2 === null || c2.retain === void 0) && (a2(), c2 = { retain: 0 }), c2.retain += l2.length);
        c2 !== null && c2.retain === void 0 && a2();
      }
      this._changes = e;
    }
    return (
      /** @type {any} */
      e
    );
  }
};
var kf = (t, e) => {
  const n = [];
  for (; e._item !== null && e !== t; ) {
    if (e._item.parentSub !== null)
      n.unshift(e._item.parentSub);
    else {
      let s2 = 0, r = (
        /** @type {AbstractType<any>} */
        e._item.parent._start
      );
      for (; r !== e._item && r !== null; )
        !r.deleted && r.countable && (s2 += r.length), r = r.right;
      n.unshift(s2);
    }
    e = /** @type {AbstractType<any>} */
    e._item.parent;
  }
  return n;
};
var Ka = 80;
var ki = 0;
var xf = class {
  /**
   * @param {Item} p
   * @param {number} index
   */
  constructor(e, n) {
    e.marker = true, this.p = e, this.index = n, this.timestamp = ki++;
  }
};
var Df = (t) => {
  t.timestamp = ki++;
};
var Wa = (t, e, n) => {
  t.p.marker = false, t.p = e, e.marker = true, t.index = n, t.timestamp = ki++;
};
var Tf = (t, e, n) => {
  if (t.length >= Ka) {
    const s2 = t.reduce((r, i) => r.timestamp < i.timestamp ? r : i);
    return Wa(s2, e, n), s2;
  } else {
    const s2 = new xf(e, n);
    return t.push(s2), s2;
  }
};
var Vs = (t, e) => {
  if (t._start === null || e === 0 || t._searchMarker === null)
    return null;
  const n = t._searchMarker.length === 0 ? null : t._searchMarker.reduce((i, o2) => os(e - i.index) < os(e - o2.index) ? i : o2);
  let s2 = t._start, r = 0;
  for (n !== null && (s2 = n.p, r = n.index, Df(n)); s2.right !== null && r < e; ) {
    if (!s2.deleted && s2.countable) {
      if (e < r + s2.length)
        break;
      r += s2.length;
    }
    s2 = s2.right;
  }
  for (; s2.left !== null && r > e; )
    s2 = s2.left, !s2.deleted && s2.countable && (r -= s2.length);
  for (; s2.left !== null && s2.left.id.client === s2.id.client && s2.left.id.clock + s2.left.length === s2.id.clock; )
    s2 = s2.left, !s2.deleted && s2.countable && (r -= s2.length);
  return n !== null && os(n.index - r) < /** @type {YText|YArray<any>} */
  s2.parent.length / Ka ? (Wa(n, s2, r), n) : Tf(t._searchMarker, s2, r);
};
var yn = (t, e, n) => {
  for (let s2 = t.length - 1; s2 >= 0; s2--) {
    const r = t[s2];
    if (n > 0) {
      let i = r.p;
      for (i.marker = false; i && (i.deleted || !i.countable); )
        i = i.left, i && !i.deleted && i.countable && (r.index -= i.length);
      if (i === null || i.marker === true) {
        t.splice(s2, 1);
        continue;
      }
      r.p = i, i.marker = true;
    }
    (e < r.index || n > 0 && e === r.index) && (r.index = vt(e, r.index + n));
  }
};
var Ks = (t, e, n) => {
  const s2 = t, r = e.changedParentTypes;
  for (; ke(r, t, () => []).push(n), t._item !== null; )
    t = /** @type {AbstractType<any>} */
    t._item.parent;
  Fa(s2._eH, n, e);
};
var L = class {
  constructor() {
    this._item = null, this._map = /* @__PURE__ */ new Map(), this._start = null, this.doc = null, this._length = 0, this._eH = To(), this._dEH = To(), this._searchMarker = null;
  }
  /**
   * @return {AbstractType<any>|null}
   */
  get parent() {
    return this._item ? (
      /** @type {AbstractType<any>} */
      this._item.parent
    ) : null;
  }
  /**
   * Integrate this type into the Yjs instance.
   *
   * * Save this struct in the os
   * * This type is sent to other client
   * * Observer functions are fired
   *
   * @param {Doc} y The Yjs instance
   * @param {Item|null} item
   */
  _integrate(e, n) {
    this.doc = e, this._item = n;
  }
  /**
   * @return {AbstractType<EventType>}
   */
  _copy() {
    throw de();
  }
  /**
   * Makes a copy of this data type that can be included somewhere else.
   *
   * Note that the content is only readable _after_ it has been included somewhere in the Ydoc.
   *
   * @return {AbstractType<EventType>}
   */
  clone() {
    throw de();
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} _encoder
   */
  _write(e) {
  }
  /**
   * The first non-deleted item
   */
  get _first() {
    let e = this._start;
    for (; e !== null && e.deleted; )
      e = e.right;
    return e;
  }
  /**
   * Creates YEvent and calls all type observers.
   * Must be implemented by each type.
   *
   * @param {Transaction} transaction
   * @param {Set<null|string>} _parentSubs Keys changed on this type. `null` if list was modified.
   */
  _callObserver(e, n) {
    !e.local && this._searchMarker && (this._searchMarker.length = 0);
  }
  /**
   * Observe all events that are created on this type.
   *
   * @param {function(EventType, Transaction):void} f Observer function
   */
  observe(e) {
    Lo(this._eH, e);
  }
  /**
   * Observe all events that are created by this type and its children.
   *
   * @param {function(Array<YEvent<any>>,Transaction):void} f Observer function
   */
  observeDeep(e) {
    Lo(this._dEH, e);
  }
  /**
   * Unregister an observer function.
   *
   * @param {function(EventType,Transaction):void} f Observer function
   */
  unobserve(e) {
    Io(this._eH, e);
  }
  /**
   * Unregister an observer function.
   *
   * @param {function(Array<YEvent<any>>,Transaction):void} f Observer function
   */
  unobserveDeep(e) {
    Io(this._dEH, e);
  }
  /**
   * @abstract
   * @return {any}
   */
  toJSON() {
  }
};
var Ya = (t, e, n) => {
  e < 0 && (e = t._length + e), n < 0 && (n = t._length + n);
  let s2 = n - e;
  const r = [];
  let i = t._start;
  for (; i !== null && s2 > 0; ) {
    if (i.countable && !i.deleted) {
      const o2 = i.content.getContent();
      if (o2.length <= e)
        e -= o2.length;
      else {
        for (let c2 = e; c2 < o2.length && s2 > 0; c2++)
          r.push(o2[c2]), s2--;
        e = 0;
      }
    }
    i = i.right;
  }
  return r;
};
var Ga = (t) => {
  const e = [];
  let n = t._start;
  for (; n !== null; ) {
    if (n.countable && !n.deleted) {
      const s2 = n.content.getContent();
      for (let r = 0; r < s2.length; r++)
        e.push(s2[r]);
    }
    n = n.right;
  }
  return e;
};
var mn = (t, e) => {
  let n = 0, s2 = t._start;
  for (; s2 !== null; ) {
    if (s2.countable && !s2.deleted) {
      const r = s2.content.getContent();
      for (let i = 0; i < r.length; i++)
        e(r[i], n++, t);
    }
    s2 = s2.right;
  }
};
var qa = (t, e) => {
  const n = [];
  return mn(t, (s2, r) => {
    n.push(e(s2, r, t));
  }), n;
};
var Lf = (t) => {
  let e = t._start, n = null, s2 = 0;
  return {
    [Symbol.iterator]() {
      return this;
    },
    next: () => {
      if (n === null) {
        for (; e !== null && e.deleted; )
          e = e.right;
        if (e === null)
          return {
            done: true,
            value: void 0
          };
        n = e.content.getContent(), s2 = 0, e = e.right;
      }
      const r = n[s2++];
      return n.length <= s2 && (n = null), {
        done: false,
        value: r
      };
    }
  };
};
var Ja = (t, e) => {
  const n = Vs(t, e);
  let s2 = t._start;
  for (n !== null && (s2 = n.p, e -= n.index); s2 !== null; s2 = s2.right)
    if (!s2.deleted && s2.countable) {
      if (e < s2.length)
        return s2.content.getContent()[e];
      e -= s2.length;
    }
};
var vs = (t, e, n, s2) => {
  let r = n;
  const i = t.doc, o2 = i.clientID, c2 = i.store, a2 = n === null ? e._start : n.right;
  let l2 = [];
  const u = () => {
    l2.length > 0 && (r = new P(A(o2, j(c2, o2)), r, r && r.lastId, a2, a2 && a2.id, e, null, new ft(l2)), r.integrate(t, 0), l2 = []);
  };
  s2.forEach((h) => {
    if (h === null)
      l2.push(h);
    else
      switch (h.constructor) {
        case Number:
        case Object:
        case Boolean:
        case Array:
        case String:
          l2.push(h);
          break;
        default:
          switch (u(), h.constructor) {
            case Uint8Array:
            case ArrayBuffer:
              r = new P(A(o2, j(c2, o2)), r, r && r.lastId, a2, a2 && a2.id, e, null, new Rn(new Uint8Array(
                /** @type {Uint8Array} */
                h
              ))), r.integrate(t, 0);
              break;
            case me:
              r = new P(A(o2, j(c2, o2)), r, r && r.lastId, a2, a2 && a2.id, e, null, new Nn(
                /** @type {Doc} */
                h
              )), r.integrate(t, 0);
              break;
            default:
              if (h instanceof L)
                r = new P(A(o2, j(c2, o2)), r, r && r.lastId, a2, a2 && a2.id, e, null, new Oe(h)), r.integrate(t, 0);
              else
                throw new Error("Unexpected content type in insert operation");
          }
      }
  }), u();
};
var Xa = () => Ce("Length exceeded!");
var Za = (t, e, n, s2) => {
  if (n > e._length)
    throw Xa();
  if (n === 0)
    return e._searchMarker && yn(e._searchMarker, n, s2.length), vs(t, e, null, s2);
  const r = n, i = Vs(e, n);
  let o2 = e._start;
  for (i !== null && (o2 = i.p, n -= i.index, n === 0 && (o2 = o2.prev, n += o2 && o2.countable && !o2.deleted ? o2.length : 0)); o2 !== null; o2 = o2.right)
    if (!o2.deleted && o2.countable) {
      if (n <= o2.length) {
        n < o2.length && Ke(t, A(o2.id.client, o2.id.clock + n));
        break;
      }
      n -= o2.length;
    }
  return e._searchMarker && yn(e._searchMarker, r, s2.length), vs(t, e, o2, s2);
};
var If = (t, e, n) => {
  let r = (e._searchMarker || []).reduce((i, o2) => o2.index > i.index ? o2 : i, { index: 0, p: e._start }).p;
  if (r)
    for (; r.right; )
      r = r.right;
  return vs(t, e, r, n);
};
var Qa = (t, e, n, s2) => {
  if (s2 === 0)
    return;
  const r = n, i = s2, o2 = Vs(e, n);
  let c2 = e._start;
  for (o2 !== null && (c2 = o2.p, n -= o2.index); c2 !== null && n > 0; c2 = c2.right)
    !c2.deleted && c2.countable && (n < c2.length && Ke(t, A(c2.id.client, c2.id.clock + n)), n -= c2.length);
  for (; s2 > 0 && c2 !== null; )
    c2.deleted || (s2 < c2.length && Ke(t, A(c2.id.client, c2.id.clock + s2)), c2.delete(t), s2 -= c2.length), c2 = c2.right;
  if (s2 > 0)
    throw Xa();
  e._searchMarker && yn(
    e._searchMarker,
    r,
    -i + s2
    /* in case we remove the above exception */
  );
};
var Cs = (t, e, n) => {
  const s2 = e._map.get(n);
  s2 !== void 0 && s2.delete(t);
};
var xi = (t, e, n, s2) => {
  const r = e._map.get(n) || null, i = t.doc, o2 = i.clientID;
  let c2;
  if (s2 == null)
    c2 = new ft([s2]);
  else
    switch (s2.constructor) {
      case Number:
      case Object:
      case Boolean:
      case Array:
      case String:
        c2 = new ft([s2]);
        break;
      case Uint8Array:
        c2 = new Rn(
          /** @type {Uint8Array} */
          s2
        );
        break;
      case me:
        c2 = new Nn(
          /** @type {Doc} */
          s2
        );
        break;
      default:
        if (s2 instanceof L)
          c2 = new Oe(s2);
        else
          throw new Error("Unexpected content type");
    }
  new P(A(o2, j(i.store, o2)), r, r && r.lastId, null, null, e, n, c2).integrate(t, 0);
};
var Di = (t, e) => {
  const n = t._map.get(e);
  return n !== void 0 && !n.deleted ? n.content.getContent()[n.length - 1] : void 0;
};
var el = (t) => {
  const e = {};
  return t._map.forEach((n, s2) => {
    n.deleted || (e[s2] = n.content.getContent()[n.length - 1]);
  }), e;
};
var tl = (t, e) => {
  const n = t._map.get(e);
  return n !== void 0 && !n.deleted;
};
var Mf = (t, e) => {
  const n = {};
  return t._map.forEach((s2, r) => {
    let i = s2;
    for (; i !== null && (!e.sv.has(i.id.client) || i.id.clock >= (e.sv.get(i.id.client) || 0)); )
      i = i.left;
    i !== null && _t(i, e) && (n[r] = i.content.getContent()[i.length - 1]);
  }), n;
};
var Yn = (t) => Yd(
  t.entries(),
  /** @param {any} entry */
  (e) => !e[1].deleted
);
var $f = class extends Bs {
};
var be = class _be extends L {
  constructor() {
    super(), this._prelimContent = [], this._searchMarker = [];
  }
  /**
   * Construct a new YArray containing the specified items.
   * @template {Object<string,any>|Array<any>|number|null|string|Uint8Array} T
   * @param {Array<T>} items
   * @return {YArray<T>}
   */
  static from(e) {
    const n = new _be();
    return n.push(e), n;
  }
  /**
   * Integrate this type into the Yjs instance.
   *
   * * Save this struct in the os
   * * This type is sent to other client
   * * Observer functions are fired
   *
   * @param {Doc} y The Yjs instance
   * @param {Item} item
   */
  _integrate(e, n) {
    super._integrate(e, n), this.insert(
      0,
      /** @type {Array<any>} */
      this._prelimContent
    ), this._prelimContent = null;
  }
  /**
   * @return {YArray<T>}
   */
  _copy() {
    return new _be();
  }
  /**
   * Makes a copy of this data type that can be included somewhere else.
   *
   * Note that the content is only readable _after_ it has been included somewhere in the Ydoc.
   *
   * @return {YArray<T>}
   */
  clone() {
    const e = new _be();
    return e.insert(0, this.toArray().map(
      (n) => n instanceof L ? (
        /** @type {typeof el} */
        n.clone()
      ) : n
    )), e;
  }
  get length() {
    return this._prelimContent === null ? this._length : this._prelimContent.length;
  }
  /**
   * Creates YArrayEvent and calls observers.
   *
   * @param {Transaction} transaction
   * @param {Set<null|string>} parentSubs Keys changed on this type. `null` if list was modified.
   */
  _callObserver(e, n) {
    super._callObserver(e, n), Ks(this, e, new $f(this, e));
  }
  /**
   * Inserts new content at an index.
   *
   * Important: This function expects an array of content. Not just a content
   * object. The reason for this "weirdness" is that inserting several elements
   * is very efficient when it is done as a single operation.
   *
   * @example
   *  // Insert character 'a' at position 0
   *  yarray.insert(0, ['a'])
   *  // Insert numbers 1, 2 at position 1
   *  yarray.insert(1, [1, 2])
   *
   * @param {number} index The index to insert content at.
   * @param {Array<T>} content The array of content
   */
  insert(e, n) {
    this.doc !== null ? k(this.doc, (s2) => {
      Za(
        s2,
        this,
        e,
        /** @type {any} */
        n
      );
    }) : this._prelimContent.splice(e, 0, ...n);
  }
  /**
   * Appends content to this YArray.
   *
   * @param {Array<T>} content Array of content to append.
   *
   * @todo Use the following implementation in all types.
   */
  push(e) {
    this.doc !== null ? k(this.doc, (n) => {
      If(
        n,
        this,
        /** @type {any} */
        e
      );
    }) : this._prelimContent.push(...e);
  }
  /**
   * Prepends content to this YArray.
   *
   * @param {Array<T>} content Array of content to prepend.
   */
  unshift(e) {
    this.insert(0, e);
  }
  /**
   * Deletes elements starting from an index.
   *
   * @param {number} index Index at which to start deleting elements
   * @param {number} length The number of elements to remove. Defaults to 1.
   */
  delete(e, n = 1) {
    this.doc !== null ? k(this.doc, (s2) => {
      Qa(s2, this, e, n);
    }) : this._prelimContent.splice(e, n);
  }
  /**
   * Returns the i-th element from a YArray.
   *
   * @param {number} index The index of the element to return from the YArray
   * @return {T}
   */
  get(e) {
    return Ja(this, e);
  }
  /**
   * Transforms this YArray to a JavaScript Array.
   *
   * @return {Array<T>}
   */
  toArray() {
    return Ga(this);
  }
  /**
   * Returns a portion of this YArray into a JavaScript Array selected
   * from start to end (end not included).
   *
   * @param {number} [start]
   * @param {number} [end]
   * @return {Array<T>}
   */
  slice(e = 0, n = this.length) {
    return Ya(this, e, n);
  }
  /**
   * Transforms this Shared Type to a JSON object.
   *
   * @return {Array<any>}
   */
  toJSON() {
    return this.map((e) => e instanceof L ? e.toJSON() : e);
  }
  /**
   * Returns an Array with the result of calling a provided function on every
   * element of this YArray.
   *
   * @template M
   * @param {function(T,number,YArray<T>):M} f Function that produces an element of the new Array
   * @return {Array<M>} A new array with each element being the result of the
   *                 callback function
   */
  map(e) {
    return qa(
      this,
      /** @type {any} */
      e
    );
  }
  /**
   * Executes a provided function once on every element of this YArray.
   *
   * @param {function(T,number,YArray<T>):void} f A function to execute on every element of this YArray.
   */
  forEach(e) {
    mn(this, e);
  }
  /**
   * @return {IterableIterator<T>}
   */
  [Symbol.iterator]() {
    return Lf(this);
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   */
  _write(e) {
    e.writeTypeRef(sp);
  }
};
var Of = (t) => new be();
var Pf = class extends Bs {
  /**
   * @param {YMap<T>} ymap The YArray that changed.
   * @param {Transaction} transaction
   * @param {Set<any>} subs The keys that changed.
   */
  constructor(e, n, s2) {
    super(e, n), this.keysChanged = s2;
  }
};
var Se = class _Se extends L {
  /**
   *
   * @param {Iterable<readonly [string, any]>=} entries - an optional iterable to initialize the YMap
   */
  constructor(e) {
    super(), this._prelimContent = null, e === void 0 ? this._prelimContent = /* @__PURE__ */ new Map() : this._prelimContent = new Map(e);
  }
  /**
   * Integrate this type into the Yjs instance.
   *
   * * Save this struct in the os
   * * This type is sent to other client
   * * Observer functions are fired
   *
   * @param {Doc} y The Yjs instance
   * @param {Item} item
   */
  _integrate(e, n) {
    super._integrate(e, n), this._prelimContent.forEach((s2, r) => {
      this.set(r, s2);
    }), this._prelimContent = null;
  }
  /**
   * @return {YMap<MapType>}
   */
  _copy() {
    return new _Se();
  }
  /**
   * Makes a copy of this data type that can be included somewhere else.
   *
   * Note that the content is only readable _after_ it has been included somewhere in the Ydoc.
   *
   * @return {YMap<MapType>}
   */
  clone() {
    const e = new _Se();
    return this.forEach((n, s2) => {
      e.set(s2, n instanceof L ? (
        /** @type {typeof value} */
        n.clone()
      ) : n);
    }), e;
  }
  /**
   * Creates YMapEvent and calls observers.
   *
   * @param {Transaction} transaction
   * @param {Set<null|string>} parentSubs Keys changed on this type. `null` if list was modified.
   */
  _callObserver(e, n) {
    Ks(this, e, new Pf(this, e, n));
  }
  /**
   * Transforms this Shared Type to a JSON object.
   *
   * @return {Object<string,any>}
   */
  toJSON() {
    const e = {};
    return this._map.forEach((n, s2) => {
      if (!n.deleted) {
        const r = n.content.getContent()[n.length - 1];
        e[s2] = r instanceof L ? r.toJSON() : r;
      }
    }), e;
  }
  /**
   * Returns the size of the YMap (count of key/value pairs)
   *
   * @return {number}
   */
  get size() {
    return [...Yn(this._map)].length;
  }
  /**
   * Returns the keys for each element in the YMap Type.
   *
   * @return {IterableIterator<string>}
   */
  keys() {
    return lr(
      Yn(this._map),
      /** @param {any} v */
      (e) => e[0]
    );
  }
  /**
   * Returns the values for each element in the YMap Type.
   *
   * @return {IterableIterator<MapType>}
   */
  values() {
    return lr(
      Yn(this._map),
      /** @param {any} v */
      (e) => e[1].content.getContent()[e[1].length - 1]
    );
  }
  /**
   * Returns an Iterator of [key, value] pairs
   *
   * @return {IterableIterator<[string, MapType]>}
   */
  entries() {
    return lr(
      Yn(this._map),
      /** @param {any} v */
      (e) => (
        /** @type {any} */
        [e[0], e[1].content.getContent()[e[1].length - 1]]
      )
    );
  }
  /**
   * Executes a provided function on once on every key-value pair.
   *
   * @param {function(MapType,string,YMap<MapType>):void} f A function to execute on every element of this YArray.
   */
  forEach(e) {
    this._map.forEach((n, s2) => {
      n.deleted || e(n.content.getContent()[n.length - 1], s2, this);
    });
  }
  /**
   * Returns an Iterator of [key, value] pairs
   *
   * @return {IterableIterator<[string, MapType]>}
   */
  [Symbol.iterator]() {
    return this.entries();
  }
  /**
   * Remove a specified element from this YMap.
   *
   * @param {string} key The key of the element to remove.
   */
  delete(e) {
    this.doc !== null ? k(this.doc, (n) => {
      Cs(n, this, e);
    }) : this._prelimContent.delete(e);
  }
  /**
   * Adds or updates an element with a specified key and value.
   * @template {MapType} VAL
   *
   * @param {string} key The key of the element to add to this YMap
   * @param {VAL} value The value of the element to add
   * @return {VAL}
   */
  set(e, n) {
    return this.doc !== null ? k(this.doc, (s2) => {
      xi(
        s2,
        this,
        e,
        /** @type {any} */
        n
      );
    }) : this._prelimContent.set(e, n), n;
  }
  /**
   * Returns a specified element from this YMap.
   *
   * @param {string} key
   * @return {MapType|undefined}
   */
  get(e) {
    return (
      /** @type {any} */
      Di(this, e)
    );
  }
  /**
   * Returns a boolean indicating whether the specified key exists or not.
   *
   * @param {string} key The key to test.
   * @return {boolean}
   */
  has(e) {
    return tl(this, e);
  }
  /**
   * Removes all elements from this YMap.
   */
  clear() {
    this.doc !== null ? k(this.doc, (e) => {
      this.forEach(function(n, s2, r) {
        Cs(e, r, s2);
      });
    }) : this._prelimContent.clear();
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   */
  _write(e) {
    e.writeTypeRef(rp);
  }
};
var Rf = (t) => new Se();
var Fe = (t, e) => t === e || typeof t == "object" && typeof e == "object" && t && e && $h(t, e);
var Ur = class {
  /**
   * @param {Item|null} left
   * @param {Item|null} right
   * @param {number} index
   * @param {Map<string,any>} currentAttributes
   */
  constructor(e, n, s2, r) {
    this.left = e, this.right = n, this.index = s2, this.currentAttributes = r;
  }
  /**
   * Only call this if you know that this.right is defined
   */
  forward() {
    this.right === null && ce(), this.right.content.constructor === N ? this.right.deleted || Bt(
      this.currentAttributes,
      /** @type {ContentFormat} */
      this.right.content
    ) : this.right.deleted || (this.index += this.right.length), this.left = this.right, this.right = this.right.right;
  }
};
var Ro = (t, e, n) => {
  for (; e.right !== null && n > 0; )
    e.right.content.constructor === N ? e.right.deleted || Bt(
      e.currentAttributes,
      /** @type {ContentFormat} */
      e.right.content
    ) : e.right.deleted || (n < e.right.length && Ke(t, A(e.right.id.client, e.right.id.clock + n)), e.index += e.right.length, n -= e.right.length), e.left = e.right, e.right = e.right.right;
  return e;
};
var Gn = (t, e, n, s2) => {
  const r = /* @__PURE__ */ new Map(), i = s2 ? Vs(e, n) : null;
  if (i) {
    const o2 = new Ur(i.p.left, i.p, i.index, r);
    return Ro(t, o2, n - i.index);
  } else {
    const o2 = new Ur(null, e._start, 0, r);
    return Ro(t, o2, n);
  }
};
var nl = (t, e, n, s2) => {
  for (; n.right !== null && (n.right.deleted === true || n.right.content.constructor === N && Fe(
    s2.get(
      /** @type {ContentFormat} */
      n.right.content.key
    ),
    /** @type {ContentFormat} */
    n.right.content.value
  )); )
    n.right.deleted || s2.delete(
      /** @type {ContentFormat} */
      n.right.content.key
    ), n.forward();
  const r = t.doc, i = r.clientID;
  s2.forEach((o2, c2) => {
    const a2 = n.left, l2 = n.right, u = new P(A(i, j(r.store, i)), a2, a2 && a2.lastId, l2, l2 && l2.id, e, null, new N(c2, o2));
    u.integrate(t, 0), n.right = u, n.forward();
  });
};
var Bt = (t, e) => {
  const { key: n, value: s2 } = e;
  s2 === null ? t.delete(n) : t.set(n, s2);
};
var sl = (t, e) => {
  for (; t.right !== null; ) {
    if (!(t.right.deleted || t.right.content.constructor === N && Fe(
      e[
        /** @type {ContentFormat} */
        t.right.content.key
      ] ?? null,
      /** @type {ContentFormat} */
      t.right.content.value
    ))) break;
    t.forward();
  }
};
var rl = (t, e, n, s2) => {
  const r = t.doc, i = r.clientID, o2 = /* @__PURE__ */ new Map();
  for (const c2 in s2) {
    const a2 = s2[c2], l2 = n.currentAttributes.get(c2) ?? null;
    if (!Fe(l2, a2)) {
      o2.set(c2, l2);
      const { left: u, right: h } = n;
      n.right = new P(A(i, j(r.store, i)), u, u && u.lastId, h, h && h.id, e, null, new N(c2, a2)), n.right.integrate(t, 0), n.forward();
    }
  }
  return o2;
};
var hr = (t, e, n, s2, r) => {
  n.currentAttributes.forEach((d, f2) => {
    r[f2] === void 0 && (r[f2] = null);
  });
  const i = t.doc, o2 = i.clientID;
  sl(n, r);
  const c2 = rl(t, e, n, r), a2 = s2.constructor === String ? new _e(
    /** @type {string} */
    s2
  ) : s2 instanceof L ? new Oe(s2) : new Ct(s2);
  let { left: l2, right: u, index: h } = n;
  e._searchMarker && yn(e._searchMarker, n.index, a2.getLength()), u = new P(A(o2, j(i.store, o2)), l2, l2 && l2.lastId, u, u && u.id, e, null, a2), u.integrate(t, 0), n.right = u, n.index = h, n.forward(), nl(t, e, n, c2);
};
var No = (t, e, n, s2, r) => {
  const i = t.doc, o2 = i.clientID;
  sl(n, r);
  const c2 = rl(t, e, n, r);
  e: for (; n.right !== null && (s2 > 0 || c2.size > 0 && (n.right.deleted || n.right.content.constructor === N)); ) {
    if (!n.right.deleted)
      switch (n.right.content.constructor) {
        case N: {
          const { key: a2, value: l2 } = (
            /** @type {ContentFormat} */
            n.right.content
          ), u = r[a2];
          if (u !== void 0) {
            if (Fe(u, l2))
              c2.delete(a2);
            else {
              if (s2 === 0)
                break e;
              c2.set(a2, l2);
            }
            n.right.delete(t);
          } else
            n.currentAttributes.set(a2, l2);
          break;
        }
        default:
          s2 < n.right.length && Ke(t, A(n.right.id.client, n.right.id.clock + s2)), s2 -= n.right.length;
          break;
      }
    n.forward();
  }
  if (s2 > 0) {
    let a2 = "";
    for (; s2 > 0; s2--)
      a2 += `
`;
    n.right = new P(A(o2, j(i.store, o2)), n.left, n.left && n.left.lastId, n.right, n.right && n.right.id, e, null, new _e(a2)), n.right.integrate(t, 0), n.forward();
  }
  nl(t, e, n, c2);
};
var il = (t, e, n, s2, r) => {
  let i = e;
  const o2 = te();
  for (; i && (!i.countable || i.deleted); ) {
    if (!i.deleted && i.content.constructor === N) {
      const l2 = (
        /** @type {ContentFormat} */
        i.content
      );
      o2.set(l2.key, l2);
    }
    i = i.right;
  }
  let c2 = 0, a2 = false;
  for (; e !== i; ) {
    if (n === e && (a2 = true), !e.deleted) {
      const l2 = e.content;
      if (l2.constructor === N) {
        const { key: u, value: h } = (
          /** @type {ContentFormat} */
          l2
        ), d = s2.get(u) ?? null;
        (o2.get(u) !== l2 || d === h) && (e.delete(t), c2++, !a2 && (r.get(u) ?? null) === h && d !== h && (d === null ? r.delete(u) : r.set(u, d))), !a2 && !e.deleted && Bt(
          r,
          /** @type {ContentFormat} */
          l2
        );
      }
    }
    e = /** @type {Item} */
    e.right;
  }
  return c2;
};
var Nf = (t, e) => {
  for (; e && e.right && (e.right.deleted || !e.right.countable); )
    e = e.right;
  const n = /* @__PURE__ */ new Set();
  for (; e && (e.deleted || !e.countable); ) {
    if (!e.deleted && e.content.constructor === N) {
      const s2 = (
        /** @type {ContentFormat} */
        e.content.key
      );
      n.has(s2) ? e.delete(t) : n.add(s2);
    }
    e = e.left;
  }
};
var Uf = (t) => {
  let e = 0;
  return k(
    /** @type {Doc} */
    t.doc,
    (n) => {
      let s2 = (
        /** @type {Item} */
        t._start
      ), r = t._start, i = te();
      const o2 = Mr(i);
      for (; r; )
        r.deleted === false && (r.content.constructor === N ? Bt(
          o2,
          /** @type {ContentFormat} */
          r.content
        ) : (e += il(n, s2, r, i, o2), i = Mr(o2), s2 = r)), r = r.right;
    }
  ), e;
};
var Ff = (t) => {
  const e = /* @__PURE__ */ new Set(), n = t.doc;
  for (const [s2, r] of t.afterState.entries()) {
    const i = t.beforeState.get(s2) || 0;
    r !== i && za(
      t,
      /** @type {Array<Item|GC>} */
      n.store.clients.get(s2),
      i,
      r,
      (o2) => {
        !o2.deleted && /** @type {Item} */
        o2.content.constructor === N && o2.constructor !== re && e.add(
          /** @type {any} */
          o2.parent
        );
      }
    );
  }
  k(n, (s2) => {
    Ta(t, t.deleteSet, (r) => {
      if (r instanceof re || !/** @type {YText} */
      r.parent._hasFormatting || e.has(
        /** @type {YText} */
        r.parent
      ))
        return;
      const i = (
        /** @type {YText} */
        r.parent
      );
      r.content.constructor === N ? e.add(i) : Nf(s2, r);
    });
    for (const r of e)
      Uf(r);
  });
};
var Uo = (t, e, n) => {
  const s2 = n, r = Mr(e.currentAttributes), i = e.right;
  for (; n > 0 && e.right !== null; ) {
    if (e.right.deleted === false)
      switch (e.right.content.constructor) {
        case Oe:
        case Ct:
        case _e:
          n < e.right.length && Ke(t, A(e.right.id.client, e.right.id.clock + n)), n -= e.right.length, e.right.delete(t);
          break;
      }
    e.forward();
  }
  i && il(t, i, e.right, r, e.currentAttributes);
  const o2 = (
    /** @type {AbstractType<any>} */
    /** @type {Item} */
    (e.left || e.right).parent
  );
  return o2._searchMarker && yn(o2._searchMarker, e.index, -s2 + n), e;
};
var jf = class extends Bs {
  /**
   * @param {YText} ytext
   * @param {Transaction} transaction
   * @param {Set<any>} subs The keys that changed
   */
  constructor(e, n, s2) {
    super(e, n), this.childListChanged = false, this.keysChanged = /* @__PURE__ */ new Set(), s2.forEach((r) => {
      r === null ? this.childListChanged = true : this.keysChanged.add(r);
    });
  }
  /**
   * @type {{added:Set<Item>,deleted:Set<Item>,keys:Map<string,{action:'add'|'update'|'delete',oldValue:any}>,delta:Array<{insert?:Array<any>|string, delete?:number, retain?:number}>}}
   */
  get changes() {
    if (this._changes === null) {
      const e = {
        keys: this.keys,
        delta: this.delta,
        added: /* @__PURE__ */ new Set(),
        deleted: /* @__PURE__ */ new Set()
      };
      this._changes = e;
    }
    return (
      /** @type {any} */
      this._changes
    );
  }
  /**
   * Compute the changes in the delta format.
   * A {@link https://quilljs.com/docs/delta/|Quill Delta}) that represents the changes on the document.
   *
   * @type {Array<{insert?:string|object|AbstractType<any>, delete?:number, retain?:number, attributes?: Object<string,any>}>}
   *
   * @public
   */
  get delta() {
    if (this._delta === null) {
      const e = (
        /** @type {Doc} */
        this.target.doc
      ), n = [];
      k(e, (s2) => {
        const r = /* @__PURE__ */ new Map(), i = /* @__PURE__ */ new Map();
        let o2 = this.target._start, c2 = null;
        const a2 = {};
        let l2 = "", u = 0, h = 0;
        const d = () => {
          if (c2 !== null) {
            let f2 = null;
            switch (c2) {
              case "delete":
                h > 0 && (f2 = { delete: h }), h = 0;
                break;
              case "insert":
                (typeof l2 == "object" || l2.length > 0) && (f2 = { insert: l2 }, r.size > 0 && (f2.attributes = {}, r.forEach((p, g) => {
                  p !== null && (f2.attributes[g] = p);
                }))), l2 = "";
                break;
              case "retain":
                u > 0 && (f2 = { retain: u }, Mh(a2) || (f2.attributes = Dh({}, a2))), u = 0;
                break;
            }
            f2 && n.push(f2), c2 = null;
          }
        };
        for (; o2 !== null; ) {
          switch (o2.content.constructor) {
            case Oe:
            case Ct:
              this.adds(o2) ? this.deletes(o2) || (d(), c2 = "insert", l2 = o2.content.getContent()[0], d()) : this.deletes(o2) ? (c2 !== "delete" && (d(), c2 = "delete"), h += 1) : o2.deleted || (c2 !== "retain" && (d(), c2 = "retain"), u += 1);
              break;
            case _e:
              this.adds(o2) ? this.deletes(o2) || (c2 !== "insert" && (d(), c2 = "insert"), l2 += /** @type {ContentString} */
              o2.content.str) : this.deletes(o2) ? (c2 !== "delete" && (d(), c2 = "delete"), h += o2.length) : o2.deleted || (c2 !== "retain" && (d(), c2 = "retain"), u += o2.length);
              break;
            case N: {
              const { key: f2, value: p } = (
                /** @type {ContentFormat} */
                o2.content
              );
              if (this.adds(o2)) {
                if (!this.deletes(o2)) {
                  const g = r.get(f2) ?? null;
                  Fe(g, p) ? p !== null && o2.delete(s2) : (c2 === "retain" && d(), Fe(p, i.get(f2) ?? null) ? delete a2[f2] : a2[f2] = p);
                }
              } else if (this.deletes(o2)) {
                i.set(f2, p);
                const g = r.get(f2) ?? null;
                Fe(g, p) || (c2 === "retain" && d(), a2[f2] = g);
              } else if (!o2.deleted) {
                i.set(f2, p);
                const g = a2[f2];
                g !== void 0 && (Fe(g, p) ? g !== null && o2.delete(s2) : (c2 === "retain" && d(), p === null ? delete a2[f2] : a2[f2] = p));
              }
              o2.deleted || (c2 === "insert" && d(), Bt(
                r,
                /** @type {ContentFormat} */
                o2.content
              ));
              break;
            }
          }
          o2 = o2.right;
        }
        for (d(); n.length > 0; ) {
          const f2 = n[n.length - 1];
          if (f2.retain !== void 0 && f2.attributes === void 0)
            n.pop();
          else
            break;
        }
      }), this._delta = n;
    }
    return (
      /** @type {any} */
      this._delta
    );
  }
};
var Ie = class _Ie extends L {
  /**
   * @param {String} [string] The initial value of the YText.
   */
  constructor(e) {
    super(), this._pending = e !== void 0 ? [() => this.insert(0, e)] : [], this._searchMarker = [], this._hasFormatting = false;
  }
  /**
   * Number of characters of this text type.
   *
   * @type {number}
   */
  get length() {
    return this._length;
  }
  /**
   * @param {Doc} y
   * @param {Item} item
   */
  _integrate(e, n) {
    super._integrate(e, n);
    try {
      this._pending.forEach((s2) => s2());
    } catch (s2) {
      console.error(s2);
    }
    this._pending = null;
  }
  _copy() {
    return new _Ie();
  }
  /**
   * Makes a copy of this data type that can be included somewhere else.
   *
   * Note that the content is only readable _after_ it has been included somewhere in the Ydoc.
   *
   * @return {YText}
   */
  clone() {
    const e = new _Ie();
    return e.applyDelta(this.toDelta()), e;
  }
  /**
   * Creates YTextEvent and calls observers.
   *
   * @param {Transaction} transaction
   * @param {Set<null|string>} parentSubs Keys changed on this type. `null` if list was modified.
   */
  _callObserver(e, n) {
    super._callObserver(e, n);
    const s2 = new jf(this, e, n);
    Ks(this, e, s2), !e.local && this._hasFormatting && (e._needFormattingCleanup = true);
  }
  /**
   * Returns the unformatted string representation of this YText type.
   *
   * @public
   */
  toString() {
    let e = "", n = this._start;
    for (; n !== null; )
      !n.deleted && n.countable && n.content.constructor === _e && (e += /** @type {ContentString} */
      n.content.str), n = n.right;
    return e;
  }
  /**
   * Returns the unformatted string representation of this YText type.
   *
   * @return {string}
   * @public
   */
  toJSON() {
    return this.toString();
  }
  /**
   * Apply a {@link Delta} on this shared YText type.
   *
   * @param {any} delta The changes to apply on this element.
   * @param {object}  opts
   * @param {boolean} [opts.sanitize] Sanitize input delta. Removes ending newlines if set to true.
   *
   *
   * @public
   */
  applyDelta(e, { sanitize: n = true } = {}) {
    this.doc !== null ? k(this.doc, (s2) => {
      const r = new Ur(null, this._start, 0, /* @__PURE__ */ new Map());
      for (let i = 0; i < e.length; i++) {
        const o2 = e[i];
        if (o2.insert !== void 0) {
          const c2 = !n && typeof o2.insert == "string" && i === e.length - 1 && r.right === null && o2.insert.slice(-1) === `
` ? o2.insert.slice(0, -1) : o2.insert;
          (typeof c2 != "string" || c2.length > 0) && hr(s2, this, r, c2, o2.attributes || {});
        } else o2.retain !== void 0 ? No(s2, this, r, o2.retain, o2.attributes || {}) : o2.delete !== void 0 && Uo(s2, r, o2.delete);
      }
    }) : this._pending.push(() => this.applyDelta(e));
  }
  /**
   * Returns the Delta representation of this YText type.
   *
   * @param {Snapshot} [snapshot]
   * @param {Snapshot} [prevSnapshot]
   * @param {function('removed' | 'added', ID):any} [computeYChange]
   * @return {any} The Delta representation of this type.
   *
   * @public
   */
  toDelta(e, n, s2) {
    const r = [], i = /* @__PURE__ */ new Map(), o2 = (
      /** @type {Doc} */
      this.doc
    );
    let c2 = "", a2 = this._start;
    function l2() {
      if (c2.length > 0) {
        const h = {};
        let d = false;
        i.forEach((p, g) => {
          d = true, h[g] = p;
        });
        const f2 = { insert: c2 };
        d && (f2.attributes = h), r.push(f2), c2 = "";
      }
    }
    const u = () => {
      for (; a2 !== null; ) {
        if (_t(a2, e) || n !== void 0 && _t(a2, n))
          switch (a2.content.constructor) {
            case _e: {
              const h = i.get("ychange");
              e !== void 0 && !_t(a2, e) ? (h === void 0 || h.user !== a2.id.client || h.type !== "removed") && (l2(), i.set("ychange", s2 ? s2("removed", a2.id) : { type: "removed" })) : n !== void 0 && !_t(a2, n) ? (h === void 0 || h.user !== a2.id.client || h.type !== "added") && (l2(), i.set("ychange", s2 ? s2("added", a2.id) : { type: "added" })) : h !== void 0 && (l2(), i.delete("ychange")), c2 += /** @type {ContentString} */
              a2.content.str;
              break;
            }
            case Oe:
            case Ct: {
              l2();
              const h = {
                insert: a2.content.getContent()[0]
              };
              if (i.size > 0) {
                const d = (
                  /** @type {Object<string,any>} */
                  {}
                );
                h.attributes = d, i.forEach((f2, p) => {
                  d[p] = f2;
                });
              }
              r.push(h);
              break;
            }
            case N:
              _t(a2, e) && (l2(), Bt(
                i,
                /** @type {ContentFormat} */
                a2.content
              ));
              break;
          }
        a2 = a2.right;
      }
      l2();
    };
    return e || n ? k(o2, (h) => {
      e && Rr(h, e), n && Rr(h, n), u();
    }, "cleanup") : u(), r;
  }
  /**
   * Insert text at a given index.
   *
   * @param {number} index The index at which to start inserting.
   * @param {String} text The text to insert at the specified position.
   * @param {TextAttributes} [attributes] Optionally define some formatting
   *                                    information to apply on the inserted
   *                                    Text.
   * @public
   */
  insert(e, n, s2) {
    if (n.length <= 0)
      return;
    const r = this.doc;
    r !== null ? k(r, (i) => {
      const o2 = Gn(i, this, e, !s2);
      s2 || (s2 = {}, o2.currentAttributes.forEach((c2, a2) => {
        s2[a2] = c2;
      })), hr(i, this, o2, n, s2);
    }) : this._pending.push(() => this.insert(e, n, s2));
  }
  /**
   * Inserts an embed at a index.
   *
   * @param {number} index The index to insert the embed at.
   * @param {Object | AbstractType<any>} embed The Object that represents the embed.
   * @param {TextAttributes} [attributes] Attribute information to apply on the
   *                                    embed
   *
   * @public
   */
  insertEmbed(e, n, s2) {
    const r = this.doc;
    r !== null ? k(r, (i) => {
      const o2 = Gn(i, this, e, !s2);
      hr(i, this, o2, n, s2 || {});
    }) : this._pending.push(() => this.insertEmbed(e, n, s2 || {}));
  }
  /**
   * Deletes text starting from an index.
   *
   * @param {number} index Index at which to start deleting.
   * @param {number} length The number of characters to remove. Defaults to 1.
   *
   * @public
   */
  delete(e, n) {
    if (n === 0)
      return;
    const s2 = this.doc;
    s2 !== null ? k(s2, (r) => {
      Uo(r, Gn(r, this, e, true), n);
    }) : this._pending.push(() => this.delete(e, n));
  }
  /**
   * Assigns properties to a range of text.
   *
   * @param {number} index The position where to start formatting.
   * @param {number} length The amount of characters to assign properties to.
   * @param {TextAttributes} attributes Attribute information to apply on the
   *                                    text.
   *
   * @public
   */
  format(e, n, s2) {
    if (n === 0)
      return;
    const r = this.doc;
    r !== null ? k(r, (i) => {
      const o2 = Gn(i, this, e, false);
      o2.right !== null && No(i, this, o2, n, s2);
    }) : this._pending.push(() => this.format(e, n, s2));
  }
  /**
   * Removes an attribute.
   *
   * @note Xml-Text nodes don't have attributes. You can use this feature to assign properties to complete text-blocks.
   *
   * @param {String} attributeName The attribute name that is to be removed.
   *
   * @public
   */
  removeAttribute(e) {
    this.doc !== null ? k(this.doc, (n) => {
      Cs(n, this, e);
    }) : this._pending.push(() => this.removeAttribute(e));
  }
  /**
   * Sets or updates an attribute.
   *
   * @note Xml-Text nodes don't have attributes. You can use this feature to assign properties to complete text-blocks.
   *
   * @param {String} attributeName The attribute name that is to be set.
   * @param {any} attributeValue The attribute value that is to be set.
   *
   * @public
   */
  setAttribute(e, n) {
    this.doc !== null ? k(this.doc, (s2) => {
      xi(s2, this, e, n);
    }) : this._pending.push(() => this.setAttribute(e, n));
  }
  /**
   * Returns an attribute value that belongs to the attribute name.
   *
   * @note Xml-Text nodes don't have attributes. You can use this feature to assign properties to complete text-blocks.
   *
   * @param {String} attributeName The attribute name that identifies the
   *                               queried value.
   * @return {any} The queried attribute value.
   *
   * @public
   */
  getAttribute(e) {
    return (
      /** @type {any} */
      Di(this, e)
    );
  }
  /**
   * Returns all attribute name/value pairs in a JSON Object.
   *
   * @note Xml-Text nodes don't have attributes. You can use this feature to assign properties to complete text-blocks.
   *
   * @return {Object<string, any>} A JSON Object that describes the attributes.
   *
   * @public
   */
  getAttributes() {
    return el(this);
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   */
  _write(e) {
    e.writeTypeRef(ip);
  }
};
var Hf = (t) => new Ie();
var dr = class {
  /**
   * @param {YXmlFragment | YXmlElement} root
   * @param {function(AbstractType<any>):boolean} [f]
   */
  constructor(e, n = () => true) {
    this._filter = n, this._root = e, this._currentNode = /** @type {Item} */
    e._start, this._firstCall = true;
  }
  [Symbol.iterator]() {
    return this;
  }
  /**
   * Get the next node.
   *
   * @return {IteratorResult<YXmlElement|YXmlText|YXmlHook>} The next node.
   *
   * @public
   */
  next() {
    let e = this._currentNode, n = e && e.content && /** @type {any} */
    e.content.type;
    if (e !== null && (!this._firstCall || e.deleted || !this._filter(n)))
      do
        if (n = /** @type {any} */
        e.content.type, !e.deleted && (n.constructor === Me || n.constructor === Ee) && n._start !== null)
          e = n._start;
        else
          for (; e !== null; )
            if (e.right !== null) {
              e = e.right;
              break;
            } else e.parent === this._root ? e = null : e = /** @type {AbstractType<any>} */
            e.parent._item;
      while (e !== null && (e.deleted || !this._filter(
        /** @type {ContentType} */
        e.content.type
      )));
    return this._firstCall = false, e === null ? { value: void 0, done: true } : (this._currentNode = e, { value: (
      /** @type {any} */
      e.content.type
    ), done: false });
  }
};
var Ee = class _Ee extends L {
  constructor() {
    super(), this._prelimContent = [];
  }
  /**
   * @type {YXmlElement|YXmlText|null}
   */
  get firstChild() {
    const e = this._first;
    return e ? e.content.getContent()[0] : null;
  }
  /**
   * Integrate this type into the Yjs instance.
   *
   * * Save this struct in the os
   * * This type is sent to other client
   * * Observer functions are fired
   *
   * @param {Doc} y The Yjs instance
   * @param {Item} item
   */
  _integrate(e, n) {
    super._integrate(e, n), this.insert(
      0,
      /** @type {Array<any>} */
      this._prelimContent
    ), this._prelimContent = null;
  }
  _copy() {
    return new _Ee();
  }
  /**
   * Makes a copy of this data type that can be included somewhere else.
   *
   * Note that the content is only readable _after_ it has been included somewhere in the Ydoc.
   *
   * @return {YXmlFragment}
   */
  clone() {
    const e = new _Ee();
    return e.insert(0, this.toArray().map((n) => n instanceof L ? n.clone() : n)), e;
  }
  get length() {
    return this._prelimContent === null ? this._length : this._prelimContent.length;
  }
  /**
   * Create a subtree of childNodes.
   *
   * @example
   * const walker = elem.createTreeWalker(dom => dom.nodeName === 'div')
   * for (let node in walker) {
   *   // `node` is a div node
   *   nop(node)
   * }
   *
   * @param {function(AbstractType<any>):boolean} filter Function that is called on each child element and
   *                          returns a Boolean indicating whether the child
   *                          is to be included in the subtree.
   * @return {YXmlTreeWalker} A subtree and a position within it.
   *
   * @public
   */
  createTreeWalker(e) {
    return new dr(this, e);
  }
  /**
   * Returns the first YXmlElement that matches the query.
   * Similar to DOM's {@link querySelector}.
   *
   * Query support:
   *   - tagname
   * TODO:
   *   - id
   *   - attribute
   *
   * @param {CSS_Selector} query The query on the children.
   * @return {YXmlElement|YXmlText|YXmlHook|null} The first element that matches the query or null.
   *
   * @public
   */
  querySelector(e) {
    e = e.toUpperCase();
    const s2 = new dr(this, (r) => r.nodeName && r.nodeName.toUpperCase() === e).next();
    return s2.done ? null : s2.value;
  }
  /**
   * Returns all YXmlElements that match the query.
   * Similar to Dom's {@link querySelectorAll}.
   *
   * @todo Does not yet support all queries. Currently only query by tagName.
   *
   * @param {CSS_Selector} query The query on the children
   * @return {Array<YXmlElement|YXmlText|YXmlHook|null>} The elements that match this query.
   *
   * @public
   */
  querySelectorAll(e) {
    return e = e.toUpperCase(), Le(new dr(this, (n) => n.nodeName && n.nodeName.toUpperCase() === e));
  }
  /**
   * Creates YXmlEvent and calls observers.
   *
   * @param {Transaction} transaction
   * @param {Set<null|string>} parentSubs Keys changed on this type. `null` if list was modified.
   */
  _callObserver(e, n) {
    Ks(this, e, new Vf(this, n, e));
  }
  /**
   * Get the string representation of all the children of this YXmlFragment.
   *
   * @return {string} The string representation of all children.
   */
  toString() {
    return qa(this, (e) => e.toString()).join("");
  }
  /**
   * @return {string}
   */
  toJSON() {
    return this.toString();
  }
  /**
   * Creates a Dom Element that mirrors this YXmlElement.
   *
   * @param {Document} [_document=document] The document object (you must define
   *                                        this when calling this method in
   *                                        nodejs)
   * @param {Object<string, any>} [hooks={}] Optional property to customize how hooks
   *                                             are presented in the DOM
   * @param {any} [binding] You should not set this property. This is
   *                               used if DomBinding wants to create a
   *                               association to the created DOM type.
   * @return {Node} The {@link https://developer.mozilla.org/en-US/docs/Web/API/Element|Dom Element}
   *
   * @public
   */
  toDOM(e = document, n = {}, s2) {
    const r = e.createDocumentFragment();
    return s2 !== void 0 && s2._createAssociation(r, this), mn(this, (i) => {
      r.insertBefore(i.toDOM(e, n, s2), null);
    }), r;
  }
  /**
   * Inserts new content at an index.
   *
   * @example
   *  // Insert character 'a' at position 0
   *  xml.insert(0, [new Y.XmlText('text')])
   *
   * @param {number} index The index to insert content at
   * @param {Array<YXmlElement|YXmlText>} content The array of content
   */
  insert(e, n) {
    this.doc !== null ? k(this.doc, (s2) => {
      Za(s2, this, e, n);
    }) : this._prelimContent.splice(e, 0, ...n);
  }
  /**
   * Inserts new content at an index.
   *
   * @example
   *  // Insert character 'a' at position 0
   *  xml.insert(0, [new Y.XmlText('text')])
   *
   * @param {null|Item|YXmlElement|YXmlText} ref The index to insert content at
   * @param {Array<YXmlElement|YXmlText>} content The array of content
   */
  insertAfter(e, n) {
    if (this.doc !== null)
      k(this.doc, (s2) => {
        const r = e && e instanceof L ? e._item : e;
        vs(s2, this, r, n);
      });
    else {
      const s2 = (
        /** @type {Array<any>} */
        this._prelimContent
      ), r = e === null ? 0 : s2.findIndex((i) => i === e) + 1;
      if (r === 0 && e !== null)
        throw Ce("Reference item not found");
      s2.splice(r, 0, ...n);
    }
  }
  /**
   * Deletes elements starting from an index.
   *
   * @param {number} index Index at which to start deleting elements
   * @param {number} [length=1] The number of elements to remove. Defaults to 1.
   */
  delete(e, n = 1) {
    this.doc !== null ? k(this.doc, (s2) => {
      Qa(s2, this, e, n);
    }) : this._prelimContent.splice(e, n);
  }
  /**
   * Transforms this YArray to a JavaScript Array.
   *
   * @return {Array<YXmlElement|YXmlText|YXmlHook>}
   */
  toArray() {
    return Ga(this);
  }
  /**
   * Appends content to this YArray.
   *
   * @param {Array<YXmlElement|YXmlText>} content Array of content to append.
   */
  push(e) {
    this.insert(this.length, e);
  }
  /**
   * Prepends content to this YArray.
   *
   * @param {Array<YXmlElement|YXmlText>} content Array of content to prepend.
   */
  unshift(e) {
    this.insert(0, e);
  }
  /**
   * Returns the i-th element from a YArray.
   *
   * @param {number} index The index of the element to return from the YArray
   * @return {YXmlElement|YXmlText}
   */
  get(e) {
    return Ja(this, e);
  }
  /**
   * Returns a portion of this YXmlFragment into a JavaScript Array selected
   * from start to end (end not included).
   *
   * @param {number} [start]
   * @param {number} [end]
   * @return {Array<YXmlElement|YXmlText>}
   */
  slice(e = 0, n = this.length) {
    return Ya(this, e, n);
  }
  /**
   * Executes a provided function on once on every child element.
   *
   * @param {function(YXmlElement|YXmlText,number, typeof self):void} f A function to execute on every element of this YArray.
   */
  forEach(e) {
    mn(this, e);
  }
  /**
   * Transform the properties of this type to binary and write it to an
   * BinaryEncoder.
   *
   * This is called when this Item is sent to a remote peer.
   *
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder The encoder to write data to.
   */
  _write(e) {
    e.writeTypeRef(cp);
  }
};
var zf = (t) => new Ee();
var Me = class _Me extends Ee {
  constructor(e = "UNDEFINED") {
    super(), this.nodeName = e, this._prelimAttrs = /* @__PURE__ */ new Map();
  }
  /**
   * @type {YXmlElement|YXmlText|null}
   */
  get nextSibling() {
    const e = this._item ? this._item.next : null;
    return e ? (
      /** @type {YXmlElement|YXmlText} */
      /** @type {ContentType} */
      e.content.type
    ) : null;
  }
  /**
   * @type {YXmlElement|YXmlText|null}
   */
  get prevSibling() {
    const e = this._item ? this._item.prev : null;
    return e ? (
      /** @type {YXmlElement|YXmlText} */
      /** @type {ContentType} */
      e.content.type
    ) : null;
  }
  /**
   * Integrate this type into the Yjs instance.
   *
   * * Save this struct in the os
   * * This type is sent to other client
   * * Observer functions are fired
   *
   * @param {Doc} y The Yjs instance
   * @param {Item} item
   */
  _integrate(e, n) {
    super._integrate(e, n), /** @type {Map<string, any>} */
    this._prelimAttrs.forEach((s2, r) => {
      this.setAttribute(r, s2);
    }), this._prelimAttrs = null;
  }
  /**
   * Creates an Item with the same effect as this Item (without position effect)
   *
   * @return {YXmlElement}
   */
  _copy() {
    return new _Me(this.nodeName);
  }
  /**
   * Makes a copy of this data type that can be included somewhere else.
   *
   * Note that the content is only readable _after_ it has been included somewhere in the Ydoc.
   *
   * @return {YXmlElement<KV>}
   */
  clone() {
    const e = new _Me(this.nodeName), n = this.getAttributes();
    return Lh(n, (s2, r) => {
      typeof s2 == "string" && e.setAttribute(r, s2);
    }), e.insert(0, this.toArray().map((s2) => s2 instanceof L ? s2.clone() : s2)), e;
  }
  /**
   * Returns the XML serialization of this YXmlElement.
   * The attributes are ordered by attribute-name, so you can easily use this
   * method to compare YXmlElements
   *
   * @return {string} The string representation of this type.
   *
   * @public
   */
  toString() {
    const e = this.getAttributes(), n = [], s2 = [];
    for (const c2 in e)
      s2.push(c2);
    s2.sort();
    const r = s2.length;
    for (let c2 = 0; c2 < r; c2++) {
      const a2 = s2[c2];
      n.push(a2 + '="' + e[a2] + '"');
    }
    const i = this.nodeName.toLocaleLowerCase(), o2 = n.length > 0 ? " " + n.join(" ") : "";
    return `<${i}${o2}>${super.toString()}</${i}>`;
  }
  /**
   * Removes an attribute from this YXmlElement.
   *
   * @param {string} attributeName The attribute name that is to be removed.
   *
   * @public
   */
  removeAttribute(e) {
    this.doc !== null ? k(this.doc, (n) => {
      Cs(n, this, e);
    }) : this._prelimAttrs.delete(e);
  }
  /**
   * Sets or updates an attribute.
   *
   * @template {keyof KV & string} KEY
   *
   * @param {KEY} attributeName The attribute name that is to be set.
   * @param {KV[KEY]} attributeValue The attribute value that is to be set.
   *
   * @public
   */
  setAttribute(e, n) {
    this.doc !== null ? k(this.doc, (s2) => {
      xi(s2, this, e, n);
    }) : this._prelimAttrs.set(e, n);
  }
  /**
   * Returns an attribute value that belongs to the attribute name.
   *
   * @template {keyof KV & string} KEY
   *
   * @param {KEY} attributeName The attribute name that identifies the
   *                               queried value.
   * @return {KV[KEY]|undefined} The queried attribute value.
   *
   * @public
   */
  getAttribute(e) {
    return (
      /** @type {any} */
      Di(this, e)
    );
  }
  /**
   * Returns whether an attribute exists
   *
   * @param {string} attributeName The attribute name to check for existence.
   * @return {boolean} whether the attribute exists.
   *
   * @public
   */
  hasAttribute(e) {
    return (
      /** @type {any} */
      tl(this, e)
    );
  }
  /**
   * Returns all attribute name/value pairs in a JSON Object.
   *
   * @param {Snapshot} [snapshot]
   * @return {{ [Key in Extract<keyof KV,string>]?: KV[Key]}} A JSON Object that describes the attributes.
   *
   * @public
   */
  getAttributes(e) {
    return (
      /** @type {any} */
      e ? Mf(this, e) : el(this)
    );
  }
  /**
   * Creates a Dom Element that mirrors this YXmlElement.
   *
   * @param {Document} [_document=document] The document object (you must define
   *                                        this when calling this method in
   *                                        nodejs)
   * @param {Object<string, any>} [hooks={}] Optional property to customize how hooks
   *                                             are presented in the DOM
   * @param {any} [binding] You should not set this property. This is
   *                               used if DomBinding wants to create a
   *                               association to the created DOM type.
   * @return {Node} The {@link https://developer.mozilla.org/en-US/docs/Web/API/Element|Dom Element}
   *
   * @public
   */
  toDOM(e = document, n = {}, s2) {
    const r = e.createElement(this.nodeName), i = this.getAttributes();
    for (const o2 in i) {
      const c2 = i[o2];
      typeof c2 == "string" && r.setAttribute(o2, c2);
    }
    return mn(this, (o2) => {
      r.appendChild(o2.toDOM(e, n, s2));
    }), s2 !== void 0 && s2._createAssociation(r, this), r;
  }
  /**
   * Transform the properties of this type to binary and write it to an
   * BinaryEncoder.
   *
   * This is called when this Item is sent to a remote peer.
   *
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder The encoder to write data to.
   */
  _write(e) {
    e.writeTypeRef(op), e.writeKey(this.nodeName);
  }
};
var Bf = (t) => new Me(t.readKey());
var Vf = class extends Bs {
  /**
   * @param {YXmlElement|YXmlText|YXmlFragment} target The target on which the event is created.
   * @param {Set<string|null>} subs The set of changed attributes. `null` is included if the
   *                   child list changed.
   * @param {Transaction} transaction The transaction instance with wich the
   *                                  change was created.
   */
  constructor(e, n, s2) {
    super(e, s2), this.childListChanged = false, this.attributesChanged = /* @__PURE__ */ new Set(), n.forEach((r) => {
      r === null ? this.childListChanged = true : this.attributesChanged.add(r);
    });
  }
};
var Nt = class _Nt extends Se {
  /**
   * @param {string} hookName nodeName of the Dom Node.
   */
  constructor(e) {
    super(), this.hookName = e;
  }
  /**
   * Creates an Item with the same effect as this Item (without position effect)
   */
  _copy() {
    return new _Nt(this.hookName);
  }
  /**
   * Makes a copy of this data type that can be included somewhere else.
   *
   * Note that the content is only readable _after_ it has been included somewhere in the Ydoc.
   *
   * @return {YXmlHook}
   */
  clone() {
    const e = new _Nt(this.hookName);
    return this.forEach((n, s2) => {
      e.set(s2, n);
    }), e;
  }
  /**
   * Creates a Dom Element that mirrors this YXmlElement.
   *
   * @param {Document} [_document=document] The document object (you must define
   *                                        this when calling this method in
   *                                        nodejs)
   * @param {Object.<string, any>} [hooks] Optional property to customize how hooks
   *                                             are presented in the DOM
   * @param {any} [binding] You should not set this property. This is
   *                               used if DomBinding wants to create a
   *                               association to the created DOM type
   * @return {Element} The {@link https://developer.mozilla.org/en-US/docs/Web/API/Element|Dom Element}
   *
   * @public
   */
  toDOM(e = document, n = {}, s2) {
    const r = n[this.hookName];
    let i;
    return r !== void 0 ? i = r.createDom(this) : i = document.createElement(this.hookName), i.setAttribute("data-yjs-hook", this.hookName), s2 !== void 0 && s2._createAssociation(i, this), i;
  }
  /**
   * Transform the properties of this type to binary and write it to an
   * BinaryEncoder.
   *
   * This is called when this Item is sent to a remote peer.
   *
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder The encoder to write data to.
   */
  _write(e) {
    e.writeTypeRef(ap), e.writeKey(this.hookName);
  }
};
var Kf = (t) => new Nt(t.readKey());
var dt = class _dt extends Ie {
  /**
   * @type {YXmlElement|YXmlText|null}
   */
  get nextSibling() {
    const e = this._item ? this._item.next : null;
    return e ? (
      /** @type {YXmlElement|YXmlText} */
      /** @type {ContentType} */
      e.content.type
    ) : null;
  }
  /**
   * @type {YXmlElement|YXmlText|null}
   */
  get prevSibling() {
    const e = this._item ? this._item.prev : null;
    return e ? (
      /** @type {YXmlElement|YXmlText} */
      /** @type {ContentType} */
      e.content.type
    ) : null;
  }
  _copy() {
    return new _dt();
  }
  /**
   * Makes a copy of this data type that can be included somewhere else.
   *
   * Note that the content is only readable _after_ it has been included somewhere in the Ydoc.
   *
   * @return {YXmlText}
   */
  clone() {
    const e = new _dt();
    return e.applyDelta(this.toDelta()), e;
  }
  /**
   * Creates a Dom Element that mirrors this YXmlText.
   *
   * @param {Document} [_document=document] The document object (you must define
   *                                        this when calling this method in
   *                                        nodejs)
   * @param {Object<string, any>} [hooks] Optional property to customize how hooks
   *                                             are presented in the DOM
   * @param {any} [binding] You should not set this property. This is
   *                               used if DomBinding wants to create a
   *                               association to the created DOM type.
   * @return {Text} The {@link https://developer.mozilla.org/en-US/docs/Web/API/Element|Dom Element}
   *
   * @public
   */
  toDOM(e = document, n, s2) {
    const r = e.createTextNode(this.toString());
    return s2 !== void 0 && s2._createAssociation(r, this), r;
  }
  toString() {
    return this.toDelta().map((e) => {
      const n = [];
      for (const r in e.attributes) {
        const i = [];
        for (const o2 in e.attributes[r])
          i.push({ key: o2, value: e.attributes[r][o2] });
        i.sort((o2, c2) => o2.key < c2.key ? -1 : 1), n.push({ nodeName: r, attrs: i });
      }
      n.sort((r, i) => r.nodeName < i.nodeName ? -1 : 1);
      let s2 = "";
      for (let r = 0; r < n.length; r++) {
        const i = n[r];
        s2 += `<${i.nodeName}`;
        for (let o2 = 0; o2 < i.attrs.length; o2++) {
          const c2 = i.attrs[o2];
          s2 += ` ${c2.key}="${c2.value}"`;
        }
        s2 += ">";
      }
      s2 += e.insert;
      for (let r = n.length - 1; r >= 0; r--)
        s2 += `</${n[r].nodeName}>`;
      return s2;
    }).join("");
  }
  /**
   * @return {string}
   */
  toJSON() {
    return this.toString();
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   */
  _write(e) {
    e.writeTypeRef(lp);
  }
};
var Wf = (t) => new dt();
var Ti = class {
  /**
   * @param {ID} id
   * @param {number} length
   */
  constructor(e, n) {
    this.id = e, this.length = n;
  }
  /**
   * @type {boolean}
   */
  get deleted() {
    throw de();
  }
  /**
   * Merge this struct with the item to the right.
   * This method is already assuming that `this.id.clock + this.length === this.id.clock`.
   * Also this method does *not* remove right from StructStore!
   * @param {AbstractStruct} right
   * @return {boolean} wether this merged with right
   */
  mergeWith(e) {
    return false;
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder The encoder to write data to.
   * @param {number} offset
   * @param {number} encodingRef
   */
  write(e, n, s2) {
    throw de();
  }
  /**
   * @param {Transaction} transaction
   * @param {number} offset
   */
  integrate(e, n) {
    throw de();
  }
};
var Yf = 0;
var re = class extends Ti {
  get deleted() {
    return true;
  }
  delete() {
  }
  /**
   * @param {GC} right
   * @return {boolean}
   */
  mergeWith(e) {
    return this.constructor !== e.constructor ? false : (this.length += e.length, true);
  }
  /**
   * @param {Transaction} transaction
   * @param {number} offset
   */
  integrate(e, n) {
    n > 0 && (this.id.clock += n, this.length -= n), Ha(e.doc.store, this);
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(e, n) {
    e.writeInfo(Yf), e.writeLen(this.length - n);
  }
  /**
   * @param {Transaction} transaction
   * @param {StructStore} store
   * @return {null | number}
   */
  getMissing(e, n) {
    return null;
  }
};
var Rn = class _Rn {
  /**
   * @param {Uint8Array} content
   */
  constructor(e) {
    this.content = e;
  }
  /**
   * @return {number}
   */
  getLength() {
    return 1;
  }
  /**
   * @return {Array<any>}
   */
  getContent() {
    return [this.content];
  }
  /**
   * @return {boolean}
   */
  isCountable() {
    return true;
  }
  /**
   * @return {ContentBinary}
   */
  copy() {
    return new _Rn(this.content);
  }
  /**
   * @param {number} offset
   * @return {ContentBinary}
   */
  splice(e) {
    throw de();
  }
  /**
   * @param {ContentBinary} right
   * @return {boolean}
   */
  mergeWith(e) {
    return false;
  }
  /**
   * @param {Transaction} transaction
   * @param {Item} item
   */
  integrate(e, n) {
  }
  /**
   * @param {Transaction} transaction
   */
  delete(e) {
  }
  /**
   * @param {StructStore} store
   */
  gc(e) {
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(e, n) {
    e.writeBuf(this.content);
  }
  /**
   * @return {number}
   */
  getRef() {
    return 3;
  }
};
var Gf = (t) => new Rn(t.readBuf());
var wn = class _wn {
  /**
   * @param {number} len
   */
  constructor(e) {
    this.len = e;
  }
  /**
   * @return {number}
   */
  getLength() {
    return this.len;
  }
  /**
   * @return {Array<any>}
   */
  getContent() {
    return [];
  }
  /**
   * @return {boolean}
   */
  isCountable() {
    return false;
  }
  /**
   * @return {ContentDeleted}
   */
  copy() {
    return new _wn(this.len);
  }
  /**
   * @param {number} offset
   * @return {ContentDeleted}
   */
  splice(e) {
    const n = new _wn(this.len - e);
    return this.len = e, n;
  }
  /**
   * @param {ContentDeleted} right
   * @return {boolean}
   */
  mergeWith(e) {
    return this.len += e.len, true;
  }
  /**
   * @param {Transaction} transaction
   * @param {Item} item
   */
  integrate(e, n) {
    ws(e.deleteSet, n.id.client, n.id.clock, this.len), n.markDeleted();
  }
  /**
   * @param {Transaction} transaction
   */
  delete(e) {
  }
  /**
   * @param {StructStore} store
   */
  gc(e) {
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(e, n) {
    e.writeLen(this.len - n);
  }
  /**
   * @return {number}
   */
  getRef() {
    return 1;
  }
};
var qf = (t) => new wn(t.readLen());
var ol = (t, e) => new me({ guid: t, ...e, shouldLoad: e.shouldLoad || e.autoLoad || false });
var Nn = class _Nn {
  /**
   * @param {Doc} doc
   */
  constructor(e) {
    e._item && console.error("This document was already integrated as a sub-document. You should create a second instance instead with the same guid."), this.doc = e;
    const n = {};
    this.opts = n, e.gc || (n.gc = false), e.autoLoad && (n.autoLoad = true), e.meta !== null && (n.meta = e.meta);
  }
  /**
   * @return {number}
   */
  getLength() {
    return 1;
  }
  /**
   * @return {Array<any>}
   */
  getContent() {
    return [this.doc];
  }
  /**
   * @return {boolean}
   */
  isCountable() {
    return true;
  }
  /**
   * @return {ContentDoc}
   */
  copy() {
    return new _Nn(ol(this.doc.guid, this.opts));
  }
  /**
   * @param {number} offset
   * @return {ContentDoc}
   */
  splice(e) {
    throw de();
  }
  /**
   * @param {ContentDoc} right
   * @return {boolean}
   */
  mergeWith(e) {
    return false;
  }
  /**
   * @param {Transaction} transaction
   * @param {Item} item
   */
  integrate(e, n) {
    this.doc._item = n, e.subdocsAdded.add(this.doc), this.doc.shouldLoad && e.subdocsLoaded.add(this.doc);
  }
  /**
   * @param {Transaction} transaction
   */
  delete(e) {
    e.subdocsAdded.has(this.doc) ? e.subdocsAdded.delete(this.doc) : e.subdocsRemoved.add(this.doc);
  }
  /**
   * @param {StructStore} store
   */
  gc(e) {
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(e, n) {
    e.writeString(this.doc.guid), e.writeAny(this.opts);
  }
  /**
   * @return {number}
   */
  getRef() {
    return 9;
  }
};
var Jf = (t) => new Nn(ol(t.readString(), t.readAny()));
var Ct = class _Ct {
  /**
   * @param {Object} embed
   */
  constructor(e) {
    this.embed = e;
  }
  /**
   * @return {number}
   */
  getLength() {
    return 1;
  }
  /**
   * @return {Array<any>}
   */
  getContent() {
    return [this.embed];
  }
  /**
   * @return {boolean}
   */
  isCountable() {
    return true;
  }
  /**
   * @return {ContentEmbed}
   */
  copy() {
    return new _Ct(this.embed);
  }
  /**
   * @param {number} offset
   * @return {ContentEmbed}
   */
  splice(e) {
    throw de();
  }
  /**
   * @param {ContentEmbed} right
   * @return {boolean}
   */
  mergeWith(e) {
    return false;
  }
  /**
   * @param {Transaction} transaction
   * @param {Item} item
   */
  integrate(e, n) {
  }
  /**
   * @param {Transaction} transaction
   */
  delete(e) {
  }
  /**
   * @param {StructStore} store
   */
  gc(e) {
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(e, n) {
    e.writeJSON(this.embed);
  }
  /**
   * @return {number}
   */
  getRef() {
    return 5;
  }
};
var Xf = (t) => new Ct(t.readJSON());
var N = class _N {
  /**
   * @param {string} key
   * @param {Object} value
   */
  constructor(e, n) {
    this.key = e, this.value = n;
  }
  /**
   * @return {number}
   */
  getLength() {
    return 1;
  }
  /**
   * @return {Array<any>}
   */
  getContent() {
    return [];
  }
  /**
   * @return {boolean}
   */
  isCountable() {
    return false;
  }
  /**
   * @return {ContentFormat}
   */
  copy() {
    return new _N(this.key, this.value);
  }
  /**
   * @param {number} _offset
   * @return {ContentFormat}
   */
  splice(e) {
    throw de();
  }
  /**
   * @param {ContentFormat} _right
   * @return {boolean}
   */
  mergeWith(e) {
    return false;
  }
  /**
   * @param {Transaction} _transaction
   * @param {Item} item
   */
  integrate(e, n) {
    const s2 = (
      /** @type {YText} */
      n.parent
    );
    s2._searchMarker = null, s2._hasFormatting = true;
  }
  /**
   * @param {Transaction} transaction
   */
  delete(e) {
  }
  /**
   * @param {StructStore} store
   */
  gc(e) {
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(e, n) {
    e.writeKey(this.key), e.writeJSON(this.value);
  }
  /**
   * @return {number}
   */
  getRef() {
    return 6;
  }
};
var Zf = (t) => new N(t.readKey(), t.readJSON());
var Ss = class _Ss {
  /**
   * @param {Array<any>} arr
   */
  constructor(e) {
    this.arr = e;
  }
  /**
   * @return {number}
   */
  getLength() {
    return this.arr.length;
  }
  /**
   * @return {Array<any>}
   */
  getContent() {
    return this.arr;
  }
  /**
   * @return {boolean}
   */
  isCountable() {
    return true;
  }
  /**
   * @return {ContentJSON}
   */
  copy() {
    return new _Ss(this.arr);
  }
  /**
   * @param {number} offset
   * @return {ContentJSON}
   */
  splice(e) {
    const n = new _Ss(this.arr.slice(e));
    return this.arr = this.arr.slice(0, e), n;
  }
  /**
   * @param {ContentJSON} right
   * @return {boolean}
   */
  mergeWith(e) {
    return this.arr = this.arr.concat(e.arr), true;
  }
  /**
   * @param {Transaction} transaction
   * @param {Item} item
   */
  integrate(e, n) {
  }
  /**
   * @param {Transaction} transaction
   */
  delete(e) {
  }
  /**
   * @param {StructStore} store
   */
  gc(e) {
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(e, n) {
    const s2 = this.arr.length;
    e.writeLen(s2 - n);
    for (let r = n; r < s2; r++) {
      const i = this.arr[r];
      e.writeString(i === void 0 ? "undefined" : JSON.stringify(i));
    }
  }
  /**
   * @return {number}
   */
  getRef() {
    return 2;
  }
};
var Qf = (t) => {
  const e = t.readLen(), n = [];
  for (let s2 = 0; s2 < e; s2++) {
    const r = t.readString();
    r === "undefined" ? n.push(void 0) : n.push(JSON.parse(r));
  }
  return new Ss(n);
};
var ft = class _ft {
  /**
   * @param {Array<any>} arr
   */
  constructor(e) {
    this.arr = e;
  }
  /**
   * @return {number}
   */
  getLength() {
    return this.arr.length;
  }
  /**
   * @return {Array<any>}
   */
  getContent() {
    return this.arr;
  }
  /**
   * @return {boolean}
   */
  isCountable() {
    return true;
  }
  /**
   * @return {ContentAny}
   */
  copy() {
    return new _ft(this.arr);
  }
  /**
   * @param {number} offset
   * @return {ContentAny}
   */
  splice(e) {
    const n = new _ft(this.arr.slice(e));
    return this.arr = this.arr.slice(0, e), n;
  }
  /**
   * @param {ContentAny} right
   * @return {boolean}
   */
  mergeWith(e) {
    return this.arr = this.arr.concat(e.arr), true;
  }
  /**
   * @param {Transaction} transaction
   * @param {Item} item
   */
  integrate(e, n) {
  }
  /**
   * @param {Transaction} transaction
   */
  delete(e) {
  }
  /**
   * @param {StructStore} store
   */
  gc(e) {
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(e, n) {
    const s2 = this.arr.length;
    e.writeLen(s2 - n);
    for (let r = n; r < s2; r++) {
      const i = this.arr[r];
      e.writeAny(i);
    }
  }
  /**
   * @return {number}
   */
  getRef() {
    return 8;
  }
};
var ep = (t) => {
  const e = t.readLen(), n = [];
  for (let s2 = 0; s2 < e; s2++)
    n.push(t.readAny());
  return new ft(n);
};
var _e = class __e {
  /**
   * @param {string} str
   */
  constructor(e) {
    this.str = e;
  }
  /**
   * @return {number}
   */
  getLength() {
    return this.str.length;
  }
  /**
   * @return {Array<any>}
   */
  getContent() {
    return this.str.split("");
  }
  /**
   * @return {boolean}
   */
  isCountable() {
    return true;
  }
  /**
   * @return {ContentString}
   */
  copy() {
    return new __e(this.str);
  }
  /**
   * @param {number} offset
   * @return {ContentString}
   */
  splice(e) {
    const n = new __e(this.str.slice(e));
    this.str = this.str.slice(0, e);
    const s2 = this.str.charCodeAt(e - 1);
    return s2 >= 55296 && s2 <= 56319 && (this.str = this.str.slice(0, e - 1) + "�", n.str = "�" + n.str.slice(1)), n;
  }
  /**
   * @param {ContentString} right
   * @return {boolean}
   */
  mergeWith(e) {
    return this.str += e.str, true;
  }
  /**
   * @param {Transaction} transaction
   * @param {Item} item
   */
  integrate(e, n) {
  }
  /**
   * @param {Transaction} transaction
   */
  delete(e) {
  }
  /**
   * @param {StructStore} store
   */
  gc(e) {
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(e, n) {
    e.writeString(n === 0 ? this.str : this.str.slice(n));
  }
  /**
   * @return {number}
   */
  getRef() {
    return 4;
  }
};
var tp = (t) => new _e(t.readString());
var np = [
  Of,
  Rf,
  Hf,
  Bf,
  zf,
  Kf,
  Wf
];
var sp = 0;
var rp = 1;
var ip = 2;
var op = 3;
var cp = 4;
var ap = 5;
var lp = 6;
var Oe = class _Oe {
  /**
   * @param {AbstractType<any>} type
   */
  constructor(e) {
    this.type = e;
  }
  /**
   * @return {number}
   */
  getLength() {
    return 1;
  }
  /**
   * @return {Array<any>}
   */
  getContent() {
    return [this.type];
  }
  /**
   * @return {boolean}
   */
  isCountable() {
    return true;
  }
  /**
   * @return {ContentType}
   */
  copy() {
    return new _Oe(this.type._copy());
  }
  /**
   * @param {number} offset
   * @return {ContentType}
   */
  splice(e) {
    throw de();
  }
  /**
   * @param {ContentType} right
   * @return {boolean}
   */
  mergeWith(e) {
    return false;
  }
  /**
   * @param {Transaction} transaction
   * @param {Item} item
   */
  integrate(e, n) {
    this.type._integrate(e.doc, n);
  }
  /**
   * @param {Transaction} transaction
   */
  delete(e) {
    let n = this.type._start;
    for (; n !== null; )
      n.deleted ? n.id.clock < (e.beforeState.get(n.id.client) || 0) && e._mergeStructs.push(n) : n.delete(e), n = n.right;
    this.type._map.forEach((s2) => {
      s2.deleted ? s2.id.clock < (e.beforeState.get(s2.id.client) || 0) && e._mergeStructs.push(s2) : s2.delete(e);
    }), e.changed.delete(this.type);
  }
  /**
   * @param {StructStore} store
   */
  gc(e) {
    let n = this.type._start;
    for (; n !== null; )
      n.gc(e, true), n = n.right;
    this.type._start = null, this.type._map.forEach(
      /** @param {Item | null} item */
      (s2) => {
        for (; s2 !== null; )
          s2.gc(e, true), s2 = s2.left;
      }
    ), this.type._map = /* @__PURE__ */ new Map();
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(e, n) {
    this.type._write(e);
  }
  /**
   * @return {number}
   */
  getRef() {
    return 7;
  }
};
var up = (t) => new Oe(np[t.readTypeRef()](t));
var Es = (t, e, n) => {
  const { client: s2, clock: r } = e.id, i = new P(
    A(s2, r + n),
    e,
    A(s2, r + n - 1),
    e.right,
    e.rightOrigin,
    e.parent,
    e.parentSub,
    e.content.splice(n)
  );
  return e.deleted && i.markDeleted(), e.keep && (i.keep = true), e.redone !== null && (i.redone = A(e.redone.client, e.redone.clock + n)), e.right = i, i.right !== null && (i.right.left = i), t._mergeStructs.push(i), i.parentSub !== null && i.right === null && i.parent._map.set(i.parentSub, i), e.length = n, i;
};
var P = class _P extends Ti {
  /**
   * @param {ID} id
   * @param {Item | null} left
   * @param {ID | null} origin
   * @param {Item | null} right
   * @param {ID | null} rightOrigin
   * @param {AbstractType<any>|ID|null} parent Is a type if integrated, is null if it is possible to copy parent from left or right, is ID before integration to search for it.
   * @param {string | null} parentSub
   * @param {AbstractContent} content
   */
  constructor(e, n, s2, r, i, o2, c2, a2) {
    super(e, a2.getLength()), this.origin = s2, this.left = n, this.right = r, this.rightOrigin = i, this.parent = o2, this.parentSub = c2, this.redone = null, this.content = a2, this.info = this.content.isCountable() ? yo : 0;
  }
  /**
   * This is used to mark the item as an indexed fast-search marker
   *
   * @type {boolean}
   */
  set marker(e) {
    (this.info & sr) > 0 !== e && (this.info ^= sr);
  }
  get marker() {
    return (this.info & sr) > 0;
  }
  /**
   * If true, do not garbage collect this Item.
   */
  get keep() {
    return (this.info & go) > 0;
  }
  set keep(e) {
    this.keep !== e && (this.info ^= go);
  }
  get countable() {
    return (this.info & yo) > 0;
  }
  /**
   * Whether this item was deleted or not.
   * @type {Boolean}
   */
  get deleted() {
    return (this.info & nr) > 0;
  }
  set deleted(e) {
    this.deleted !== e && (this.info ^= nr);
  }
  markDeleted() {
    this.info |= nr;
  }
  /**
   * Return the creator clientID of the missing op or define missing items and return null.
   *
   * @param {Transaction} transaction
   * @param {StructStore} store
   * @return {null | number}
   */
  getMissing(e, n) {
    if (this.origin && this.origin.client !== this.id.client && this.origin.clock >= j(n, this.origin.client))
      return this.origin.client;
    if (this.rightOrigin && this.rightOrigin.client !== this.id.client && this.rightOrigin.clock >= j(n, this.rightOrigin.client))
      return this.rightOrigin.client;
    if (this.parent && this.parent.constructor === Tt && this.id.client !== this.parent.client && this.parent.clock >= j(n, this.parent.client))
      return this.parent.client;
    if (this.origin && (this.left = Mo(e, n, this.origin), this.origin = this.left.lastId), this.rightOrigin && (this.right = Ke(e, this.rightOrigin), this.rightOrigin = this.right.id), this.left && this.left.constructor === re || this.right && this.right.constructor === re)
      this.parent = null;
    else if (!this.parent)
      this.left && this.left.constructor === _P && (this.parent = this.left.parent, this.parentSub = this.left.parentSub), this.right && this.right.constructor === _P && (this.parent = this.right.parent, this.parentSub = this.right.parentSub);
    else if (this.parent.constructor === Tt) {
      const s2 = ur(n, this.parent);
      s2.constructor === re ? this.parent = null : this.parent = /** @type {ContentType} */
      s2.content.type;
    }
    return null;
  }
  /**
   * @param {Transaction} transaction
   * @param {number} offset
   */
  integrate(e, n) {
    if (n > 0 && (this.id.clock += n, this.left = Mo(e, e.doc.store, A(this.id.client, this.id.clock - 1)), this.origin = this.left.lastId, this.content = this.content.splice(n), this.length -= n), this.parent) {
      if (!this.left && (!this.right || this.right.left !== null) || this.left && this.left.right !== this.right) {
        let s2 = this.left, r;
        if (s2 !== null)
          r = s2.right;
        else if (this.parentSub !== null)
          for (r = /** @type {AbstractType<any>} */
          this.parent._map.get(this.parentSub) || null; r !== null && r.left !== null; )
            r = r.left;
        else
          r = /** @type {AbstractType<any>} */
          this.parent._start;
        const i = /* @__PURE__ */ new Set(), o2 = /* @__PURE__ */ new Set();
        for (; r !== null && r !== this.right; ) {
          if (o2.add(r), i.add(r), Wn(this.origin, r.origin)) {
            if (r.id.client < this.id.client)
              s2 = r, i.clear();
            else if (Wn(this.rightOrigin, r.rightOrigin))
              break;
          } else if (r.origin !== null && o2.has(ur(e.doc.store, r.origin)))
            i.has(ur(e.doc.store, r.origin)) || (s2 = r, i.clear());
          else
            break;
          r = r.right;
        }
        this.left = s2;
      }
      if (this.left !== null) {
        const s2 = this.left.right;
        this.right = s2, this.left.right = this;
      } else {
        let s2;
        if (this.parentSub !== null)
          for (s2 = /** @type {AbstractType<any>} */
          this.parent._map.get(this.parentSub) || null; s2 !== null && s2.left !== null; )
            s2 = s2.left;
        else
          s2 = /** @type {AbstractType<any>} */
          this.parent._start, this.parent._start = this;
        this.right = s2;
      }
      this.right !== null ? this.right.left = this : this.parentSub !== null && (this.parent._map.set(this.parentSub, this), this.left !== null && this.left.delete(e)), this.parentSub === null && this.countable && !this.deleted && (this.parent._length += this.length), Ha(e.doc.store, this), this.content.integrate(e, this), Oo(
        e,
        /** @type {AbstractType<any>} */
        this.parent,
        this.parentSub
      ), /** @type {AbstractType<any>} */
      (this.parent._item !== null && /** @type {AbstractType<any>} */
      this.parent._item.deleted || this.parentSub !== null && this.right !== null) && this.delete(e);
    } else
      new re(this.id, this.length).integrate(e, 0);
  }
  /**
   * Returns the next non-deleted item
   */
  get next() {
    let e = this.right;
    for (; e !== null && e.deleted; )
      e = e.right;
    return e;
  }
  /**
   * Returns the previous non-deleted item
   */
  get prev() {
    let e = this.left;
    for (; e !== null && e.deleted; )
      e = e.left;
    return e;
  }
  /**
   * Computes the last content address of this Item.
   */
  get lastId() {
    return this.length === 1 ? this.id : A(this.id.client, this.id.clock + this.length - 1);
  }
  /**
   * Try to merge two items
   *
   * @param {Item} right
   * @return {boolean}
   */
  mergeWith(e) {
    if (this.constructor === e.constructor && Wn(e.origin, this.lastId) && this.right === e && Wn(this.rightOrigin, e.rightOrigin) && this.id.client === e.id.client && this.id.clock + this.length === e.id.clock && this.deleted === e.deleted && this.redone === null && e.redone === null && this.content.constructor === e.content.constructor && this.content.mergeWith(e.content)) {
      const n = (
        /** @type {AbstractType<any>} */
        this.parent._searchMarker
      );
      return n && n.forEach((s2) => {
        s2.p === e && (s2.p = this, !this.deleted && this.countable && (s2.index -= this.length));
      }), e.keep && (this.keep = true), this.right = e.right, this.right !== null && (this.right.left = this), this.length += e.length, true;
    }
    return false;
  }
  /**
   * Mark this Item as deleted.
   *
   * @param {Transaction} transaction
   */
  delete(e) {
    if (!this.deleted) {
      const n = (
        /** @type {AbstractType<any>} */
        this.parent
      );
      this.countable && this.parentSub === null && (n._length -= this.length), this.markDeleted(), ws(e.deleteSet, this.id.client, this.id.clock, this.length), Oo(e, n, this.parentSub), this.content.delete(e);
    }
  }
  /**
   * @param {StructStore} store
   * @param {boolean} parentGCd
   */
  gc(e, n) {
    if (!this.deleted)
      throw ce();
    this.content.gc(e), n ? yf(e, this, new re(this.id, this.length)) : this.content = new wn(this.length);
  }
  /**
   * Transform the properties of this type to binary and write it to an
   * BinaryEncoder.
   *
   * This is called when this Item is sent to a remote peer.
   *
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder The encoder to write data to.
   * @param {number} offset
   */
  write(e, n) {
    const s2 = n > 0 ? A(this.id.client, this.id.clock + n - 1) : this.origin, r = this.rightOrigin, i = this.parentSub, o2 = this.content.getRef() & Ms | (s2 === null ? 0 : ie) | // origin is defined
    (r === null ? 0 : De) | // right origin is defined
    (i === null ? 0 : un);
    if (e.writeInfo(o2), s2 !== null && e.writeLeftID(s2), r !== null && e.writeRightID(r), s2 === null && r === null) {
      const c2 = (
        /** @type {AbstractType<any>} */
        this.parent
      );
      if (c2._item !== void 0) {
        const a2 = c2._item;
        if (a2 === null) {
          const l2 = pf(c2);
          e.writeParentInfo(true), e.writeString(l2);
        } else
          e.writeParentInfo(false), e.writeLeftID(a2.id);
      } else c2.constructor === String ? (e.writeParentInfo(true), e.writeString(c2)) : c2.constructor === Tt ? (e.writeParentInfo(false), e.writeLeftID(c2)) : ce();
      i !== null && e.writeString(i);
    }
    this.content.write(e, n);
  }
};
var cl = (t, e) => hp[e & Ms](t);
var hp = [
  () => {
    ce();
  },
  // GC is not ItemContent
  qf,
  // 1
  Qf,
  // 2
  Gf,
  // 3
  tp,
  // 4
  Xf,
  // 5
  Zf,
  // 6
  up,
  // 7
  ep,
  // 8
  Jf,
  // 9
  () => {
    ce();
  }
  // 10 - Skip is not ItemContent
];
var dp = 10;
var oe = class extends Ti {
  get deleted() {
    return true;
  }
  delete() {
  }
  /**
   * @param {Skip} right
   * @return {boolean}
   */
  mergeWith(e) {
    return this.constructor !== e.constructor ? false : (this.length += e.length, true);
  }
  /**
   * @param {Transaction} transaction
   * @param {number} offset
   */
  integrate(e, n) {
    ce();
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(e, n) {
    e.writeInfo(dp), b(e.restEncoder, this.length - n);
  }
  /**
   * @param {Transaction} transaction
   * @param {StructStore} store
   * @return {null | number}
   */
  getMissing(e, n) {
    return null;
  }
};
var al = (
  /** @type {any} */
  typeof globalThis < "u" ? globalThis : typeof window < "u" ? window : typeof global < "u" ? global : {}
);
var ll = "__ $YJS$ __";
al[ll] === true && console.error("Yjs was already imported. This breaks constructor checks and will lead to issues! - https://github.com/yjs/yjs/issues/438");
al[ll] = true;
var fr = 3e4;
var fp = class extends ea {
  /**
   * @param {Y.Doc} doc
   */
  constructor(e) {
    super(), this.doc = e, this.clientID = e.clientID, this.states = /* @__PURE__ */ new Map(), this.meta = /* @__PURE__ */ new Map(), this._checkInterval = /** @type {any} */
    setInterval(() => {
      const n = $t();
      this.getLocalState() !== null && fr / 2 <= n - /** @type {{lastUpdated:number}} */
      this.meta.get(this.clientID).lastUpdated && this.setLocalState(this.getLocalState());
      const s2 = [];
      this.meta.forEach((r, i) => {
        i !== this.clientID && fr <= n - r.lastUpdated && this.states.has(i) && s2.push(i);
      }), s2.length > 0 && Li(this, s2, "timeout");
    }, pe(fr / 10)), e.on("destroy", () => {
      this.destroy();
    }), this.setLocalState({});
  }
  destroy() {
    this.emit("destroy", [this]), this.setLocalState(null), super.destroy(), clearInterval(this._checkInterval);
  }
  /**
   * @return {Object<string,any>|null}
   */
  getLocalState() {
    return this.states.get(this.clientID) || null;
  }
  /**
   * @param {Object<string,any>|null} state
   */
  setLocalState(e) {
    const n = this.clientID, s2 = this.meta.get(n), r = s2 === void 0 ? 0 : s2.clock + 1, i = this.states.get(n);
    e === null ? this.states.delete(n) : this.states.set(n, e), this.meta.set(n, {
      clock: r,
      lastUpdated: $t()
    });
    const o2 = [], c2 = [], a2 = [], l2 = [];
    e === null ? l2.push(n) : i == null ? e != null && o2.push(n) : (c2.push(n), xt(i, e) || a2.push(n)), (o2.length > 0 || a2.length > 0 || l2.length > 0) && this.emit("change", [{ added: o2, updated: a2, removed: l2 }, "local"]), this.emit("update", [{ added: o2, updated: c2, removed: l2 }, "local"]);
  }
  /**
   * @param {string} field
   * @param {any} value
   */
  setLocalStateField(e, n) {
    const s2 = this.getLocalState();
    s2 !== null && this.setLocalState({
      ...s2,
      [e]: n
    });
  }
  /**
   * @return {Map<number,Object<string,any>>}
   */
  getStates() {
    return this.states;
  }
};
var Li = (t, e, n) => {
  const s2 = [];
  for (let r = 0; r < e.length; r++) {
    const i = e[r];
    if (t.states.has(i)) {
      if (t.states.delete(i), i === t.clientID) {
        const o2 = (
          /** @type {MetaClientState} */
          t.meta.get(i)
        );
        t.meta.set(i, {
          clock: o2.clock + 1,
          lastUpdated: $t()
        });
      }
      s2.push(i);
    }
  }
  s2.length > 0 && (t.emit("change", [{ added: [], updated: [], removed: s2 }, n]), t.emit("update", [{ added: [], updated: [], removed: s2 }, n]));
};
var nn = (t, e, n = t.states) => {
  const s2 = e.length, r = Y();
  b(r, s2);
  for (let i = 0; i < s2; i++) {
    const o2 = e[i], c2 = n.get(o2) || null, a2 = (
      /** @type {MetaClientState} */
      t.meta.get(o2).clock
    );
    b(r, o2), b(r, a2), ct(r, JSON.stringify(c2));
  }
  return T(r);
};
var pp = (t, e, n) => {
  const s2 = qe(e), r = $t(), i = [], o2 = [], c2 = [], a2 = [], l2 = S(s2);
  for (let u = 0; u < l2; u++) {
    const h = S(s2);
    let d = S(s2);
    const f2 = JSON.parse(He(s2)), p = t.meta.get(h), g = t.states.get(h), m = p === void 0 ? 0 : p.clock;
    (m < d || m === d && f2 === null && t.states.has(h)) && (f2 === null ? h === t.clientID && t.getLocalState() != null ? d++ : t.states.delete(h) : t.states.set(h, f2), t.meta.set(h, {
      clock: d,
      lastUpdated: r
    }), p === void 0 && f2 !== null ? i.push(h) : p !== void 0 && f2 === null ? a2.push(h) : f2 !== null && (xt(f2, g) || c2.push(h), o2.push(h)));
  }
  (i.length > 0 || c2.length > 0 || a2.length > 0) && t.emit("change", [{
    added: i,
    updated: c2,
    removed: a2
  }, n]), (i.length > 0 || o2.length > 0 || a2.length > 0) && t.emit("update", [{
    added: i,
    updated: o2,
    removed: a2
  }, n]);
};
var ul = 0;
var Ii = 1;
var hl = 2;
var Fr = (t, e) => {
  b(t, ul);
  const n = df(e);
  I(t, n);
};
var dl = (t, e, n) => {
  b(t, Ii), I(t, af(e, n));
};
var gp = (t, e, n) => dl(e, n, W(t));
var fl = (t, e, n, s2) => {
  try {
    rf(e, W(t), n);
  } catch (r) {
    s2 == null ? void 0 : s2(
      /** @type {Error} */
      r
    ), console.error("Caught error while handling a Yjs update", r);
  }
};
var yp = (t, e) => {
  b(t, hl), I(t, e);
};
var mp = fl;
var wp = (t, e, n, s2, r) => {
  const i = S(t);
  switch (i) {
    case ul:
      gp(t, e, n);
      break;
    case Ii:
      fl(t, n, s2, r);
      break;
    case hl:
      mp(t, n, s2, r);
      break;
    default:
      throw new Error("Unknown message type");
  }
  return i;
};
var pl = /* @__PURE__ */ new Map();
var bp = class {
  /**
   * @param {string} room
   */
  constructor(e) {
    this.room = e, this.onmessage = null, this._onChange = (n) => n.key === e && this.onmessage !== null && this.onmessage({ data: Qh(n.newValue || "") }), jh(this._onChange);
  }
  /**
   * @param {ArrayBuffer} buf
   */
  postMessage(e) {
    ra.setItem(this.room, Zh(Yh(e)));
  }
  close() {
    Hh(this._onChange);
  }
};
var vp = typeof BroadcastChannel > "u" ? bp : BroadcastChannel;
var Mi = (t) => ke(pl, t, () => {
  const e = Ve(), n = new vp(t);
  return n.onmessage = (s2) => e.forEach((r) => r(s2.data, "broadcastchannel")), {
    bc: n,
    subs: e
  };
});
var Cp = (t, e) => (Mi(t).subs.add(e), e);
var Sp = (t, e) => {
  const n = Mi(t), s2 = n.subs.delete(e);
  return s2 && n.subs.size === 0 && (n.bc.close(), pl.delete(t)), s2;
};
var At = (t, e, n = null) => {
  const s2 = Mi(t);
  s2.bc.postMessage(e), s2.subs.forEach((r) => r(e, n));
};
var Ep = (t) => Ih(t, (e, n) => `${encodeURIComponent(n)}=${encodeURIComponent(e)}`).join("&");
var _p = "useandom-26T198340PX75pxJACKVERYMINDBUSHWOLF_GQZbfghjklqvwyzrict";
var Ap = (t = 21) => {
  let e = "", n = crypto.getRandomValues(new Uint8Array(t |= 0));
  for (; t--; )
    e += _p[n[t] & 63];
  return e;
};
var kp = 0;
var xp = (t, e, n) => {
  S(t) === kp && n(e, He(t));
};
var Dp = typeof window > "u";
var Un = [];
Un[0] = (t, e, n, s2, r) => {
  b(t, 0);
  const i = wp(
    e,
    t,
    n.doc,
    n
  );
  s2 && i === Ii && !n.synced && (n.synced = true);
};
Un[3] = (t, e, n, s2, r) => {
  b(t, 1), I(
    t,
    nn(
      n.awareness,
      Array.from(n.awareness.getStates().keys())
    )
  );
};
Un[1] = (t, e, n, s2, r) => {
  pp(
    n.awareness,
    W(e),
    n
  );
};
Un[2] = (t, e, n, s2, r) => {
  xp(
    e,
    n.doc,
    (i, o2) => Tp(n, o2)
  );
};
function Tp(t, e) {
  console.warn(`Permission denied to access ${t.url}.
${e}`);
}
function gl(t, e, n) {
  const s2 = qe(e), r = Y(), i = S(s2), o2 = t.messageHandlers[i];
  return o2 ? o2(r, s2, t, n, i) : console.error("Unable to compute message"), r;
}
function Fo(t) {
  if (t.shouldConnect && t.ws === null) {
    if (!t._WS)
      throw new Error(
        "No WebSocket implementation available, did you forget to pass options.WebSocketPolyfill?"
      );
    const e = new t._WS(t.url);
    e.binaryType = "arraybuffer", t.ws = e, t.wsconnecting = true, t.wsconnected = false, t.synced = false, e.addEventListener("message", (n) => {
      if (typeof n.data == "string") {
        if (n.data.startsWith("__YPS:")) {
          const r = n.data.slice(6);
          t.emit("custom-message", [r]);
        }
        return;
      }
      t.wsLastMessageReceived = $t();
      const s2 = gl(t, new Uint8Array(n.data), true);
      oi(s2) > 1 && e.send(T(s2));
    }), e.addEventListener("error", (n) => {
      t.emit("connection-error", [n, t]);
    }), e.addEventListener("close", (n) => {
      if (t.emit("connection-close", [n, t]), t.ws = null, t.wsconnecting = false, t.wsconnected) {
        t.wsconnected = false, t.synced = false;
        const s2 = Array.from(
          t.awareness.getStates().keys()
        ).filter((r) => r !== t.doc.clientID);
        Li(
          t.awareness,
          s2,
          t
        );
        for (const r of s2)
          t.awareness.meta.delete(r);
        t.emit("status", [{ status: "disconnected" }]);
      } else t.wsUnsuccessfulReconnects++;
      setTimeout(
        () => {
          t.shouldConnect && Promise.resolve(t._reconnectWS()).catch((s2) => {
            console.error("Reconnection failed", s2);
          });
        },
        ri(
          Gu(2, t.wsUnsuccessfulReconnects) * 100,
          t.maxBackoffTime
        )
      );
    }), e.addEventListener("open", () => {
      t.wsLastMessageReceived = $t(), t.wsconnecting = false, t.wsconnected = true, t.wsUnsuccessfulReconnects = 0, t.emit("status", [{ status: "connected" }]);
      const n = Y();
      if (b(n, 0), Fr(n, t.doc), e.send(T(n)), t.awareness.getLocalState() !== null) {
        t.awareness.setLocalState(t.awareness.getLocalState());
        const s2 = Y();
        b(s2, 1), I(
          s2,
          nn(t.awareness, [
            t.doc.clientID
          ])
        ), e.send(T(s2));
      }
    }), t.emit("status", [{ status: "connecting" }]);
  }
}
function pr(t, e) {
  const n = t.ws;
  t.wsconnected && n && n.readyState === n.OPEN && n.send(e), t.bcconnected && At(t.bcChannel, e, t);
}
var Lp = typeof WebSocket > "u" ? null : WebSocket;
var Ip = class extends ea {
  constructor(t, e, n, {
    connect: s2 = true,
    awareness: r = new fp(n),
    params: i = {},
    isPrefixedUrl: o2 = false,
    WebSocketPolyfill: c2 = Lp,
    resyncInterval: a2 = -1,
    maxBackoffTime: l2 = 2500,
    disableBc: u = Dp
  } = {}) {
    super();
    __publicField(this, "maxBackoffTime");
    __publicField(this, "bcChannel");
    __publicField(this, "url");
    __publicField(this, "roomname");
    __publicField(this, "doc");
    __publicField(this, "_WS");
    __publicField(this, "awareness");
    __publicField(this, "wsconnected");
    __publicField(this, "wsconnecting");
    __publicField(this, "bcconnected");
    __publicField(this, "disableBc");
    __publicField(this, "wsUnsuccessfulReconnects");
    __publicField(this, "messageHandlers");
    __publicField(this, "_synced");
    __publicField(this, "ws");
    __publicField(this, "wsLastMessageReceived");
    __publicField(this, "shouldConnect");
    __publicField(this, "_resyncInterval");
    __publicField(this, "_bcSubscriber");
    __publicField(this, "_updateHandler");
    __publicField(this, "_awarenessUpdateHandler");
    __publicField(this, "_unloadHandler");
    for (; t[t.length - 1] === "/"; )
      t = t.slice(0, t.length - 1);
    const h = Ep(i);
    this.maxBackoffTime = l2, this.bcChannel = `${t}/${e}`, this.url = o2 ? t : `${t}/${e}${h.length === 0 ? "" : `?${h}`}`, this.roomname = e, this.doc = n, this._WS = c2, this.awareness = r, this.wsconnected = false, this.wsconnecting = false, this.bcconnected = false, this.disableBc = u, this.wsUnsuccessfulReconnects = 0, this.messageHandlers = Un.slice(), this._synced = false, this.ws = null, this.wsLastMessageReceived = 0, this.shouldConnect = s2, this._resyncInterval = 0, a2 > 0 && (this._resyncInterval = setInterval(() => {
      if (this.ws && this.ws.readyState === WebSocket.OPEN) {
        const d = Y();
        b(d, 0), Fr(d, n), this.ws.send(T(d));
      }
    }, a2)), this._bcSubscriber = (d, f2) => {
      if (f2 !== this) {
        const p = gl(this, new Uint8Array(d), false);
        oi(p) > 1 && At(this.bcChannel, T(p), this);
      }
    }, this._updateHandler = (d, f2) => {
      if (f2 !== this) {
        const p = Y();
        b(p, 0), yp(p, d), pr(this, T(p));
      }
    }, this.doc.on("update", this._updateHandler), this._awarenessUpdateHandler = ({ added: d, updated: f2, removed: p }, g) => {
      const m = d.concat(f2).concat(p), w = Y();
      b(w, 1), I(
        w,
        nn(r, m)
      ), pr(this, T(w));
    }, this._unloadHandler = () => {
      Li(
        this.awareness,
        [n.clientID],
        "window unload"
      );
    }, typeof window < "u" ? window.addEventListener("unload", this._unloadHandler) : typeof process < "u" && typeof process.on == "function" && process.on("exit", this._unloadHandler), r.on("change", this._awarenessUpdateHandler), clearInterval(r._checkInterval), s2 && this.connect();
  }
  /**
   * @type {boolean}
   */
  get synced() {
    return this._synced;
  }
  set synced(t) {
    this._synced !== t && (this._synced = t, this.emit("synced", [t]), this.emit("sync", [t]));
  }
  destroy() {
    this._resyncInterval !== 0 && clearInterval(this._resyncInterval), this.disconnect(), typeof window < "u" ? window.removeEventListener("unload", this._unloadHandler) : typeof process < "u" && typeof process.off == "function" && process.off("exit", this._unloadHandler), this.awareness.off("change", this._awarenessUpdateHandler), this.doc.off("update", this._updateHandler), super.destroy();
  }
  connectBc() {
    if (this.disableBc) return;
    this.bcconnected || (Cp(this.bcChannel, this._bcSubscriber), this.bcconnected = true);
    const t = Y();
    b(t, 0), Fr(t, this.doc), At(this.bcChannel, T(t), this);
    const e = Y();
    b(e, 0), dl(e, this.doc), At(this.bcChannel, T(e), this);
    const n = Y();
    b(n, 3), At(
      this.bcChannel,
      T(n),
      this
    );
    const s2 = Y();
    b(s2, 1), I(
      s2,
      nn(this.awareness, [
        this.doc.clientID
      ])
    ), At(
      this.bcChannel,
      T(s2),
      this
    );
  }
  disconnectBc() {
    const t = Y();
    b(t, 1), I(
      t,
      nn(
        this.awareness,
        [this.doc.clientID],
        /* @__PURE__ */ new Map()
      )
    ), pr(this, T(t)), this.bcconnected && (Sp(this.bcChannel, this._bcSubscriber), this.bcconnected = false);
  }
  disconnect() {
    this.shouldConnect = false, this.disconnectBc(), this.ws !== null && this.ws.close();
  }
  /**
   * Called by the close handler to re-establish the WebSocket.
   * Subclasses (e.g. YProvider) override this to refresh dynamic
   * params before reconnecting.
   */
  _reconnectWS() {
    Fo(this);
  }
  connect() {
    this.shouldConnect = true, !this.wsconnected && this.ws === null && (Fo(this), this.connectBc());
  }
};
function jo(t, e, n) {
  if (typeof t !== n)
    throw new Error(
      `Invalid "${e}" parameter provided to YProvider. Expected: ${n}, received: ${t}`
    );
}
var _e2, _$i_instances, t_fn, _a2;
var $i = (_a2 = class extends Ip {
  constructor(t, e, n, s2 = {}) {
    jo(t, "host", "string"), jo(e, "room", "string"), t = t.replace(/^(http|https|ws|wss):\/\//, ""), t.endsWith("/") && (t = t.slice(0, -1));
    const r = `${s2.protocol || (t.startsWith("localhost:") || t.startsWith("127.0.0.1:") || t.startsWith("192.168.") || t.startsWith("10.") || t.startsWith("172.") && t.split(".")[1] >= "16" && t.split(".")[1] <= "31" ? "ws" : "wss")}://${t}${s2.prefix || `/parties/${s2.party || "main"}`}`, i = s2.connectionId ?? Ap(10), { params: o2, connect: c2 = true, ...a2 } = s2, l2 = {
      ...a2,
      isPrefixedUrl: !!s2.prefix,
      connect: false
    };
    super(r, e, n ?? new me(), l2);
    __privateAdd(this, _$i_instances);
    __publicField(this, "id");
    __privateAdd(this, _e2);
    this.id = i, __privateSet(this, _e2, o2), c2 && this.connect();
  }
  async connect() {
    try {
      await __privateMethod(this, _$i_instances, t_fn).call(this), super.connect();
    } catch (t) {
      throw console.error("Failed to open connecton to PartyServer", t), t;
    }
  }
  async _reconnectWS() {
    try {
      await __privateMethod(this, _$i_instances, t_fn).call(this);
    } catch (t) {
      console.error(
        "Failed to refresh params, reconnecting with stale params",
        t
      );
    }
    super._reconnectWS();
  }
  sendMessage(t) {
    var _a4;
    (_a4 = this.ws) == null ? void 0 : _a4.send(`__YPS:${t}`);
  }
}, _e2 = new WeakMap(), _$i_instances = new WeakSet(), t_fn = async function() {
  const t = typeof __privateGet(this, _e2) == "function" ? await __privateGet(this, _e2).call(this) : __privateGet(this, _e2), e = new URLSearchParams([["_pk", this.id]]);
  if (t)
    for (const [s2, r] of Object.entries(t))
      r != null && e.append(s2, r);
  const n = new URL(this.url);
  n.search = e.toString(), this.url = n.toString();
}, _a2);
var yl = ["data-playhtml-hover", "data-playhtml-focus"];
var Mp = [
  "__playhtml-element",
  "ph-flash",
  "ph-inspect-highlight",
  "ph-inspect-highlight-hover",
  "ph-inspect-selected",
  "playhtml-loading"
];
var $p = {
  "aria-busy": "true",
  "aria-live": "polite"
};
var Op = {
  defaultData: (t) => sn(t),
  myDefaultAwareness: { hover: false, focus: false },
  onMount: ({ getElement: t, setData: e, setMyAwareness: n }) => {
    const s2 = t(), r = e, i = Pp(s2, (a2) => {
      const l2 = a2.filter(
        (u) => u.type !== "attributes" || !yl.includes(u.attributeName || "")
      );
      l2.length !== 0 && r((u) => {
        Rp(u, l2);
      });
    });
    s2.__playhtml_observer = i, s2.addEventListener("mouseenter", () => {
      n({ hover: true, focus: s2.hasAttribute("data-playhtml-focus") }), s2.setAttribute("data-playhtml-hover", "");
    }), s2.addEventListener("mouseleave", () => {
      n({ hover: false, focus: s2.hasAttribute("data-playhtml-focus") }), s2.removeAttribute("data-playhtml-hover");
    }), s2.addEventListener("focusin", () => {
      n({ hover: s2.hasAttribute("data-playhtml-hover"), focus: true }), s2.setAttribute("data-playhtml-focus", "");
    }), s2.addEventListener("focusout", () => {
      n({ hover: s2.hasAttribute("data-playhtml-hover"), focus: false }), s2.removeAttribute("data-playhtml-focus");
    });
    const o2 = (a2, l2) => {
      if (a2.formState && (l2.formState = a2.formState), a2.children && l2.children)
        for (let u = 0; u < a2.children.length; u++) {
          const h = a2.children[u];
          h.nodeType === "HTMLElement" && l2.children[u] && o2(
            h,
            l2.children[u]
          );
        }
    }, c2 = () => {
      r((a2) => {
        const l2 = sn(s2);
        o2(l2, a2), l2.children && a2.children.splice(0, a2.children.length, ...l2.children);
      });
    };
    s2.addEventListener("input", c2), s2.addEventListener("change", c2);
  },
  updateElement: ({ element: t, data: e }) => {
    const n = sn(t);
    if (Oi(n, e))
      return;
    const s2 = t.__playhtml_observer;
    s2 && (s2.takeRecords(), s2.disconnect()), us(t, e), s2 && s2.observe(t, {
      childList: true,
      attributes: true,
      subtree: false,
      characterData: true
    });
  },
  updateElementAwareness: ({ element: t, awareness: e }) => {
    const n = e.some((r) => r == null ? void 0 : r.hover), s2 = e.some((r) => r == null ? void 0 : r.focus);
    n ? t.setAttribute("data-playhtml-hover", "") : t.removeAttribute("data-playhtml-hover"), s2 ? t.setAttribute("data-playhtml-focus", "") : t.removeAttribute("data-playhtml-focus");
  }
};
function Ho(t) {
  return t.nodeType === "HTMLElement";
}
function Oi(t, e) {
  if (t.nodeType !== e.nodeType)
    return false;
  if (t.nodeType === "Text" && e.nodeType === "Text")
    return t.textContent === e.textContent;
  if (Ho(t) && Ho(e)) {
    if (t.tagName !== e.tagName)
      return false;
    const n = jr(t.attributes), s2 = jr(e.attributes);
    if (Object.keys(n).length !== Object.keys(s2).length)
      return false;
    for (const [a2, l2] of Object.entries(n))
      if (s2[a2] !== l2)
        return false;
    const r = t.formState, i = e.formState;
    if ((r || i) && (!r || !i || r.checked !== i.checked || r.value !== i.value || r.selectedIndex !== i.selectedIndex))
      return false;
    const o2 = _s(t.children), c2 = _s(e.children);
    if (o2.length !== c2.length)
      return false;
    for (let a2 = 0; a2 < o2.length; a2++)
      if (!Oi(o2[a2], c2[a2]))
        return false;
  }
  return true;
}
function Pp(t, e, n) {
  const s2 = { childList: true, attributes: true, subtree: false, characterData: true, ...n }, r = (o2) => {
    const c2 = o2.filter((a2) => {
      if (a2.target !== t)
        return false;
      if (s2.childList && a2.type === "childList")
        return true;
      if (s2.attributes && a2.type === "attributes")
        if (s2.attributeFilter) {
          if (s2.attributeFilter.includes(a2.attributeName || ""))
            return true;
        } else
          return true;
      return !!(s2.characterData && a2.type === "characterData" || s2.subtree && a2.type === "childList");
    });
    e(c2);
  }, i = new MutationObserver(r);
  return i.observe(t, s2), i;
}
function Rp(t, e) {
  e.forEach((n) => {
    switch (n.type) {
      case "attributes":
        Np(t, n);
        break;
      case "childList":
        Up(t, n);
        break;
      case "characterData":
        Fp(t, n);
        break;
    }
  });
}
function Np(t, e) {
  if (t.nodeType !== "Text" && e.target instanceof HTMLElement) {
    const n = e.attributeName, s2 = e.target.getAttribute(n), r = Ni(e.target);
    if (s2 !== null) {
      const i = Ui(
        n,
        s2,
        r
      );
      i === null ? n in t.attributes && delete t.attributes[n] : t.attributes[n] !== i && (t.attributes[n] = i);
    } else n in t.attributes && delete t.attributes[n];
  }
}
function Up(t, e) {
  if (t.nodeType === "Text" || !(e.target instanceof HTMLElement))
    return;
  const n = [];
  e.target.childNodes.forEach((r) => {
    Pi(r) && n.push(sn(r));
  });
  const s2 = _s(t.children);
  s2.length === t.children.length && zp(s2, n) || t.children.splice(0, t.children.length, ...n);
}
function Fp(t, e) {
  const n = e.target;
  switch (t.nodeType) {
    case "Text":
      if (n instanceof Text)
        return t.textContent = n.textContent || "", true;
      break;
  }
  return false;
}
function jp(t) {
  return t instanceof HTMLElement || t instanceof Text;
}
function ml(t) {
  return t instanceof HTMLElement && t.classList.contains("ph-inspect-label");
}
function Pi(t) {
  return jp(t) && !ml(t);
}
function Ri(t, e) {
  return !!(t == null ? void 0 : t.split(/\s+/).includes(e));
}
function Hp(t) {
  return t.nodeType === "HTMLElement" && Ri(t.attributes.class, "ph-inspect-label");
}
function _s(t) {
  return t.filter((e) => !Hp(e));
}
function zp(t, e) {
  return t.length !== e.length ? false : t.every(
    (n, s2) => Oi(n, e[s2])
  );
}
function wl(t, e) {
  const n = new Set(Mp);
  return e && Ri(t, "playhtml-loading") && n.add(e), n;
}
function Ni(t) {
  const e = t.getAttribute("class") || void 0;
  return {
    classValue: e,
    localClassNames: wl(
      e,
      t.getAttribute("loading-class")
    )
  };
}
function Bp(t) {
  return {
    classValue: t.class,
    localClassNames: wl(
      t.class,
      t["loading-class"]
    )
  };
}
function bl(t, e, n) {
  return yl.includes(t) ? true : Ri(n.classValue, "playhtml-loading") && $p[t] === e;
}
function Ui(t, e, n) {
  return bl(t, e, n) ? null : t === "class" ? Vp(
    e,
    n.localClassNames
  ) || null : e;
}
function jr(t) {
  const e = {}, n = Bp(t);
  for (const [s2, r] of Object.entries(t)) {
    const i = Ui(s2, r, n);
    i !== null && (e[s2] = i);
  }
  return e;
}
function Vp(t, e) {
  return t.split(/\s+/).filter((n) => n && !e.has(n)).join(" ");
}
function vl(t, e) {
  return t.split(/\s+/).filter((n) => e.has(n)).join(" ");
}
function Kp(t, e, n) {
  const s2 = [
    ...t.split(/\s+/).filter(Boolean),
    ...vl(e, n).split(/\s+/).filter(Boolean)
  ];
  return Array.from(new Set(s2)).join(" ");
}
function Wp(t) {
  if (t instanceof HTMLInputElement) {
    const e = {};
    return t.type === "checkbox" || t.type === "radio" ? e.checked = t.checked : e.value = t.value, e;
  }
  if (t instanceof HTMLTextAreaElement)
    return { value: t.value };
  if (t instanceof HTMLSelectElement)
    return { selectedIndex: t.selectedIndex, value: t.value };
}
function sn(t) {
  if (t instanceof Text)
    return {
      nodeType: "Text",
      textContent: t.textContent || ""
    };
  const e = {
    nodeType: "HTMLElement",
    tagName: t.tagName.toLowerCase(),
    attributes: {},
    children: []
  }, n = Ni(t);
  for (const r of t.attributes) {
    const i = Ui(
      r.name,
      r.value,
      n
    );
    i !== null && (e.attributes[r.name] = i);
  }
  const s2 = Wp(t);
  return s2 && (e.formState = s2), t.childNodes.forEach((r) => {
    Pi(r) && e.children.push(sn(r));
  }), e;
}
function Yp(t, e) {
  e && (t instanceof HTMLInputElement ? (t.type === "checkbox" || t.type === "radio") && e.checked !== void 0 ? t.checked = e.checked : e.value !== void 0 && (t.value = e.value) : t instanceof HTMLTextAreaElement && e.value !== void 0 ? t.value = e.value : t instanceof HTMLSelectElement && e.selectedIndex !== void 0 && (t.selectedIndex = e.selectedIndex));
}
function us(t, e) {
  Gp(t, e), e.nodeType === "HTMLElement" && (qp(t, e), Yp(t, e.formState), Xp(t, e));
}
function Gp(t, e) {
  e && e.nodeType === "Text" && t.textContent !== e.textContent && (t.textContent = e.textContent || "");
}
function qp(t, e) {
  if (!e)
    return;
  const n = e.attributes && typeof e.attributes == "object" ? jr(e.attributes) : {}, s2 = Ni(t);
  for (const [r, i] of Object.entries(n)) {
    const o2 = r === "class" ? Kp(
      i,
      t.getAttribute("class") || "",
      s2.localClassNames
    ) : i;
    t.getAttribute(r) !== o2 && t.setAttribute(r, o2);
  }
  Array.from(t.attributes).forEach((r) => {
    if (!bl(r.name, r.value, s2)) {
      if (r.name === "class") {
        const i = vl(
          r.value,
          s2.localClassNames
        );
        if (!("class" in n) && i) {
          t.setAttribute("class", i);
          return;
        }
      }
      r.name in n || t.removeAttribute(r.name);
    }
  });
}
function zo(t) {
  return t.nodeType === "Text" ? document.createTextNode(t.textContent) : document.createElement(t.tagName);
}
function Jp(t, e) {
  return e.nodeType === "Text" ? t instanceof Text : t instanceof HTMLElement && t.tagName.toLowerCase() === e.tagName;
}
function Xp(t, e) {
  const n = Array.from(t.childNodes).filter(Pi), s2 = _s(e.children), r = Math.min(n.length, s2.length);
  for (let i = 0; i < r; i++) {
    const o2 = n[i], c2 = s2[i];
    if (Jp(o2, c2))
      us(o2, c2);
    else {
      const a2 = zo(c2);
      t.replaceChild(a2, o2), us(a2, c2);
    }
  }
  for (let i = r; i < s2.length; i++) {
    const o2 = s2[i], c2 = zo(o2), a2 = Array.from(t.childNodes).find(ml);
    t.insertBefore(c2, a2 ?? null), us(c2, o2);
  }
  for (let i = n.length - 1; i >= r; i--)
    t.removeChild(n[i]);
}
var Hr = 16;
var et = 512;
var Cl = /[\u0000-\u001f\u007f]/;
var Zp = 150;
function Qp() {
  return `hsl(${Math.floor(Math.random() * 360)}, 70%, 60%)`;
}
function Bo(t) {
  return t !== null && typeof t == "object" && !Array.isArray(t);
}
function Sl(t) {
  if (!Bo(t) || !Vo(t.publicKey))
    return null;
  const e = Bo(t.playerStyle) ? t.playerStyle : {}, n = Array.isArray(e.colorPalette) ? e.colorPalette.filter(Vo).slice(0, Hr) : [], s2 = {
    publicKey: t.publicKey,
    playerStyle: { colorPalette: n }
  }, r = Ko(t.name);
  r !== void 0 && (s2.name = r);
  const i = Ko(e.cursorStyle);
  return i !== void 0 && (s2.playerStyle.cursorStyle = i), Number.isFinite(t.createdAt) && (s2.createdAt = Number(t.createdAt)), s2;
}
function Vo(t) {
  return typeof t == "string" && t.length > 0 && t.length <= et && !Cl.test(t);
}
function Ko(t) {
  if (typeof t == "string" && !Cl.test(t))
    return t.slice(0, et);
}
function eg() {
  const t = crypto.getRandomValues(new Uint8Array(16)).reduce((s2, r) => s2 + r.toString(16).padStart(2, "0"), ""), e = Math.floor(Math.random() * 360), n = [
    `hsl(${e}, 70%, 60%)`,
    `hsl(${(e + 120) % 360}, 70%, 60%)`,
    `hsl(${(e + 240) % 360}, 70%, 60%)`
  ];
  return {
    publicKey: t,
    playerStyle: {
      colorPalette: n
    },
    createdAt: Date.now()
  };
}
function tg(t) {
  var _a4, _b;
  const e = (_b = (_a4 = t.playerStyle) == null ? void 0 : _a4.colorPalette) == null ? void 0 : _b[0];
  return typeof e == "string" && e.length > 0;
}
function zr(t) {
  try {
    localStorage.setItem(
      El,
      JSON.stringify(t)
    );
  } catch (e) {
    console.warn("Failed to save player identity to localStorage:", e);
  }
}
function ng(t) {
  return tg(t) ? false : (t.playerStyle || (t.playerStyle = { colorPalette: [] }), Array.isArray(t.playerStyle.colorPalette) || (t.playerStyle.colorPalette = []), t.playerStyle.colorPalette[0] = Qp(), true);
}
var El = "playhtml_player_identity";
var qn = null;
function _l() {
  if (qn) return qn;
  const t = localStorage.getItem(El);
  if (t)
    try {
      const n = JSON.parse(t), s2 = Sl(n);
      if (s2)
        return (ng(s2) || JSON.stringify(s2) !== JSON.stringify(n)) && zr(s2), qn = s2, s2;
    } catch {
      console.warn(
        "Failed to parse stored player identity, generating new one"
      );
    }
  const e = eg();
  return zr(e), qn = e, e;
}
var Fi = 512;
var As = 4096;
function gr(t) {
  if (!z(t))
    throw new Error("Presence message must be an object");
  switch (t.type) {
    case "presence-join":
      return sg(t), t;
    case "presence-update":
      return Yo(t.channel), rg(t.channel, t.value), t;
    case "presence-clear":
      return Yo(t.channel), t;
    default:
      throw new Error("Unsupported presence message type");
  }
}
function sg(t) {
  Dl(t), ks(t.page, "page", Fi), t.identity !== void 0 && Al(t.identity);
}
function rg(t, e) {
  if (e === void 0)
    throw new Error("Presence value must not be undefined");
  Dl(e), t === "cursor" && ig(e), t === "identity" && Al(e);
}
function ig(t) {
  if (!z(t))
    throw new Error("cursor presence value must be an object");
  if (!("cursor" in t))
    throw new Error("cursor presence value must include cursor");
  if (t.cursor !== null && ag(t.cursor), t.zone !== void 0 && t.zone !== null && lg(t.zone), ks(t.page, "page", Fi), t.at !== void 0 && !Number.isFinite(t.at))
    throw new Error("cursor at must be a finite number");
}
function og(t) {
  try {
    return kl(t), true;
  } catch {
    return false;
  }
}
function Al(t) {
  kl(t);
}
function kl(t) {
  if (!z(t))
    throw new Error("identity must be an object");
  if (Wo(
    t,
    ["publicKey", "name", "playerStyle", "createdAt"],
    "identity"
  ), yr(
    t.publicKey,
    "identity.publicKey",
    et
  ), !z(t.playerStyle))
    throw new Error("identity.playerStyle must be an object");
  Wo(
    t.playerStyle,
    ["colorPalette", "cursorStyle"],
    "identity.playerStyle"
  );
  const e = t.playerStyle.colorPalette;
  if (!Array.isArray(e))
    throw new Error("identity.playerStyle.colorPalette must be an array");
  if (e.length > Hr)
    throw new Error(
      `identity.playerStyle.colorPalette must have ${Hr} colors or less`
    );
  yr(
    e[0],
    "identity.playerStyle.colorPalette[0]",
    et
  );
  for (let n = 1; n < e.length; n++)
    yr(
      e[n],
      `identity.playerStyle.colorPalette[${n}]`,
      et
    );
  if (ks(
    t.name,
    "identity.name",
    et
  ), ks(
    t.playerStyle.cursorStyle,
    "identity.playerStyle.cursorStyle",
    et
  ), t.createdAt !== void 0 && !Number.isFinite(t.createdAt))
    throw new Error("identity.createdAt must be a finite number");
}
function Wo(t, e, n) {
  for (const s2 of Object.keys(t))
    if (!e.includes(s2))
      throw new Error(`${n} must only include public presence fields`);
}
function cg(t) {
  try {
    return xl(t), true;
  } catch {
    return false;
  }
}
function ag(t) {
  xl(t);
}
function xl(t) {
  if (!z(t))
    throw new Error("cursor must be an object");
  if (!Number.isFinite(t.x))
    throw new Error("cursor.x must be a finite number");
  if (!Number.isFinite(t.y))
    throw new Error("cursor.y must be a finite number");
  if (typeof t.pointer != "string" || t.pointer.length === 0)
    throw new Error("cursor.pointer must be a non-empty string");
}
function lg(t) {
  if (!z(t))
    throw new Error("cursor zone must be an object");
  if (typeof t.zoneId != "string" || t.zoneId.length === 0)
    throw new Error("cursor zoneId must be a non-empty string");
  if (!Number.isFinite(t.relX))
    throw new Error("cursor zone relX must be a finite number");
  if (!Number.isFinite(t.relY))
    throw new Error("cursor zone relY must be a finite number");
}
function Yo(t) {
  if (typeof t != "string" || t.length === 0)
    throw new Error("Presence channel must be a non-empty string");
  ji(t, "Presence channel", 128);
}
function yr(t, e, n) {
  if (typeof t != "string" || t.length === 0)
    throw new Error(`${e} must be a non-empty string`);
  ji(t, e, n);
}
function ks(t, e, n) {
  if (t !== void 0) {
    if (typeof t != "string")
      throw new Error(`${e} must be a string`);
    ji(t, e, n);
  }
}
function ji(t, e, n) {
  if (t.length > n)
    throw new Error(`${e} must be ${n} characters or less`);
  if (/[\u0000-\u001f\u007f]/.test(t))
    throw new Error(`${e} must not contain control characters`);
}
function Dl(t) {
  try {
    if (JSON.stringify(t) === void 0)
      throw new Error("Presence value must be JSON-serializable");
  } catch {
    throw new Error("Presence value must be JSON-serializable");
  }
}
function z(t) {
  return t !== null && typeof t == "object" && !Array.isArray(t);
}
function mr(t) {
  return t !== null && typeof t == "object" && Object.getPrototypeOf(t) === Object.prototype;
}
function xs(t, e) {
  if (e != null) {
    if (Array.isArray(e)) {
      t.splice(0, t.length, ...e);
      return;
    }
    if (mr(e)) {
      for (const n of Object.keys(t))
        n in e || delete t[n];
      for (const [n, s2] of Object.entries(e))
        Array.isArray(s2) ? (Array.isArray(t[n]) || (t[n] = []), xs(t[n], s2)) : mr(s2) ? (mr(t[n]) || (t[n] = {}), xs(t[n], s2)) : t[n] = s2;
      return;
    }
    t = e;
  }
}
function We(t) {
  try {
    if (typeof structuredClone == "function")
      return structuredClone(t);
  } catch {
  }
  return t == null ? t : typeof t == "object" ? JSON.parse(JSON.stringify(t)) : t;
}
function Tl(t) {
  const [e, n] = t.split("#");
  if (!e || !n)
    throw new Error("Invalid data-source attribute value");
  const s2 = e.indexOf("/"), r = s2 === -1 ? e : e.slice(0, s2), i = s2 === -1 ? "/" : e.slice(s2);
  return { domain: r, path: i, elementId: n };
}
var ug = "LOCAL";
function hg(t) {
  return t ? t.replace(/^www\./i, "") : ug;
}
function Go(t) {
  if (!t) return "/";
  const e = t.replace(/\.[^/.]+$/, "");
  return e.startsWith("/") ? e : `/${e}`;
}
function Ds(t) {
  return Math.round(t * 10) / 10 + 0;
}
function Qt(t) {
  const e = t == null ? void 0 : t.trim();
  if (!e) return null;
  const n = e.startsWith("#") ? e.slice(1) : e;
  return document.getElementById(n) ?? document.querySelector(e);
}
function qo(t) {
  return Qt(t.getAttribute(mg));
}
var Jo = 1;
var Xo = 60;
function dg(t) {
  const e = t.getAttribute(wg);
  if (e == null) return Jo;
  const n = parseFloat(e);
  return Number.isFinite(n) ? Math.max(0, Math.min(1, n)) : Jo;
}
function fg(t) {
  const e = t.getAttribute(bg);
  if (e == null) return Xo;
  const n = parseFloat(e);
  return !Number.isFinite(n) || n < 0 ? Xo : n;
}
function Zo(t, e, n) {
  return Math.min(t, Math.max(t * e, n));
}
function Br(t, e, n) {
  const s2 = t.getBoundingClientRect(), r = e.getBoundingClientRect();
  return {
    x: s2.left - r.left - e.clientLeft - n.x,
    y: s2.top - r.top - e.clientTop - n.y
  };
}
function pg(t, e, n, s2, r = Br(t, e, n)) {
  const i = dg(t), o2 = fg(t), c2 = t.offsetWidth, a2 = t.offsetHeight, l2 = Zo(c2, i, o2), u = Zo(a2, i, o2), h = l2 - c2 - r.x, d = e.clientWidth - l2 - r.x, f2 = u - a2 - r.y, p = e.clientHeight - u - r.y;
  return {
    x: d < h ? 0 : Math.min(d, Math.max(h, s2.x)),
    y: p < f2 ? 0 : Math.min(p, Math.max(f2, s2.y))
  };
}
function gg(t) {
  return {
    x: Ds(t.x),
    y: Ds(t.y)
  };
}
var yg = "can-duplicate-to";
var mg = "can-move-bounds";
var wg = "can-move-bounds-min-visible";
var bg = "can-move-bounds-min-visible-px";
var Z = ((t) => (t.CanPlay = "can-play", t.CanMove = "can-move", t.CanSpin = "can-spin", t.CanGrow = "can-grow", t.CanToggle = "can-toggle", t.CanDuplicate = "can-duplicate", t.CanHover = "can-hover", t.CanMirror = "can-mirror", t))(Z || {});
function Fn(t) {
  return t.getAttribute("data-source") ? vg(t) : t.id;
}
function vg(t) {
  const e = t.getAttribute("data-source");
  if (!e)
    throw new Error("Element has no data-source attribute");
  const [n, s2] = e.split("#");
  if (!n || !s2)
    throw new Error("Invalid data-source attribute");
  return s2;
}
var Ll = `url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg'  width='44' height='53' viewport='0 0 100 100' style='fill:black;font-size:26px;'><text y='40%'>🚿</text></svg>")
      16 0,
    auto`;
var Cg = `url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg'  width='40' height='48' viewport='0 0 100 100' style='fill:black;font-size:24px;'><text y='50%'>✂️</text></svg>") 16 0,auto`;
function Qo(t, { getData: e, getElement: n, getLocalData: s2, setLocalData: r }) {
  const i = e(), o2 = s2(), c2 = n();
  if (o2.isHovering = true, t.altKey) {
    if (i.scale <= 0.5) {
      c2.style.cursor = "not-allowed";
      return;
    }
    c2.style.cursor = Cg;
  } else {
    if (i.scale >= i.maxScale) {
      c2.style.cursor = "not-allowed";
      return;
    }
    c2.style.cursor = Ll;
  }
  r(o2);
}
function Jn(t) {
  if ("touches" in t) {
    const { clientX: e, clientY: n } = t.touches[0];
    return { clientX: e, clientY: n };
  }
  return { clientX: t.clientX, clientY: t.clientY };
}
var Il = {
  "can-move": {
    defaultData: { x: 0, y: 0 },
    defaultLocalData: { startMouseX: 0, startMouseY: 0 },
    updateElement: ({ element: t, data: e }) => {
      t.style.transform = `translate(${e.x}px, ${e.y}px)`;
    },
    onDragStart: (t, { data: e, element: n, setLocalData: s2 }) => {
      const { clientX: r, clientY: i } = Jn(t), o2 = qo(n), c2 = o2 ? Br(n, o2, e) : void 0;
      s2({
        startMouseX: r,
        startMouseY: i,
        dragX: e.x,
        dragY: e.y,
        boundsBaseX: c2 == null ? void 0 : c2.x,
        boundsBaseY: c2 == null ? void 0 : c2.y
      });
    },
    onDrag: (t, { data: e, localData: n, setData: s2, setLocalData: r, element: i }) => {
      var _a4, _b;
      const { clientX: o2, clientY: c2 } = Jn(t), a2 = e.x + o2 - n.startMouseX, l2 = e.y + c2 - n.startMouseY, u = qo(i);
      if (u) {
        const w = {
          x: n.dragX ?? e.x,
          y: n.dragY ?? e.y
        }, C = w.x + o2 - n.startMouseX, v = w.y + c2 - n.startMouseY, E = n.boundsBaseX !== void 0 && n.boundsBaseY !== void 0 ? { x: n.boundsBaseX, y: n.boundsBaseY } : Br(i, u, w), x = pg(
          i,
          u,
          w,
          {
            x: C,
            y: v
          },
          E
        ), y2 = gg(x);
        s2(y2);
        const _ = x.x - w.x, V = x.y - w.y;
        r({
          startMouseX: n.startMouseX + _,
          startMouseY: n.startMouseY + V,
          dragX: y2.x,
          dragY: y2.y,
          boundsBaseX: E.x,
          boundsBaseY: E.y
        });
        return;
      }
      const { top: h, left: d, bottom: f2, right: p } = i.getBoundingClientRect(), g = ((_a4 = window.visualViewport) == null ? void 0 : _a4.width) ?? window.innerWidth, m = ((_b = window.visualViewport) == null ? void 0 : _b.height) ?? window.innerHeight;
      p > g && o2 > n.startMouseX || f2 > m && c2 > n.startMouseY || d < 0 && o2 < n.startMouseX || h < 0 && c2 < n.startMouseY || (s2({
        x: Ds(a2),
        y: Ds(l2)
      }), r({ startMouseX: o2, startMouseY: c2 }));
    },
    resetShortcut: "shiftKey"
  },
  "can-spin": {
    defaultData: { rotation: 0 },
    defaultLocalData: { startMouseX: 0 },
    updateElement: ({ element: t, data: e }) => {
      t.style.transform = `rotate(${e.rotation}deg)`;
    },
    onDragStart: (t, { setLocalData: e }) => {
      const { clientX: n } = Jn(t);
      e({
        startMouseX: n
      });
    },
    onDrag: (t, { data: e, localData: n, setData: s2, setLocalData: r }) => {
      const { clientX: i } = Jn(t);
      let o2 = Math.abs(i - n.startMouseX) * 2, c2 = e.rotation;
      i > n.startMouseX ? c2 += o2 : i < n.startMouseX && (c2 -= o2), s2({ rotation: c2 }), r({ startMouseX: i });
    },
    resetShortcut: "shiftKey"
  },
  "can-toggle": {
    defaultData: { on: false },
    updateElement: ({ element: t, data: e }) => {
      const n = typeof e == "object" ? e.on : e;
      t.classList.toggle("toggled", n), t.classList.toggle("clicked", n);
    },
    onClick: (t, { data: e, setData: n }) => {
      const s2 = typeof e == "object" ? e.on : e;
      n({ on: !s2 });
    },
    resetShortcut: "shiftKey"
  },
  "can-grow": {
    defaultData: { scale: 1 },
    defaultLocalData: { maxScale: 2, isHovering: false },
    updateElement: ({ element: t, data: e }) => {
      t.style.transform = `scale(${e.scale})`;
    },
    onClick: (t, { data: e, element: n, setData: s2, localData: r }) => {
      let { scale: i } = e;
      if (t.altKey) {
        if (e.scale <= 0.5)
          return;
        i -= 0.1;
      } else {
        if (n.style.cursor = Ll, e.scale >= r.maxScale)
          return;
        i += 0.1;
      }
      s2({ ...e, scale: i });
    },
    onMount: (t) => {
      const e = t.getElement();
      let n = false;
      const s2 = (o2) => Qo(o2, t), r = (o2) => {
        Qo(o2, t), !n && (n = true, document.addEventListener("keydown", s2), document.addEventListener("keyup", s2));
      }, i = () => {
        n && (n = false, document.removeEventListener("keydown", s2), document.removeEventListener("keyup", s2));
      };
      e.addEventListener("mouseenter", r), e.addEventListener("mouseleave", i);
    },
    resetShortcut: "shiftKey"
  },
  // TODO: add ability to add max # of duplicates
  // TODO: add lifespan to automatically prune
  // TODO: add limit per person / per timeframe.
  "can-duplicate": {
    defaultData: [],
    defaultLocalData: [],
    updateElement: ({ data: t, localData: e, setLocalData: n, element: s2 }) => {
      var _a4;
      const r = s2.getAttribute(
        "can-duplicate"
        /* CanDuplicate */
      ), i = Qt(r);
      let o2 = document.getElementById((_a4 = e.slice(-1)) == null ? void 0 : _a4[0]) ?? null;
      if (!i) {
        console.error(
          `Element ${r} not found. Cannot duplicate.`
        );
        return;
      }
      const c2 = Qt(
        s2.getAttribute(yg)
      );
      function a2(u) {
        if (c2) {
          c2.appendChild(u);
          return;
        }
        i.parentNode.insertBefore(
          u,
          (o2 || i).nextSibling
        );
      }
      const l2 = new Set(e);
      for (const u of t) {
        if (l2.has(u)) continue;
        const h = i.cloneNode(true);
        Object.assign(h, { ...i }), h.id = u, a2(h), e.push(u), window.playhtml.setupPlayElement(h), o2 = h;
      }
      n(e);
    },
    onClick: (t, { data: e, element: n, setData: s2 }) => {
      const r = Qt(
        n.getAttribute(
          "can-duplicate"
          /* CanDuplicate */
        )
      );
      if (!r) return;
      const i = r.id + "-" + Math.random().toString(36).substr(2, 9);
      s2((o2) => {
        o2.push(i);
      });
    },
    isValidElementForTag: (t) => {
      const e = t.getAttribute(
        "can-duplicate"
        /* CanDuplicate */
      );
      return e ? (Qt(e) || console.warn(
        `can-duplicate element (${t.id}) duplicate element ("${e}") not found.`
      ), true) : false;
    }
  },
  // TODO: auto-duplicate :hover CSS rules to [data-playhtml-hover] via CSSOM
  // so users don't need to manually rewrite their hover styles.
  "can-hover": {
    defaultData: {},
    myDefaultAwareness: { hover: false },
    onMount: ({ getElement: t, setMyAwareness: e }) => {
      const n = t();
      n.addEventListener("mouseenter", () => {
        e({ hover: true }), n.setAttribute("data-playhtml-hover", "");
      }), n.addEventListener("mouseleave", () => {
        e({ hover: false }), n.removeAttribute("data-playhtml-hover");
      });
    },
    updateElement: () => {
    },
    updateElementAwareness: ({ element: t, awareness: e }) => {
      e.some((n) => n == null ? void 0 : n.hover) ? t.setAttribute("data-playhtml-hover", "") : t.removeAttribute("data-playhtml-hover");
    }
  },
  "can-mirror": Op
};
function Sg() {
  const t = [];
  if (document.querySelectorAll("[shared]").forEach((e) => {
    var _a4;
    const n = e, s2 = n.id;
    if (!s2) return;
    const r = `${window.location.host}${Go(
      window.location.pathname
    )}#${s2}`;
    t.push({
      type: "source",
      elementId: s2,
      dataSource: r,
      normalized: r,
      permissions: ((_a4 = n.getAttribute("shared")) == null ? void 0 : _a4.includes("read-only")) ? "read-only" : "read-write",
      element: n
    });
  }), document.querySelectorAll("[data-source]").forEach((e) => {
    const n = e, s2 = n.getAttribute("data-source") || "", [r, i] = s2.split("#");
    if (!r || !i) return;
    const o2 = r.indexOf("/"), c2 = o2 === -1 ? r : r.slice(0, o2), a2 = o2 === -1 ? "/" : r.slice(o2), l2 = `${c2}${Go(a2)}#${i}`;
    t.push({
      type: "consumer",
      elementId: i,
      dataSource: s2,
      normalized: l2,
      element: n
    });
  }), t.length > 0)
    try {
      console.table(
        t.map((e) => ({
          type: e.type,
          elementId: e.elementId,
          dataSource: e.dataSource,
          normalized: e.normalized,
          permissions: e.permissions || ""
        }))
      );
    } catch {
    }
  return t;
}
function Eg(t) {
  let e = false, n = false, s2 = false;
  async function r() {
    if (!s2) {
      if (e) {
        n = true;
        return;
      }
      e = true;
      try {
        await t();
      } finally {
        e = false, n && !s2 && (n = false, await r());
      }
    }
  }
  return {
    trigger: r,
    destroy() {
      s2 = true, n = false;
    }
  };
}
function _g(t) {
  const e = () => {
    t.trigger();
  }, n = () => {
    t.trigger();
  };
  window.addEventListener("popstate", e);
  const s2 = window.navigation;
  return s2 && typeof s2.addEventListener == "function" && s2.addEventListener("navigate", n), () => {
    window.removeEventListener("popstate", e), s2 && typeof s2.removeEventListener == "function" && s2.removeEventListener("navigate", n);
  };
}
function Ml(t) {
  document.dispatchEvent(
    new CustomEvent("playhtml:navigated", { detail: { room: t } })
  );
}
function bn() {
  return bn = Object.assign || function(t) {
    for (var e = 1; e < arguments.length; e++) {
      var n = arguments[e];
      for (var s2 in n)
        Object.prototype.hasOwnProperty.call(n, s2) && (t[s2] = n[s2]);
    }
    return t;
  }, bn.apply(this, arguments);
}
function Ag(t, e) {
  t.prototype = Object.create(e.prototype), t.prototype.constructor = t, Vr(t, e);
}
function Vr(t, e) {
  return Vr = Object.setPrototypeOf || function(s2, r) {
    return s2.__proto__ = r, s2;
  }, Vr(t, e);
}
function kg(t) {
  if (t === void 0)
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  return t;
}
var $l = function() {
  function t(n) {
    this.trigger = n, this.observing = /* @__PURE__ */ new Map();
  }
  var e = t.prototype;
  return e.registerConnection = function(s2) {
    var r = this.observing.get(s2.observable);
    r || (r = {
      byKey: /* @__PURE__ */ new Set(),
      iterate: false
    }, this.observing.set(s2.observable, r)), s2.type === "iterate" ? r.iterate = true : r.byKey.add(s2.key);
  }, e.removeObservers = function() {
    var s2 = this;
    this.observing.forEach(function(r, i) {
      r.iterate && i[R].connections.iterate.delete(s2), r.byKey.forEach(function(o2) {
        i[R].connections.byKey.get(o2).delete(s2);
      });
    }), this.observing.clear();
  }, t;
}();
var at = [];
var Ws = function(t) {
  Ag(e, t);
  function e(s2, r, i) {
    var o2;
    if (o2 = t.call(this, function() {
      return o2._trigger();
    }) || this, o2.func = s2, o2.options = r, o2.effect = i, o2.isInitial = true, o2.reaction = function() {
      at.push(kg(o2));
      try {
        o2.func();
      } finally {
        at.pop();
      }
      o2.effect && (!o2.isInitial || o2.options.fireImmediately) && o2.effect(), o2.isInitial = false;
    }, !i && !o2.options.fireImmediately)
      throw new Error("if no effect function passed, should always fireImmediately");
    return o2.reaction(), o2;
  }
  var n = e.prototype;
  return n._trigger = function() {
    if (at.includes(this))
      throw new Error("already running reaction");
    this.removeObservers(), this.reaction();
  }, e;
}($l);
function Ol() {
  return !!at.length;
}
function Pl() {
  return at.length ? at[at.length - 1] : void 0;
}
function xg(t, e, n) {
  var s2 = bn({
    name: "unnamed",
    fireImmediately: true
  }, n), r = new Ws(t, s2, e);
  return r;
}
var hs = 0;
function Rl() {
  return hs > 0;
}
function Kr(t) {
  hs++;
  try {
    return t();
  } finally {
    hs--, hs === 0 && Tg();
  }
}
var Wr = false;
function Nl() {
  return Wr;
}
function Ul(t) {
  Wr = true;
  try {
    t();
  } finally {
    Wr = false;
  }
}
function Dg(t) {
  return function() {
    return Ul(t);
  };
}
var Yr = [];
function Tg() {
  var t = [].concat(Yr);
  Yr = [], Fl(t);
}
function Fl(t) {
  var e = /* @__PURE__ */ new Set();
  t.forEach(function(n) {
    var s2;
    (n.type === "add" || n.type === "delete") && n.observable[R].connections.iterate.forEach(function(r) {
      e.add(r);
    }), (s2 = n.observable[R].connections.byKey.get(n.key)) == null || s2.forEach(function(r) {
      e.add(r);
    });
  }), e.forEach(function(n) {
    n.trigger();
  });
}
function Xn(t) {
  if (Rl()) {
    Yr.push(t);
    return;
  }
  Fl([t]);
}
function ec(t, e) {
  if (t.type === "iterate")
    t.observable[R].connections.iterate.add(e);
  else {
    var n = t.observable[R].connections.byKey.get(t.key);
    n || (n = /* @__PURE__ */ new Set(), t.observable[R].connections.byKey.set(t.key, n)), n.add(e);
  }
}
function Zn(t, e) {
  if (!Nl()) {
    var n = Pl();
    n && (ec(t, n), n.registerConnection(t)), e && (ec(t, e), e.registerConnection(t));
  }
}
var Hi = Symbol("$skipreactive");
var R = Symbol("$reactive");
var ae = Symbol("$reactiveproxy");
function jn(t, e) {
  return !!(t && t[ae] && t[ae].implicitObserver === e);
}
function zi(t) {
  return t[Hi] = true, t;
}
function jl(t) {
  return !!(t && !jn(t) && t[R]);
}
function Gr(t, e, n) {
  if (n === void 0 && (n = false), t[Hi] || jn(t, e))
    return t;
  var s2 = Lg(t, n);
  if (!e)
    return s2;
  var r = s2[R].proxiesWithImplicitObserver.get(e);
  if (!r) {
    var i = {
      implicitObserver: e
    };
    Object.setPrototypeOf(i, Hl), r = new Proxy(s2[R].raw, i), s2[R].proxiesWithImplicitObserver.set(e, r);
  }
  return r;
}
var vn = Gr;
function Lg(t, e) {
  if (e === void 0 && (e = false), jn(t))
    return t;
  if (jl(t))
    return t[R].proxy;
  if (t[R] || t[ae])
    throw new Error("unexpected");
  var n = {
    connections: {
      iterate: /* @__PURE__ */ new Set(),
      byKey: /* @__PURE__ */ new Map()
    },
    proxy: {},
    raw: t,
    proxiesWithImplicitObserver: /* @__PURE__ */ new Map(),
    shallow: e
  };
  Object.defineProperty(t, R, {
    enumerable: false,
    writable: true,
    configurable: true,
    value: n
  });
  var s2 = new Proxy(t, Hl);
  return n.proxy = s2, s2;
}
var Hl = {
  // Read:
  has: function(e, n) {
    var s2 = Reflect.has(e, n);
    return typeof n == "symbol" || Zn({
      observable: e,
      key: n,
      type: "has"
    }, this.implicitObserver), s2;
  },
  get: function(e, n, s2) {
    if (n === ae)
      return {
        implicitObserver: this.implicitObserver
      };
    var r = Reflect.get(e, n, s2);
    if (typeof n == "symbol")
      return n.toString() === "Symbol($reactiveproxy)" && console.error("warning, Symbol($reactiveproxy) passed, but does not match $reactiveproxy. Multiple Reactive libraries loaded?"), r;
    if (n === "length" && Array.isArray(e) ? Zn({
      observable: e,
      type: "iterate"
    }, this.implicitObserver) : Zn({
      observable: e,
      key: n,
      type: "get"
    }, this.implicitObserver), jl(r))
      return Gr(r, this.implicitObserver);
    if (e[R].shallow)
      return r;
    if (typeof r == "object" && r !== null && !jn(r, this.implicitObserver) && !Object.isFrozen(r)) {
      var i = Reflect.getOwnPropertyDescriptor(e, n);
      if ((!i || !(i.writable === false && i.configurable === false)) && (Ol() || this.implicitObserver))
        return Gr(r, this.implicitObserver);
    }
    return r;
  },
  ownKeys: function(e) {
    return Zn({
      observable: e,
      type: "iterate"
    }, this.implicitObserver), Reflect.ownKeys(e);
  },
  // Write:
  set: function(e, n, s2, r) {
    return Kr(function() {
      if (typeof n == "symbol")
        return Reflect.set(e, n, s2, r);
      var i = Object.hasOwnProperty.call(e, n), o2 = Reflect.get(e, n, r), c2 = Reflect.set(e, n, s2, r);
      if (!i)
        Xn({
          observable: e,
          key: n,
          value: s2,
          type: "add"
        });
      else if (s2 !== o2)
        if (n === "length" && Array.isArray(e)) {
          if (!(o2 < s2))
            for (var a2 = s2 + 1; a2 <= o2; a2++)
              Xn({
                observable: e,
                key: "" + (a2 - 1),
                oldValue: void 0,
                type: "delete"
              });
        } else
          Xn({
            observable: e,
            key: n,
            value: s2,
            oldValue: o2,
            type: "update"
          });
      return c2;
    });
  },
  deleteProperty: function(e, n) {
    return Kr(function() {
      if (typeof n == "symbol")
        return Reflect.deleteProperty(e, n);
      var s2 = Object.hasOwnProperty.call(e, n), r = Reflect.get(e, n), i = Reflect.deleteProperty(e, n);
      return s2 && Xn({
        observable: e,
        key: n,
        oldValue: r,
        type: "delete"
      }), i;
    });
  },
  preventExtensions: function(e) {
    throw new Error("Dynamic observable objects cannot be frozen");
  }
};
function Ig(t, e) {
  var n = bn({
    name: "unnamed",
    fireImmediately: true
  }, e), s2 = new Ws(t, n);
  return s2;
}
function Mg(t, e, n) {
  var s2 = bn({
    name: "unnamed",
    fireImmediately: true
  }, n), r = new Ws(function() {
    t(e);
  }, s2);
  return e = vn(e, r), s2.fireImmediately && r.trigger(), r;
}
var zl = function() {
  function t() {
    this._observable = vn({
      _key: 1
    });
  }
  var e = t.prototype;
  return e.reportObserved = function(s2) {
    return vn(this._observable, s2)._key;
  }, e.reportChanged = function() {
    this._observable._key++;
  }, t;
}();
function $g(t, e, n) {
  return new zl();
}
var Og = Object.freeze(Object.defineProperty({
  __proto__: null,
  $reactive: R,
  $reactiveproxy: ae,
  $skipreactive: Hi,
  Atom: zl,
  Observer: $l,
  Reaction: Ws,
  autorun: Ig,
  autorunAsync: Mg,
  createAtom: $g,
  hasRunningReaction: Ol,
  isActionRunning: Rl,
  isReactive: jn,
  isTrackingDisabled: Nl,
  markRaw: zi,
  reaction: xg,
  reactive: vn,
  runInAction: Kr,
  runningReaction: Pl,
  untracked: Ul,
  untrackedCB: Dg
}, Symbol.toStringTag, { value: "Module" }));
var qr;
var Jr;
var Pg = (t) => t();
function Rg(t, e) {
  if (Jr)
    return Jr(t, e);
  Pg(t);
}
function pt(t, e, n) {
  if (qr)
    return qr.apply(null, arguments);
  throw new Error("observable implementation not provided. Call enableReactiveBindings, enableVueBindings or enableMobxBindings.");
}
function Ng(t) {
  qr = function(e, n, s2) {
    const r = t.createAtom(e);
    return n && n(), r;
  }, Jr = (e, n) => t.reaction(e, n, {
    fireImmediately: false
  });
}
var tc = /* @__PURE__ */ new WeakSet();
function Ug(t) {
  if (tc.has(t))
    return t;
  tc.add(t);
  let e;
  const n = /* @__PURE__ */ new Map();
  function s2() {
    if (!e) {
      const h = (d) => {
        (d.changes.added.size || d.changes.deleted.size || d.changes.keys.size || d.changes.delta.length) && e.reportChanged();
      };
      e = pt("map", () => {
        t.observe(h);
      }, () => {
        t.unobserve(h);
      });
    }
    e.reportObserved(t._implicitObserver);
  }
  function r(h) {
    let d = n.get(h);
    if (!d) {
      const f2 = (p) => {
        d.reportChanged();
      };
      d = pt(h + "", () => {
        t.observe(f2);
      }, () => {
        t.unobserve(f2);
      }), n.set(h, d);
    }
    d.reportObserved(t._implicitObserver);
  }
  const i = t.get;
  t.get = function(h) {
    if (typeof h != "number")
      throw new Error("unexpected");
    return r(h), Reflect.apply(i, this, arguments);
  };
  function o2(h) {
    const d = t[h];
    t[h] = function() {
      return s2(), Reflect.apply(d, this, arguments);
    };
  }
  function c2(h) {
    let d = t, f2 = Object.getOwnPropertyDescriptor(d, h);
    if (f2 || (d = Object.getPrototypeOf(d), f2 = Object.getOwnPropertyDescriptor(d, h)), f2 || (d = Object.getPrototypeOf(d), f2 = Object.getOwnPropertyDescriptor(d, h)), !f2)
      throw new Error("property not found");
    const p = f2.get;
    f2.get = function() {
      return this._disableTracking || s2(), Reflect.apply(p, this, arguments);
    }, Object.defineProperty(t, h, f2);
  }
  function a2(h, d) {
    let f2 = t, p = Object.getOwnPropertyDescriptor(f2, h);
    if (p || (f2 = Object.getPrototypeOf(f2), p = Object.getOwnPropertyDescriptor(f2, h)), p || (f2 = Object.getPrototypeOf(f2), p = Object.getOwnPropertyDescriptor(f2, h)), !p)
      throw new Error("property not found");
    Object.defineProperty(t, d, p);
  }
  o2("forEach"), o2("toJSON"), o2("toArray"), o2("slice"), o2("map"), a2("length", "lengthUntracked"), c2("length");
  const l2 = t.push;
  t.push = function(h) {
    this._disableTracking = true;
    const d = l2.call(this, h);
    return this._disableTracking = false, d;
  };
  const u = t.slice;
  return t.slice = function(h, d) {
    this._disableTracking = true;
    const f2 = u.call(this, h, d);
    return this._disableTracking = false, f2;
  }, t;
}
var nc = /* @__PURE__ */ new WeakSet();
function Fg(t) {
  if (nc.has(t))
    return t;
  nc.add(t);
  let e;
  function n() {
    if (!e) {
      let i = Array.from(t.share.keys());
      const o2 = (c2) => {
        const a2 = Array.from(t.share.keys());
        JSON.stringify(i) !== JSON.stringify(a2) && (i = a2, e.reportChanged());
      };
      e = pt("map", () => {
        t.on("beforeObserverCalls", o2);
      }, () => {
        t.off("beforeObserverCalls", o2);
      });
    }
    e.reportObserved(t._implicitObserver);
  }
  const s2 = t.get;
  t.get = function(i) {
    if (typeof i != "string")
      throw new Error("unexpected");
    const o2 = Reflect.apply(s2, this, arguments);
    return Ys(o2), o2;
  };
  function r(i) {
    const o2 = t[i];
    let c2;
    t[i] = function() {
      let a2, l2 = arguments;
      return n(), c2 && c2.removeObservers(), c2 = Rg(() => (a2 = Reflect.apply(o2, t, l2), a2), () => e.reportChanged()), a2;
    };
  }
  return r("toJSON"), Object.defineProperty(t, "keys", {
    get: () => (n(), Object.keys(t.share))
  }), t;
}
var sc = /* @__PURE__ */ new WeakSet();
function jg(t) {
  if (sc.has(t))
    return t;
  sc.add(t);
  let e;
  const n = /* @__PURE__ */ new Map();
  function s2() {
    if (!e) {
      const c2 = (a2) => {
        (a2.changes.added.size || a2.changes.deleted.size || a2.changes.keys.size || a2.changes.delta.length) && e.reportChanged();
      };
      e = pt("map", () => {
        t.observe(c2);
      }, () => {
        t.unobserve(c2);
      });
    }
    e.reportObserved(t._implicitObserver);
  }
  function r(c2) {
    let a2 = n.get(c2);
    if (!a2) {
      const l2 = (u) => {
        u.keysChanged.has(c2) && (u.changes.added.size || u.changes.deleted.size || u.changes.keys.size || u.changes.delta.length) && a2.reportChanged();
      };
      a2 = pt(c2, () => {
        t.observe(l2);
      }, () => {
        t.unobserve(l2);
      }), n.set(c2, a2);
    }
    a2.reportObserved(t._implicitObserver);
  }
  const i = t.get;
  t.get = function(c2) {
    if (typeof c2 != "string")
      throw new Error("unexpected");
    return r(c2), Reflect.apply(i, this, arguments);
  };
  function o2(c2) {
    const a2 = t[c2];
    t[c2] = function() {
      return s2(), Reflect.apply(a2, this, arguments);
    };
  }
  return o2("values"), o2("entries"), o2("keys"), o2("forEach"), o2("toJSON"), t;
}
var rc = /* @__PURE__ */ new WeakSet();
function ic(t) {
  if (rc.has(t))
    return t;
  rc.add(t);
  let e;
  const n = (r) => {
    e.reportChanged();
  };
  e = pt("text", () => {
    t.observe(n);
  }, () => {
    t.unobserve(n);
  });
  function s2(r) {
    const i = t[r];
    t[r] = function() {
      return e.reportObserved(this._implicitObserver), Reflect.apply(i, this, arguments);
    };
  }
  return s2("toString"), s2("toJSON"), t;
}
var oc = /* @__PURE__ */ new WeakSet();
function cc(t) {
  if (oc.has(t))
    return t;
  oc.add(t);
  let e;
  const n = (i) => {
    (i.changes.added.size || i.changes.deleted.size || i.changes.keys.size || i.changes.delta.length) && e.reportChanged();
  };
  e = pt("xml", () => {
    t.observe(n);
  }, () => {
    t.unobserve(n);
  });
  function s2(i) {
    const o2 = t[i];
    t[i] = function() {
      return e.reportObserved(this._implicitObserver), Reflect.apply(o2, this, arguments);
    };
  }
  function r(i) {
    let o2 = t, c2 = Object.getOwnPropertyDescriptor(o2, i);
    if (c2 || (o2 = Object.getPrototypeOf(o2), c2 = Object.getOwnPropertyDescriptor(o2, i)), c2 || (o2 = Object.getPrototypeOf(o2), c2 = Object.getOwnPropertyDescriptor(o2, i)), !c2)
      throw new Error("property not found");
    const a2 = c2.get;
    c2.get = function() {
      return e.reportObserved(this._implicitObserver), Reflect.apply(a2, this, arguments);
    }, Object.defineProperty(t, i, c2);
  }
  return s2("toString"), s2("toDOM"), s2("toArray"), s2("getAttribute"), r("firstChild"), t;
}
function Ys(t) {
  return t instanceof dt || t instanceof Ie ? ic(t) : t instanceof be ? Ug(t) : t instanceof Se ? jg(t) : t instanceof me || Object.prototype.hasOwnProperty.call(t, "autoLoad") ? Fg(t) : t instanceof Ee || t instanceof Me ? cc(t) : t;
}
function ac(t) {
  t.share.forEach((e) => {
    e.constructor !== L && Ys(e);
  });
}
function lc(t, e) {
  for (let s2 = t.length - 1; s2 >= e; s2--) {
    let r = t[s2];
    if (!r.deleted) {
      var n;
      if (r instanceof re)
        continue;
      (n = r.content) == null || n.getContent().forEach((i) => {
        i instanceof L && Ys(i);
      });
    }
  }
}
var uc = /* @__PURE__ */ new WeakSet();
function Hg(t) {
  uc.has(t) || (uc.add(t), Ys(t), t.store.clients.forEach((e) => {
    e && lc(e, 0);
  }), ac(t), t.on("beforeObserverCalls", (e) => {
    ac(t), e.afterState.forEach((n, s2) => {
      const r = e.beforeState.get(s2) || 0;
      if (r !== n) {
        const i = e.doc.store.clients.get(s2);
        if (!i)
          return;
        const o2 = ge(i, r);
        lc(i, o2);
      }
    });
  }));
}
var Cn = class {
  constructor(e) {
    this.value = void 0, this.value = e;
  }
};
function zg(t) {
  return ArrayBuffer.isView(t) ? new Cn(t) : new Cn(Object.freeze(t));
}
function Bg(t) {
  const e = function() {
    var c2;
    let a2 = (c2 = this[ae]) == null ? void 0 : c2.implicitObserver;
    return t._implicitObserver = a2, t.slice.bind(t).apply(t, arguments).map((u) => {
      const h = Gs(u, a2);
      return a2 && typeof h == "object" ? vn(h, a2) : h;
    });
  }, n = function(c2) {
    return c2.map((a2) => {
      const l2 = Bi(a2);
      let u = ye(l2) || l2;
      if (u instanceof Cn && (u = u.value), u instanceof L && u.parent)
        throw new Error("Not supported: reassigning object that already occurs in the tree.");
      return u;
    });
  }, s2 = function() {
    return [].findIndex.apply(e.apply(this), arguments);
  }, r = {
    // get length() {
    //   return arr.length;
    // },
    // set length(val: number) {
    //   throw new Error("set length of yjs array is unsupported");
    // },
    slice: e,
    unshift: (...o2) => (t.unshift(n(o2)), t.lengthUntracked),
    push: (...o2) => (t.push(n(o2)), t.lengthUntracked),
    insert: t.insert.bind(t),
    toJSON: t.toJSON.bind(t),
    forEach: function() {
      return [].forEach.apply(e.apply(this), arguments);
    },
    every: function() {
      return [].every.apply(e.apply(this), arguments);
    },
    filter: function() {
      return [].filter.apply(e.apply(this), arguments);
    },
    find: function() {
      return [].find.apply(e.apply(this), arguments);
    },
    findIndex: s2,
    some: function() {
      return [].some.apply(e.apply(this), arguments);
    },
    includes: function() {
      return [].includes.apply(e.apply(this), arguments);
    },
    map: function() {
      return [].map.apply(e.apply(this), arguments);
    },
    indexOf: function() {
      const o2 = arguments[0];
      return s2.call(this, (c2) => Yg(c2, o2));
    },
    splice: function() {
      let o2 = arguments[0] < 0 ? t.length - Math.abs(arguments[0]) : arguments[0], c2 = arguments[1], a2 = Array.from(Array.from(arguments).slice(2)), l2 = e.apply(this, [o2, Number.isInteger(c2) ? o2 + c2 : void 0]);
      return t.doc ? t.doc.transact(() => {
        t.delete(o2, c2), t.insert(o2, n(a2));
      }) : (t.delete(o2, c2), t.insert(o2, n(a2))), l2;
    }
    // toJSON = () => {
    //   return this.arr.toJSON() slice();
    // };
    // delete = this.arr.delete.bind(this.arr) as (Y.Array<T>)["delete"];
  }, i = [];
  for (let o2 in r)
    i[o2] = r[o2];
  return i;
}
function Yt(t) {
  if (typeof t == "string" && t.trim().length) {
    const e = Number(t);
    if (Number.isInteger(e))
      return e;
  }
  return t;
}
function hc(t, e = new be()) {
  if (e[R])
    throw new Error("unexpected");
  const n = Bg(e), s2 = new Proxy(n, {
    set: (r, i, o2) => {
      throw typeof Yt(i) != "number" ? new Error() : new Error("array assignment is not implemented / supported");
    },
    get: (r, i, o2) => {
      const c2 = Yt(i);
      if (c2 === qs)
        return e;
      if (typeof c2 == "number") {
        let u;
        if (o2 && o2[ae]) {
          var a2;
          u = (a2 = o2[ae]) == null ? void 0 : a2.implicitObserver, e._implicitObserver = u;
        }
        let h = e.get(c2);
        return h = Gs(h, u), h;
      }
      if (c2 === Symbol.toStringTag)
        return "Array";
      if (c2 === Symbol.iterator) {
        const u = e.slice();
        return Reflect.get(u, c2);
      }
      return c2 === "length" ? e.length : Reflect.get(r, c2, o2);
    },
    // getOwnPropertyDescriptor: (target, pArg) => {
    //   const p = propertyToNumber(pArg);
    //   if (typeof p === "number" && p < arr.length && p >= 0) {
    //     return { configurable: true, enumerable: true, value: arr.get(p) };
    //   } else {
    //     return undefined;
    //   }
    // },
    deleteProperty: (r, i) => {
      const o2 = Yt(i);
      if (typeof o2 != "number")
        throw new Error();
      return o2 < e.lengthUntracked && o2 >= 0 ? (e.delete(o2), true) : false;
    },
    has: (r, i) => {
      const o2 = Yt(i);
      return typeof o2 != "number" ? Reflect.has(r, o2) : o2 < e.lengthUntracked && o2 >= 0;
    },
    getOwnPropertyDescriptor(r, i) {
      const o2 = Yt(i);
      if (o2 === "length")
        return {
          enumerable: false,
          configurable: false,
          writable: true
        };
      if (typeof o2 == "number" && o2 >= 0 && o2 < e.lengthUntracked)
        return {
          enumerable: true,
          configurable: true,
          writable: true
        };
    },
    ownKeys: (r) => {
      const i = [];
      for (let o2 = 0; o2 < e.length; o2++)
        i.push(o2 + "");
      return i.push("length"), i;
    }
  });
  return n.push.apply(s2, t), s2;
}
function dc(t, e = new Se()) {
  if (e[R])
    throw new Error("unexpected");
  const n = new Proxy({}, {
    set: (s2, r, i) => {
      if (typeof r != "string")
        throw new Error();
      const o2 = Bi(i);
      let c2 = ye(o2) || o2;
      if (c2 instanceof Cn && (c2 = c2.value), c2 instanceof L && c2.parent)
        throw new Error("Not supported: reassigning object that already occurs in the tree.");
      return e.set(r, c2), true;
    },
    get: (s2, r, i) => {
      if (r === qs)
        return e;
      if (typeof r != "string")
        return Reflect.get(s2, r);
      let o2;
      if (i && i[ae]) {
        var c2;
        o2 = (c2 = i[ae]) == null ? void 0 : c2.implicitObserver, e._implicitObserver = o2;
      }
      let a2 = e.get(r);
      return a2 = Gs(a2, o2), a2;
    },
    deleteProperty: (s2, r) => {
      if (typeof r != "string")
        throw new Error();
      return e.has(r) ? (e.delete(r), true) : false;
    },
    has: (s2, r) => !!(typeof r == "string" && e.has(r)),
    getOwnPropertyDescriptor(s2, r) {
      if (typeof r == "string" && e.has(r))
        return {
          enumerable: true,
          configurable: true
        };
    },
    ownKeys: (s2) => Array.from(e.keys())
  });
  rn.set(e, n);
  for (let s2 in t)
    n[s2] = t[s2];
  return n;
}
function Vg(t) {
  return t instanceof L;
}
var rn = /* @__PURE__ */ new WeakMap();
function Gs(t, e) {
  if (Vg(t)) {
    if (t._implicitObserver = e, t instanceof be || t instanceof Se) {
      if (!rn.has(t)) {
        const n = Bi(t);
        rn.set(t, n);
      }
      t = rn.get(t);
    } else if (t instanceof Me || t instanceof Ee || t instanceof dt || t instanceof Nt || t instanceof Ie)
      zi(t), t.__v_skip = true;
    else
      throw new Error("unknown YType");
    return t;
  } else {
    if (t === null)
      return null;
    if (typeof t == "object")
      return zg(t);
  }
  return t;
}
function Bi(t) {
  if (t == null)
    return t;
  if (t = ye(t) || t, t instanceof be)
    return hc([], t);
  if (t instanceof Se)
    return dc({}, t);
  if (typeof t == "string")
    return t;
  if (Array.isArray(t))
    return hc(t);
  if (t instanceof Me || t instanceof Ee || t instanceof dt || t instanceof Nt)
    return t;
  if (t instanceof Ie)
    return t;
  if (typeof t == "object")
    return t instanceof Cn ? t : dc(t);
  if (typeof t == "number" || typeof t == "boolean")
    return t;
  throw new Error("invalid");
}
function Kg(t) {
  for (let [e, n] of Object.entries(t))
    if (Array.isArray(n)) {
      if (n.length !== 0)
        throw new Error("Root Array initializer must always be empty array");
    } else if (n && typeof n == "object") {
      if (Object.keys(n).length !== 0 || Object.getPrototypeOf(n) !== Object.prototype)
        throw new Error("Root Object initializer must always be {}");
    } else if (n !== "xml" && n !== "text")
      throw new Error("unknown Root initializer");
}
function fc(t, e, n) {
  let s2 = e[n];
  if (!s2) {
    n !== "__v_raw" && n !== "__v_isRef" && n !== "__v_isReadonly" && console.warn("property not found on root doc", n);
    return;
  }
  return s2 === "xml" ? t.getXmlFragment(n) : s2 === "text" ? t.getText(n) : Array.isArray(s2) ? t.getArray(n) : t.getMap(n);
}
function Wg(t, e) {
  if (t[R])
    throw new Error("unexpected");
  Kg(e);
  const n = new Proxy({}, {
    set: (s2, r, i) => {
      throw typeof r != "string" ? new Error() : new Error("cannot set new elements on root doc");
    },
    get: (s2, r, i) => {
      if (r === qs)
        return t;
      if (typeof r != "string")
        return Reflect.get(s2, r);
      let o2;
      if (i && i[ae]) {
        var c2;
        o2 = (c2 = i[ae]) == null ? void 0 : c2.implicitObserver, t._implicitObserver = o2;
      }
      if (r === "toJSON") {
        for (let u of Object.keys(e))
          fc(t, e, u);
        return Reflect.get(t, r);
      }
      let a2 = fc(t, e, r);
      return a2 = Gs(a2, o2), a2;
    },
    deleteProperty: (s2, r) => {
      throw new Error("deleteProperty not available for doc");
    },
    has: (s2, r) => !!(typeof r == "string" && t.share.has(r)),
    getOwnPropertyDescriptor(s2, r) {
      if (typeof r == "string" && t.share.has(r) || r === "toJSON")
        return {
          enumerable: true,
          configurable: true
        };
    },
    ownKeys: (s2) => Array.from(t.share.keys())
  });
  return rn.set(t, n), n;
}
Ng(Og);
var qs = Symbol("INTERNAL_SYMBOL");
function Bl(t) {
  const e = ye(t);
  if (!(e instanceof me))
    throw new Error("store is not a valid syncedStore that maps to a Y.Doc");
  return e;
}
function ye(t) {
  if (typeof t != "object" || t === null)
    return;
  const e = t[qs];
  return e && (zi(e), e.__v_skip = true), e;
}
function Yg(t, e) {
  if (t === e)
    return true;
  if (typeof t == "object" && typeof e == "object") {
    const n = ye(t), s2 = ye(e);
    return !n || !s2 ? false : n === s2;
  }
  return false;
}
function Vl(t, e = new me()) {
  return Hg(e), Wg(e, t);
}
var Vi = globalThis;
var pc = (t) => t;
var Ts = Vi.trustedTypes;
var gc = Ts ? Ts.createPolicy("lit-html", { createHTML: (t) => t }) : void 0;
var Kl = "$lit$";
var Ue = `lit$${Math.random().toFixed(9).slice(2)}$`;
var Wl = "?" + Ue;
var Gg = `<${Wl}>`;
var gt = document;
var Sn = () => gt.createComment("");
var En = (t) => t === null || typeof t != "object" && typeof t != "function";
var Ki = Array.isArray;
var qg = (t) => Ki(t) || typeof (t == null ? void 0 : t[Symbol.iterator]) == "function";
var wr = `[ 	
\f\r]`;
var Gt = /<(?:(!--|\/[^a-zA-Z])|(\/?[a-zA-Z][^>\s]*)|(\/?$))/g;
var yc = /-->/g;
var mc = />/g;
var Je = RegExp(`>|${wr}(?:([^\\s"'>=/]+)(${wr}*=${wr}*(?:[^ 	
\f\r"'\`<>=]|("|')|))|$)`, "g");
var wc = /'/g;
var bc = /"/g;
var Yl = /^(?:script|style|textarea|title)$/i;
var Gl = (t) => (e, ...n) => ({ _$litType$: t, strings: e, values: n });
var f0 = Gl(1);
var p0 = Gl(2);
var Ye = Symbol.for("lit-noChange");
var F = Symbol.for("lit-nothing");
var vc = /* @__PURE__ */ new WeakMap();
var nt = gt.createTreeWalker(gt, 129);
function ql(t, e) {
  if (!Ki(t) || !t.hasOwnProperty("raw")) throw Error("invalid template strings array");
  return gc !== void 0 ? gc.createHTML(e) : e;
}
var Jg = (t, e) => {
  const n = t.length - 1, s2 = [];
  let r, i = e === 2 ? "<svg>" : e === 3 ? "<math>" : "", o2 = Gt;
  for (let c2 = 0; c2 < n; c2++) {
    const a2 = t[c2];
    let l2, u, h = -1, d = 0;
    for (; d < a2.length && (o2.lastIndex = d, u = o2.exec(a2), u !== null); ) d = o2.lastIndex, o2 === Gt ? u[1] === "!--" ? o2 = yc : u[1] !== void 0 ? o2 = mc : u[2] !== void 0 ? (Yl.test(u[2]) && (r = RegExp("</" + u[2], "g")), o2 = Je) : u[3] !== void 0 && (o2 = Je) : o2 === Je ? u[0] === ">" ? (o2 = r ?? Gt, h = -1) : u[1] === void 0 ? h = -2 : (h = o2.lastIndex - u[2].length, l2 = u[1], o2 = u[3] === void 0 ? Je : u[3] === '"' ? bc : wc) : o2 === bc || o2 === wc ? o2 = Je : o2 === yc || o2 === mc ? o2 = Gt : (o2 = Je, r = void 0);
    const f2 = o2 === Je && t[c2 + 1].startsWith("/>") ? " " : "";
    i += o2 === Gt ? a2 + Gg : h >= 0 ? (s2.push(l2), a2.slice(0, h) + Kl + a2.slice(h) + Ue + f2) : a2 + Ue + (h === -2 ? c2 : f2);
  }
  return [ql(t, i + (t[n] || "<?>") + (e === 2 ? "</svg>" : e === 3 ? "</math>" : "")), s2];
};
var _n = class __n {
  constructor({ strings: e, _$litType$: n }, s2) {
    let r;
    this.parts = [];
    let i = 0, o2 = 0;
    const c2 = e.length - 1, a2 = this.parts, [l2, u] = Jg(e, n);
    if (this.el = __n.createElement(l2, s2), nt.currentNode = this.el.content, n === 2 || n === 3) {
      const h = this.el.content.firstChild;
      h.replaceWith(...h.childNodes);
    }
    for (; (r = nt.nextNode()) !== null && a2.length < c2; ) {
      if (r.nodeType === 1) {
        if (r.hasAttributes()) for (const h of r.getAttributeNames()) if (h.endsWith(Kl)) {
          const d = u[o2++], f2 = r.getAttribute(h).split(Ue), p = /([.?@])?(.*)/.exec(d);
          a2.push({ type: 1, index: i, name: p[2], strings: f2, ctor: p[1] === "." ? Zg : p[1] === "?" ? Qg : p[1] === "@" ? ey : Js }), r.removeAttribute(h);
        } else h.startsWith(Ue) && (a2.push({ type: 6, index: i }), r.removeAttribute(h));
        if (Yl.test(r.tagName)) {
          const h = r.textContent.split(Ue), d = h.length - 1;
          if (d > 0) {
            r.textContent = Ts ? Ts.emptyScript : "";
            for (let f2 = 0; f2 < d; f2++) r.append(h[f2], Sn()), nt.nextNode(), a2.push({ type: 2, index: ++i });
            r.append(h[d], Sn());
          }
        }
      } else if (r.nodeType === 8) if (r.data === Wl) a2.push({ type: 2, index: i });
      else {
        let h = -1;
        for (; (h = r.data.indexOf(Ue, h + 1)) !== -1; ) a2.push({ type: 7, index: i }), h += Ue.length - 1;
      }
      i++;
    }
  }
  static createElement(e, n) {
    const s2 = gt.createElement("template");
    return s2.innerHTML = e, s2;
  }
};
function Ut(t, e, n = t, s2) {
  var _a4, _b;
  if (e === Ye) return e;
  let r = s2 !== void 0 ? (_a4 = n._$Co) == null ? void 0 : _a4[s2] : n._$Cl;
  const i = En(e) ? void 0 : e._$litDirective$;
  return (r == null ? void 0 : r.constructor) !== i && ((_b = r == null ? void 0 : r._$AO) == null ? void 0 : _b.call(r, false), i === void 0 ? r = void 0 : (r = new i(t), r._$AT(t, n, s2)), s2 !== void 0 ? (n._$Co ?? (n._$Co = []))[s2] = r : n._$Cl = r), r !== void 0 && (e = Ut(t, r._$AS(t, e.values), r, s2)), e;
}
var Xg = class {
  constructor(e, n) {
    this._$AV = [], this._$AN = void 0, this._$AD = e, this._$AM = n;
  }
  get parentNode() {
    return this._$AM.parentNode;
  }
  get _$AU() {
    return this._$AM._$AU;
  }
  u(e) {
    const { el: { content: n }, parts: s2 } = this._$AD, r = ((e == null ? void 0 : e.creationScope) ?? gt).importNode(n, true);
    nt.currentNode = r;
    let i = nt.nextNode(), o2 = 0, c2 = 0, a2 = s2[0];
    for (; a2 !== void 0; ) {
      if (o2 === a2.index) {
        let l2;
        a2.type === 2 ? l2 = new Vt(i, i.nextSibling, this, e) : a2.type === 1 ? l2 = new a2.ctor(i, a2.name, a2.strings, this, e) : a2.type === 6 && (l2 = new ty(i, this, e)), this._$AV.push(l2), a2 = s2[++c2];
      }
      o2 !== (a2 == null ? void 0 : a2.index) && (i = nt.nextNode(), o2++);
    }
    return nt.currentNode = gt, r;
  }
  p(e) {
    let n = 0;
    for (const s2 of this._$AV) s2 !== void 0 && (s2.strings !== void 0 ? (s2._$AI(e, s2, n), n += s2.strings.length - 2) : s2._$AI(e[n])), n++;
  }
};
var Vt = class _Vt {
  get _$AU() {
    var _a4;
    return ((_a4 = this._$AM) == null ? void 0 : _a4._$AU) ?? this._$Cv;
  }
  constructor(e, n, s2, r) {
    this.type = 2, this._$AH = F, this._$AN = void 0, this._$AA = e, this._$AB = n, this._$AM = s2, this.options = r, this._$Cv = (r == null ? void 0 : r.isConnected) ?? true;
  }
  get parentNode() {
    let e = this._$AA.parentNode;
    const n = this._$AM;
    return n !== void 0 && (e == null ? void 0 : e.nodeType) === 11 && (e = n.parentNode), e;
  }
  get startNode() {
    return this._$AA;
  }
  get endNode() {
    return this._$AB;
  }
  _$AI(e, n = this) {
    e = Ut(this, e, n), En(e) ? e === F || e == null || e === "" ? (this._$AH !== F && this._$AR(), this._$AH = F) : e !== this._$AH && e !== Ye && this._(e) : e._$litType$ !== void 0 ? this.$(e) : e.nodeType !== void 0 ? this.T(e) : qg(e) ? this.k(e) : this._(e);
  }
  O(e) {
    return this._$AA.parentNode.insertBefore(e, this._$AB);
  }
  T(e) {
    this._$AH !== e && (this._$AR(), this._$AH = this.O(e));
  }
  _(e) {
    this._$AH !== F && En(this._$AH) ? this._$AA.nextSibling.data = e : this.T(gt.createTextNode(e)), this._$AH = e;
  }
  $(e) {
    var _a4;
    const { values: n, _$litType$: s2 } = e, r = typeof s2 == "number" ? this._$AC(e) : (s2.el === void 0 && (s2.el = _n.createElement(ql(s2.h, s2.h[0]), this.options)), s2);
    if (((_a4 = this._$AH) == null ? void 0 : _a4._$AD) === r) this._$AH.p(n);
    else {
      const i = new Xg(r, this), o2 = i.u(this.options);
      i.p(n), this.T(o2), this._$AH = i;
    }
  }
  _$AC(e) {
    let n = vc.get(e.strings);
    return n === void 0 && vc.set(e.strings, n = new _n(e)), n;
  }
  k(e) {
    Ki(this._$AH) || (this._$AH = [], this._$AR());
    const n = this._$AH;
    let s2, r = 0;
    for (const i of e) r === n.length ? n.push(s2 = new _Vt(this.O(Sn()), this.O(Sn()), this, this.options)) : s2 = n[r], s2._$AI(i), r++;
    r < n.length && (this._$AR(s2 && s2._$AB.nextSibling, r), n.length = r);
  }
  _$AR(e = this._$AA.nextSibling, n) {
    var _a4;
    for ((_a4 = this._$AP) == null ? void 0 : _a4.call(this, false, true, n); e !== this._$AB; ) {
      const s2 = pc(e).nextSibling;
      pc(e).remove(), e = s2;
    }
  }
  setConnected(e) {
    var _a4;
    this._$AM === void 0 && (this._$Cv = e, (_a4 = this._$AP) == null ? void 0 : _a4.call(this, e));
  }
};
var Js = class {
  get tagName() {
    return this.element.tagName;
  }
  get _$AU() {
    return this._$AM._$AU;
  }
  constructor(e, n, s2, r, i) {
    this.type = 1, this._$AH = F, this._$AN = void 0, this.element = e, this.name = n, this._$AM = r, this.options = i, s2.length > 2 || s2[0] !== "" || s2[1] !== "" ? (this._$AH = Array(s2.length - 1).fill(new String()), this.strings = s2) : this._$AH = F;
  }
  _$AI(e, n = this, s2, r) {
    const i = this.strings;
    let o2 = false;
    if (i === void 0) e = Ut(this, e, n, 0), o2 = !En(e) || e !== this._$AH && e !== Ye, o2 && (this._$AH = e);
    else {
      const c2 = e;
      let a2, l2;
      for (e = i[0], a2 = 0; a2 < i.length - 1; a2++) l2 = Ut(this, c2[s2 + a2], n, a2), l2 === Ye && (l2 = this._$AH[a2]), o2 || (o2 = !En(l2) || l2 !== this._$AH[a2]), l2 === F ? e = F : e !== F && (e += (l2 ?? "") + i[a2 + 1]), this._$AH[a2] = l2;
    }
    o2 && !r && this.j(e);
  }
  j(e) {
    e === F ? this.element.removeAttribute(this.name) : this.element.setAttribute(this.name, e ?? "");
  }
};
var Zg = class extends Js {
  constructor() {
    super(...arguments), this.type = 3;
  }
  j(e) {
    this.element[this.name] = e === F ? void 0 : e;
  }
};
var Qg = class extends Js {
  constructor() {
    super(...arguments), this.type = 4;
  }
  j(e) {
    this.element.toggleAttribute(this.name, !!e && e !== F);
  }
};
var ey = class extends Js {
  constructor(e, n, s2, r, i) {
    super(e, n, s2, r, i), this.type = 5;
  }
  _$AI(e, n = this) {
    if ((e = Ut(this, e, n, 0) ?? F) === Ye) return;
    const s2 = this._$AH, r = e === F && s2 !== F || e.capture !== s2.capture || e.once !== s2.once || e.passive !== s2.passive, i = e !== F && (s2 === F || r);
    r && this.element.removeEventListener(this.name, this, s2), i && this.element.addEventListener(this.name, this, e), this._$AH = e;
  }
  handleEvent(e) {
    var _a4;
    typeof this._$AH == "function" ? this._$AH.call(((_a4 = this.options) == null ? void 0 : _a4.host) ?? this.element, e) : this._$AH.handleEvent(e);
  }
};
var ty = class {
  constructor(e, n, s2) {
    this.element = e, this.type = 6, this._$AN = void 0, this._$AM = n, this.options = s2;
  }
  get _$AU() {
    return this._$AM._$AU;
  }
  _$AI(e) {
    Ut(this, e);
  }
};
var ny = { I: Vt };
var sy = Vi.litHtmlPolyfillSupport;
sy == null ? void 0 : sy(_n, Vt), (Vi.litHtmlVersions ?? (Vi.litHtmlVersions = [])).push("3.3.3");
var ry = (t, e, n) => {
  const s2 = e;
  let r = s2._$litPart$;
  return r === void 0 && (s2._$litPart$ = r = new Vt(e.insertBefore(Sn(), null), null, void 0, {})), r._$AI(t), r;
};
var Cc = (t, e = 300) => {
  let n;
  return function(...s2) {
    clearTimeout(n), n = setTimeout(() => t.apply(this, s2), e);
  };
};
var iy = class {
  constructor(e, n = {}) {
    __publicField(this, "defaultData");
    __publicField(this, "localData");
    __publicField(this, "awareness", []);
    __publicField(this, "awarenessByStableId", /* @__PURE__ */ new Map());
    __publicField(this, "selfAwareness");
    __publicField(this, "element");
    __publicField(this, "_data");
    __publicField(this, "onChange");
    __publicField(this, "onAwarenessChange");
    __publicField(this, "debouncedOnChange");
    __publicField(this, "resetShortcut");
    // TODO: change this to receive the delta instead of the whole data object so you don't have to maintain
    // internal state for expressing the delta.
    __publicField(this, "updateElement");
    __publicField(this, "view");
    __publicField(this, "updateElementAwareness");
    __publicField(this, "triggerAwarenessUpdate");
    __publicField(this, "devMode");
    // Set while a `view` render is in flight, so setData/setLocalData/
    // setMyAwareness can detect (and reject) writes made synchronously during
    // render — a re-render loop.
    __publicField(this, "isRendering", false);
    // Cleanup returned by onMount, invoked on destroy()/removePlayElement so
    // rAF loops, timers, and event listeners set up in onMount don't leak.
    __publicField(this, "onUnmount");
    // Allows the runtime to wire up capability descendants emitted by a view
    // (e.g. mount points for `define`d capabilities). Driven by descendantObserver.
    __publicField(this, "onAfterRender");
    __publicField(this, "descendantObserver");
    __publicField(this, "dataUpdateListeners", /* @__PURE__ */ new Set());
    __publicField(this, "scheduleSetupDataWrite");
    __publicField(this, "clickListener");
    __publicField(this, "touchStartListener");
    __publicField(this, "mouseDownListener");
    __publicField(this, "resetShortcutListener");
    __publicField(this, "activeDragCleanup");
    // event handlers
    __publicField(this, "onClick");
    __publicField(this, "onDrag");
    __publicField(this, "onDragStart");
    const {
      element: s2,
      onChange: r,
      onAwarenessChange: i,
      defaultData: o2,
      defaultLocalData: c2,
      myDefaultAwareness: a2,
      data: l2,
      awareness: u,
      updateElement: h,
      view: d,
      updateElementAwareness: f2,
      onMount: p,
      debounceMs: g,
      triggerAwarenessUpdate: m,
      devMode: w
    } = e;
    this.scheduleSetupDataWrite = n.scheduleSetupDataWrite, this.element = s2, this.view = d, this.devMode = w, this.defaultData = o2 instanceof Function ? o2(s2) : o2, this.localData = c2 instanceof Function ? c2(s2) : c2, this.triggerAwarenessUpdate = m, this.onChange = r, this.debouncedOnChange = Cc(this.onChange, g), this.onAwarenessChange = i, this.updateElement = h, this.updateElementAwareness = f2;
    const C = l2 === void 0 ? this.defaultData : l2;
    u !== void 0 && (this.awareness = u);
    const v = a2 instanceof Function ? a2(s2) : a2;
    if (v !== void 0 && this.setMyAwareness(v), this._data = C, this.__data = C, this.reinitializeElementData(e), p) {
      const E = p(this.getSetupData());
      typeof E == "function" && (this.onUnmount = E);
    }
  }
  /**
   * Tears down anything onMount set up (rAF loops, timers, listeners).
   * Called by removePlayElement / unregister(). Idempotent.
   */
  destroy() {
    var _a4;
    (_a4 = this.descendantObserver) == null ? void 0 : _a4.disconnect(), this.descendantObserver = void 0, this.clickListener && (this.element.removeEventListener("click", this.clickListener), this.clickListener = void 0), this.touchStartListener && (this.element.removeEventListener("touchstart", this.touchStartListener), this.touchStartListener = void 0), this.mouseDownListener && (this.element.removeEventListener("mousedown", this.mouseDownListener), this.mouseDownListener = void 0), this.resetShortcutListener && (this.element.removeEventListener("click", this.resetShortcutListener), this.resetShortcutListener = void 0), this.removeActiveDragListeners(), this.onClick = void 0, this.onDrag = void 0, this.onDragStart = void 0, this.resetShortcut = void 0;
    const e = this.onUnmount;
    if (this.onUnmount = void 0, e)
      try {
        e();
      } catch (n) {
        console.error(`[playhtml] onMount cleanup for "${this.element.id}" threw`, n);
      }
  }
  reinitializeElementData({
    element: e,
    onChange: n,
    onAwarenessChange: s2,
    updateElement: r,
    view: i,
    updateElementAwareness: o2,
    onClick: c2,
    onDrag: a2,
    onDragStart: l2,
    resetShortcut: u,
    debounceMs: h,
    triggerAwarenessUpdate: d,
    devMode: f2
  }) {
    this.triggerAwarenessUpdate = d, this.onChange = n, this.debouncedOnChange = Cc(this.onChange, h), this.onAwarenessChange = s2, this.updateElement = r, this.view = i, this.devMode = f2, i && this.updateElement && (console.error(
      `[playhtml] "${e.id}" provides both \`view\` and \`updateElement\`. They are mutually exclusive — \`view\` is used and \`updateElement\` is ignored.`
    ), this.updateElement = void 0), i && (c2 || a2 || l2) && (console.error(
      `[playhtml] "${e.id}" provides a \`view\` alongside onClick/onDrag/onDragStart. In view mode these are ignored — attach events inside the template (e.g. @click). `
    ), c2 = void 0, a2 = void 0, l2 = void 0), this.setEventHandlers({ onClick: c2, onDrag: a2, onDragStart: l2 }), u && !this.resetShortcutListener && (e.reset = this.reset, this.resetShortcutListener = (p) => {
      switch (this.resetShortcut) {
        case "ctrlKey":
          if (!p.ctrlKey)
            return;
          break;
        case "altKey":
          if (!p.altKey)
            return;
          break;
        case "shiftKey":
          if (!p.shiftKey)
            return;
          break;
        case "metaKey":
          if (!p.metaKey)
            return;
          break;
        default:
          return;
      }
      this.reset(), p.preventDefault(), p.stopPropagation();
    }, e.addEventListener("click", this.resetShortcutListener)), this.resetShortcut = u;
  }
  setEventHandlers({
    onClick: e,
    onDrag: n,
    onDragStart: s2
  }) {
    const r = this.element, i = !!(this.onDrag || this.onDragStart);
    if (this.view) {
      i && this.removeActiveDragListeners(), this.onClick = void 0, this.onDrag = void 0, this.onDragStart = void 0;
      return;
    }
    const o2 = !!(n || s2);
    i && !o2 && this.removeActiveDragListeners(), e && !this.clickListener && (this.clickListener = (c2) => {
      var _a4;
      (_a4 = this.onClick) == null ? void 0 : _a4.call(this, c2, this.getEventHandlerData());
    }, r.addEventListener("click", this.clickListener)), o2 && !this.touchStartListener && (this.touchStartListener = (c2) => {
      var _a4;
      if (!this.onDrag && !this.onDragStart) return;
      c2.preventDefault(), this.removeActiveDragListeners(), r.classList.add("cursordown"), (_a4 = this.onDragStart) == null ? void 0 : _a4.call(this, c2, this.getEventHandlerData());
      const a2 = (u) => {
        var _a5;
        u.preventDefault(), (_a5 = this.onDrag) == null ? void 0 : _a5.call(this, u, this.getEventHandlerData());
      }, l2 = () => {
        r.classList.remove("cursordown"), document.removeEventListener("touchmove", a2), document.removeEventListener("touchend", l2), this.activeDragCleanup === l2 && (this.activeDragCleanup = void 0);
      };
      this.activeDragCleanup = l2, document.addEventListener("touchmove", a2), document.addEventListener("touchend", l2);
    }, r.addEventListener("touchstart", this.touchStartListener)), o2 && !this.mouseDownListener && (this.mouseDownListener = (c2) => {
      var _a4;
      if (!this.onDrag && !this.onDragStart) return;
      c2.preventDefault(), this.removeActiveDragListeners(), (_a4 = this.onDragStart) == null ? void 0 : _a4.call(this, c2, this.getEventHandlerData()), r.classList.add("cursordown");
      const a2 = (u) => {
        var _a5;
        u.preventDefault(), (_a5 = this.onDrag) == null ? void 0 : _a5.call(this, u, this.getEventHandlerData());
      }, l2 = () => {
        r.classList.remove("cursordown"), document.removeEventListener("mousemove", a2), document.removeEventListener("mouseup", l2), this.activeDragCleanup === l2 && (this.activeDragCleanup = void 0);
      };
      this.activeDragCleanup = l2, document.addEventListener("mousemove", a2), document.addEventListener("mouseup", l2);
    }, r.addEventListener("mousedown", this.mouseDownListener)), this.onClick = e, this.onDrag = n, this.onDragStart = s2;
  }
  removeActiveDragListeners() {
    var _a4;
    (_a4 = this.activeDragCleanup) == null ? void 0 : _a4.call(this), this.activeDragCleanup = void 0, this.element.classList.remove("cursordown");
  }
  get data() {
    return this._data;
  }
  onDataUpdate(e) {
    return this.dataUpdateListeners.add(e), () => {
      this.dataUpdateListeners.delete(e);
    };
  }
  setLocalData(e) {
    this.rejectWriteDuringRender("setLocalData") || (typeof e == "function" ? e(this.localData) : this.localData = e, this.view && this.render());
  }
  /**
   * // PRIVATE USE ONLY \\
   *
   * Updates the internal state with the given data and handles all the downstream effects. Should only be used by the sync code to ensure one-way
   * reactivity.
   * (e.g. calling `updateElement`/`view` and `onChange`)
   */
  set __data(e) {
    this._data = e, this.render();
    for (const n of this.dataUpdateListeners)
      n();
  }
  /**
   * Renders the element from current state: runs `view` and patches the
   * result into the DOM via lit-html, or falls back to imperative
   * `updateElement`. Safe to call repeatedly — lit-html diffs.
   */
  render() {
    var _a4;
    if (this.view) {
      this.isRendering = true;
      try {
        ry(
          this.view(this.getEventHandlerData()),
          this.element
        );
      } finally {
        this.isRendering = false;
      }
      return;
    }
    (_a4 = this.updateElement) == null ? void 0 : _a4.call(this, this.getEventHandlerData());
  }
  /**
   * Begins binding capability descendants emitted by this view (mount points
   * for `define`d capabilities / `register`ed ids). Binds the current children
   * once, then re-binds only when the subtree's child structure changes — so a
   * text/attribute-only re-render (a ticking timer) does no scanning at all.
   * Called once by the runtime after `onAfterRender` is wired.
   */
  observeDescendants() {
    !this.onAfterRender || this.descendantObserver || (this.onAfterRender(this.element), typeof MutationObserver < "u" && (this.descendantObserver = new MutationObserver(() => {
      var _a4;
      (_a4 = this.onAfterRender) == null ? void 0 : _a4.call(this, this.element);
    }), this.descendantObserver.observe(this.element, {
      childList: true,
      subtree: true
    })));
  }
  /**
   * Re-runs the view and repaints from current state. No-op for elements
   * without a `view` (it's the view-repaint primitive), and a no-op during an
   * in-flight render (calling it from inside `view` would recurse).
   */
  requestUpdate() {
    !this.view || this.isRendering || this.render();
  }
  /** Warns and returns true if a write was attempted during a view render. */
  rejectWriteDuringRender(e) {
    return this.isRendering ? (console.error(
      `[playhtml] ${e}() was called during a view render for "${this.element.id}". Views must be pure — drive writes from @event handlers (e.g. @click) instead. Ignoring this write.`
    ), true) : false;
  }
  updateAwareness(e, n) {
    var _a4;
    this.awareness = e, this.awarenessByStableId = n;
    try {
      (_a4 = this.updateElementAwareness) == null ? void 0 : _a4.call(this, this.getAwarenessEventHandlerData());
    } catch (s2) {
      console.error(
        "[playhtml] updateElementAwareness callback threw:",
        s2
      );
    }
    this.view && this.render();
  }
  getEventHandlerData() {
    return {
      element: this.element,
      data: this.data,
      localData: this.localData,
      awareness: this.awareness,
      awarenessByStableId: this.awarenessByStableId,
      setData: (e) => this.setData(e),
      setLocalData: (e) => this.setLocalData(e),
      setMyAwareness: (e) => this.setMyAwareness(e),
      requestUpdate: () => this.requestUpdate()
    };
  }
  getAwarenessEventHandlerData() {
    return {
      ...this.getEventHandlerData(),
      myAwareness: this.selfAwareness
    };
  }
  getSetupData() {
    return {
      getElement: () => this.element,
      getData: () => this.data,
      getLocalData: () => this.localData,
      getAwareness: () => this.awareness,
      setData: (e) => this.setSetupData(e),
      setLocalData: (e) => this.setLocalData(e),
      setMyAwareness: (e) => this.setMyAwareness(e),
      requestUpdate: () => this.requestUpdate()
    };
  }
  setSetupData(e) {
    if (!this.scheduleSetupDataWrite) {
      this.setData(e);
      return;
    }
    this.rejectWriteDuringRender("setData") || this.scheduleSetupDataWrite(() => {
      this.onChange(e);
    });
  }
  /**
   * Public setter for element data.
   *
   * Semantics:
   * - Mutator form: setData((draft) => { ... })
   *   When data is backed by SyncedStore/Yjs (dataMode = "syncedstore"),
   *   the draft is a live CRDT proxy. You can mutate nested arrays/objects
   *   and the change will be merged across clients without conflicts.
   *   Example:
   *     setData(d => { d.list.push(item); });
   *
   * - Value form: setData(value)
   *   Replaces the entire data snapshot. Use this when you need canonical
   *   replacement semantics (e.g., snapshot from a mirror) or when running
   *   in legacy plain mode. Example:
   *     setData({ on: true });
   *
   * Notes:
   * - In plain mode, only the value form results in a sync; mutating draft
   *   is a no-op. Prefer the mutator form for merge-friendly edits.
   * - Directly mutating eventData.data may work in SyncedStore mode, but the
   *   recommended portable pattern is setData(draft => { ... }).
   */
  setData(e) {
    this.rejectWriteDuringRender("setData") || this.onChange(e);
  }
  // TODO: this should be keyed on the element to avoid conflicts
  setMyAwareness(e) {
    var _a4;
    this.rejectWriteDuringRender("setMyAwareness") || e !== this.selfAwareness && (this.selfAwareness = e, this.onAwarenessChange(e), (_a4 = this.triggerAwarenessUpdate) == null ? void 0 : _a4.call(this));
  }
  setDataDebounced(e) {
    this.debouncedOnChange(e);
  }
  /**
   * Resets the element to its default state.
   */
  reset() {
    this.defaultData !== void 0 && this.setData(this.defaultData);
  }
};
async function oy(t, e) {
  const n = new TextEncoder().encode(`${t}-${e.outerHTML}}`), s2 = await crypto.subtle.digest("SHA-1", n);
  return Array.from(new Uint8Array(s2)).map((o2) => o2.toString(16).padStart(2, "0")).join("");
}
function yt(t, e) {
  var _a4, _b;
  const n = t.__playhtml_cursors__;
  return ((_a4 = t.__playhtml_identity__) == null ? void 0 : _a4.publicKey) ?? ((_b = n == null ? void 0 : n.playerIdentity) == null ? void 0 : _b.publicKey) ?? String(e);
}
function cy(t) {
  const e = [], n = Array.from(t.keys()).sort((s2, r) => s2 - r);
  for (const s2 of n) {
    const r = t.get(s2);
    if (!r) continue;
    const i = Object.keys(r).filter((o2) => !o2.startsWith("__")).sort();
    for (const o2 of i) {
      const c2 = r[o2];
      if (c2 == null || typeof c2 != "object") continue;
      const a2 = c2, l2 = Object.keys(a2).sort();
      for (const u of l2)
        try {
          e.push(
            `${s2}:${o2}:${u}:${JSON.stringify(a2[u])}`
          );
        } catch {
        }
    }
  }
  return e.join("|");
}
var ay = class {
  constructor(e = 200) {
    __publicField(this, "cellSize");
    __publicField(this, "grid", /* @__PURE__ */ new Map());
    this.cellSize = e;
  }
  getCellKey(e, n) {
    const s2 = Math.floor(e / this.cellSize), r = Math.floor(n / this.cellSize);
    return `${s2},${r}`;
  }
  getNearbyCellKeys(e, n, s2) {
    const r = [], i = Math.ceil(s2 / this.cellSize), o2 = Math.floor(e / this.cellSize), c2 = Math.floor(n / this.cellSize);
    for (let a2 = -i; a2 <= i; a2++)
      for (let l2 = -i; l2 <= i; l2++)
        r.push(`${o2 + a2},${c2 + l2}`);
    return r;
  }
  insert(e) {
    const n = this.getCellKey(e.x, e.y);
    this.grid.has(n) || this.grid.set(n, /* @__PURE__ */ new Map()), this.grid.get(n).set(e.id, e);
  }
  remove(e, n, s2) {
    if (n !== void 0 && s2 !== void 0) {
      const r = this.getCellKey(n, s2), i = this.grid.get(r);
      if (i && i.has(e))
        return i.delete(e), i.size === 0 && this.grid.delete(r), true;
    } else
      for (const [r, i] of this.grid)
        if (i.has(e))
          return i.delete(e), i.size === 0 && this.grid.delete(r), true;
    return false;
  }
  update(e, n, s2) {
    n !== void 0 && s2 !== void 0 ? this.remove(e.id, n, s2) : this.remove(e.id), this.insert(e);
  }
  findNearby(e, n, s2, r) {
    const i = [], o2 = this.getNearbyCellKeys(e, n, s2), c2 = s2 * s2;
    for (const a2 of o2) {
      const l2 = this.grid.get(a2);
      if (l2)
        for (const u of l2.values()) {
          if (r && u.id === r) continue;
          const h = u.x - e, d = u.y - n;
          h * h + d * d <= c2 && i.push(u);
        }
    }
    return i;
  }
  getAll() {
    const e = [];
    for (const n of this.grid.values())
      e.push(...n.values());
    return e;
  }
  clear() {
    this.grid.clear();
  }
  // Debug info
  getCellCount() {
    return this.grid.size;
  }
  getItemCount() {
    let e = 0;
    for (const n of this.grid.values())
      e += n.size;
    return e;
  }
};
var Qn = "__playhtml_identity__";
var Sc = "__playhtml_cursors__";
function Wi(t) {
  var _a4, _b;
  const e = (_b = (_a4 = t.playerStyle) == null ? void 0 : _a4.colorPalette) == null ? void 0 : _b[0];
  if (e == null || e === "")
    throw new Error(
      "[playhtml] Player identity must have playerStyle.colorPalette[0] (primary color)."
    );
  return e;
}
function Ec(t) {
  if (!t.publicKey)
    throw new Error("[playhtml] Player identity must have publicKey.");
  Wi(t);
}
function qt(t, e) {
  return {
    pid: t.publicKey,
    name: t.name,
    color: Wi(t),
    isMe: e
  };
}
function Jl(t, e) {
  let n = t;
  Ec(n);
  const s2 = /* @__PURE__ */ new Set(), r = /* @__PURE__ */ new Set(), i = /* @__PURE__ */ new WeakSet();
  let o2 = null, c2 = null, a2 = "";
  function l2() {
    e.getAwareness().setLocalStateField(Qn, n);
  }
  function u() {
    var _a4;
    ((_a4 = e.getAwareness().getLocalState()) == null ? void 0 : _a4[Qn]) || l2();
  }
  function h(y2, _, V) {
    for (const O of y2)
      try {
        O(_);
      } catch (K) {
        console.error(`[playhtml] ${V} subscriber threw:`, K);
      }
  }
  function d() {
    h(s2, n, "users self-change");
  }
  function f2() {
    if (r.size === 0) return;
    const y2 = v();
    h(r, y2, "users change");
  }
  function p() {
    const y2 = e.getAwareness();
    o2 !== y2 && (o2 = y2, !i.has(y2) && (i.add(y2), a2 = g(y2.getStates()), y2.on("change", m)));
  }
  function g(y2) {
    var _a4;
    const _ = [], V = Array.from(y2.keys()).sort((O, K) => O - K);
    for (const O of V) {
      const K = y2.get(O);
      if (!K) continue;
      const Vn = K[Qn] ?? ((_a4 = K[Sc]) == null ? void 0 : _a4.playerIdentity);
      try {
        _.push(`${O}:${JSON.stringify(Vn ?? null)}`);
      } catch {
        _.push(`${O}:null`);
      }
    }
    return _.join("|");
  }
  function m() {
    const y2 = e.getAwareness().getStates(), _ = g(y2);
    _ !== a2 && (a2 = _, f2());
  }
  function w() {
    if (u(), p(), !c2 && e.onCursorPresencesChange) {
      const y2 = e.onCursorPresencesChange(() => {
        f2();
      });
      y2 && (c2 = y2);
    }
  }
  function C() {
    const y2 = e.getAwareness(), _ = y2.getLocalState();
    return _ ? yt(_, y2.clientID) : n.publicKey;
  }
  function v() {
    var _a4;
    const y2 = /* @__PURE__ */ new Map(), _ = e.getAwareness(), V = _.getStates(), O = C(), K = /* @__PURE__ */ new Map();
    V.forEach((Pe, le) => {
      const ue = yt(Pe, le), Kn = ue === O, Wt = K.get(ue);
      if (Wt === void 0) {
        K.set(ue, le);
        return;
      }
      if (Kn && le === _.clientID) {
        K.set(ue, le);
        return;
      }
      Kn && Wt === _.clientID || le > Wt && K.set(ue, le);
    });
    for (const [Pe, le] of K) {
      const ue = V.get(le);
      if (!ue) continue;
      const Kn = Pe === O, Wt = ue[Sc], po = ue[Qn] ?? (Wt == null ? void 0 : Wt.playerIdentity);
      if (Kn)
        y2.set(Pe, qt(n, true));
      else if (po)
        try {
          y2.set(Pe, qt(po, false));
        } catch {
        }
    }
    y2.has(O) || y2.set(O, qt(n, true));
    const Vn = (_a4 = e.getCursorPresences) == null ? void 0 : _a4.call(e);
    if (Vn)
      for (const [Pe, le] of Vn) {
        if (!le.playerIdentity) continue;
        const ue = Pe === O;
        try {
          y2.set(
            Pe,
            ue ? qt(n, true) : qt(le.playerIdentity, false)
          );
        } catch {
        }
      }
    return Array.from(y2.values());
  }
  function E(y2) {
    y2(), zr(n), l2(), d(), f2();
  }
  const x = {
    get pid() {
      return n.publicKey;
    },
    get name() {
      return n.name;
    },
    set name(y2) {
      n.name !== y2 && E(() => {
        n.name = y2;
      });
    },
    get color() {
      return Wi(n);
    },
    set color(y2) {
      if (y2 == null || y2 === "")
        throw new Error(
          "[playhtml] users.me.color cannot be set to empty; player identity must have a primary color."
        );
      n.playerStyle.colorPalette[0] !== y2 && E(() => {
        n.playerStyle.colorPalette[0] = y2;
      });
    }
  };
  return u(), {
    me: x,
    getAll() {
      return w(), v();
    },
    onChange(y2) {
      return w(), r.add(y2), y2(v()), () => {
        r.delete(y2);
      };
    },
    onSelfChange(y2) {
      return s2.add(y2), () => {
        s2.delete(y2);
      };
    },
    adoptIdentity(y2) {
      Ec(y2), n !== y2 && E(() => {
        n = y2;
      });
    },
    getIdentity() {
      return n;
    },
    destroy() {
      s2.clear(), r.clear(), c2 == null ? void 0 : c2(), c2 = null, o2 = null;
    }
  };
}
function ly() {
  return _l();
}
function br(t) {
  return Array.from(new Set(t.map((e) => e.color)));
}
var uy = class {
  constructor(e = {}) {
    __publicField(this, "listening", false);
    __publicField(this, "message", "");
    __publicField(this, "chatElement", null);
    __publicField(this, "styleElement", null);
    __publicField(this, "keydownHandler", null);
    __publicField(this, "timeout", null);
    __publicField(this, "options");
    this.options = e, this.initialize();
  }
  initialize() {
    this.setupKeyboardHandlers(), this.createChatElement();
  }
  setupKeyboardHandlers() {
    this.keydownHandler = (e) => {
      if (this.timeout && clearTimeout(this.timeout), this.timeout = setTimeout(() => {
        this.setListening(false), this.setMessage("");
      }, 1e4), !this.listening)
        e.key === "/" && (this.setMessage(""), this.setListening(true), e.preventDefault(), e.stopPropagation());
      else if (!e.metaKey && !e.ctrlKey && !e.altKey) {
        if (e.key === "Enter")
          this.setListening(false);
        else if (e.key === "Escape")
          this.setListening(false), this.setMessage("");
        else if (e.key === "Backspace")
          this.setMessage(this.message.slice(0, -1));
        else if (e.key.length === 1) {
          const n = this.message.length < 42 ? this.message + e.key : this.message;
          this.setMessage(n);
        }
        return e.preventDefault(), e.stopPropagation(), false;
      }
    }, document.addEventListener("keydown", this.keydownHandler);
  }
  setListening(e) {
    this.listening = e, this.updateChatDisplay();
  }
  setMessage(e) {
    var _a4, _b;
    this.message = e, this.updateChatDisplay(), (_b = (_a4 = this.options).onMessageUpdate) == null ? void 0 : _b.call(_a4, e.length > 0 ? e : null);
  }
  createChatElement() {
    this.chatElement || (this.styleElement = document.createElement("style"), this.styleElement.textContent = `
      .playhtml-chat-container {
        box-sizing: border-box;
        position: fixed;
        bottom: 24px;
        right: 32px;
        padding: 8px;
        height: 48px;
        border-radius: 24px;
        min-width: 4.4em;
        background-color: rgba(52, 199, 89, 1);
        color: white;
        display: flex;
        justify-content: end;
        align-items: center;
        gap: 8px;
        font-family: system-ui, "Segoe UI", Roboto, Helvetica, Arial, sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol";
        font-weight: 320;
        z-index: 1000000;
      }
      
      .playhtml-chat-input {
        box-sizing: border-box;
        padding: 0px 4px 0px 4px;
        margin: 0px;
        font-size: 24px;
        line-height: 1;
        white-space: nowrap;
        background: transparent;
        border: none;
        outline: none;
        color: white;
      }
      
      .playhtml-chat-button {
        box-sizing: border-box;
        width: 32px;
        height: 32px;
        border-radius: 50%;
        display: flex;
        justify-content: center;
        align-items: center;
        font-size: 24px;
        font-weight: 250;
        padding: 0px;
        margin: 0px;
        border: 0.5px solid rgba(255,255,255,0.75);
        cursor: pointer;
        color: white;
        background-color: transparent;
      }
    `, document.head.appendChild(this.styleElement), this.chatElement = document.createElement("div"), this.chatElement.className = "playhtml-chat-container", this.chatElement.style.display = "none", document.body.appendChild(this.chatElement));
  }
  updateChatDisplay() {
    var _a4;
    this.chatElement && (this.listening || this.message ? (this.chatElement.innerHTML = `
        <div class="playhtml-chat-input">${this.message || "..."}</div>
        <div class="playhtml-chat-button">&times;</div>
      `, this.chatElement.style.display = "flex", (_a4 = this.chatElement.querySelector(
      ".playhtml-chat-button"
    )) == null ? void 0 : _a4.addEventListener("click", () => {
      this.setListening(false), this.setMessage("");
    })) : this.chatElement.style.display = "none");
  }
  showCTA() {
    this.chatElement && !this.listening && !this.message && (this.chatElement.innerHTML = '<div class="playhtml-chat-input">Type / to reply</div>', this.chatElement.style.display = "flex");
  }
  hideCTA() {
    this.chatElement && !this.listening && !this.message && (this.chatElement.style.display = "none");
  }
  getCurrentMessage() {
    return this.message.length > 0 ? this.message : null;
  }
  destroy() {
    this.keydownHandler && (document.removeEventListener("keydown", this.keydownHandler), this.keydownHandler = null), this.timeout !== null && (clearTimeout(this.timeout), this.timeout = null), this.chatElement && (this.chatElement.remove(), this.chatElement = null), this.styleElement && (this.styleElement.remove(), this.styleElement = null);
  }
};
function es(t) {
  return t === void 0 ? document.body : typeof t == "string" ? document.querySelector(t) : typeof t == "function" ? t() : t;
}
var hy = 60;
var dy = 30;
var fy = 30;
var py = 15e4;
function gy(t) {
  const e = Math.max(1, Math.ceil(t));
  return e <= fy ? hy : Math.min(
    dy,
    py / (e * (e - 1))
  );
}
function yy(t) {
  return 1e3 / gy(t);
}
var Xl = "identity";
var Yi = "element:";
var Gi = "presence:";
var my = /* @__PURE__ */ new Set([
  "playerIdentity",
  "cursor",
  "isMe"
]);
function vr(t) {
  return my.has(t);
}
function Zl(t) {
  return t.startsWith(Yi);
}
function Ql(t) {
  return t.startsWith(Gi);
}
function Cr(t) {
  return `${Gi}${t}`;
}
function wy(t) {
  return t.slice(Gi.length);
}
var Xs = 3e4;
var by = 1e4;
function vy(t, e, n = Xs) {
  return Number.isFinite(t) ? e - Number(t) <= n : true;
}
function Cy(t, e, n = Xs) {
  return !z(t) || !("at" in t) ? true : vy(t.at, e, n);
}
function eu(t, e = by) {
  const n = setInterval(() => {
    t();
  }, e);
  return () => clearInterval(n);
}
function Sy(t, e = Date.now()) {
  return { at: e, value: t };
}
function _c(t) {
  return z(t) && "value" in t && "at" in t ? t.value : t;
}
function tu(t) {
  try {
    const e = JSON.stringify(t);
    return e === void 0 ? 1 / 0 : new TextEncoder().encode(e).byteLength;
  } catch {
    return 1 / 0;
  }
}
function nu(t) {
  const e = t[Xl];
  return z(e) && typeof e.publicKey == "string" ? e.publicKey : void 0;
}
function su(t, e, n, s2) {
  if (tu(n) > As)
    return console.warn(
      `[playhtml] Failed to publish ${s2}:`,
      new Error(
        `Presence value must be ${As} bytes or less`
      )
    ), false;
  try {
    return t.update(e, n), true;
  } catch (r) {
    return console.warn(`[playhtml] Failed to publish ${s2}:`, r), false;
  }
}
function Ls(t, e, n) {
  try {
    t.clear(e);
  } catch (s2) {
    console.warn(`[playhtml] Failed to clear ${n}:`, s2);
  }
}
function ve(t, e) {
  try {
    return t(), true;
  } catch (n) {
    return console.error(`[playhtml] ${e} callback threw:`, n), false;
  }
}
function Ey(t) {
  return z(t) && "cursor" in t;
}
function Ac(t) {
  return z(t) ? Object.values(t).every(z) : false;
}
function _y(t) {
  return z(t) ? Object.values(t).every(
    (e) => Array.isArray(e) && e.every((n) => typeof n == "string")
  ) : false;
}
function Ay(t) {
  return t == null ? null : typeof t == "string" ? t : null;
}
function ky(t) {
  return typeof t == "string" ? t : void 0;
}
var xy = class {
  constructor(e) {
    this.peerStore = e;
  }
  get peers() {
    return this.peerStore.getPeers();
  }
  getRemotePresences(e) {
    const n = /* @__PURE__ */ new Map(), s2 = Array.from(this.peers.keys()).sort();
    for (const r of s2) {
      const i = this.getPresenceForConnection(r);
      if (!i || i.playerIdentity.publicKey === e) continue;
      const o2 = i.playerIdentity.publicKey, c2 = n.get(o2);
      (!c2 || kc(c2, i)) && n.set(o2, i);
    }
    return n;
  }
  getPresenceByStableId(e) {
    let n = null;
    for (const s2 of Array.from(this.peers.keys()).sort()) {
      const r = this.getPresenceForConnection(s2);
      (r == null ? void 0 : r.playerIdentity.publicKey) === e && (!n || kc(n, r)) && (n = r);
    }
    return n;
  }
  getPresenceForConnection(e) {
    const n = this.peers.get(e);
    if (!n) return null;
    const s2 = n.identity;
    if (!og(s2)) return null;
    const r = n.cursor;
    let i = null, o2, c2 = ky(n.page), a2 = null;
    if (r !== void 0) {
      if (!Ey(r)) return null;
      if (r.cursor !== null) {
        if (!cg(r.cursor)) return null;
        i = r.cursor;
      }
      o2 = r.at, c2 = r.page ?? c2, a2 = r.zone ?? null;
    }
    return {
      cursor: i,
      playerIdentity: s2,
      lastSeen: o2,
      message: Ay(n.message),
      page: c2,
      zone: a2
    };
  }
};
function kc(t, e) {
  if (e.cursor && !t.cursor) return true;
  if (!e.cursor && t.cursor) return false;
  const n = xc(t.lastSeen);
  return xc(e.lastSeen) > n;
}
function xc(t) {
  return Number.isFinite(t) ? Number(t) : Number.NEGATIVE_INFINITY;
}
var en = "__playhtml_cursors__";
function he(t) {
  var _a4, _b;
  const e = (_b = (_a4 = t.playerStyle) == null ? void 0 : _a4.colorPalette) == null ? void 0 : _b[0];
  if (e == null || e === "")
    throw new Error(
      "[playhtml] Player identity must have playerStyle.colorPalette[0] (primary color)."
    );
  return e;
}
function Dy(t) {
  if (!t.publicKey)
    throw new Error("[playhtml] Player identity must have publicKey.");
  he(t);
}
function Dc(t, e) {
  var _a4;
  const n = t == null ? void 0 : t[en];
  if (!(n == null ? void 0 : n.cursor) || !((_a4 = n.playerIdentity) == null ? void 0 : _a4.publicKey))
    return null;
  try {
    he(n.playerIdentity);
  } catch {
    return null;
  }
  return n;
}
function Sr(t, e) {
  return Math.sqrt(
    Math.pow(t.x - e.x, 2) + Math.pow(t.y - e.y, 2)
  );
}
function ru() {
  var _a4;
  return ((_a4 = window.visualViewport) == null ? void 0 : _a4.scale) ?? 1;
}
function Ty(t, e, n) {
  const s2 = ru();
  if (n === "relative") {
    const r = window.visualViewport, i = (r == null ? void 0 : r.width) ?? window.innerWidth, o2 = (r == null ? void 0 : r.height) ?? window.innerHeight, c2 = (r == null ? void 0 : r.offsetLeft) ?? 0, a2 = (r == null ? void 0 : r.offsetTop) ?? 0;
    return {
      x: (t + c2) / (i * s2) * 100,
      y: (e + a2) / (o2 * s2) * 100
    };
  } else {
    const r = window.visualViewport, i = r ? r.pageLeft : window.scrollX, o2 = r ? r.pageTop : window.scrollY, c2 = (r == null ? void 0 : r.offsetLeft) ?? 0, a2 = (r == null ? void 0 : r.offsetTop) ?? 0;
    return {
      x: (t + c2) / s2 + i,
      y: (e + a2) / s2 + o2
    };
  }
}
function Ly(t, e, n) {
  const s2 = ru();
  if (n === "relative") {
    const r = window.visualViewport, i = (r == null ? void 0 : r.width) ?? window.innerWidth, o2 = (r == null ? void 0 : r.height) ?? window.innerHeight, c2 = (r == null ? void 0 : r.offsetLeft) ?? 0, a2 = (r == null ? void 0 : r.offsetTop) ?? 0;
    return {
      x: t / 100 * i * s2 - c2,
      y: e / 100 * o2 * s2 - a2
    };
  } else {
    const r = window.visualViewport, i = r ? r.pageLeft : window.scrollX, o2 = r ? r.pageTop : window.scrollY, c2 = (r == null ? void 0 : r.offsetLeft) ?? 0, a2 = (r == null ? void 0 : r.offsetTop) ?? 0;
    return {
      x: (t - i) * s2 - c2,
      y: (e - o2) * s2 - a2
    };
  }
}
var Iy = class {
  constructor(e, n, s2 = {}) {
    __publicField(this, "position");
    __publicField(this, "velocity");
    __publicField(this, "target");
    __publicField(this, "stiffness");
    __publicField(this, "damping");
    __publicField(this, "mass");
    __publicField(this, "animationFrame", null);
    __publicField(this, "onUpdate");
    __publicField(this, "animate", () => {
      const e = 0.016666666666666666, n = (this.target.x - this.position.x) * this.stiffness, s2 = (this.target.y - this.position.y) * this.stiffness, r = this.velocity.x * this.damping, i = this.velocity.y * this.damping, o2 = (n - r) / this.mass, c2 = (s2 - i) / this.mass;
      this.velocity.x += o2 * e, this.velocity.y += c2 * e, this.position.x += this.velocity.x * e, this.position.y += this.velocity.y * e, this.onUpdate(this.position);
      const a2 = Math.sqrt(
        Math.pow(this.target.x - this.position.x, 2) + Math.pow(this.target.y - this.position.y, 2)
      ), l2 = Math.sqrt(
        Math.pow(this.velocity.x, 2) + Math.pow(this.velocity.y, 2)
      );
      a2 < 0.5 && l2 < 0.5 ? (this.position = { ...this.target }, this.velocity = { x: 0, y: 0 }, this.onUpdate(this.position), this.animationFrame = null) : this.animationFrame = requestAnimationFrame(this.animate);
    });
    this.position = { ...e }, this.velocity = { x: 0, y: 0 }, this.target = { ...e }, this.onUpdate = n, this.stiffness = s2.stiffness ?? 170, this.damping = s2.damping ?? 26, this.mass = s2.mass ?? 0.5;
  }
  setTarget(e) {
    this.target = { ...e }, this.animationFrame === null && this.animate();
  }
  // Immediately jump to a position without spring animation.
  // Used when the viewport changes (scroll/zoom) and cursors need to
  // track content instantly.
  snapTo(e) {
    this.animationFrame !== null && (cancelAnimationFrame(this.animationFrame), this.animationFrame = null), this.position = { ...e }, this.target = { ...e }, this.velocity = { x: 0, y: 0 }, this.onUpdate(this.position);
  }
  destroy() {
    this.animationFrame !== null && (cancelAnimationFrame(this.animationFrame), this.animationFrame = null);
  }
};
function iu(t) {
  t = t.replace(/"/g, "'"), t = t.replace(/>\s{1,}</g, "><"), t = t.replace(/\s{2,}/g, " ");
  const e = /[\r\n%#()<>?[\\\]^`{|}]/g;
  return t.replace(e, encodeURIComponent);
}
function ou(t) {
  return `<svg
    xmlns="http://www.w3.org/2000/svg"
    viewBox="10 9 18 18"
    width="18"
    height="18"
    fill="none"
    fillRule="evenodd"
  >
    <g fill="rgba(0,0,0,.2)" transform="translate(1,1)">
      <path d="m12 24.4219v-16.015l11.591 11.619h-6.781l-.411.124z" />
      <path d="m21.0845 25.0962-3.605 1.535-4.682-11.089 3.686-1.553z" />
    </g>
    <g fill="white">
      <path d="m12 24.4219v-16.015l11.591 11.619h-6.781l-.411.124z" />
      <path d="m21.0845 25.0962-3.605 1.535-4.682-11.089 3.686-1.553z" />
    </g>
    <g fill="${t}">
      <path d="m19.751 24.4155-1.844.774-3.1-7.374 1.841-.775z" />
      <path d="m13 10.814v11.188l2.969-2.866.428-.139h4.768z" />
    </g>
  </svg>`;
}
function Er(t) {
  return `url("data:image/svg+xml,${iu(ou(t))}"), auto`;
}
function Tc(t) {
  return t.startsWith("--") ? t : t.replace(/[A-Z]/g, (e) => "-" + e.toLowerCase());
}
function _r(t, e, n) {
  const s2 = /* @__PURE__ */ new Set();
  for (const r of Object.keys(n)) {
    if (/^\d+$/.test(r)) continue;
    s2.add(r);
    const i = n[r];
    t.style.setProperty(Tc(r), String(i));
  }
  for (const r of e)
    s2.has(r) || t.style.removeProperty(Tc(r));
  e.clear();
  for (const r of s2)
    e.add(r);
}
function My(t) {
  if (!t.startsWith('url("'))
    return;
  const e = t.indexOf('")', 5);
  if (e !== -1)
    return t.slice(5, e);
}
function ze() {
  const t = window.location.pathname;
  return t.length <= Fi ? t : void 0;
}
function $y(t) {
  if (t.trim() !== t || /["'<>\s\u0000-\u001f\u007f]/.test(t))
    return false;
  try {
    const e = new URL(t, window.location.href);
    return e.protocol === "http:" || e.protocol === "https:" ? true : e.protocol !== "data:" ? false : /^data:image\/(?:png|jpeg|jpg|gif|webp);base64,/i.test(t);
  } catch {
    return false;
  }
}
function ts(t) {
  return t.replace(/&/g, "&amp;").replace(/"/g, "&quot;").replace(/</g, "&lt;").replace(/>/g, "&gt;");
}
function Oy(t) {
  const e = t.trim();
  return /^#[0-9a-fA-F]{3,8}$/.test(e) || /^rgba?\([0-9.,%\s]+\)$/.test(e) || /^hsla?\([0-9.,%\s]+\)$/.test(e) || /^[a-zA-Z]+$/.test(e) ? e : "black";
}
var Py = class {
  constructor(e, n = {}, s2, r) {
    __publicField(this, "cursors", /* @__PURE__ */ new Map());
    __publicField(this, "cursorAnimators", /* @__PURE__ */ new Map());
    // Spring animators for each cursor
    __publicField(this, "spatialGrid", new ay(300));
    // 300px cell size
    __publicField(this, "proximityUsers", /* @__PURE__ */ new Set());
    __publicField(this, "currentCursor", null);
    __publicField(this, "users");
    __publicField(this, "ownsUsers", false);
    __publicField(this, "unsubscribeSelfChange", null);
    __publicField(this, "unsubscribeUsersChange", null);
    __publicField(this, "awarenessUpdateTimeout", null);
    __publicField(this, "lastUpdate", 0);
    __publicField(this, "pointerFrame", null);
    __publicField(this, "pendingPointerSample", null);
    __publicField(this, "cursorEventCleanups", []);
    __publicField(this, "ownCursorSvgCache", null);
    __publicField(this, "visibilityThreshold");
    __publicField(this, "isStylesAdded", false);
    __publicField(this, "globalApiListeners", /* @__PURE__ */ new Map());
    __publicField(this, "activeAnimationCleanups", /* @__PURE__ */ new Map());
    // stableId -> cleanup fn
    __publicField(this, "chat", null);
    __publicField(this, "currentMessage", null);
    __publicField(this, "otherUsersWithMessages", /* @__PURE__ */ new Set());
    __publicField(this, "lastSentMessage", null);
    __publicField(this, "cursorPresenceChangeCallbacks", /* @__PURE__ */ new Map());
    __publicField(this, "coordinateMode");
    // Cursor view over the shared PeerStore; only present (and only read) in
    // transport mode. Assigned in setupPresenceTransportHandling.
    __publicField(this, "presenceStore", null);
    __publicField(this, "presenceTransportUnsubscribe", null);
    __publicField(this, "serverCursorMaxHz", null);
    __publicField(this, "zones", /* @__PURE__ */ new Map());
    __publicField(this, "currentZone", null);
    __publicField(this, "cursorZoneState", /* @__PURE__ */ new Map());
    // stableId -> previous zoneId
    // Keys most recently written by getCursorStyle for each cursor. Tracked so
    // re-applying can remove stale keys when a style function returns fewer
    // properties than the previous call.
    __publicField(this, "cursorStyleKeys", /* @__PURE__ */ new Map());
    __publicField(this, "lastKnownContainer", null);
    // Maps Yjs clientId -> stableId (publicKey). Multiple clientIds can map to
    // the same stableId when a user has multiple tabs open. Used to:
    // (a) skip rendering our own cursor from other tabs
    // (b) collapse multiple tabs from the same remote user into one cursor
    // (c) clean up cursor elements correctly when a clientId disconnects
    __publicField(this, "clientIdToStableId", /* @__PURE__ */ new Map());
    // Tracks pending fade-out removal timeouts by stableId so they can be
    // cancelled if a new update arrives for the same stableId (e.g. when one
    // tab disconnects but another tab of the same user is still active).
    __publicField(this, "pendingRemovals", /* @__PURE__ */ new Map());
    // Tracks the previously observed color/name so handleSelfIdentityChange
    // can emit CursorEvents only for fields that actually changed (mirroring what
    // the pre-refactor window.cursors setters and configure() did).
    __publicField(this, "lastKnownColor", "");
    __publicField(this, "lastKnownName");
    __publicField(this, "lastKnownAllColors", []);
    this.provider = e, this.options = n, this.presenceTransport = s2, r ? (this.users = r, n.playerIdentity && this.users.adoptIdentity(n.playerIdentity)) : (this.users = Jl(
      n.playerIdentity ?? _l(),
      {
        getAwareness: () => this.provider.awareness,
        getCursorPresences: () => this.getCursorPresences(),
        onCursorPresencesChange: (i) => this.onCursorPresencesChange(i)
      }
    ), this.ownsUsers = true), Dy(this.playerIdentity), this.visibilityThreshold = n.visibilityThreshold || void 0, this.coordinateMode = n.coordinateMode || "absolute", this.options.enableChat === true && (this.chat = new uy({
      onMessageUpdate: (i) => {
        this.currentMessage = i, this.updateCursorAwareness();
      }
    })), this.lastKnownColor = he(this.playerIdentity), this.lastKnownName = this.playerIdentity.name, this.unsubscribeSelfChange = this.users.onSelfChange(() => {
      this.handleSelfIdentityChange();
    }), this.initialize(), this.setupGlobalAPI(), this.unsubscribeUsersChange = this.users.onChange((i) => {
      this.handleUsersChange(i);
    });
  }
  // When the cursor container is a non-body element with a CSS transform,
  // cursors are stored and rendered in container-local coordinates. The
  // matrix is read live from getComputedStyle so host pan/zoom updates flow
  // through automatically. Returns null for the document.body default
  // (identity, fast path) so today's behavior is preserved.
  getContainerMatrix() {
    const e = es(this.options.container);
    if (!e || e === document.body || typeof DOMMatrixReadOnly > "u") return null;
    const n = getComputedStyle(e).transform;
    return { matrix: !n || n === "none" ? new DOMMatrixReadOnly() : new DOMMatrixReadOnly(n), rect: e.getBoundingClientRect(), el: e };
  }
  clientToStorage(e, n) {
    const s2 = this.getContainerMatrix();
    if (s2) {
      const r = s2.matrix.a, i = s2.matrix.b, o2 = s2.matrix.c, c2 = s2.matrix.d, a2 = r * c2 - i * o2;
      if (a2 === 0) return { x: 0, y: 0 };
      const l2 = e - s2.rect.left, u = n - s2.rect.top;
      return {
        x: (c2 * l2 - o2 * u) / a2,
        y: (r * u - i * l2) / a2
      };
    }
    return Ty(e, n, this.coordinateMode);
  }
  storageToClient(e, n) {
    const s2 = this.getContainerMatrix();
    if (s2) {
      const r = s2.matrix.a, i = s2.matrix.b, o2 = s2.matrix.c, c2 = s2.matrix.d;
      return {
        x: r * e + o2 * n + s2.rect.left,
        y: i * e + c2 * n + s2.rect.top
      };
    }
    return Ly(e, n, this.coordinateMode);
  }
  // Cursor client no longer owns identity mutation/persistence — it reads the
  // live reference from the users module, which is the sole mutator.
  get playerIdentity() {
    return this.users.getIdentity();
  }
  // React to any users.me mutation (color/name/whole-identity adopt):
  // invalidate the cached own-cursor SVG, refresh the document cursor style,
  // republish our cursor awareness, and emit the CursorEvents subscribers
  // (window.cursors.on) already rely on, only for fields that changed. The
  // identity channel itself is republished by the shared transport's
  // onSelfChange re-join (see acquirePresenceTransport), not here.
  handleSelfIdentityChange() {
    this.ownCursorSvgCache = null;
    const e = he(this.playerIdentity);
    document.documentElement.style.cursor = Er(e), this.updateCursorAwareness();
    const n = this.playerIdentity.name;
    e !== this.lastKnownColor && (this.lastKnownColor = e, this.emitGlobalEvent("color", e)), n !== this.lastKnownName && (this.lastKnownName = n, this.emitGlobalEvent("name", n));
  }
  handleUsersChange(e) {
    const n = br(e);
    n.length === this.lastKnownAllColors.length && n.every(
      (s2, r) => s2 === this.lastKnownAllColors[r]
    ) || (this.lastKnownAllColors = n, this.emitGlobalEvent("allColors", n));
  }
  initialize() {
    this.addCursorStyles(), this.setupCursorTracking(), this.presenceTransport ? this.setupPresenceTransportHandling() : this.setupAwarenessHandling(), document.documentElement.style.cursor = Er(
      he(this.playerIdentity)
    );
  }
  setupAwarenessHandling() {
    this.provider.awareness.on("change", ({ added: e, updated: n, removed: s2 }) => {
      this.handleAwarenessChange(e, n, s2);
    }), this.updateCursorAwareness(), this.syncExistingAwareness();
  }
  setupPresenceTransportHandling() {
    if (!this.presenceTransport) return;
    this.presenceStore = new xy(this.presenceTransport.peers), this.publishPresenceTransportState();
    const e = () => this.onPeerCursorChange(), n = this.presenceTransport.peers.subscribe(
      "cursor",
      e
    ), s2 = this.presenceTransport.peers.subscribe(
      "identity",
      e
    ), r = this.presenceTransport.subscribe((i) => {
      this.handlePresenceControlMessage(i);
    });
    this.presenceTransportUnsubscribe = () => {
      n(), s2(), r();
    };
  }
  onPeerCursorChange() {
    this.renderPresenceStore();
  }
  handlePresenceControlMessage(e) {
    e.type === "presence-rate" && this.handlePresenceRate(e.channel, e.hz);
  }
  handlePresenceRate(e, n) {
    e === "cursor" && (!Number.isFinite(n) || n <= 0 || (this.serverCursorMaxHz = n));
  }
  renderPresenceStore() {
    const e = /* @__PURE__ */ new Set();
    for (const [n, s2] of this.cursorPresenceEntries())
      e.add(n), s2.cursor ? this.updateCursor(n, {
        ...s2,
        cursor: s2.cursor
      }) : this.removeCursor(n), s2.message ? this.otherUsersWithMessages.add(n) : this.otherUsersWithMessages.delete(n);
    for (const n of Array.from(this.cursors.keys()))
      e.has(n) || (this.otherUsersWithMessages.delete(n), this.removeCursor(n));
    this.rebuildSpatialGrid(), this.updateChatCTA(), this.checkProximityOptimized(), this.notifyCursorPresenceListeners();
  }
  syncExistingAwareness() {
    const e = this.provider.awareness.getStates(), n = Array.from(e.keys());
    this.handleAwarenessChange(n, [], []);
  }
  handleAwarenessChange(e, n, s2) {
    const r = this.provider.awareness.getStates(), i = this.provider.awareness.clientID, o2 = this.playerIdentity.publicKey;
    [...e, ...n].forEach((c2) => {
      var _a4;
      const a2 = r.get(c2);
      let l2 = a2 ? yt(a2, c2) : String(c2);
      if (l2 === String(c2) && (l2 = this.clientIdToStableId.get(c2) ?? l2), this.clientIdToStableId.set(c2, l2), c2 === i || l2 === o2)
        return;
      const u = Dc(a2);
      u ? this.updateCursor(l2, u) : this.hasOtherClientForStableId(l2, c2) || this.removeCursor(l2), ((_a4 = a2 == null ? void 0 : a2[en]) == null ? void 0 : _a4.message) ? this.otherUsersWithMessages.add(l2) : this.otherUsersWithMessages.delete(l2);
    }), s2.forEach((c2) => {
      const a2 = this.clientIdToStableId.get(c2);
      this.clientIdToStableId.delete(c2), a2 && (this.otherUsersWithMessages.delete(a2), this.hasOtherClientForStableId(a2, c2) || this.removeCursor(a2));
    }), this.rebuildSpatialGrid(), this.updateChatCTA(), this.checkProximityOptimized(), this.notifyCursorPresenceListeners();
  }
  // Returns true if any clientId other than the excluded one maps to the given stableId.
  hasOtherClientForStableId(e, n) {
    for (const [s2, r] of this.clientIdToStableId)
      if (r === e && s2 !== n) return true;
    return false;
  }
  *cursorPresenceEntries(e = {}) {
    if (this.presenceTransport && this.presenceStore) {
      const i = this.presenceStore.getRemotePresences(
        this.playerIdentity.publicKey
      );
      for (const [o2, c2] of i)
        yield [o2, c2];
      return;
    }
    const n = this.provider.awareness.getStates(), s2 = this.provider.awareness.clientID, r = this.playerIdentity.publicKey;
    for (const [i, o2] of n) {
      const c2 = yt(
        o2,
        i
      );
      if (!e.includeLocalAwareness && (i === s2 || c2 === r))
        continue;
      const a2 = Dc(
        o2
      );
      a2 && (yield [c2, a2]);
    }
  }
  *activeCursorPresenceEntries(e = {}) {
    const n = e.now ?? Date.now();
    for (const [s2, r] of this.cursorPresenceEntries())
      r.cursor && (e.freshOnly && !this.isFreshCursorPresence(r, n) || (yield [
        s2,
        {
          ...r,
          cursor: r.cursor
        }
      ]));
  }
  // A stricter, SYNCHRONOUS freshness check for proximity (freshOnly): between
  // PeerStore sweep ticks a cursor can be technically stale but not yet swept,
  // and it must not participate in proximity. Distinct from PeerStore's
  // sweep-based channel deletion — both run, at different cadences.
  isFreshCursorPresence(e, n) {
    return this.presenceTransport ? Number.isFinite(e.lastSeen) ? n - Number(e.lastSeen) <= Xs : false : true;
  }
  rebuildSpatialGrid() {
    this.spatialGrid.clear();
    for (const [e, n] of this.activeCursorPresenceEntries()) {
      const s2 = this.storageToClient(
        n.cursor.x,
        n.cursor.y
      );
      this.spatialGrid.insert({
        id: e,
        x: s2.x,
        y: s2.y,
        data: n
      });
    }
  }
  checkProximityOptimized() {
    var _a4, _b, _c2, _d2;
    if (!this.currentCursor) return;
    const e = /* @__PURE__ */ new Set(), n = this.options.proximityThreshold || Zp, s2 = this.storageToClient(this.currentCursor.x, this.currentCursor.y), r = this.spatialGrid.findNearby(
      s2.x,
      s2.y,
      n
    );
    for (const i of r) {
      const o2 = i.data;
      if (!o2.cursor) continue;
      const c2 = this.storageToClient(o2.cursor.x, o2.cursor.y);
      if (Sr(
        {
          x: s2.x,
          y: s2.y,
          pointer: this.currentCursor.pointer
        },
        {
          x: c2.x,
          y: c2.y,
          pointer: o2.cursor.pointer
        }
      ) < n && (e.add(i.id), !this.proximityUsers.has(i.id))) {
        const u = {
          ours: { x: this.currentCursor.x, y: this.currentCursor.y },
          theirs: { x: o2.cursor.x, y: o2.cursor.y }
        }, h = o2.cursor.x - this.currentCursor.x, d = o2.cursor.y - this.currentCursor.y, f2 = Math.atan2(d, h);
        (_b = (_a4 = this.options).onProximityEntered) == null ? void 0 : _b.call(
          _a4,
          o2.playerIdentity,
          u,
          f2
        );
      }
    }
    for (const i of this.proximityUsers)
      e.has(i) || ((_d2 = (_c2 = this.options).onProximityLeft) == null ? void 0 : _d2.call(_c2, i));
    this.proximityUsers = e;
  }
  addCursorStyles() {
    if (this.isStylesAdded || document.getElementById("playhtml-cursor-styles"))
      return;
    const e = document.createElement("style");
    e.id = "playhtml-cursor-styles", e.textContent = `
      .playhtml-cursor-other {
        position: fixed;
        width: 32px;
        height: 32px;
        pointer-events: none;
        z-index: 999999;
        transition: all 0.1s ease;
        transform-origin: center;
      }
      
      .playhtml-cursor-fade-in {
        animation: cursorFadeIn 0.3s ease-out;
      }
      
      .playhtml-cursor-fade-out {
        animation: cursorFadeOut 0.3s ease-out;
        opacity: 0;
      }
      
      @keyframes cursorFadeIn {
        from { opacity: 0; transform: scale(0.8); }
        to { opacity: 1; transform: scale(1); }
      }
      
      @keyframes cursorFadeOut {
        from { opacity: 1; transform: scale(1); }
        to { opacity: 0; transform: scale(0.8); }
      }
    `;
    const n = es(this.options.container);
    n && n !== document.body ? n.appendChild(e) : document.head.appendChild(e), this.isStylesAdded = true;
  }
  // Build the onUpdate callback for a SpringAnimator. Position values are
  // always in pixel space — zone-relative coordinates are resolved to pixels
  // before being fed to the spring.
  createCursorPositionCallback(e) {
    return (n) => {
      const s2 = this.cursors.get(e);
      s2 && (s2.style.position = this.coordinateMode === "absolute" || this.getContainerMatrix() ? "absolute" : "fixed", s2.style.left = `${n.x}px`, s2.style.top = `${n.y}px`, s2.style.zIndex = "999999", s2.style.pointerEvents = "none");
    };
  }
  // Resolves storage coordinates to the coordinate space used for positioning.
  // In absolute mode (default container) storage coords are document coords
  // and cursors use position:absolute, so no conversion is needed. With a
  // transformed cursor container, storage coords are container-local — and
  // since cursors are appended inside that container with position:absolute,
  // the CSS transform composes them into the right viewport pixels for free,
  // so again no conversion is needed. Relative mode converts to viewport
  // pixels for position:fixed.
  resolveTargetCoords(e, n) {
    return this.coordinateMode === "absolute" || this.getContainerMatrix() ? { x: e, y: n } : this.storageToClient(e, n);
  }
  // Apply zone-specific cursor styling, or revert to global styling when
  // leaving a zone. Uses applyTrackedStyles so keys written by a previous
  // call that aren't in the new style object are removed, rather than
  // lingering on the element.
  applyZoneStyling(e, n, s2, r) {
    var _a4;
    let i = this.cursorStyleKeys.get(r);
    if (i || (i = /* @__PURE__ */ new Set(), this.cursorStyleKeys.set(r, i)), s2) {
      const o2 = this.zones.get(s2);
      if ((_a4 = o2 == null ? void 0 : o2.options) == null ? void 0 : _a4.getCursorStyle) {
        _r(
          e,
          i,
          o2.options.getCursorStyle(n)
        );
        return;
      }
    }
    this.options.getCursorStyle ? _r(
      e,
      i,
      this.options.getCursorStyle(n)
    ) : _r(e, i, {});
  }
  hitTestZones(e, n) {
    let s2 = null, r = 1 / 0;
    for (const [i, { element: o2 }] of this.zones) {
      if (!document.contains(o2)) continue;
      const c2 = o2.getBoundingClientRect();
      if (e >= c2.left && e <= c2.right && n >= c2.top && n <= c2.bottom) {
        const a2 = c2.width * c2.height;
        a2 < r && (r = a2, s2 = {
          zoneId: i,
          relX: Math.max(0, Math.min(1, (e - c2.left) / c2.width)),
          relY: Math.max(0, Math.min(1, (n - c2.top) / c2.height))
        });
      }
    }
    return s2;
  }
  getOwnCursorSvg() {
    var _a4;
    const e = he(this.playerIdentity);
    return ((_a4 = this.ownCursorSvgCache) == null ? void 0 : _a4.color) !== e && (this.ownCursorSvgCache = {
      color: e,
      svg: iu(ou(e))
    }), this.ownCursorSvgCache.svg;
  }
  addCursorEventListener(e, n, s2, r) {
    e.addEventListener(n, s2, r), this.cursorEventCleanups.push(() => {
      e.removeEventListener(n, s2, r);
    });
  }
  requestPointerFrame(e) {
    return typeof window.requestAnimationFrame == "function" ? window.requestAnimationFrame(e) : window.setTimeout(() => e(performance.now()), 1e3 / 60);
  }
  cancelPointerFrame(e) {
    typeof window.cancelAnimationFrame == "function" ? window.cancelAnimationFrame(e) : window.clearTimeout(e);
  }
  queuePointerSample(e, n, s2) {
    this.pendingPointerSample = { clientX: e, clientY: n, input: s2 }, this.pointerFrame === null && (this.pointerFrame = this.requestPointerFrame(() => {
      this.pointerFrame = null;
      const r = this.pendingPointerSample;
      this.pendingPointerSample = null, r && this.processPointerSample(r);
    }));
  }
  processPointerSample(e) {
    let n = e.input;
    if (e.input === "mouse") {
      n = "mouse";
      const r = document.elementFromPoint(e.clientX, e.clientY);
      if (r) {
        const i = window.getComputedStyle(r), o2 = My(i.cursor);
        o2 && !o2.includes(this.getOwnCursorSvg()) && (n = o2);
      }
      n === "mouse" ? document.documentElement.style.cursor = Er(
        he(this.playerIdentity)
      ) : document.documentElement.style.cursor = "auto";
    }
    const s2 = this.clientToStorage(e.clientX, e.clientY);
    this.currentCursor = {
      x: s2.x,
      y: s2.y,
      pointer: n
    }, this.currentZone = this.hitTestZones(e.clientX, e.clientY), this.scheduleCursorAwarenessUpdate(), this.updateAllCursorVisibility();
  }
  setupCursorTracking() {
    const e = (o2) => {
      const c2 = o2;
      this.queuePointerSample(c2.clientX, c2.clientY, "mouse");
    }, n = (o2) => {
      const a2 = o2.touches[0];
      a2 && this.queuePointerSample(a2.clientX, a2.clientY, "touch");
    }, s2 = () => {
    };
    this.addCursorEventListener(document, "mousemove", e), this.addCursorEventListener(document, "touchmove", n, {
      passive: true
    }), this.addCursorEventListener(document, "touchend", s2), this.addCursorEventListener(document, "mouseleave", () => {
      this.showAllCursors();
    });
    let r = null;
    const i = () => {
      r === null && (r = requestAnimationFrame(() => {
        r = null, this.repositionAllCursors();
      }));
    };
    this.addCursorEventListener(window, "scroll", i, {
      passive: true
    }), this.addCursorEventListener(window, "resize", i), window.visualViewport && (this.addCursorEventListener(
      window.visualViewport,
      "scroll",
      i
    ), this.addCursorEventListener(
      window.visualViewport,
      "resize",
      i
    )), this.addCursorEventListener(window, "beforeunload", () => {
      this.presenceTransport ? this.presenceTransport.clear("cursor") : this.provider.awareness.setLocalStateField(en, null);
    });
  }
  // Re-derive positions for all remote cursors. In relative mode, this is
  // needed on every scroll/resize since cursors use position:fixed with
  // viewport-relative coords. In absolute mode, non-zone cursors use
  // position:absolute and the browser handles scroll — only zone cursors
  // need re-resolution (getBoundingClientRect changes on scroll).
  repositionAllCursors() {
    for (const [e, n] of this.activeCursorPresenceEntries())
      this.snapCursorToPresence(e, n);
    this.updateAllCursorVisibility();
  }
  snapCursorToPresence(e, n) {
    const s2 = this.cursorAnimators.get(e);
    if (!s2) return;
    if (n.zone) {
      const i = this.zones.get(n.zone.zoneId);
      if (i && document.contains(i.element)) {
        const o2 = i.element.getBoundingClientRect(), c2 = this.cursors.get(e), a2 = c2 ? c2.offsetWidth / 2 : 0, l2 = c2 ? c2.offsetHeight / 2 : 0, u = o2.left + n.zone.relX * o2.width - a2, h = o2.top + n.zone.relY * o2.height - l2;
        this.coordinateMode === "absolute" ? s2.snapTo({
          x: u + window.scrollX,
          y: h + window.scrollY
        }) : s2.snapTo({ x: u, y: h });
        return;
      }
    }
    if (this.coordinateMode === "absolute" || this.getContainerMatrix()) return;
    const r = this.storageToClient(
      n.cursor.x,
      n.cursor.y
    );
    s2.snapTo({ x: r.x, y: r.y });
  }
  getActiveCursorConnectionCount() {
    const e = this.currentCursor ? 1 : 0;
    let n = 0;
    for (const s2 of this.activeCursorPresenceEntries({
      freshOnly: true
    }))
      n++;
    return Math.max(1, e + n);
  }
  scheduleCursorAwarenessUpdate() {
    if (this.awarenessUpdateTimeout !== null) return;
    const n = performance.now() - this.lastUpdate, s2 = yy(
      this.getActiveCursorConnectionCount()
    ), r = this.serverCursorMaxHz ? 1e3 / this.serverCursorMaxHz : 0, i = Math.max(s2, r);
    n >= i ? this.updateCursorAwareness() : this.awarenessUpdateTimeout = setTimeout(() => {
      this.awarenessUpdateTimeout = null, this.updateCursorAwareness();
    }, i - n);
  }
  updateCursorAwareness() {
    this.awarenessUpdateTimeout !== null && (clearTimeout(this.awarenessUpdateTimeout), this.awarenessUpdateTimeout = null);
    const e = Date.now(), n = {
      cursor: this.currentCursor,
      playerIdentity: this.playerIdentity,
      lastSeen: e,
      message: this.currentMessage,
      page: ze(),
      zone: this.currentZone
    };
    if (this.presenceTransport) {
      this.presenceTransport.update("cursor", {
        cursor: n.cursor,
        page: n.page,
        zone: n.zone,
        at: e
      }), this.currentMessage !== this.lastSentMessage && (this.presenceTransport.update("message", this.currentMessage), this.lastSentMessage = this.currentMessage), this.lastUpdate = performance.now(), this.checkProximityOptimized(), this.notifyCursorPresenceListeners();
      return;
    }
    this.provider.awareness.setLocalStateField(
      en,
      n
    ), this.lastUpdate = performance.now();
  }
  publishPresenceTransportState() {
    if (!this.presenceTransport) return;
    const e = ze();
    this.presenceTransport.join({
      identity: this.playerIdentity,
      page: e
    }), this.currentCursor && this.presenceTransport.update("cursor", {
      cursor: this.currentCursor,
      page: e,
      zone: this.currentZone,
      at: Date.now()
    }), (this.currentMessage !== null || this.lastSentMessage !== null) && (this.presenceTransport.update("message", this.currentMessage), this.lastSentMessage = this.currentMessage);
  }
  getContainer() {
    const e = es(this.options.container) ?? document.body;
    return this.lastKnownContainer = e, e;
  }
  refreshContainer() {
    const e = es(this.options.container) ?? document.body;
    if (e === this.lastKnownContainer) return;
    const n = this.lastKnownContainer;
    this.lastKnownContainer = e, n && n.querySelectorAll(".playhtml-cursor-other").forEach((o2) => e.appendChild(o2));
    const r = document.getElementById("playhtml-cursor-styles");
    r && r.parentElement !== e && e.appendChild(r);
  }
  // Re-invoke getCursorStyle for all currently rendered cursors. Called after
  // SPA navigation so consumers can re-evaluate per-page visibility decisions.
  refreshCursorStyles() {
    if (!(!this.options.getCursorStyle && this.zones.size === 0))
      for (const [e, n] of this.cursors.entries()) {
        const s2 = this.findAwarenessByStableId(e);
        if (!s2) continue;
        const r = this.cursorZoneState.get(e) ?? null;
        this.applyZoneStyling(n, s2, r, e);
      }
  }
  findAwarenessByStableId(e) {
    for (const [n, s2] of this.activeCursorPresenceEntries())
      if (n === e) return s2;
    return null;
  }
  updateCursor(e, n) {
    const s2 = this.pendingRemovals.get(e);
    if (s2) {
      clearTimeout(s2), this.pendingRemovals.delete(e);
      const w = this.cursors.get(e);
      w && (w.classList.remove("playhtml-cursor-fade-out"), w.classList.add("playhtml-cursor-fade-in"));
    }
    const r = n.playerIdentity;
    if (this.options.shouldRenderCursor && !this.options.shouldRenderCursor(n)) {
      this.removeCursor(e);
      return;
    }
    let i = this.cursors.get(e);
    const o2 = n.cursor, c2 = i && i.dataset.pointerType !== o2.pointer;
    !i || c2 ? (i && i.remove(), i = this.createCursorElement(
      r,
      o2.pointer,
      n.message,
      e,
      n
    ), i.dataset.pointerType = o2.pointer, this.cursors.set(e, i), this.getContainer().appendChild(i)) : i && (this.updateCursorMessage(i, r, n.message), this.updateCursorName(i, r));
    const a2 = n.zone, l2 = this.cursorZoneState.get(e) ?? null, u = (a2 == null ? void 0 : a2.zoneId) ?? null, h = l2 !== u;
    this.cursorZoneState.set(e, u);
    let d, f2 = false;
    if (a2) {
      const w = this.zones.get(a2.zoneId);
      if (w && document.contains(w.element)) {
        const C = w.element.getBoundingClientRect(), v = this.cursors.get(e), E = v ? v.offsetWidth / 2 : 0, x = v ? v.offsetHeight / 2 : 0, y2 = C.left + a2.relX * C.width - E, _ = C.top + a2.relY * C.height - x;
        this.coordinateMode === "absolute" ? d = {
          x: y2 + window.scrollX,
          y: _ + window.scrollY
        } : d = { x: y2, y: _ }, f2 = true;
      } else
        d = this.resolveTargetCoords(o2.x, o2.y);
    } else
      d = this.resolveTargetCoords(o2.x, o2.y);
    this.applyZoneStyling(
      i,
      n,
      f2 ? u : null,
      e
    );
    let p = d.x, g = d.y;
    if (this.coordinateMode === "relative") {
      const C = window.innerWidth, v = window.innerHeight;
      p = Math.max(
        -18,
        Math.min(C - 2, d.x)
      ), g = Math.max(
        -18,
        Math.min(v - 2, d.y)
      );
    }
    let m = this.cursorAnimators.get(e);
    if (m || (m = new Iy(
      { x: p, y: g },
      this.createCursorPositionCallback(e)
    ), this.cursorAnimators.set(e, m)), h ? m.snapTo({ x: p, y: g }) : m.setTarget({ x: p, y: g }), this.currentCursor) {
      const w = this.storageToClient(this.currentCursor.x, this.currentCursor.y), C = Sr(
        { x: d.x, y: d.y, pointer: o2.pointer },
        {
          x: w.x,
          y: w.y,
          pointer: this.currentCursor.pointer
        }
      ), v = this.visibilityThreshold ? C < this.visibilityThreshold : true;
      i.style.display = v ? "block" : "none", i.style.opacity = v ? "1" : "0", i.dataset.animating || (i.style.transform = v ? "scale(1)" : "scale(0.8)");
    } else
      i.style.display = "block", i.style.opacity = "1", i.dataset.animating || (i.style.transform = "scale(1)");
  }
  createCursorElement(e, n = "mouse", s2, r, i) {
    const o2 = document.createElement("div");
    if (o2.className = "playhtml-cursor-other playhtml-cursor-fade-in", r && this.options.onCustomCursorRender) {
      const a2 = this.options.onCustomCursorRender(
        r,
        o2
      );
      if (a2)
        return a2;
    }
    const c2 = Oy(he(e));
    switch (n) {
      case "mouse":
        o2.innerHTML = this.getMouseCursorSVG(c2);
        break;
      case "touch":
        o2.innerHTML = this.getTouchCursorSVG(c2);
        break;
      default:
        o2.innerHTML = $y(n) ? this.getCustomCursorSVG(c2, n) : this.getMouseCursorSVG(c2);
        break;
    }
    if (this.options.cursorStyle && (o2.style.cssText += this.options.cursorStyle), this.updateCursorMessage(o2, e, s2), this.updateCursorName(o2, e), this.options.getCursorStyle && i) {
      const a2 = this.options.getCursorStyle(i);
      Object.assign(o2.style, a2);
    }
    return o2;
  }
  getMouseCursorSVG(e) {
    return `
      <svg
        height="32"
        viewBox="0 0 32 32"
        width="32"
        xmlns="http://www.w3.org/2000/svg"
        style="pointer-events: none;"
      >
        <g fill="none" fillRule="evenodd" transform="translate(10 7)">
          <path
            d="m6.148 18.473 1.863-1.003 1.615-.839-2.568-4.816h4.332l-11.379-11.408v16.015l3.316-3.221z"
            fill="#fff"
          />
          <path
            d="m6.431 17 1.765-.941-2.775-5.202h3.604l-8.025-8.043v11.188l2.53-2.442z"
            fill="${ts(e)}"
          />
        </g>
      </svg>
    `;
  }
  getTouchCursorSVG(e) {
    return `
      <svg
        height="32"
        viewBox="0 0 32 32"
        width="32"
        xmlns="http://www.w3.org/2000/svg"
        style="pointer-events: none;"
      >
        <g fill="none" fillRule="evenodd" transform="translate(9 8)">
          <path
            d="m3.8852309 13.5522788c.15029277.1354048.25406355.2326609.57471053.5372549.31406586.2983172.46594413.439273.60482646.5572091.05791893.0487853.10729946.1792495.12686364.3731628.01609788.1595565.01049553.3375341-.0090192.5090254-.00674888.0593077-.01325791.1020883-.01698742.1224696-.04186639.2287942.13249226.4401222.36507344.4424801.20929712.0021219.37056581.00472.79741331.0123273.10679864.0019014.10679864.0019014.21395196.0037648 1.16029156.0199598 1.75290683.01448 2.1782236-.039003.45462139-.05716.92282087-.6061887 1.32754658-1.2951218.3429437.6096032.818651 1.2048784 1.2990136 1.282277.1525992.0243739.3372104.0319365.5511764.0270146.1595258-.0036697.328349-.0141847.4987188-.0294071.1284742-.0114791.2308379-.0230173.2919821-.0309462.2259121-.0292954.3737346-.2515956.31337-.4712558-.0130388-.0474468-.0339905-.1345046-.0551176-.2441066-.0244927-.1270617-.0421932-.2511642-.0502379-.3642189-.0051002-.0716765-.0061057-.1365707-.0028638-.1926702.0056365-.097781.007395-.1525378.0101327-.2790463.0010457-.0470941.0010457-.0470941.0024433-.0883088.0052898-.134881.0234093-.2629524.0820463-.5422232.0251901-.1212103.1472903-.3531692.3395862-.6402332.0572734-.0854992.1198813-.1747825.1869659-.2669588.127207-.1747861.2641214-.3514011.4010853-.5204043.0820457-.1012383.1454717-.1769623.1807968-.2180763.2962199-.424403.6120842-1.1191696.7281396-1.5253635.111416-.3904017.2005405-1.10937558.2553074-1.81604479.0300143-.40088807.0411211-.72405394.0411211-1.23097561.0000507-.08891816.0000507-.08891816.0002032-.16234685.0002858-.12025251.0003032-.16573976-.0000887-.22195195-.0010706-.15358041-.0055478-.30580145-.0203882-.6940256-.0319191-.81365149-.4778003-1.3396911-1.1348711-1.44115781-.5589865-.08632026-1.2393839.37795756-1.2393839.37795756s-.1514404-.5228127-.2537197-.6842075c-.1661957-.25934741-.5941748-.58982828-.9213451-.65421118-.3365014-.0653413-.7354024-.05811592-1.1017193.00667481-.3207944.05740454-.64034865.34382687-.82518751.65277182-.13223727.22039488-.00786932-.01169164-.14013104-.2396787-.1830552-.31402315-.60932935-.59522407-1.01524567-.67822294-.34396352-.07112559-.73801897-.04403625-1.09795562.06293793-.46304125.13836397-.53675291.49073282-.55516748.38984626-.06158674-.3382385-.06727482-.3160095-.105656-.55729603-.14258072-.89527436-.30213161-1.51473549-.54406219-2.05528331.01391678.0310773-.08860981-.20214701-.12592279-.28256779-.06461002-.13925416-.12910532-.2652956-.19999629-.38652204-.21850342-.37364978-.46891278-.65340904-.7830908-.81233894-.54561037-.27629378-1.3634177-.14183064-1.75105565.31064856-.38495968.44966797-.4491432 1.20149287-.3521966 2.13184003.03702376.36121263.16678627 1.02066144.28444961 1.50812387.04160602.1691894.07805979.32348903.14491578.60851331.01149723.04848415.01149723.04848415.02309483.09698036.05172236.21571896.09707607.39320067.15122332.5879629-.00568154-.02030261.09701461.344086.11888835.42472961.00727686.02691587.00727686.02691587.01448296.05395339.04082856.15377935.08074083.31959314.14309954.5963099.03412572.1521447.06742545.31468601.09999775.48699018.08883553.46993091.089274.37207374.00375852.27186198-.05907319-.06922522-.11463055-.13209255-.16830659-.19003644-.09976937-.10770214-.19148509-.19677225-.2785569-.2678141-.6343975-.51905295-1.02312991-.74839425-1.55681885-.79878106-.87541567-.08410158-1.70619803.53426712-1.83111632 1.36882761-.07682697.51169638-.05207639.74723271.18463583 1.19942735.13026223.24432805.35060714.53942202.76172732 1.04735429.02515953.031068.02515953.031068.05030428.06206416.50464537.62186746.55962098.69095396.67961467.86473786.32435479.4706845 1.1139501 1.8221455 1.25748612 2.0035872z"
            fill="${ts(e)}"
          />
          <path
            d="m1.68266944 9.2716401c-.02488625-.03067752-.02488625-.03067752-.04970567-.06132555-.37729166-.46613768-.58418002-.74321015-.68156241-.9258495-.15281729-.29195235-.1611316-.37107459-.10605794-.73788601.06473349-.43247455.53181583-.78013371 1.01829549-.73339767.33660502.03178017.63068475.20527903 1.15339692.63295262.0565942.04617564.12482853.1124417.20288232.19670163.04616569.04983637.09513192.10524534.14800114.16720042.0794093.0930562.34702847.42052231.30761424.37286894.05814283.06991619.09971852.12407704.14721655.19045018.0941062.13434104.14705111.20894642.21874992.30454484-.0336171-.04487143.21473082.29843305.26732159.34863333.27859812.26593456.68203289.04195871.65675979-.31244785-.00421914-.05916537-.01812774-.12308431-.04717934-.23466885-.11487425-.81923739-.15505751-1.08218312-.24678252-1.56739907-.03407352-.18024544-.06905328-.35098727-.10521102-.5121905-.06435409-.28557213-.10635725-.46007245-.14994794-.62425526-.00774801-.02907063-.00774801-.02907063-.01552357-.05783095-.02300644-.08481964-.12725123-.45470311-.12030828-.42989063-.05134381-.18468043-.0945453-.35373996-.14431997-.56133562-.01130896-.04728909-.01130896-.04728909-.02259904-.09489949-.06649254-.28350912-.10387999-.44176072-.14606063-.6132721-.10998732-.45567652-.23425389-1.08719519-.2671036-1.40768017-.07546665-.72422018-.02339381-1.33418457.17582284-1.56688778.15554834-.1815673.59641015-.25405339.84271752-.12932486.16107512.0814814.32204278.26131571.47435101.521769.05764302.09857191.11172763.20426801.16708381.32357735.0335256.07225783.13292567.29837003.12172905.27336705.21032209.46992469.354801 1.03086841.48791736 1.86671535.03939531.24766201.08813662.52823537.15063928.87150416.01857903.10178746.01857903.10178746.03722922.20314381.30139226 1.63533599.27933797 1.51139381.28367122 1.64182468.01580667.47578071.71810567.4869267.74900255.01188722.00979855-.15065269.00630989-.2851661-.01107827-.67146517-.00245496-.05465243-.00245496-.05465243-.00481877-.10910149-.01521525-.35590459-.01433687-.56066672.00670546-.67705709.03834708-.21223125.22887-.4499778.40434754-.50241339.24641865-.07323589.51640341-.09179599.73269877-.04707051.20703808.0423346.44864736.20171736.51796318.32062499.08353628.14399789.15516008.36337367.21006107.63530456.04431149.21947986.07480439.45493493.0962536.70624261.00667352.0781897.01103024.13859819.01772256.23854675.00285005.04183594.00285005.04183594.00568968.07635213.00160285.01731471.00160285.01731471.00551199.04467336.00303535.01917374.00303535.01917374.01734216.06773608.00727602.13782339.00727602.13782339.56081544.18893151.16530264-.19982737.16530264-.19982737.16077268-.23486454.02708074-.1183491.04365279-.250265.06727822-.49813693.01508098-.16112409.02268576-.24033521.03157416-.32249887.036794-.34012028.0835164-.55621578.140511-.65120691.0707148-.11819408.3197845-.28280909.4314962-.30279961.2805348-.04961763.5886064-.0551978.8264635-.00901194.1077347.021202.3705429.22413969.4327499.32121002.1277282.20156171.2519621.8513817.3219188 1.49611734-.0110122.04228902-.0110122.04228902.1607163.28760404.5903408-.06730286.5903408-.06730286.5737568-.17389206.0155734-.03799147.0279666-.08191522.0455068-.15013809.0421947-.1597068.0701719-.25243998.1118273-.35635899.0288165-.07188915.0591935-.13335501.0903398-.18227881.120675-.18992919.4330876-.31896311.7070596-.2766556.2942545.0454396.4817569.26665023.4998934.72896761.0145423.38042999.0188438.52667972.0198445.67022961.0003693.0529684.0003531.09548963.0000723.21509672-.0001536.07391241-.0001536.07391241-.000205.16397385 0 .48892448-.010469.79353263-.0389535 1.17400348-.0506294.653266-.1361064 1.34281542-.228649 1.66708482-.094456.330596-.3764591.9508823-.5997469 1.2734975-.0158389.0153017-.0838055.0964468-.1706932.2036597-.1445918.1784155-.2892331.364998-.4248114.5512865-.0725632.099704-.140705.1968792-.2036767.2908847-.2436695.3637558-.4000227.6607868-.4506249.9042828-.0664376.3164194-.0901813.4842425-.0973169.666189-.0017426.0515155-.0017426.0515155-.0028439.1014735-.0025547.1180556-.0040857.165727-.0090621.2520573-.0052398.0906702-.0037444.1871795.0035093.2891187.0103883.145992.0000001.3454812.0000001.3454812s-.1266332-.0118299-.2678551-.0085813c-.1725177.0039685-.3159859-.0019087-.4151297-.0177442-.143046-.0230487-.5293508-.5064503-.7271506-.8830611-.3022704-.5764228-1.03604858-.5484427-1.33684295-.0394061-.27130191.4618137-.65965243.9172085-.77493336.9317029-.37460536.047106-.95471158.0524702-2.07175566.0332544-.10679478-.0018572-.10679478-.0018572-.21348729-.0037567-.42889761-.0076439-.41241496.0647655-.40363307-.0124079.02506967-.2203068.02222332-.1790312.00000011-.3992999-.03726222-.36933-.15125405-.6704984-.38877094-.8705429-.12286946-.1043424-.26983033-.2407345-.56500741-.5211097-.33722428-.3203411-.44283686-.4193233-.57299128-.5337266l-.80130455-.8907189c-.08795856-.1124788-.86002339-1.4339349-1.21248613-1.9454077-.13710846-.19857111-.18839645-.26302343-.71461353-.9114734zm9.50873056.0037599v3.459c0 .5.75.5.75 0v-3.459c0-.5-.75-.5-.75 0zm-2.03159602-.00057241.016 3.47300001c.00230346.4999947.7522955.4965395.74999204-.0034552l-.016-3.47299999c-.00230346-.4999947-.7522955-.49653951-.74999204.00345518zm-1.20911102 3.45357381-.021-3.42599996c-.00306475-.4999906-.75305066-.49539349-.74998592.00459712l.021 3.42600004c.00306475.4999906.75305066.4953935.74998592-.0045972z"
            fill="#fff"
          />
        </g>
      </svg>
    `;
  }
  getCustomCursorSVG(e, n) {
    return `
      <svg
        height="32"
        viewBox="0 0 32 32"
        width="32"
        xmlns="http://www.w3.org/2000/svg"
        style="filter: drop-shadow(0 0 0.25rem ${ts(e)}); pointer-events: none;"
      >
        <g fill="none" fillRule="evenodd" transform="translate(9 8)">
          <image href="${ts(n)}" width="32" height="32"></image>
        </g>
      </svg>
    `;
  }
  removeCursor(e) {
    const n = this.cursors.get(e);
    if (n) {
      n.classList.remove("playhtml-cursor-fade-in"), n.classList.add("playhtml-cursor-fade-out");
      const r = this.pendingRemovals.get(e);
      r && clearTimeout(r);
      const i = setTimeout(() => {
        n.remove(), this.cursors.delete(e), this.pendingRemovals.delete(e);
      }, 300);
      this.pendingRemovals.set(e, i);
    }
    const s2 = this.cursorAnimators.get(e);
    s2 && (s2.destroy(), this.cursorAnimators.delete(e)), this.cursorZoneState.delete(e), this.cursorStyleKeys.delete(e);
  }
  setupGlobalAPI() {
    const e = this;
    window.cursors = {
      get allColors() {
        return br(e.users.getAll());
      },
      get color() {
        return e.users.me.color;
      },
      set color(n) {
        e.users.me.color = n;
      },
      get name() {
        return e.users.me.name;
      },
      set name(n) {
        e.users.me.name = n;
      },
      on: (n, s2) => {
        e.globalApiListeners.has(n) || e.globalApiListeners.set(n, /* @__PURE__ */ new Set()), e.globalApiListeners.get(n).add(s2);
      },
      off: (n, s2) => {
        const r = e.globalApiListeners.get(n);
        r && r.delete(s2);
      }
    };
  }
  emitGlobalEvent(e, n) {
    const s2 = this.globalApiListeners.get(e);
    if (s2)
      for (const r of s2)
        try {
          r(n);
        } catch (i) {
          console.error(
            `[playhtml] cursors "${e}" subscriber threw:`,
            i
          );
        }
  }
  updateChatCTA() {
    this.chat && (this.otherUsersWithMessages.size > 0 ? this.chat.showCTA() : this.chat.hideCTA());
  }
  updateCursorMessage(e, n, s2) {
    const r = e.querySelector(".playhtml-cursor-message");
    if (r && r.remove(), s2) {
      const i = document.createElement("div");
      i.className = "playhtml-cursor-message", i.style.cssText = `
        position: absolute;
        font-size: 16px;
        font-style: normal;
        font-family: system-ui, "Segoe UI", Roboto, Helvetica, Arial, sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol";
        color: white;
        padding: 4px 9px 4px 9px;
        border-radius: 16px 16px 16px 16px;
        white-space: nowrap;
        background-color: rgba(52,199,89,1);
        top: 17px;
        left: 22px;
      `, i.textContent = s2, e.appendChild(i);
    }
  }
  updateCursorName(e, n) {
    const s2 = e.querySelector(".playhtml-cursor-name");
    s2 && s2.remove();
    const r = n == null ? void 0 : n.name;
    if (r && n) {
      const i = he(n), o2 = r.length > 10 ? r.slice(0, 10) + ".." : r, c2 = this.opacifyColor(i, 0.6), a2 = this.opacifyColor(i, 0.3), l2 = this.getContrastColor(i), u = document.createElement("div");
      u.className = "playhtml-cursor-name", u.style.cssText = `
        position: absolute;
        white-space: nowrap;
        padding: 4px 6px;
        font-size: 12px;
        background: ${i};
        border-radius: 14px;
        top: 14px;
        left: 18px;
        opacity: 0.75;
        border: 1px solid ${c2};
        box-shadow: 1px 1px 4px 2px ${a2};
        color: ${l2};
      `, u.textContent = o2, e.appendChild(u);
    }
  }
  opacifyColor(e, n) {
    if (e.startsWith("#")) {
      const s2 = e.replace("#", ""), r = parseInt(s2.substring(0, 2), 16), i = parseInt(s2.substring(2, 4), 16), o2 = parseInt(s2.substring(4, 6), 16);
      return `rgba(${r}, ${i}, ${o2}, ${n})`;
    } else {
      if (e.startsWith("rgba"))
        return e.replace(/[\d\.]+\)$/, `${n})`);
      if (e.startsWith("rgb"))
        return e.replace("rgb", "rgba").replace(")", `, ${n})`);
      if (e.startsWith("hsl")) {
        const s2 = e.match(
          /hsl\((\d+(?:\.\d+)?),\s*(\d+(?:\.\d+)?)%,\s*(\d+(?:\.\d+)?)%\)/
        );
        if (s2) {
          const [, r, i, o2] = s2.map(Number), [c2, a2, l2] = this.hslToRgb(r, i, o2);
          return `rgba(${c2}, ${a2}, ${l2}, ${n})`;
        }
      }
    }
    return e;
  }
  hslToRgb(e, n, s2) {
    e /= 360, n /= 100, s2 /= 100;
    let r, i, o2;
    if (n === 0)
      r = i = o2 = s2;
    else {
      const c2 = (u, h, d) => (d < 0 && (d += 1), d > 1 && (d -= 1), d < 0.16666666666666666 ? u + (h - u) * 6 * d : d < 0.5 ? h : d < 0.6666666666666666 ? u + (h - u) * (0.6666666666666666 - d) * 6 : u), a2 = s2 < 0.5 ? s2 * (1 + n) : s2 + n - s2 * n, l2 = 2 * s2 - a2;
      r = c2(l2, a2, e + 1 / 3), i = c2(l2, a2, e), o2 = c2(l2, a2, e - 1 / 3);
    }
    return [Math.round(r * 255), Math.round(i * 255), Math.round(o2 * 255)];
  }
  getLuminance(e) {
    let n, s2, r;
    if (e.startsWith("#")) {
      const a2 = e.replace("#", "");
      n = parseInt(a2.substring(0, 2), 16), s2 = parseInt(a2.substring(2, 4), 16), r = parseInt(a2.substring(4, 6), 16);
    } else if (e.startsWith("rgb")) {
      const a2 = e.match(/\d+/g);
      if (!a2 || a2.length < 3) return 0;
      [n, s2, r] = a2.map(Number);
    } else if (e.startsWith("hsl")) {
      const a2 = e.match(/\d+(\.\d+)?/g);
      if (!a2 || a2.length < 3) return 0;
      const [l2, u, h] = a2.map(Number);
      [n, s2, r] = this.hslToRgb(l2, u, h);
    } else
      return 0;
    const [i, o2, c2] = [n / 255, s2 / 255, r / 255].map(
      (a2) => a2 <= 0.03928 ? a2 / 12.92 : Math.pow((a2 + 0.055) / 1.055, 2.4)
    );
    return 0.2126 * i + 0.7152 * o2 + 0.0722 * c2;
  }
  getContrastColor(e) {
    return this.getLuminance(e) > 0.5 ? "#000000" : "#ffffff";
  }
  updateAllCursorVisibility() {
    if (!this.currentCursor || !this.visibilityThreshold) return;
    const e = this.storageToClient(this.currentCursor.x, this.currentCursor.y);
    this.cursors.forEach((n, s2) => {
      var _a4;
      const i = (_a4 = this.spatialGrid.getAll().find((o2) => o2.id === s2)) == null ? void 0 : _a4.data;
      if (i && i.cursor) {
        const o2 = this.storageToClient(i.cursor.x, i.cursor.y), a2 = Sr(
          { ...e, pointer: this.currentCursor.pointer },
          { ...o2, pointer: i.cursor.pointer }
        ) < this.visibilityThreshold;
        n.style.display = a2 ? "block" : "none", n.style.opacity = a2 ? "1" : "0", n.dataset.animating || (n.style.transform = a2 ? "scale(1)" : "scale(0.8)");
      }
    });
  }
  showAllCursors() {
    this.cursors.forEach((e) => {
      e.style.display = "block", e.style.opacity = "1", e.dataset.animating || (e.style.transform = "scale(1)");
    });
  }
  configure(e) {
    if (Object.assign(this.options, e), e.shouldRenderCursor !== void 0)
      for (const [n, s2] of this.activeCursorPresenceEntries())
        this.updateCursor(n, s2);
    e.visibilityThreshold !== void 0 && (this.visibilityThreshold = e.visibilityThreshold, this.updateAllCursorVisibility()), e.playerIdentity !== void 0 && this.users.adoptIdentity(e.playerIdentity);
  }
  registerZone(e, n) {
    if (!e.id)
      throw new Error("[playhtml] Zone element must have an id attribute.");
    this.zones.set(e.id, { element: e, options: n });
  }
  unregisterZone(e) {
    this.zones.delete(e);
  }
  hideCursor(e) {
    const n = this.cursors.get(e);
    n && (n.style.display = "none");
  }
  showCursor(e) {
    const n = this.cursors.get(e);
    n && (n.style.display = "block");
  }
  destroy() {
    var _a4, _b, _c2;
    (_a4 = this.unsubscribeSelfChange) == null ? void 0 : _a4.call(this), this.unsubscribeSelfChange = null, (_b = this.unsubscribeUsersChange) == null ? void 0 : _b.call(this), this.unsubscribeUsersChange = null, this.ownsUsers && this.users.destroy(), this.cursorEventCleanups.forEach((e) => e()), this.cursorEventCleanups = [], this.pointerFrame !== null && (this.cancelPointerFrame(this.pointerFrame), this.pointerFrame = null), this.pendingPointerSample = null, this.awarenessUpdateTimeout !== null && (clearTimeout(this.awarenessUpdateTimeout), this.awarenessUpdateTimeout = null), this.cursors.forEach((e) => e.remove()), this.cursors.clear(), this.cursorAnimators.forEach((e) => e.destroy()), this.cursorAnimators.clear(), this.spatialGrid.clear(), this.zones.clear(), this.cursorZoneState.clear(), this.cursorStyleKeys.clear(), this.clientIdToStableId.clear(), this.pendingRemovals.forEach((e) => clearTimeout(e)), this.pendingRemovals.clear(), this.chat && this.chat.destroy(), this.presenceTransport ? ((_c2 = this.presenceTransportUnsubscribe) == null ? void 0 : _c2.call(this), this.presenceTransportUnsubscribe = null, this.presenceTransport.clear("cursor")) : this.provider.awareness.setLocalStateField(en, null);
  }
  // Debug method to inspect spatial partitioning efficiency
  getDebugInfo() {
    const e = this.spatialGrid.getItemCount(), n = this.spatialGrid.getCellCount();
    return {
      totalCursors: e,
      gridCells: n,
      avgCursorsPerCell: n > 0 ? e / n : 0
    };
  }
  // Instance-level subscription API (mirrors window.cursors.on/off)
  on(e, n) {
    this.globalApiListeners.has(e) || this.globalApiListeners.set(e, /* @__PURE__ */ new Set()), this.globalApiListeners.get(e).add(n);
  }
  off(e, n) {
    const s2 = this.globalApiListeners.get(e);
    s2 && s2.delete(n);
  }
  // Snapshot of current cursor-related values for consumers
  getSnapshot() {
    return {
      allColors: br(this.users.getAll()),
      color: he(this.playerIdentity),
      name: this.playerIdentity.name ?? void 0
    };
  }
  // Get my player identity (including stable publicKey)
  getMyPlayerIdentity() {
    return this.playerIdentity;
  }
  // Get the provider (needed for awareness access)
  getProvider() {
    return this.provider;
  }
  // Get all cursor presences keyed by stable ID (slim shape for rendering).
  // Cursor coordinates are converted from storage (e.g. viewport % when coordinateMode is "relative")
  // to client pixel coordinates so consumers can use them directly for CSS left/top.
  getCursorPresences() {
    const e = /* @__PURE__ */ new Map();
    if (this.presenceTransport) {
      const n = ze(), s2 = this.currentCursor ? this.storageToClient(this.currentCursor.x, this.currentCursor.y) : null;
      e.set(this.playerIdentity.publicKey, {
        cursor: s2 && this.currentCursor ? {
          x: s2.x,
          y: s2.y,
          pointer: this.currentCursor.pointer
        } : null,
        playerIdentity: this.playerIdentity,
        zone: this.currentZone,
        page: n
      });
    }
    for (const [n, s2] of this.cursorPresenceEntries({
      includeLocalAwareness: !this.presenceTransport
    })) {
      const r = s2.cursor ? this.storageToClient(s2.cursor.x, s2.cursor.y) : null;
      e.set(n, {
        cursor: r && s2.cursor ? {
          x: r.x,
          y: r.y,
          pointer: s2.cursor.pointer
        } : null,
        playerIdentity: s2.playerIdentity,
        zone: s2.zone,
        // Expose the reader's pathname so consumers can group presences by
        // page — e.g. a docs sidebar can show "who is reading which page"
        // without maintaining a parallel room-per-page structure.
        page: s2.page
      });
    }
    return e;
  }
  // Subscribe to cursor presence changes
  onCursorPresencesChange(e) {
    const n = Math.random().toString(36);
    return this.cursorPresenceChangeCallbacks.set(n, e), () => {
      this.cursorPresenceChangeCallbacks.delete(n);
    };
  }
  // Notify listeners of cursor presence changes
  notifyCursorPresenceListeners() {
    const e = this.getCursorPresences();
    this.cursorPresenceChangeCallbacks.forEach((n) => n(e));
  }
  /**
   * Apply a CSS class to a specific cursor element identified by the player's stableId (publicKey).
   * The class is added to the actual rendered cursor DOM element and removed after `durationMs`.
   * Returns true if the cursor element was found and the animation was applied.
   */
  triggerCursorAnimation(e, n, s2 = 1500) {
    const r = this.activeAnimationCleanups.get(e);
    if (r && r(), e === this.playerIdentity.publicKey)
      return this.triggerSelfCursorAnimation(n, s2);
    const o2 = this.cursors.get(e);
    if (!o2) return false;
    const c2 = o2.querySelector("svg") ?? o2;
    o2.dataset.animating = "true";
    const a2 = o2.style.display, l2 = o2.style.opacity;
    o2.style.display = "block", o2.style.opacity = "1";
    const u = o2.style.transition;
    o2.style.transition = "none", c2.classList.add(n);
    let h = false;
    const d = () => {
      h || (h = true, c2.classList.remove(n), delete o2.dataset.animating, o2.style.transition = u, o2.style.display = a2, o2.style.opacity = l2, this.activeAnimationCleanups.delete(e));
    };
    this.activeAnimationCleanups.set(e, d);
    const f2 = () => {
      d(), c2.removeEventListener("animationend", f2);
    };
    return c2.addEventListener("animationend", f2), window.setTimeout(f2, s2), true;
  }
  // Create a temporary ghost cursor at the local player's position and animate it
  triggerSelfCursorAnimation(e, n) {
    if (!this.currentCursor) return false;
    const s2 = he(this.playerIdentity), r = this.storageToClient(this.currentCursor.x, this.currentCursor.y), i = document.createElement("div");
    i.className = `playhtml-cursor-other ${e}`, i.style.cssText = `
      position: fixed;
      left: ${r.x - 16}px;
      top: ${r.y - 16}px;
      width: 32px;
      height: 32px;
      z-index: 999999;
      pointer-events: none;
      opacity: 0.3;
      transform-origin: top left;
    `, i.innerHTML = this.getMouseCursorSVG(s2), document.body.appendChild(i);
    const o2 = this.playerIdentity.publicKey;
    let c2 = false;
    const a2 = () => {
      c2 || (c2 = true, i.removeEventListener("animationend", l2), i.remove(), this.activeAnimationCleanups.delete(o2));
    };
    this.activeAnimationCleanups.set(o2, a2);
    const l2 = () => a2();
    return i.addEventListener("animationend", l2), window.setTimeout(a2, n), true;
  }
};
var ns = "__presence__";
var Lc = "__playhtml_cursors__";
var Xr = "__playhtml_identity__";
var Ry = /* @__PURE__ */ new Set(["playerIdentity", "cursor", "isMe"]);
function cu(t) {
  const e = /* @__PURE__ */ new Map(), n = /* @__PURE__ */ new WeakSet();
  let s2 = null, r = 0;
  function i() {
    return t.getAwareness();
  }
  function o2() {
    t.publishIdentity !== false && Ny(i(), t.getPlayerIdentity());
  }
  function c2(g, m) {
    var _a4, _b;
    const w = [], C = Array.from(g.keys()).sort((v, E) => v - E);
    for (const v of C) {
      const E = g.get(v);
      if (!E) continue;
      let x;
      m === "cursor" ? x = (_a4 = E[Lc]) == null ? void 0 : _a4.cursor : x = (_b = E[ns]) == null ? void 0 : _b[m];
      try {
        w.push(`${v}:${JSON.stringify(x ?? null)}`);
      } catch {
        w.push(`${v}:null`);
      }
    }
    return w.join("|");
  }
  function a2() {
    if (e.size === 0) return;
    const g = i().getStates();
    let m = null;
    const w = () => (m || (m = f2()), m);
    for (const C of e.values()) {
      const v = c2(
        g,
        C.channel
      );
      v !== C.lastFingerprint && (C.lastFingerprint = v, C.callback(w()));
    }
  }
  function l2() {
    const g = i();
    s2 !== g && (s2 = g, !n.has(g) && (n.add(g), g.on("change", a2)));
  }
  function u() {
    e.size > 0 && l2();
  }
  function h(g, m) {
    const w = g[Lc], C = g[Xr] ?? (w == null ? void 0 : w.playerIdentity), v = (w == null ? void 0 : w.cursor) ?? null, E = g[ns] ?? {}, x = {
      playerIdentity: C,
      cursor: v,
      isMe: m
    };
    for (const [y2, _] of Object.entries(E))
      !Ry.has(y2) && _ != null && (x[y2] = _);
    return x;
  }
  function d() {
    const g = i(), m = g.getLocalState();
    return m ? yt(m, g.clientID) : t.getPlayerIdentity().publicKey;
  }
  function f2() {
    const g = /* @__PURE__ */ new Map(), m = i(), w = m.getStates(), C = m.clientID, v = d();
    let E = false;
    const x = /* @__PURE__ */ new Map();
    w.forEach((y2, _) => {
      const V = yt(y2, _), O = V === v, K = x.get(V);
      if (K === void 0) {
        x.set(V, _);
        return;
      }
      if (O && _ === C) {
        x.set(V, _);
        return;
      }
      O && K === C || _ > K && x.set(V, _);
    });
    for (const [y2, _] of x) {
      const V = w.get(_);
      if (!V) continue;
      const O = y2 === v;
      O && (E = true), g.set(y2, h(V, O));
    }
    if (!E) {
      const y2 = m.getLocalState() ?? {}, _ = h(y2, true);
      _.playerIdentity = t.getPlayerIdentity(), g.set(v, _);
    }
    return p(g, v), g;
  }
  function p(g, m) {
    var _a4;
    const w = (_a4 = t.getCursorPresences) == null ? void 0 : _a4.call(t);
    if (w)
      for (const [C, v] of w) {
        const E = g.get(C);
        g.set(C, {
          ...E,
          playerIdentity: v.playerIdentity ?? (E == null ? void 0 : E.playerIdentity),
          cursor: v.cursor ?? null,
          isMe: C === m
        });
      }
  }
  return {
    setMyPresence(g, m) {
      o2(), u();
      const w = i(), v = (w.getLocalState() ?? {})[ns] ?? {};
      let E;
      if (m == null) {
        const { [g]: x, ...y2 } = v;
        E = y2;
      } else
        E = { ...v, [g]: m };
      w.setLocalStateField(ns, E);
    },
    getPresences() {
      return o2(), u(), f2();
    },
    onPresenceChange(g, m) {
      if (o2(), g === "cursor" && t.onCursorPresencesChange) {
        const E = t.onCursorPresencesChange(() => {
          m(f2());
        });
        return m(f2()), E;
      }
      const w = String(r++), C = i().getStates(), v = c2(C, g);
      return e.set(w, { channel: g, callback: m, lastFingerprint: v }), l2(), m(f2()), () => {
        e.delete(w);
      };
    },
    getMyIdentity() {
      return t.getPlayerIdentity();
    }
  };
}
function Ny(t, e) {
  var _a4;
  ((_a4 = t.getLocalState()) == null ? void 0 : _a4[Xr]) || t.setLocalStateField(Xr, e);
}
var au = /* @__PURE__ */ new Map();
function lu(t, e) {
  au.set(t, e);
}
function Uy() {
  const t = [];
  return document.querySelectorAll("[shared]").forEach((e) => {
    if (!e.id) return;
    let n = "read-write";
    const s2 = e.getAttribute("shared");
    if (s2 && s2 !== "") {
      const r = s2.toLowerCase();
      (r.includes("read-only") || r === "ro") && (n = "read-only");
    }
    t.push({
      elementId: e.id,
      permissions: n,
      path: window.location.pathname
    });
  }), t;
}
function Fy() {
  const t = [];
  return document.querySelectorAll("[data-source]").forEach((e) => {
    const n = e.getAttribute("data-source");
    if (n)
      try {
        const { domain: s2, path: r, elementId: i } = Tl(n);
        t.push({ domain: s2, path: r, elementId: i });
      } catch {
      }
  }), t;
}
function jy(t, e) {
  if (t.hasAttribute("data-source") && t.hasAttribute("data-source-read-only")) return true;
  const r = e ?? Fn(t);
  return r ? au.get(r) === "read-only" : false;
}
var J = "__page__";
function uu(t) {
  return `${J}:${t}`;
}
function hu(t, e, n) {
  var _a4;
  const { getStorePlay: s2, yObserverByKey: r } = e, i = uu(t);
  if (r.has(i)) return;
  const o2 = ye((_a4 = s2()[J]) == null ? void 0 : _a4[t]);
  if (!o2 || typeof o2.observeDeep != "function") return;
  let c2 = false;
  const a2 = () => {
    c2 || (c2 = true, queueMicrotask(() => {
      var _a5;
      c2 = false;
      const l2 = (_a5 = s2()[J]) == null ? void 0 : _a5[t];
      if (!l2) return;
      const u = We(l2);
      for (const h of n)
        h(u);
    }));
  };
  o2.observeDeep(a2), r.set(i, a2);
}
function du(t) {
  var _a4;
  const { channelListeners: e, getStorePlay: n } = t;
  for (const [s2, r] of e) {
    const i = (_a4 = n()[J]) == null ? void 0 : _a4[s2];
    if (!i) continue;
    hu(s2, t, r);
    const o2 = We(i);
    for (const c2 of r)
      c2(o2);
  }
}
function Hy(t, e, n) {
  var _a4;
  const {
    ensureProxy: s2,
    getProxy: r,
    getDoc: i,
    getStorePlay: o2,
    proxyByTagAndId: c2,
    yObserverByKey: a2,
    channelRefCounts: l2,
    channelListeners: u
  } = n, h = () => o2(), d = () => i();
  (_a4 = h())[J] ?? (_a4[J] = {}), s2(J, t, e), u.has(t) || u.set(t, /* @__PURE__ */ new Set());
  const f2 = u.get(t), p = /* @__PURE__ */ new Set();
  function g() {
    hu(t, n, f2);
  }
  const m = (l2.get(t) ?? 0) + 1;
  l2.set(t, m), g();
  let w = false;
  return {
    getData() {
      var _a5;
      if (w) throw new Error(`PageDataChannel "${t}" has been destroyed`);
      return We(((_a5 = h()[J]) == null ? void 0 : _a5[t]) ?? e);
    },
    setData(C) {
      if (w) throw new Error(`PageDataChannel "${t}" has been destroyed`);
      let v = r(J, t);
      v == null && (v = s2(J, t, e), g());
      const E = v;
      typeof C == "function" ? d().transact(() => {
        C(E);
      }) : d().transact(() => {
        xs(E, C);
      });
    },
    onUpdate(C) {
      if (w) throw new Error(`PageDataChannel "${t}" has been destroyed`);
      return f2.add(C), p.add(C), () => {
        f2.delete(C), p.delete(C);
      };
    },
    destroy() {
      var _a5;
      if (w) return;
      w = true;
      for (const v of p)
        f2.delete(v);
      p.clear();
      const C = (l2.get(t) ?? 1) - 1;
      if (l2.set(t, C), C <= 0) {
        l2.delete(t), u.delete(t);
        const v = uu(t), E = a2.get(v);
        if (E) {
          const y2 = ye((_a5 = h()[J]) == null ? void 0 : _a5[t]);
          y2 && typeof y2.unobserveDeep == "function" && y2.unobserveDeep(E), a2.delete(v);
        }
        const x = c2.get(J);
        x && x.delete(t);
      }
    }
  };
}
var zy = "playhtml.syncedStore is read-only.";
var By = /* @__PURE__ */ new Set([
  "copyWithin",
  "fill",
  "pop",
  "push",
  "reverse",
  "shift",
  "sort",
  "splice",
  "unshift"
]);
function Et() {
  throw new Error(zy);
}
function Vy(t) {
  return Array.isArray(t) ? [] : {};
}
function Ic(t, e) {
  !Array.isArray(t) || !Array.isArray(e) || (e.length = t.length);
}
function fu(t) {
  const e = /* @__PURE__ */ new WeakMap();
  function n(s2) {
    if (s2 === null || typeof s2 != "object")
      return s2;
    const r = e.get(s2);
    if (r)
      return r;
    const i = s2, o2 = Vy(i), c2 = new Proxy(o2, {
      get(a2, l2) {
        return Array.isArray(i) && By.has(l2) ? Et : n(Reflect.get(i, l2, i));
      },
      getOwnPropertyDescriptor(a2, l2) {
        if (Ic(i, o2), Array.isArray(i) && l2 === "length")
          return Reflect.getOwnPropertyDescriptor(o2, l2);
        const u = Reflect.getOwnPropertyDescriptor(
          i,
          l2
        );
        return u && ("value" in u ? {
          ...u,
          configurable: true,
          value: n(u.value),
          writable: false
        } : {
          configurable: true,
          enumerable: u.enumerable,
          value: n(
            Reflect.get(i, l2, i)
          ),
          writable: false
        });
      },
      has(a2, l2) {
        return l2 in i;
      },
      ownKeys() {
        return Ic(i, o2), Reflect.ownKeys(i);
      },
      getPrototypeOf() {
        return Reflect.getPrototypeOf(i);
      },
      set: Et,
      deleteProperty: Et,
      defineProperty: Et,
      setPrototypeOf: Et,
      preventExtensions: Et
    });
    return e.set(s2, c2), c2;
  }
  return n(t);
}
(!globalThis.EventTarget || !globalThis.Event) && console.error(`
  PartySocket requires a global 'EventTarget' class to be available!
  You can polyfill this global by adding this to your code before any partysocket imports: 
  
  \`\`\`
  import 'partysocket/event-target-polyfill';
  \`\`\`
  Please file an issue at https://github.com/partykit/partykit if you're still having trouble.
`);
var pu = class extends Event {
  constructor(t, e) {
    super("error", e);
    __publicField(this, "message");
    __publicField(this, "error");
    this.message = t.message, this.error = t;
  }
};
var gu = class extends Event {
  constructor(t = 1e3, e = "", n) {
    super("close", n);
    __publicField(this, "code");
    __publicField(this, "reason");
    __publicField(this, "wasClean", true);
    this.code = t, this.reason = e;
  }
};
var Ar = {
  Event,
  ErrorEvent: pu,
  CloseEvent: gu
};
function Ky(t, e) {
  if (!t) throw new Error(e);
}
function Wy(t) {
  return new t.constructor(t.type, t);
}
function Yy(t) {
  return "data" in t ? new MessageEvent(t.type, t) : "code" in t || "reason" in t ? new gu(t.code || 1999, t.reason || "unknown reason", t) : "error" in t ? new pu(t.error, t) : new Event(t.type, t);
}
var _a3;
var Gy = typeof process < "u" && typeof ((_a3 = process.versions) == null ? void 0 : _a3.node) < "u";
var qy = typeof navigator < "u" && navigator.product === "ReactNative";
var ss = Gy || qy ? Yy : Wy;
var Xe = {
  maxReconnectionDelay: 1e4,
  minReconnectionDelay: 3e3,
  minUptime: 5e3,
  reconnectionDelayGrowFactor: 1.3,
  connectionTimeout: 4e3,
  maxRetries: Number.POSITIVE_INFINITY,
  maxEnqueuedMessages: Number.POSITIVE_INFINITY
};
var Mc = false;
function Jy() {
}
var Xy = class Re extends EventTarget {
  constructor(e, n, s2 = {}) {
    super();
    __publicField(this, "_ws");
    __publicField(this, "_retryCount", -1);
    __publicField(this, "_uptimeTimeout");
    __publicField(this, "_connectTimeout");
    __publicField(this, "_shouldReconnect", true);
    __publicField(this, "_connectLock", false);
    __publicField(this, "_binaryType", "blob");
    __publicField(this, "_closeCalled", false);
    __publicField(this, "_didWarnAboutClosedSend", false);
    __publicField(this, "_messageQueue", []);
    __publicField(this, "_debugLogger", console.log.bind(console));
    __publicField(this, "_url");
    __publicField(this, "_protocols");
    __publicField(this, "_options");
    /**
     * An event listener to be called when the WebSocket connection's readyState changes to CLOSED
     */
    __publicField(this, "onclose", null);
    /**
     * An event listener to be called when an error occurs
     */
    __publicField(this, "onerror", null);
    /**
     * An event listener to be called when a message is received from the server
     */
    __publicField(this, "onmessage", null);
    /**
     * An event listener to be called when the WebSocket connection's readyState changes to OPEN;
     * this indicates that the connection is ready to send and receive data
     */
    __publicField(this, "onopen", null);
    __publicField(this, "_handleOpen", (e) => {
      this._debug("open event");
      const { minUptime: n = Xe.minUptime } = this._options;
      clearTimeout(this._connectTimeout), this._uptimeTimeout = setTimeout(() => this._acceptOpen(), n), Ky(this._ws, "WebSocket is not defined"), this._ws.binaryType = this._binaryType, this._messageQueue.forEach((s2) => {
        var _a4;
        (_a4 = this._ws) == null ? void 0 : _a4.send(s2);
      }), this._messageQueue = [], this.onopen && this.onopen(e), this.dispatchEvent(ss(e));
    });
    __publicField(this, "_handleMessage", (e) => {
      this._debug("message event"), this.onmessage && this.onmessage(e), this.dispatchEvent(ss(e));
    });
    __publicField(this, "_handleError", (e) => {
      this._debug("error event", e.message), this._disconnect(void 0, e.message === "TIMEOUT" ? "timeout" : void 0), this.onerror && this.onerror(e), this._debug("exec error listeners"), this.dispatchEvent(ss(e)), this._connect();
    });
    __publicField(this, "_handleClose", (e) => {
      this._debug("close event"), this._clearTimeouts(), this._shouldReconnect && this._connect(), this.onclose && this.onclose(e), this.dispatchEvent(ss(e));
    });
    this._url = e, this._protocols = n, this._options = s2, this._options.startClosed && (this._shouldReconnect = false), this._options.debugLogger && (this._debugLogger = this._options.debugLogger), this._connect();
  }
  static get CONNECTING() {
    return 0;
  }
  static get OPEN() {
    return 1;
  }
  static get CLOSING() {
    return 2;
  }
  static get CLOSED() {
    return 3;
  }
  get CONNECTING() {
    return Re.CONNECTING;
  }
  get OPEN() {
    return Re.OPEN;
  }
  get CLOSING() {
    return Re.CLOSING;
  }
  get CLOSED() {
    return Re.CLOSED;
  }
  get binaryType() {
    return this._ws ? this._ws.binaryType : this._binaryType;
  }
  set binaryType(e) {
    this._binaryType = e, this._ws && (this._ws.binaryType = e);
  }
  /**
   * Returns the number or connection retries
   */
  get retryCount() {
    return Math.max(this._retryCount, 0);
  }
  /**
   * The number of bytes of data that have been queued using calls to send() but not yet
   * transmitted to the network. This value resets to zero once all queued data has been sent.
   * This value does not reset to zero when the connection is closed; if you keep calling send(),
   * this will continue to climb. Read only
   */
  get bufferedAmount() {
    return this._messageQueue.reduce((e, n) => (typeof n == "string" ? e += n.length : n instanceof Blob ? e += n.size : e += n.byteLength, e), 0) + (this._ws ? this._ws.bufferedAmount : 0);
  }
  /**
   * The extensions selected by the server. This is currently only the empty string or a list of
   * extensions as negotiated by the connection
   */
  get extensions() {
    return this._ws ? this._ws.extensions : "";
  }
  /**
   * A string indicating the name of the sub-protocol the server selected;
   * this will be one of the strings specified in the protocols parameter when creating the
   * WebSocket object
   */
  get protocol() {
    return this._ws ? this._ws.protocol : "";
  }
  /**
   * The current state of the connection; this is one of the Ready state constants
   */
  get readyState() {
    return this._closeCalled ? Re.CLOSED : this._ws ? this._ws.readyState : this._options.startClosed ? Re.CLOSED : Re.CONNECTING;
  }
  /**
   * The URL as resolved by the constructor
   */
  get url() {
    return this._ws ? this._ws.url : "";
  }
  /**
   * Whether the websocket object is now in reconnectable state
   */
  get shouldReconnect() {
    return this._shouldReconnect;
  }
  /**
   * Closes the WebSocket connection or connection attempt, if any. If the connection is already
   * CLOSED or CLOSING, this method does nothing.
   *
   * The `close` event is dispatched synchronously (mirroring how
   * `reconnect()` dispatches its synthetic close). This guarantees
   * consumers observe a terminal event for every explicit close, even
   * if their listeners are detached right after this call — previously
   * the real (asynchronous) browser close event could fire after
   * listeners were removed and go unobserved entirely.
   */
  close(e = 1e3, n) {
    if (this._closeCalled = true, this._shouldReconnect = false, this._clearTimeouts(), !this._ws) {
      this._debug("close enqueued: no ws instance");
      return;
    }
    if (this._ws.readyState === this.CLOSED || this._ws.readyState === this.CLOSING) {
      this._debug("close: already closing or closed");
      return;
    }
    this._disconnect(e, n);
  }
  /**
   * Closes the WebSocket connection or connection attempt and connects again.
   * Resets retry counter;
   */
  reconnect(e, n) {
    this._shouldReconnect = true, this._closeCalled = false, this._didWarnAboutClosedSend = false, this._retryCount = -1, !this._ws || this._ws.readyState === this.CLOSED || this._ws.readyState === this.CLOSING ? this._connect() : (this._disconnect(e, n), this._connect());
  }
  /**
   * Enqueue specified data to be transmitted to the server over the WebSocket connection.
   *
   * @returns `true` if the message was transmitted immediately over an open
   * connection; `false` if it was buffered (sent when the connection next
   * opens — the buffer is always flushed before the `open` event is
   * dispatched) or dropped because `maxEnqueuedMessages` was reached.
   */
  send(e) {
    if (this._ws && this._ws.readyState === this.OPEN)
      return this._debug("send", e), this._ws.send(e), true;
    this._closeCalled && !this._didWarnAboutClosedSend && (this._didWarnAboutClosedSend = true, console.warn(
      "ReconnectingWebSocket: send() was called after close(). The message has been buffered, but it will only be delivered if reconnect() is called on this socket. If this socket has been discarded, the message is lost — this usually means a stale socket reference is being used."
    ));
    const { maxEnqueuedMessages: n = Xe.maxEnqueuedMessages } = this._options;
    return this._messageQueue.length < n && (this._debug("enqueue", e), this._messageQueue.push(e)), false;
  }
  /**
   * Removes and returns all messages that were passed to send() but never
   * transmitted (they were buffered while the connection wasn't open).
   *
   * Useful when a socket is being discarded and replaced (e.g. the React
   * hooks recreate the socket when connection options change): the
   * replacement socket can re-send these messages, instead of them being
   * silently lost with the old instance.
   */
  drainQueuedMessages() {
    const e = this._messageQueue;
    return this._messageQueue = [], e;
  }
  _debug(...e) {
    this._options.debug && this._debugLogger("RWS>", ...e);
  }
  _getNextDelay() {
    const {
      reconnectionDelayGrowFactor: e = Xe.reconnectionDelayGrowFactor,
      minReconnectionDelay: n = Xe.minReconnectionDelay,
      maxReconnectionDelay: s2 = Xe.maxReconnectionDelay
    } = this._options;
    let r = 0;
    return this._retryCount > 0 && (r = n * e ** (this._retryCount - 1), r > s2 && (r = s2)), this._debug("next delay", r), r;
  }
  _wait() {
    return new Promise((e) => {
      setTimeout(e, this._getNextDelay());
    });
  }
  _getNextProtocols(e) {
    if (!e) return Promise.resolve(null);
    if (typeof e == "string" || Array.isArray(e))
      return Promise.resolve(e);
    if (typeof e == "function") {
      const n = e();
      if (!n) return Promise.resolve(null);
      if (typeof n == "string" || Array.isArray(n))
        return Promise.resolve(n);
      if (n.then) return n;
    }
    throw Error("Invalid protocols");
  }
  _getNextUrl(e) {
    if (typeof e == "string") return Promise.resolve(e);
    if (typeof e == "function") {
      const n = e();
      if (typeof n == "string") return Promise.resolve(n);
      if (n.then) return n;
    }
    throw Error("Invalid URL");
  }
  _connect() {
    if (this._connectLock || !this._shouldReconnect) return;
    this._connectLock = true;
    const {
      maxRetries: e = Xe.maxRetries,
      connectionTimeout: n = Xe.connectionTimeout
    } = this._options;
    if (this._retryCount >= e) {
      this._debug("max retries reached", this._retryCount, ">=", e), this._connectLock = false;
      return;
    }
    this._retryCount++, this._debug("connect", this._retryCount), this._removeListeners(), this._wait().then(
      () => Promise.all([
        this._getNextUrl(this._url),
        this._getNextProtocols(this._protocols || null)
      ])
    ).then(([s2, r]) => {
      if (this._closeCalled) {
        this._connectLock = false;
        return;
      }
      !this._options.WebSocket && typeof WebSocket > "u" && !Mc && (console.error(`‼️ No WebSocket implementation available. You should define options.WebSocket. 

For example, if you're using node.js, run \`npm install ws\`, and then in your code:

import PartySocket from 'partysocket';
import WS from 'ws';

const partysocket = new PartySocket({
  host: "127.0.0.1:1999",
  room: "test-room",
  WebSocket: WS
});

`), Mc = true);
      const i = this._options.WebSocket || WebSocket;
      this._debug("connect", {
        url: s2,
        protocols: r
      }), this._ws = r ? new i(s2, r) : new i(s2), this._ws.binaryType = this._binaryType, this._connectLock = false, this._addListeners(), this._connectTimeout = setTimeout(
        () => this._handleTimeout(),
        n
      );
    }).catch((s2) => {
      this._connectLock = false, this._handleError(new Ar.ErrorEvent(Error(s2.message), this));
    });
  }
  _handleTimeout() {
    this._debug("timeout event"), this._handleError(new Ar.ErrorEvent(Error("TIMEOUT"), this));
  }
  _disconnect(e = 1e3, n) {
    if (this._clearTimeouts(), !!this._ws) {
      this._removeListeners();
      try {
        (this._ws.readyState === this.OPEN || this._ws.readyState === this.CONNECTING) && this._ws.close(e, n), this._handleClose(new Ar.CloseEvent(e, n, this));
      } catch {
      }
    }
  }
  _acceptOpen() {
    this._debug("accept open"), this._retryCount = 0;
  }
  _removeListeners() {
    this._ws && (this._debug("removeListeners"), this._ws.removeEventListener("open", this._handleOpen), this._ws.removeEventListener("close", this._handleClose), this._ws.removeEventListener("message", this._handleMessage), this._ws.removeEventListener("error", this._handleError), this._ws.addEventListener("error", Jy));
  }
  _addListeners() {
    this._ws && (this._debug("addListeners"), this._ws.addEventListener("open", this._handleOpen), this._ws.addEventListener("close", this._handleClose), this._ws.addEventListener("message", this._handleMessage), this._ws.addEventListener("error", this._handleError));
  }
  _clearTimeouts() {
    clearTimeout(this._connectTimeout), clearTimeout(this._uptimeTimeout);
  }
};
var Zy = (t) => t[1] !== null && t[1] !== void 0;
function Qy() {
  if (crypto == null ? void 0 : crypto.randomUUID) return crypto.randomUUID();
  let t = Date.now(), e = (performance == null ? void 0 : performance.now) && performance.now() * 1e3 || 0;
  return "xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx".replace(/[xy]/g, function(n) {
    let s2 = Math.random() * 16;
    return t > 0 ? (s2 = (t + s2) % 16 | 0, t = Math.floor(t / 16)) : (s2 = (e + s2) % 16 | 0, e = Math.floor(e / 16)), (n === "x" ? s2 : s2 & 3 | 8).toString(16);
  });
}
function yu(t, e, n = {}) {
  const {
    host: s2,
    path: r,
    protocol: i,
    room: o2,
    party: c2,
    basePath: a2,
    prefix: l2,
    query: u
  } = t;
  let h = s2.replace(/^(http|https|ws|wss):\/\//, "");
  if (h.endsWith("/") && (h = h.slice(0, -1)), r == null ? void 0 : r.startsWith("/"))
    throw new Error("path must not start with a slash");
  const d = c2 ?? "main", f2 = r ? `/${r}` : "", p = i || (h.startsWith("localhost:") || h.startsWith("127.0.0.1:") || h.startsWith("192.168.") || h.startsWith("10.") || h.startsWith("172.") && h.split(".")[1] >= "16" && h.split(".")[1] <= "31" || h.startsWith("[::ffff:7f00:1]:") ? e : `${e}s`), g = `${p}://${h}/${a2 || `${l2 || "parties"}/${d}/${o2}`}${f2}`, m = (C = {}) => `${g}?${new URLSearchParams([...Object.entries(n), ...Object.entries(C).filter(Zy)])}`, w = typeof u == "function" ? async () => m(await u()) : m(u);
  return {
    host: h,
    path: f2,
    room: o2,
    name: d,
    protocol: p,
    partyUrl: g,
    urlProvider: w
  };
}
var em = class extends Xy {
  constructor(t) {
    var _a4, _b;
    const e = $c(t);
    super(e.urlProvider, e.protocols, e.socketOptions);
    __publicField(this, "_pk");
    __publicField(this, "_pkurl");
    __publicField(this, "name");
    __publicField(this, "room");
    __publicField(this, "host");
    __publicField(this, "path");
    __publicField(this, "basePath");
    if (this.partySocketOptions = t, this.setWSProperties(e), !t.startClosed && !this.room && !this.basePath)
      throw this.close(), new Error(
        "Either room or basePath must be provided to connect. Use startClosed: true to create a socket and set them via updateProperties before calling reconnect()."
      );
    t.disableNameValidation || (((_a4 = t.party) == null ? void 0 : _a4.includes("/")) && console.warn(
      `PartySocket: party name "${t.party}" contains forward slash which may cause routing issues. Consider using a name without forward slashes or set disableNameValidation: true to bypass this warning.`
    ), ((_b = t.room) == null ? void 0 : _b.includes("/")) && console.warn(
      `PartySocket: room name "${t.room}" contains forward slash which may cause routing issues. Consider using a name without forward slashes or set disableNameValidation: true to bypass this warning.`
    ));
  }
  updateProperties(t) {
    const e = $c({
      ...this.partySocketOptions,
      ...t,
      host: t.host ?? this.host,
      room: t.room ?? this.room,
      path: t.path ?? this.path,
      basePath: t.basePath ?? this.basePath
    });
    this._url = e.urlProvider, this._protocols = e.protocols, this._options = e.socketOptions, this.setWSProperties(e);
  }
  setWSProperties(t) {
    const { _pk: e, _pkurl: n, name: s2, room: r, host: i, path: o2, basePath: c2 } = t;
    this._pk = e, this._pkurl = n, this.name = s2, this.room = r, this.host = i, this.path = o2, this.basePath = c2;
  }
  reconnect(t, e) {
    if (!this.host)
      throw new Error(
        "The host must be set before connecting, use `updateProperties` method to set it or pass it to the constructor."
      );
    if (!this.room && !this.basePath)
      throw new Error(
        "The room (or basePath) must be set before connecting, use `updateProperties` method to set it or pass it to the constructor."
      );
    super.reconnect(t, e);
  }
  get id() {
    return this._pk;
  }
  /**
   * Exposes the static PartyKit room URL without applying query parameters.
   * To access the currently connected WebSocket url, use PartySocket#url.
   */
  get roomUrl() {
    return this._pkurl;
  }
  static async fetch(t, e) {
    const n = yu(t, "http"), s2 = typeof n.urlProvider == "string" ? n.urlProvider : await n.urlProvider();
    return (t.fetch ?? fetch)(s2, e);
  }
};
function $c(t) {
  const {
    id: e,
    host: n,
    path: s2,
    party: r,
    room: i,
    protocol: o2,
    query: c2,
    protocols: a2,
    ...l2
  } = t, u = e || Qy(), h = yu(t, "ws", { _pk: u });
  return {
    _pk: u,
    _pkurl: h.partyUrl,
    name: h.name,
    room: h.room,
    host: h.host,
    path: h.path,
    basePath: t.basePath,
    protocols: a2,
    socketOptions: l2,
    urlProvider: h.urlProvider
  };
}
var tm = 1e3;
function kr(t) {
  return t === Xl ? "identity" : Zl(t) ? "element" : Ql(t) ? "presence" : "cursor";
}
var nm = class {
  constructor(e) {
    __publicField(this, "peers", /* @__PURE__ */ new Map());
    __publicField(this, "listeners", {
      cursor: /* @__PURE__ */ new Set(),
      element: /* @__PURE__ */ new Set(),
      presence: /* @__PURE__ */ new Set(),
      identity: /* @__PURE__ */ new Set()
    });
    __publicField(this, "unsubscribe");
    __publicField(this, "sweepTimer", null);
    this.unsubscribe = e.subscribe((n) => this.handleMessage(n)), this.sweepTimer = setInterval(() => {
      this.sweepExpired(Date.now());
    }, tm);
  }
  /** The live folded peer map, keyed by connection id. Views read this. */
  getPeers() {
    return this.peers;
  }
  /**
   * Subscribe to a namespace. The callback fires whenever a message touched that
   * namespace, and once immediately with the current snapshot (replay). Returns
   * an unsubscribe function.
   */
  subscribe(e, n) {
    return this.listeners[e].add(n), ve(n, "peer store namespace"), () => {
      this.listeners[e].delete(n);
    };
  }
  destroy() {
    this.sweepTimer !== null && (clearInterval(this.sweepTimer), this.sweepTimer = null), this.unsubscribe();
    for (const e of Object.values(this.listeners)) e.clear();
  }
  handleMessage(e) {
    if (e.type === "presence-sync") {
      this.applySync(e.peers), this.pruneExpired(Date.now()), this.notify(/* @__PURE__ */ new Set(["cursor", "element", "presence", "identity"]));
      return;
    }
    if (e.type === "presence-changes") {
      const n = this.applyChanges(e);
      this.pruneExpired(Date.now(), n), n.size > 0 && this.notify(n);
    }
  }
  applySync(e) {
    this.peers.clear();
    for (const [n, s2] of Object.entries(e))
      this.peers.set(n, { ...s2 });
  }
  applyChanges(e) {
    const n = /* @__PURE__ */ new Set();
    for (const [s2, r] of Object.entries(e.updates)) {
      const i = this.peers.get(s2) ?? {};
      this.peers.set(s2, i);
      for (const [o2, c2] of Object.entries(r))
        i[o2] = c2, n.add(kr(o2));
    }
    for (const [s2, r] of Object.entries(e.removes)) {
      const i = this.peers.get(s2);
      if (i) {
        for (const o2 of r)
          o2 in i && (delete i[o2], n.add(kr(o2)));
        Object.keys(i).length === 0 && this.peers.delete(s2);
      }
    }
    return n;
  }
  notify(e) {
    const n = /* @__PURE__ */ new Set();
    for (const s2 of e)
      for (const r of this.listeners[s2])
        n.has(r) || (n.add(r), ve(r, "peer store namespace"));
  }
  /** Periodic sweep for peers that went silent: prune expired channels and
   * notify only the namespaces that actually lost one (no-op when nothing
   * expired, so a quiet room never re-fires callbacks). */
  sweepExpired(e) {
    const n = /* @__PURE__ */ new Set();
    this.pruneExpired(e, n), n.size > 0 && this.notify(n);
  }
  /**
   * Delete every peer channel whose stamped `at` has aged past the staleness
   * window, recording the touched namespaces into `touched`. Never removes a
   * peer wholesale for having only unstamped channels (identity persists) —
   * only prunes a now-empty peer row. Unstamped channels (identity) are always
   * live, so they are never swept. This is the single implementation of
   * client-side staleness for cursor, element, and presence views.
   */
  pruneExpired(e, n = /* @__PURE__ */ new Set()) {
    for (const [s2, r] of this.peers) {
      for (const [i, o2] of Object.entries(r))
        Cy(o2, e, Xs) || (delete r[i], n.add(kr(i)));
      Object.keys(r).length === 0 && this.peers.delete(s2);
    }
    return n;
  }
};
var sm = 1;
var rm = 3;
var im = 15e3;
var om = 5e3;
var mu = class {
  constructor(e) {
    __publicField(this, "socket");
    __publicField(this, "room");
    __publicField(this, "listeners", /* @__PURE__ */ new Set());
    __publicField(this, "latestJoin", null);
    __publicField(this, "channelValues", /* @__PURE__ */ new Map());
    // One peer-folding layer per socket, shared by every consumer (cursors,
    // element awareness, page presence). It subscribes to this transport's raw
    // message stream and exposes per-namespace subscriptions + the folded peers.
    __publicField(this, "peers");
    // Observability state (see PresenceConnectionState). Internal, for tests/debug.
    __publicField(this, "_connectionState", "connecting");
    __publicField(this, "hasEverOpened", false);
    __publicField(this, "failedReconnects", 0);
    __publicField(this, "unreachableLogged", false);
    __publicField(this, "unreachableTimer", null);
    __publicField(this, "lastControlLogAt", /* @__PURE__ */ new Map());
    __publicField(this, "usesHandlerProperties", false);
    __publicField(this, "onMessage", (e) => {
      const n = cm(e.data);
      if (n) {
        this.handleControlMessage(n);
        for (const s2 of this.listeners)
          ve(() => s2(n), "presence transport listener");
      }
    });
    __publicField(this, "onOpen", () => {
      this._connectionState = "open", this.hasEverOpened = true, this.failedReconnects = 0, this.unreachableLogged = false, this.unreachableTimer !== null && (clearTimeout(this.unreachableTimer), this.unreachableTimer = null), this.flushCurrentState();
    });
    __publicField(this, "onCloseOrError", () => {
      this._connectionState !== "open" && (this.failedReconnects += 1), this._connectionState !== "unreachable" && (this._connectionState = "connecting"), this.failedReconnects >= rm && this.markUnreachable();
    });
    this.room = e.room;
    const n = e.socketFactory ?? ((s2) => new em(s2));
    this.socket = n({
      host: e.host,
      room: e.room,
      party: "presence",
      maxEnqueuedMessages: 0
    }), Oc(this.socket) ? (this.socket.onmessage = this.onMessage, this.socket.onopen = this.onOpen, this.socket.onclose = this.onCloseOrError, this.socket.onerror = this.onCloseOrError, this.usesHandlerProperties = true) : (this.socket.addEventListener("message", this.onMessage), this.socket.addEventListener("open", this.onOpen), this.socket.addEventListener("close", this.onCloseOrError), this.socket.addEventListener("error", this.onCloseOrError)), this.unreachableTimer = setTimeout(() => {
      this.unreachableTimer = null, this.hasEverOpened || this.markUnreachable();
    }, im), this.peers = new nm(this);
  }
  /** Observability flag: whether the realtime socket is connecting, open, or
   * has been declared unreachable. Never gates behavior — for tests/debugging. */
  get connectionState() {
    return this._connectionState;
  }
  markUnreachable() {
    this.unreachableLogged || (this.unreachableLogged = true, this._connectionState = "unreachable", console.error(
      `[playhtml] presence transport unreachable — realtime presence degraded (room ${this.room}). Cursors, element awareness, and custom presence will not sync until the connection recovers.`
    ));
  }
  /**
   * Base handling of server control messages, on EVERY socket (not just the
   * cursor client's). Consumers still layer their own reactions (e.g. the cursor
   * client's hz pacing) via subscribe(); this only guarantees rejections, the
   * channel cap, and force-close loops are never fully silent. Logging is
   * rate-limited per event type so a loop can't spam the console.
   */
  handleControlMessage(e) {
    e.type === "presence-error" ? this.logControl(
      "presence-error",
      () => console.warn(
        `[playhtml] presence server rejected a message (room ${this.room}):`,
        e.message
      )
    ) : e.type === "presence-rate" && this.logControl(
      "presence-rate",
      () => console.warn(
        `[playhtml] presence channel "${e.channel}" is rate-limited to ${e.hz}Hz (room ${this.room}).`
      )
    );
  }
  logControl(e, n) {
    const s2 = Date.now(), r = this.lastControlLogAt.get(e) ?? 0;
    s2 - r < om || (this.lastControlLogAt.set(e, s2), n());
  }
  join(e) {
    const n = We(e), s2 = {
      type: "presence-join",
      identity: n.identity,
      page: n.page
    };
    gr(s2), this.latestJoin = n, this.sendIfOpen(s2);
  }
  update(e, n) {
    const s2 = We(n), r = {
      type: "presence-update",
      channel: e,
      value: s2
    };
    gr(r), this.channelValues.set(e, s2), this.sendIfOpen(r);
  }
  clear(e) {
    this.channelValues.delete(e), this.sendIfOpen({
      type: "presence-clear",
      channel: e
    });
  }
  subscribe(e) {
    return this.listeners.add(e), () => {
      this.listeners.delete(e);
    };
  }
  destroy() {
    this.unreachableTimer !== null && (clearTimeout(this.unreachableTimer), this.unreachableTimer = null), this.peers.destroy(), this.usesHandlerProperties && Oc(this.socket) ? (this.socket.onmessage === this.onMessage && (this.socket.onmessage = null), this.socket.onopen === this.onOpen && (this.socket.onopen = null), this.socket.onclose === this.onCloseOrError && (this.socket.onclose = null), this.socket.onerror === this.onCloseOrError && (this.socket.onerror = null)) : (this.socket.removeEventListener(
      "message",
      this.onMessage
    ), this.socket.removeEventListener("open", this.onOpen), this.socket.removeEventListener("close", this.onCloseOrError), this.socket.removeEventListener("error", this.onCloseOrError)), this.socket.close(), this.listeners.clear();
  }
  flushCurrentState() {
    this.latestJoin && this.sendIfOpen({
      type: "presence-join",
      identity: this.latestJoin.identity,
      page: this.latestJoin.page
    });
    for (const [e, n] of this.channelValues)
      this.sendIfOpen({
        type: "presence-update",
        channel: e,
        value: n
      });
  }
  sendIfOpen(e) {
    gr(e), this.isSocketOpen() && this.socket.send(JSON.stringify(e));
  }
  isSocketOpen() {
    return this.socket.readyState === void 0 || this.socket.readyState === sm;
  }
};
function wu() {
  return typeof WebSocket < "u";
}
function Oc(t) {
  return "onmessage" in t && "onopen" in t && "onclose" in t && "onerror" in t;
}
function cm(t) {
  if (typeof t != "string") return null;
  let e;
  try {
    e = JSON.parse(t);
  } catch {
    return null;
  }
  if (!z(e)) return null;
  switch (e.type) {
    case "presence-sync":
      return Ac(e.peers) ? e : null;
    case "presence-changes":
      return Ac(e.updates) && _y(e.removes) ? e : null;
    case "presence-rate":
      return typeof e.channel == "string" && typeof e.hz == "number" ? e : null;
    case "presence-error":
      return typeof e.message == "string" ? e : null;
    default:
      return null;
  }
}
var Pc = `${Yi}shard:`;
var bu = 1;
var Jt = 8;
var am = 1100;
var lm = class {
  constructor(e) {
    __publicField(this, "transport");
    __publicField(this, "getIdentity");
    __publicField(this, "getPage");
    __publicField(this, "onAwareness");
    __publicField(this, "localTags", /* @__PURE__ */ new Map());
    __publicField(this, "publishedChannels", /* @__PURE__ */ new Set());
    // Shard channels whose clear may not have reached the server yet. A dropped
    // clear is otherwise permanent: publishedChannels is updated immediately, so a
    // later publish wouldn't re-issue it. Held until a trailing republish re-sends.
    __publicField(this, "pendingClears", /* @__PURE__ */ new Set());
    __publicField(this, "unsubscribe");
    __publicField(this, "stopKeepalive");
    __publicField(this, "publishScheduled", false);
    __publicField(this, "republishTimer", null);
    __publicField(this, "destroyed", false);
    __publicField(this, "lastAwarenessFingerprint", "");
    this.transport = e.transport, this.getIdentity = e.getIdentity, this.getPage = e.getPage, this.onAwareness = e.onAwareness;
    const n = () => this.emit(), s2 = this.transport.peers.subscribe("element", n), r = this.transport.peers.subscribe("identity", n);
    this.unsubscribe = () => {
      s2(), r();
    }, this.stopKeepalive = eu(() => {
      this.destroyed || this.localTags.size === 0 || this.publishLocalAwareness();
    }), this.join();
  }
  setLocalAwareness(e, n, s2) {
    this.stageLocalAwareness(e, n, s2) && (this.schedulePublish(), this.emit());
  }
  /**
   * Sets many elements' awareness at once, coalescing into a single publish and
   * a single local emit. Used to reseed retained handlers after a room change
   * without triggering one full-shard publish per element.
   */
  setLocalAwarenessBatch(e) {
    let n = false;
    for (const [s2, r, i] of e)
      this.stageLocalAwareness(s2, r, i) && (n = true);
    n && (this.schedulePublish(), this.emit());
  }
  /** Stages a local write into localTags; returns true if it changed. */
  stageLocalAwareness(e, n, s2) {
    const r = this.localTags.get(e) ?? {};
    return r[n] === s2 ? false : (this.localTags.set(e, { ...r, [n]: s2 }), true);
  }
  removeLocalAwareness(e, n) {
    const s2 = this.localTags.get(e);
    if (!s2 || !(n in s2)) return;
    const r = { ...s2 };
    delete r[n], Object.keys(r).length === 0 ? this.localTags.delete(e) : this.localTags.set(e, r), this.schedulePublish(), this.emit();
  }
  getLocalAwareness(e, n) {
    var _a4;
    return (_a4 = this.localTags.get(e)) == null ? void 0 : _a4[n];
  }
  refresh() {
    this.emit();
  }
  destroy() {
    this.destroyed = true, this.republishTimer !== null && (clearTimeout(this.republishTimer), this.republishTimer = null), this.stopKeepalive(), this.unsubscribe();
  }
  /**
   * Coalesce publishes: multiple local writes in one tick collapse into a single
   * shard-set publish on the next microtask. Bursts (e.g. every element on a
   * heavy page initializing at once) thus cost one publish, not O(N).
   */
  schedulePublish() {
    this.publishScheduled || this.destroyed || (this.publishScheduled = true, queueMicrotask(() => {
      this.publishScheduled = false, !this.destroyed && (this.publishLocalAwareness(), this.scheduleRepublish());
    }));
  }
  /**
   * After a publish, re-send the latest snapshot once things quiet down. The
   * server drops updates past its per-connection budget under a burst and never
   * tells us which; a single trailing re-publish re-sends whatever was dropped.
   */
  scheduleRepublish() {
    this.destroyed || (this.republishTimer !== null && clearTimeout(this.republishTimer), this.republishTimer = setTimeout(() => {
      if (this.republishTimer = null, !this.destroyed) {
        for (const e of this.pendingClears)
          Ls(this.transport, e, "element awareness");
        this.pendingClears.clear(), this.publishLocalAwareness();
      }
    }, am));
  }
  join() {
    try {
      this.transport.join({
        identity: this.getIdentity(),
        page: this.getPage()
      });
    } catch (e) {
      console.warn("[playhtml] Failed to join element awareness room:", e);
    }
  }
  publishLocalAwareness() {
    const e = /* @__PURE__ */ new Set(), n = hm(this.localTags), s2 = n.slice(0, Jt);
    if (n.length > Jt) {
      const r = fm(
        n.slice(Jt)
      );
      console.error(
        `[playhtml] Element awareness exceeded ${Jt} shards (~${Jt * As} bytes); dropping overflow so under-budget elements keep syncing. Affected tags: ${r.join(", ")}.`
      );
    }
    for (let r = 0; r < s2.length; r += 1) {
      const i = `${Pc}${r}`;
      su(
        this.transport,
        i,
        // Each shard already carries an `at` stamp (createElementPresenceShard)
        // so it ages out client-side if this peer disconnects ungracefully.
        s2[r],
        "element awareness"
      ) && e.add(i), this.pendingClears.delete(i);
    }
    for (const r of this.publishedChannels)
      e.has(r) || (this.pendingClears.add(r), Ls(this.transport, r, "element awareness"));
    this.publishedChannels = e;
  }
  emit() {
    const e = this.buildElementAwareness(), n = um(e);
    n !== this.lastAwarenessFingerprint && (this.lastAwarenessFingerprint = n, this.onAwareness(e));
  }
  buildElementAwareness() {
    const e = /* @__PURE__ */ new Map(), n = this.getIdentity().publicKey;
    for (const [r, i] of this.localTags)
      for (const [o2, c2] of Object.entries(i))
        Zr(e, r, o2, c2, n);
    const s2 = this.transport.peers.getPeers();
    for (const r of Array.from(s2.keys()).sort()) {
      const i = s2.get(r), o2 = nu(i);
      if (o2 === n) continue;
      const c2 = o2 ?? r;
      for (const [a2, l2] of Object.entries(i))
        if (Zl(a2)) {
          if (a2.startsWith(Pc))
            pm(e, l2, c2);
          else if (z(l2)) {
            const u = a2.slice(Yi.length);
            for (const [h, d] of Object.entries(l2))
              Zr(e, u, h, d, c2);
          }
        }
    }
    return e;
  }
};
function um(t) {
  const e = [];
  for (const n of Array.from(t.keys()).sort()) {
    const s2 = t.get(n);
    for (const r of Array.from(s2.byStableId.keys()).sort()) {
      let i;
      try {
        i = JSON.stringify(s2.byStableId.get(r)) ?? "null";
      } catch {
        i = "null";
      }
      e.push(`${n}:${r}:${i}`);
    }
  }
  return e.join("|");
}
function Zr(t, e, n, s2, r) {
  const i = `${e}:${n}`;
  let o2 = t.get(i);
  o2 || (o2 = { array: [], byStableId: /* @__PURE__ */ new Map() }, t.set(i, o2)), o2.array.push(s2), o2.byStableId.set(r, s2);
}
function hm(t) {
  const e = [];
  let n = [];
  for (const s2 of dm(t)) {
    const r = [...n, s2];
    n.length > 0 && tu(xr(r)) > As ? (e.push(xr(n)), n = [s2]) : n = r;
  }
  return n.length > 0 && e.push(xr(n)), e;
}
function dm(t) {
  const e = [];
  for (const n of Array.from(t.keys()).sort()) {
    const s2 = t.get(n);
    for (const r of Object.keys(s2).sort())
      e.push([n, r, s2[r]]);
  }
  return e;
}
function xr(t) {
  return {
    v: bu,
    // Stamp the publish time here (not at send) so shard sizing accounts for the
    // `at` field and a near-4KB shard can't tip over the cap once stamped. The
    // exact value doesn't affect byte length (always a ~13-digit epoch ms).
    at: Date.now(),
    entries: t
  };
}
function fm(t) {
  const e = /* @__PURE__ */ new Set();
  for (const n of t)
    for (const [s2] of n.entries) e.add(s2);
  return Array.from(e).sort();
}
function pm(t, e, n) {
  if (gm(e))
    for (const s2 of e.entries) {
      const [r, i, o2] = s2;
      Zr(t, r, i, o2, n);
    }
}
function gm(t) {
  return !z(t) || t.v !== bu || !Array.isArray(t.entries) ? false : t.entries.every(
    (e) => Array.isArray(e) && e.length === 3 && typeof e[0] == "string" && typeof e[1] == "string"
  );
}
var ym = 1100;
var vu = class {
  constructor(e) {
    __publicField(this, "transport");
    __publicField(this, "getIdentity");
    __publicField(this, "getPage");
    __publicField(this, "getCursorPresences");
    __publicField(this, "onCursorPresencesChange");
    __publicField(this, "localChannels", /* @__PURE__ */ new Map());
    // Wire channels whose clear may not have reached the server yet (a dropped
    // clear is otherwise permanent: localChannels no longer holds the value, so
    // the trailing republish wouldn't re-send it). Cleared channels are held here
    // until a trailing republish re-issues the clear.
    __publicField(this, "pendingClears", /* @__PURE__ */ new Set());
    __publicField(this, "listeners", /* @__PURE__ */ new Map());
    __publicField(this, "nextListenerId", 0);
    __publicField(this, "unsubscribe");
    __publicField(this, "stopKeepalive");
    __publicField(this, "republishTimer", null);
    __publicField(this, "destroyed", false);
    this.transport = e.transport, this.getIdentity = e.getIdentity, this.getPage = e.getPage, this.getCursorPresences = e.getCursorPresences, this.onCursorPresencesChange = e.onCursorPresencesChange;
    const n = () => this.emit(), s2 = this.transport.peers.subscribe("presence", n), r = this.transport.peers.subscribe("identity", n);
    this.unsubscribe = () => {
      s2(), r();
    }, this.stopKeepalive = eu(() => {
      this.destroyed || this.localChannels.size === 0 || this.republishLiveChannels();
    }), this.join();
  }
  setMyPresence(e, n) {
    if (vr(e))
      throw new Error(
        `[playhtml] "${e}" is a reserved presence field and cannot be used as a channel name. playerIdentity, cursor, and isMe are populated by playhtml; choose a different channel name.`
      );
    const s2 = Cr(e);
    if (n == null) {
      if (!this.localChannels.has(e) && this.pendingClears.has(s2))
        return;
      this.localChannels.delete(e), this.pendingClears.add(s2), Ls(this.transport, s2, "presence");
    } else
      this.localChannels.set(e, n), this.pendingClears.delete(s2), this.publishChannel(e, n);
    this.scheduleRepublish(), this.emit();
  }
  /** Publish a live channel wrapped in the staleness envelope ({at, value}). */
  publishChannel(e, n) {
    su(
      this.transport,
      Cr(e),
      Sy(n),
      "presence"
    );
  }
  getPresences() {
    return this.buildPresences();
  }
  onPresenceChange(e, n) {
    if (e === "cursor" && this.onCursorPresencesChange) {
      const r = this.onCursorPresencesChange(() => {
        n(this.buildPresences());
      });
      return ve(() => n(this.buildPresences()), "presence subscriber"), r ?? (() => {
      });
    }
    const s2 = String(this.nextListenerId++);
    return this.listeners.set(s2, {
      channel: e,
      callback: n,
      lastFingerprint: this.channelFingerprint(e)
    }), ve(() => n(this.buildPresences()), "presence subscriber"), () => {
      this.listeners.delete(s2);
    };
  }
  getMyIdentity() {
    return this.getIdentity();
  }
  destroy() {
    this.destroyed = true, this.republishTimer !== null && (clearTimeout(this.republishTimer), this.republishTimer = null), this.stopKeepalive(), this.unsubscribe();
  }
  /**
   * After a publish burst settles, re-send the latest value of every live
   * channel once, recovering anything the server dropped over its rate budget.
   */
  scheduleRepublish() {
    this.destroyed || (this.republishTimer !== null && clearTimeout(this.republishTimer), this.republishTimer = setTimeout(() => {
      if (this.republishTimer = null, !this.destroyed) {
        this.republishLiveChannels();
        for (const e of this.pendingClears)
          Ls(this.transport, e, "presence");
        this.pendingClears.clear();
      }
    }, ym));
  }
  /** Re-send every live channel with a fresh timestamp. Shared by the burst
   * recovery (scheduleRepublish) and the keepalive re-stamp. */
  republishLiveChannels() {
    for (const [e, n] of this.localChannels)
      this.publishChannel(e, n);
  }
  join() {
    try {
      this.transport.join({
        identity: this.getIdentity(),
        page: this.getPage()
      });
    } catch (e) {
      console.warn("[playhtml] Failed to join presence room:", e);
    }
  }
  /** The shared PeerStore's folded peer map. Views read all channels and filter
   * to the presence + identity namespaces they care about. */
  get peers() {
    return this.transport.peers.getPeers();
  }
  emit() {
    if (this.listeners.size === 0) return;
    let e = null;
    const n = () => (e || (e = this.buildPresences()), e);
    for (const s2 of this.listeners.values()) {
      const r = this.channelFingerprint(s2.channel);
      if (r === s2.lastFingerprint) continue;
      ve(
        () => s2.callback(n()),
        "presence subscriber"
      ) && (s2.lastFingerprint = r);
    }
  }
  /** Fingerprint of one channel across self + all remote peers, so a listener
   * only fires when its channel actually changed. Fingerprints the UNWRAPPED
   * payload so a keepalive re-stamp (which only bumps the envelope `at`) does
   * not count as a change and re-fire subscribers. */
  channelFingerprint(e) {
    var _a4;
    const n = [];
    this.localChannels.has(e) && n.push(`self:${Rc(this.localChannels.get(e))}`);
    const s2 = Cr(e);
    for (const r of Array.from(this.peers.keys()).sort()) {
      const i = (_a4 = this.peers.get(r)) == null ? void 0 : _a4[s2];
      i !== void 0 && n.push(`${r}:${Rc(_c(i))}`);
    }
    return n.join("|");
  }
  buildPresences() {
    const e = /* @__PURE__ */ new Map(), n = this.getIdentity().publicKey;
    e.set(n, this.buildSelfView());
    for (const s2 of Array.from(this.peers.keys()).sort()) {
      const r = this.peers.get(s2), i = r.identity, o2 = nu(r);
      if (o2 === n) continue;
      const c2 = o2 ?? s2, a2 = e.get(c2) ?? {
        playerIdentity: z(i) ? i : void 0,
        cursor: null,
        isMe: false
      };
      !a2.playerIdentity && z(i) && (a2.playerIdentity = i);
      for (const [l2, u] of Object.entries(r)) {
        if (!Ql(l2)) continue;
        const h = wy(l2);
        vr(h) || (a2[h] = _c(u));
      }
      e.set(c2, a2);
    }
    return this.mergeCursorPresences(e, n), e;
  }
  buildSelfView() {
    const e = {
      playerIdentity: this.getIdentity(),
      cursor: null,
      isMe: true
    };
    for (const [n, s2] of this.localChannels)
      vr(n) || (e[n] = s2);
    return e;
  }
  mergeCursorPresences(e, n) {
    var _a4;
    const s2 = (_a4 = this.getCursorPresences) == null ? void 0 : _a4.call(this);
    if (s2)
      for (const [r, i] of s2) {
        const o2 = e.get(r);
        e.set(r, {
          ...o2,
          playerIdentity: i.playerIdentity ?? (o2 == null ? void 0 : o2.playerIdentity),
          cursor: i.cursor ?? null,
          isMe: r === n
        });
      }
  }
};
function Rc(t) {
  try {
    return JSON.stringify(t ?? null);
  } catch {
    return "null";
  }
}
var mm = class {
  constructor(e) {
    __publicField(this, "inner");
    __publicField(this, "subscriptions", /* @__PURE__ */ new Map());
    __publicField(this, "nextSubscriptionId", 0);
    this.inner = e;
  }
  /** Swap the delegate, re-attaching every active subscription to it. */
  setInner(e) {
    if (e !== this.inner) {
      this.inner = e;
      for (const n of this.subscriptions.values()) {
        try {
          n.innerUnsub();
        } catch {
        }
        let s2 = () => {
        };
        ve(() => {
          s2 = e.onPresenceChange(
            n.channel,
            n.callback
          );
        }, "presence facade re-attach"), n.innerUnsub = s2;
      }
    }
  }
  setMyPresence(e, n) {
    this.inner.setMyPresence(e, n);
  }
  getPresences() {
    return this.inner.getPresences();
  }
  onPresenceChange(e, n) {
    const s2 = this.nextSubscriptionId++, r = this.inner.onPresenceChange(e, n);
    return this.subscriptions.set(s2, { channel: e, callback: n, innerUnsub: r }), () => {
      const i = this.subscriptions.get(s2);
      if (i) {
        this.subscriptions.delete(s2);
        try {
          i.innerUnsub();
        } catch {
        }
      }
    };
  }
  getMyIdentity() {
    return this.inner.getMyIdentity();
  }
};
var wm = class {
  constructor(e) {
    __publicField(this, "pendingChanges", []);
    __publicField(this, "cancelFlush", null);
    __publicField(this, "getDoc");
    this.getDoc = e;
  }
  queue(e) {
    this.pendingChanges.push({ targetDoc: this.getDoc(), apply: e }), this.requestFlush();
  }
  clear() {
    this.cancelFlush && (this.cancelFlush(), this.cancelFlush = null), this.pendingChanges = [];
  }
  flush() {
    this.cancelFlush = null;
    const e = this.pendingChanges;
    if (this.pendingChanges = [], !e.length)
      return;
    const n = /* @__PURE__ */ new Map();
    for (const s2 of e) {
      const r = n.get(s2.targetDoc) ?? [];
      r.push(s2.apply), n.set(s2.targetDoc, r);
    }
    for (const [s2, r] of n)
      s2.transact(() => {
        for (const i of r)
          i();
      });
  }
  requestFlush() {
    if (this.cancelFlush)
      return;
    const e = () => this.flush();
    if (typeof window.requestAnimationFrame == "function") {
      const s2 = window.requestAnimationFrame(e);
      this.cancelFlush = () => window.cancelAnimationFrame(s2);
      return;
    }
    const n = window.setTimeout(e, 0);
    this.cancelFlush = () => window.clearTimeout(n);
  }
};
var qi = { ATTRIBUTE: 1, CHILD: 2 };
var Ji = (t) => (...e) => ({ _$litDirective$: t, values: e });
var Xi = class {
  constructor(e) {
  }
  get _$AU() {
    return this._$AM._$AU;
  }
  _$AT(e, n, s2) {
    this._$Ct = e, this._$AM = n, this._$Ci = s2;
  }
  _$AS(e, n) {
    return this.update(e, n);
  }
  update(e, n) {
    return this.render(...n);
  }
};
var { I: bm } = ny;
var Nc = (t) => t;
var Uc = () => document.createComment("");
var Xt = (t, e, n) => {
  var _a4;
  const s2 = t._$AA.parentNode, r = e === void 0 ? t._$AB : e._$AA;
  if (n === void 0) {
    const i = s2.insertBefore(Uc(), r), o2 = s2.insertBefore(Uc(), r);
    n = new bm(i, o2, t, t.options);
  } else {
    const i = n._$AB.nextSibling, o2 = n._$AM, c2 = o2 !== t;
    if (c2) {
      let a2;
      (_a4 = n._$AQ) == null ? void 0 : _a4.call(n, t), n._$AM = t, n._$AP !== void 0 && (a2 = t._$AU) !== o2._$AU && n._$AP(a2);
    }
    if (i !== r || c2) {
      let a2 = n._$AA;
      for (; a2 !== i; ) {
        const l2 = Nc(a2).nextSibling;
        Nc(s2).insertBefore(a2, r), a2 = l2;
      }
    }
  }
  return n;
};
var Ze = (t, e, n = t) => (t._$AI(e, n), t);
var vm = {};
var Cm = (t, e = vm) => t._$AH = e;
var Sm = (t) => t._$AH;
var Dr = (t) => {
  t._$AR(), t._$AA.remove();
};
var Fc = (t, e, n) => {
  const s2 = /* @__PURE__ */ new Map();
  for (let r = e; r <= n; r++) s2.set(t[r], r);
  return s2;
};
var y0 = Ji(class extends Xi {
  constructor(t) {
    if (super(t), t.type !== qi.CHILD) throw Error("repeat() can only be used in text expressions");
  }
  dt(t, e, n) {
    let s2;
    n === void 0 ? n = e : e !== void 0 && (s2 = e);
    const r = [], i = [];
    let o2 = 0;
    for (const c2 of t) r[o2] = s2 ? s2(c2, o2) : o2, i[o2] = n(c2, o2), o2++;
    return { values: i, keys: r };
  }
  render(t, e, n) {
    return this.dt(t, e, n).values;
  }
  update(t, [e, n, s2]) {
    const r = Sm(t), { values: i, keys: o2 } = this.dt(e, n, s2);
    if (!Array.isArray(r)) return this.ut = o2, i;
    const c2 = this.ut ?? (this.ut = []), a2 = [];
    let l2, u, h = 0, d = r.length - 1, f2 = 0, p = i.length - 1;
    for (; h <= d && f2 <= p; ) if (r[h] === null) h++;
    else if (r[d] === null) d--;
    else if (c2[h] === o2[f2]) a2[f2] = Ze(r[h], i[f2]), h++, f2++;
    else if (c2[d] === o2[p]) a2[p] = Ze(r[d], i[p]), d--, p--;
    else if (c2[h] === o2[p]) a2[p] = Ze(r[h], i[p]), Xt(t, a2[p + 1], r[h]), h++, p--;
    else if (c2[d] === o2[f2]) a2[f2] = Ze(r[d], i[f2]), Xt(t, r[h], r[d]), d--, f2++;
    else if (l2 === void 0 && (l2 = Fc(o2, f2, p), u = Fc(c2, h, d)), l2.has(c2[h])) if (l2.has(c2[d])) {
      const g = u.get(o2[f2]), m = g !== void 0 ? r[g] : null;
      if (m === null) {
        const w = Xt(t, r[h]);
        Ze(w, i[f2]), a2[f2] = w;
      } else a2[f2] = Ze(m, i[f2]), Xt(t, r[h], m), r[g] = null;
      f2++;
    } else Dr(r[d]), d--;
    else Dr(r[h]), h++;
    for (; f2 <= p; ) {
      const g = Xt(t, a2[p + 1]);
      Ze(g, i[f2]), a2[f2++] = g;
    }
    for (; h <= d; ) {
      const g = r[h++];
      g !== null && Dr(g);
    }
    return this.ut = o2, Cm(t, a2), Ye;
  }
});
var m0 = Ji(class extends Xi {
  constructor(t) {
    var _a4;
    if (super(t), t.type !== qi.ATTRIBUTE || t.name !== "class" || ((_a4 = t.strings) == null ? void 0 : _a4.length) > 2) throw Error("`classMap()` can only be used in the `class` attribute and must be the only part in the attribute.");
  }
  render(t) {
    return " " + Object.keys(t).filter((e) => t[e]).join(" ") + " ";
  }
  update(t, [e]) {
    var _a4, _b;
    if (this.st === void 0) {
      this.st = /* @__PURE__ */ new Set(), t.strings !== void 0 && (this.nt = new Set(t.strings.join(" ").split(/\s/).filter((s2) => s2 !== "")));
      for (const s2 in e) e[s2] && !((_a4 = this.nt) == null ? void 0 : _a4.has(s2)) && this.st.add(s2);
      return this.render(e);
    }
    const n = t.element.classList;
    for (const s2 of this.st) s2 in e || (n.remove(s2), this.st.delete(s2));
    for (const s2 in e) {
      const r = !!e[s2];
      r === this.st.has(s2) || ((_b = this.nt) == null ? void 0 : _b.has(s2)) || (r ? (n.add(s2), this.st.add(s2)) : (n.remove(s2), this.st.delete(s2)));
    }
    return Ye;
  }
});
var Cu = "important";
var Em = " !" + Cu;
var w0 = Ji(class extends Xi {
  constructor(t) {
    var _a4;
    if (super(t), t.type !== qi.ATTRIBUTE || t.name !== "style" || ((_a4 = t.strings) == null ? void 0 : _a4.length) > 2) throw Error("The `styleMap` directive must be used in the `style` attribute and must be the only part in the attribute.");
  }
  render(t) {
    return Object.keys(t).reduce((e, n) => {
      const s2 = t[n];
      return s2 == null ? e : e + `${n = n.includes("-") ? n : n.replace(/(?:^(webkit|moz|ms|o)|)(?=[A-Z])/g, "-$&").toLowerCase()}:${s2};`;
    }, "");
  }
  update(t, [e]) {
    const { style: n } = t.element;
    if (this.ft === void 0) return this.ft = new Set(Object.keys(e)), this.render(e);
    for (const s2 of this.ft) e[s2] == null && (this.ft.delete(s2), s2.includes("-") ? n.removeProperty(s2) : n[s2] = null);
    for (const s2 in e) {
      const r = e[s2];
      if (r != null) {
        this.ft.add(s2);
        const i = typeof r == "string" && r.endsWith(Em);
        s2.includes("-") || i ? n.setProperty(s2, i ? r.slice(0, -11) : r, i ? Cu : "") : n[s2] = r;
      }
    }
    return Ye;
  }
});
var _m = "api.playhtml.fun";
var Am = "api-staging.playhtml.fun";
function km(t) {
  if (t)
    return t;
  const e = window.location.hostname;
  return e.includes("staging") || e.includes("ngrok-free") ? Am : _m;
}
var ne = Vl({ play: {} });
var mt = Bl(ne);
var Su = fu(ne.play);
var Qr = null;
function Eu({ includeSearch: t }) {
  const e = window.location.pathname.replace(/\.[^/.]+$/, "");
  return t ? e + window.location.search : e;
}
function rs(t) {
  return t.replace(/\.[^/.]+$/, "");
}
function _u(t) {
  const e = {
    domain: window.location.host,
    pathname: window.location.pathname,
    search: window.location.search
  };
  if (typeof t == "function") {
    const n = t(e);
    return n && n.startsWith("/") ? rs(n) : n;
  }
  switch (t) {
    case "page":
      return rs(e.pathname);
    case "domain":
      return "";
    case "section":
      return `/${rs(e.pathname).split("/").filter(Boolean)[0] || ""}`;
    default:
      return rs(e.pathname);
  }
}
function An(t, e) {
  const n = hg(t), s2 = e === "" ? n : `${n}-${e}`;
  return encodeURIComponent(s2);
}
var H;
var Lt = null;
var se = null;
var Qe = "";
var kn = null;
var Q = null;
var st = {
  subscribers: /* @__PURE__ */ new Set(),
  feedUnsub: null,
  getPresences() {
    return (se == null ? void 0 : se.getCursorPresences()) ?? /* @__PURE__ */ new Map();
  },
  subscribe(t) {
    return this.subscribers.add(t), () => {
      this.subscribers.delete(t);
    };
  },
  notify(t) {
    for (const e of this.subscribers)
      ve(() => e(t), "cursor presence subscriber");
  },
  // Point the hub at a freshly built cursor client: drop the previous feed and
  // forward the new client's presence changes to hub subscribers.
  connect(t) {
    var _a4;
    (_a4 = this.feedUnsub) == null ? void 0 : _a4.call(this), this.feedUnsub = t.onCursorPresencesChange((e) => {
      this.notify(e);
    });
  },
  disconnect() {
    var _a4;
    (_a4 = this.feedUnsub) == null ? void 0 : _a4.call(this), this.feedUnsub = null;
  }
};
function Te() {
  return (Q == null ? void 0 : Q.getIdentity()) ?? ly();
}
var Be = /* @__PURE__ */ new Map();
var Ge = /* @__PURE__ */ new Map();
var Au = /* @__PURE__ */ new Map();
var ku = /* @__PURE__ */ new Map();
var Is = /* @__PURE__ */ new Set();
var lt = /* @__PURE__ */ new Map();
var ei = /* @__PURE__ */ new Set();
function xm(t) {
  const e = t.getAttribute("data-source");
  if (!e) return;
  let n, s2, r;
  try {
    ({ domain: n, path: s2, elementId: r } = Tl(e));
  } catch {
    return;
  }
  const i = `${n}${s2}#${r}`;
  if (!ei.has(i) && (ei.add(i), H == null ? void 0 : H.wsconnected))
    try {
      const o2 = { domain: n, path: s2, elementId: r };
      H.sendMessage(
        JSON.stringify({
          type: "add-shared-reference",
          reference: o2
        })
      ), H.sendMessage(
        JSON.stringify({
          type: "export-permissions",
          elementIds: [r]
        })
      );
    } catch (o2) {
      console.warn(
        "[PLAYHTML] Failed to notify server of new shared reference:",
        o2
      );
    }
}
function Dm(t) {
  if (!t.id) return;
  const e = t.id, n = t.getAttribute("shared");
  let s2 = "read-write";
  if (n && n !== "") {
    const r = n.toLowerCase();
    (r.includes("read-only") || r === "ro") && (s2 = "read-only");
  }
  if (lu(e, s2), H == null ? void 0 : H.wsconnected)
    try {
      const r = {
        elementId: e,
        permissions: s2,
        path: window.location.pathname
      };
      H.sendMessage(
        JSON.stringify({
          type: "register-shared-element",
          element: r
        })
      );
    } catch (r) {
      console.warn(
        "[PLAYHTML] Failed to notify server of new shared element:",
        r
      );
    }
}
function xu(t, e, n) {
  var _a4;
  Be.has(t) || Be.set(t, /* @__PURE__ */ new Map());
  const s2 = Be.get(t);
  if (!s2.has(e)) {
    (_a4 = ne.play)[t] ?? (_a4[t] = {});
    const r = ne.play[t];
    if (r[e] === void 0) {
      const i = We(n);
      r[e] = i;
    }
    s2.set(e, r[e]);
  }
  return s2.get(e);
}
var M = /* @__PURE__ */ new Map();
var jc = /* @__PURE__ */ new WeakMap();
var Ft = /* @__PURE__ */ new Map();
var Hc = /* @__PURE__ */ new Set();
var Zi = new wm(() => mt);
var zc = /* @__PURE__ */ new Map();
var Tm = 0;
var Hn = Il;
function wt() {
  return [Z.CanPlay, ...Object.keys(Hn)];
}
function Lm(t) {
  H.sendMessage(JSON.stringify(t));
}
function Qi(t) {
  let e;
  try {
    e = JSON.parse(t);
  } catch {
    return;
  }
  if (e.type === "room-reset") {
    const i = Number(e.resetEpoch);
    if (!Number.isFinite(i)) {
      console.error("[PLAYHTML] Received room-reset without a resetEpoch"), window.location.reload();
      return;
    }
    Fm(i);
    return;
  }
  const { type: n, eventPayload: s2 } = e, r = Ft.get(n);
  if (!r) {
    if (e.permissions)
      try {
        const i = e.permissions;
        Object.entries(i).forEach(([o2, c2]) => {
          if (lu(o2, c2), c2 === "read-only") {
            const a2 = document.querySelector(
              `[data-source$="#${CSS.escape(o2)}"]`
            );
            a2 && a2.setAttribute("data-source-read-only", "");
          }
        });
      } catch {
      }
    return;
  }
  for (const i of r)
    i.onEvent(s2);
}
var q = false;
var xn = true;
var Zs = true;
var kt = false;
var Du = () => {
};
var ti = () => {
};
function Tu() {
  const t = new Promise((e, n) => {
    Du = e, ti = n;
  });
  return t.catch(() => {
  }), t;
}
var tt = Tu();
function Im(t) {
  return typeof t == "object" && t !== null && "then" in t && typeof t.then == "function";
}
var Dn = null;
var Tn = /* @__PURE__ */ new Set();
var X = "";
var fe = "";
var ut = null;
var ds = null;
var It = null;
var rt = null;
var it = null;
function Lu() {
  const t = D == null ? void 0 : D.room;
  return typeof t == "function" ? t() : t;
}
var jt = /* @__PURE__ */ new Map();
var on = null;
var G = null;
var cn = null;
var an = null;
var ln = null;
function eo(t) {
  if (!wu()) return null;
  const e = jt.get(t);
  if (e)
    return e.refCount++, e.transport;
  const n = new mu({
    host: fe,
    room: t
  }), s2 = (Q == null ? void 0 : Q.onSelfChange(() => {
    try {
      n.join({
        identity: Te(),
        page: ze()
      });
    } catch (r) {
      console.warn("[playhtml] Failed to republish identity on change:", r);
    }
  })) ?? null;
  return jt.set(t, {
    transport: n,
    refCount: 1,
    selfChangeUnsub: s2
  }), n;
}
function zn(t) {
  var _a4;
  const e = jt.get(t);
  if (e && (e.refCount--, !(e.refCount > 0))) {
    jt.delete(t);
    try {
      (_a4 = e.selfChangeUnsub) == null ? void 0 : _a4.call(e);
    } catch {
    }
    try {
      e.transport.destroy();
    } catch {
    }
  }
}
var fs = null;
var je = null;
var Mm = 5e3;
var Ln = /* @__PURE__ */ new Set();
var D = null;
var to = false;
function In(t) {
  if (typeof t != "function") {
    if (Array.isArray(t)) {
      const e = t.map(In);
      return e.length === 0 ? void 0 : e;
    }
    if (t && typeof t == "object") {
      const e = {};
      for (const n of Object.keys(t).sort()) {
        const s2 = In(t[n]);
        s2 !== void 0 && (e[n] = s2);
      }
      return Object.keys(e).length === 0 ? void 0 : e;
    }
    return t;
  }
}
function ni(t) {
  return typeof t == "function" ? true : Array.isArray(t) ? t.some(ni) : t && typeof t == "object" ? Object.values(t).some(ni) : false;
}
function $m(t, e) {
  for (const r of Object.keys(e)) {
    const i = typeof e[r] == "function", o2 = typeof t[r] == "function", c2 = t[r] !== void 0;
    if (i && c2 && !o2 || o2 && e[r] !== void 0 && !i) return true;
  }
  const n = In(t) ?? {}, s2 = In(e) ?? {};
  for (const r of Object.keys(s2))
    if (r in n && JSON.stringify(s2[r]) !== JSON.stringify(n[r]))
      return true;
  return false;
}
function Om(t) {
  return In(t) === void 0 && !ni(t);
}
function si(t) {
  if (D) {
    $m(D, t) && console.warn(
      "[playhtml] Ignoring conflicting config passed after playhtml was already configured. Config is locked to the first declaration. Declare it once up front with playhtml.configure(...) (or matching options at every call site)."
    );
    return;
  }
  if (!Om(t)) {
    if (to) {
      console.warn(
        "[playhtml] Ignoring config passed after playhtml already connected. Declare it before init() — e.g. with playhtml.configure(...) in a script that runs before any component mounts."
      );
      return;
    }
    if (D = {
      ...t,
      ...t.defaultRoomOptions ? { defaultRoomOptions: { ...t.defaultRoomOptions } } : {},
      ...t.cursors ? { cursors: { ...t.cursors } } : {}
    }, t.extraCapabilities)
      for (const [e, n] of Object.entries(t.extraCapabilities))
        Hn[e] = n;
    if (t.events)
      for (const [e, n] of Object.entries(t.events))
        Wu(e, n);
  }
}
function Pm() {
  to = true;
}
function no(t) {
  const { room: e, partykitHost: n, onError: s2, onMessage: r } = t, i = Uy(), o2 = Fy();
  o2.forEach((u) => {
    const h = `${u.domain}${u.path}#${u.elementId}`;
    ei.add(h);
  });
  const c2 = `playhtml_resetEpoch_${e}`, a2 = localStorage.getItem(c2), l2 = a2 ? parseInt(a2, 10) : null;
  return H = new $i(n, e, mt, {
    params: {
      sharedElements: JSON.stringify(i),
      sharedReferences: JSON.stringify(o2),
      clientResetEpoch: l2 !== null ? String(l2) : null
    }
  }), H.on("error", () => {
    s2 == null ? void 0 : s2();
  }), H.on("sync", Um), H.on("custom-message", r), { sharedReferences: o2 };
}
function so() {
  var _a4, _b, _c2;
  st.disconnect();
  try {
    (_a4 = se == null ? void 0 : se.destroy) == null ? void 0 : _a4.call(se);
  } catch {
  }
  se = null, on !== null && (zn(on), on = null);
  try {
    (_b = Lt == null ? void 0 : Lt.disconnect) == null ? void 0 : _b.call(Lt);
  } catch {
  }
  try {
    (_c2 = Lt == null ? void 0 : Lt.destroy) == null ? void 0 : _c2.call(Lt);
  } catch {
  }
  Lt = null;
}
function ro() {
  var _a4, _b;
  try {
    (_a4 = H == null ? void 0 : H.disconnect) == null ? void 0 : _a4.call(H);
  } catch {
  }
  try {
    (_b = H == null ? void 0 : H.destroy) == null ? void 0 : _b.call(H);
  } catch {
  }
}
function Iu() {
  Zi.clear();
  const t = mt;
  ne = Vl({ play: {} }), mt = Bl(ne), Su = fu(ne.play), Be.clear(), Ge.clear();
  try {
    t.destroy();
  } catch {
  }
}
function Mu() {
  const t = eo(X);
  if (!t) return;
  const e = X;
  cn = e;
  try {
    G = new lm({
      transport: t,
      getIdentity: Te,
      getPage: ze,
      onAwareness: ju
    });
  } catch (n) {
    cn = null, zn(e), console.error("[playhtml] Failed to build element awareness client:", n);
    return;
  }
}
function $u() {
  if (G) {
    try {
      G.destroy();
    } catch {
    }
    G = null, cn !== null && (zn(cn), cn = null);
  }
}
function Rm() {
  if (!G) return;
  const t = [];
  for (const [e, n] of M)
    for (const [s2, r] of n)
      r.selfAwareness !== void 0 && t.push([e, s2, r.selfAwareness]);
  t.length !== 0 && G.setLocalAwarenessBatch(t);
}
function Ou() {
  const t = eo(X);
  if (t) {
    const e = X;
    ln = e;
    try {
      return an = new vu({
        transport: t,
        getIdentity: Te,
        getPage: ze,
        // Route the cursor channel through the stable hub, not the current cursor
        // client instance, so the subscription survives cursor rebuilds (nav /
        // server reset) and the null-cursor window.
        getCursorPresences: () => st.getPresences(),
        onCursorPresencesChange: (n) => st.subscribe(n)
      }), an;
    } catch (n) {
      ln = null, zn(e), console.error("[playhtml] Failed to build presence client:", n);
    }
  }
  return cu({
    getAwareness: () => ((se == null ? void 0 : se.getProvider()) ?? H).awareness,
    getPlayerIdentity: Te,
    publishIdentity: false,
    getCursorPresences: () => st.getPresences(),
    onCursorPresencesChange: (e) => st.subscribe(e)
  });
}
function Pu() {
  if (an) {
    try {
      an.destroy();
    } catch {
    }
    an = null, ln !== null && (zn(ln), ln = null);
  }
}
function io() {
  if (G) return;
  if (it && rt)
    try {
      it.awareness.off("change", rt);
    } catch {
    }
  it = null, rt = null;
  const t = Qs();
  if (!t) return;
  const e = () => ho();
  t.awareness.on("change", e), it = t, rt = e;
}
function oo(t) {
  const { cursors: e, mainRoom: n, partykitHost: s2, onError: r } = t;
  if (!e.enabled) {
    Qe = "";
    return;
  }
  if (!Q)
    throw new Error("[playhtml] buildCursors requires the users module to exist first.");
  const i = { ...e };
  let o2 = H;
  if (i.room) {
    const a2 = _u(i.room), l2 = An(window.location.host, a2);
    if (l2 !== n) {
      const u = new me();
      Lt = new $i(
        s2,
        l2,
        u
      ), Lt.on("error", () => {
        r == null ? void 0 : r();
      }), o2 = Lt, Qe = l2;
    } else
      Qe = n;
  } else
    Qe = n;
  const c2 = eo(Qe) ?? void 0;
  on = c2 ? Qe : null, se = new Py(
    o2,
    i,
    c2,
    Q
  ), st.connect(se);
}
function Nm(t, e) {
  const n = `playhtml_resetEpoch_${t}`;
  localStorage.setItem(n, String(e)), console.log(
    `[PLAYHTML] Stored resetEpoch=${e} in localStorage key=${n}`
  );
}
function co(t) {
  return new Promise((e, n) => {
    if (q) {
      e();
      return;
    }
    let s2 = false, r = null;
    const i = (o2) => {
      s2 || (s2 = true, r !== null && clearTimeout(r), Ln.delete(i), o2 ? n(o2) : e());
    };
    Ln.add(i), t !== void 0 && (r = setTimeout(() => {
      i(new Error("Timed out waiting for playhtml room reset sync"));
    }, t));
  });
}
function Um(t) {
  if (t || console.error("Issue connecting to yjs..."), q) return;
  q = true;
  const e = [...Ln];
  Ln.clear(), e.forEach((n) => n());
}
function Fm(t) {
  if (Nm(X, t), fs) {
    je = Math.max(je ?? 0, t);
    return;
  }
  fs = jm(t).catch((e) => {
    console.error("[PLAYHTML] Failed to reconnect after room-reset:", e), window.location.reload();
  }).finally(() => {
    fs = null, je = null;
  });
}
async function jm(t) {
  let e = t;
  for (; ; ) {
    const n = e;
    if (je = null, await Hm(), je === null || je <= n)
      return;
    e = je;
  }
}
async function Hm() {
  var _a4, _b;
  if (!X || !fe)
    throw new Error("playhtml cannot reset before init()");
  ro(), so(), q = false, Dn = null, Tn.clear(), Iu(), no({
    room: X,
    partykitHost: fe,
    onError: D == null ? void 0 : D.onError,
    onMessage: Qi
  });
  const t = D == null ? void 0 : D.cursors;
  (t == null ? void 0 : t.enabled) && oo({
    cursors: t,
    mainRoom: X,
    partykitHost: fe,
    onError: D == null ? void 0 : D.onError
  }), Q == null ? void 0 : Q.getAll(), io(), ao(), await co(Mm), du(fo()), er(), lo(), (_a4 = se == null ? void 0 : se.refreshContainer) == null ? void 0 : _a4.call(se), (_b = se == null ? void 0 : se.refreshCursorStyles) == null ? void 0 : _b.call(se), Ml(X);
}
function zm() {
  It || (It = (t) => {
    var _a4;
    const e = Sl((_a4 = t.detail) == null ? void 0 : _a4.playerIdentity);
    if (!(e == null ? void 0 : e.playerStyle.colorPalette[0]) || !Q) return;
    const n = Te(), s2 = {
      ...e,
      ...typeof n.name == "string" ? { name: n.name } : {}
    };
    Q.adoptIdentity(s2), console.log("[playhtml] Merged extension identity via CustomEvent");
  }, document.addEventListener(
    "playhtml:configure-identity",
    It
  ), document.dispatchEvent(new CustomEvent("playhtml:ready")));
}
async function Bm() {
  var _a4, _b, _c2;
  if (xn) return;
  const t = Lu() ?? Eu(
    (D == null ? void 0 : D.defaultRoomOptions) ?? { includeSearch: false }
  ), e = An(window.location.host, t), n = e !== X, s2 = D == null ? void 0 : D.cursors, r = !!(s2 == null ? void 0 : s2.enabled), o2 = r !== (se !== null);
  let c2 = false;
  if (s2 == null ? void 0 : s2.enabled)
    if (s2.room) {
      const a2 = _u(s2.room);
      c2 = An(window.location.host, a2) !== Qe;
    } else
      c2 = n;
  for (const [a2, l2] of M)
    for (const [u, h] of [...l2.entries()]) {
      const d = h.element;
      (!d || !d.isConnected) && ((_a4 = h.destroy) == null ? void 0 : _a4.call(h), l2.delete(u), G == null ? void 0 : G.removeLocalAwareness(a2, u));
    }
  n && (ro(), $u(), Pu(), q = false, Dn = null, Tn.clear(), Iu(), no({
    room: e,
    partykitHost: fe,
    onError: D == null ? void 0 : D.onError,
    onMessage: Qi
  }), X = e, Mu(), Rm(), kn == null ? void 0 : kn.setInner(Ou())), (o2 || c2 && s2) && (so(), r && s2 && oo({
    cursors: s2,
    mainRoom: e,
    partykitHost: fe,
    onError: D == null ? void 0 : D.onError
  })), (n || o2 || c2) && (Q == null ? void 0 : Q.getAll()), n && io(), ao(), n && (await co(), du(fo())), er(), lo(), (_b = se == null ? void 0 : se.refreshContainer) == null ? void 0 : _b.call(se), (_c2 = se == null ? void 0 : se.refreshCursorStyles) == null ? void 0 : _c2.call(se), Ml(X);
}
function Vm(t = {}) {
  si(t);
}
function Km(t = {}) {
  if (kt)
    return si(t), tt;
  const e = window.playhtml;
  if (e) {
    if (Im(e.ready))
      return tt = e.ready, tt.then(
        () => {
          Zs = false;
        },
        () => {
        }
      ), kt = true, tt;
    const s2 = new Error(
      "playhtml is already set up by an incompatible instance. Make sure @playhtml/react and playhtml use matching versions."
    );
    return ti(s2), kt = true, tt;
  }
  kt = true, si(t);
  const n = Wm();
  return n.catch((s2) => {
    ti(s2);
  }), n;
}
async function Wm() {
  Pm();
  const t = D == null ? void 0 : D.host, e = (D == null ? void 0 : D.cursors) ?? {}, n = Lu() ?? Eu(
    (D == null ? void 0 : D.defaultRoomOptions) ?? { includeSearch: false }
  ), s2 = D == null ? void 0 : D.onError;
  window.playhtml = Kc, document.documentElement.dataset.playhtml = "true";
  const r = An(window.location.host, n), i = km(t);
  X = r, fe = i, console.log(
    `࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂
࿂࿂࿂࿂  ࿂    ࿂    ࿂    ࿂    ࿂  ࿂࿂࿂࿂
࿂࿂࿂࿂ booting up playhtml... ࿂࿂࿂࿂
࿂࿂࿂࿂  https://playhtml.fun  ࿂࿂࿂࿂
࿂࿂࿂࿂   ࿂     ࿂     ࿂     ࿂   ࿂࿂࿂࿂
࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂࿂`
  );
  const { sharedReferences: o2 } = no({
    room: r,
    partykitHost: i,
    onError: s2,
    onMessage: Qi
  }), c2 = e.playerIdentity ?? (D == null ? void 0 : D.playerIdentity) ?? Te();
  Q = Jl(c2, {
    getAwareness: () => H.awareness,
    getCursorPresences: () => (se == null ? void 0 : se.getCursorPresences()) ?? /* @__PURE__ */ new Map(),
    onCursorPresencesChange: (l2) => se == null ? void 0 : se.onCursorPresencesChange(l2)
  }), oo({
    cursors: e,
    mainRoom: r,
    partykitHost: i,
    onError: s2
  }), Q.getAll(), Mu(), zm(), kn = new mm(Ou());
  const a2 = document.createElement("link");
  if (a2.rel = "stylesheet", a2.href = "https://unpkg.com/playhtml@latest/dist/style.css", document.head.appendChild(a2), (D == null ? void 0 : D.developmentMode) && (Qr = await import("./development-DUJpN9V9-K2TCFIE3.js"), Qr.setupDevUI(Kc, M)), ao(), await co(), console.log("[PLAYHTML]: Setting up elements... Time to have some fun 🛝"), er(), lo(), Zs = false, Du(), o2.length > 0)
    try {
      const l2 = o2.map((u) => u.elementId);
      H.sendMessage(
        JSON.stringify({ type: "export-permissions", elementIds: l2 })
      );
    } catch (l2) {
      console.error("[PLAYHTML] Error during post-sync setup:", l2);
    }
  return H;
}
function Ym(t, e) {
  var _a4;
  return G ? G.getLocalAwareness(t, e) : (((_a4 = Qs().awareness.getLocalState()) == null ? void 0 : _a4[t]) ?? {})[e];
}
function Qs() {
  return H;
}
function Ae(t) {
  return t instanceof HTMLElement;
}
function Ru(t) {
  return t.hasAttribute("can-play") ? "none" : "animate";
}
function Nu(t) {
  if ((t.getAttribute("loading-behavior") || Ru(t)) === "none") return;
  t.classList.add("playhtml-loading");
  const n = t.getAttribute("loading-class");
  n && t.classList.add(n), t.setAttribute("aria-busy", "true"), t.setAttribute("aria-live", "polite");
}
function Uu(t) {
  if ((t.getAttribute("loading-behavior") || Ru(t)) === "none") return;
  t.classList.remove("playhtml-loading");
  const n = t.getAttribute("loading-class");
  n && t.classList.remove(n), t.removeAttribute("aria-busy"), t.removeAttribute("aria-live");
}
function ao() {
  for (const t of wt())
    Array.from(
      document.querySelectorAll(`[${t}]`)
    ).filter(Ae).forEach((n) => {
      Nu(n);
    });
}
function lo() {
  for (const t of wt())
    Array.from(
      document.querySelectorAll(`[${t}]`)
    ).filter(Ae).forEach((n) => {
      Uu(n);
    });
}
function Gm(t, e, n) {
  if (typeof n == "function") {
    const s2 = n(e);
    (D == null ? void 0 : D.developmentMode) && s2 !== void 0 && typeof s2 == "object" && console.warn(
      `[playhtml] A setData() mutator for "${t}" returned an object. Mutators must mutate the draft in place (e.g. \`d => { d.count++ }\`); the return value is ignored. To replace the whole snapshot, pass a value instead of a function.`
    );
    return;
  }
  xs(e, n);
}
function qm(t) {
  const e = Fn(t);
  return !jy(t, e);
}
function Jm(t, e, n, s2) {
  const r = n.defaultData instanceof Function ? n.defaultData(t) : n.defaultData, i = n.defaultData === void 0 ? void 0 : xu(e, s2, r), o2 = Ym(e, s2);
  return {
    ...n,
    myDefaultAwareness: o2 !== void 0 ? o2 : n.myDefaultAwareness,
    devMode: (D == null ? void 0 : D.developmentMode) ?? false,
    // Always provide a plain snapshot to render paths
    data: We(i),
    awareness: o2 !== void 0 ? [o2] : n.myDefaultAwareness !== void 0 ? [n.myDefaultAwareness] : void 0,
    element: t,
    onChange: (a2) => {
      if (i === void 0) {
        console.error(
          `[playhtml] setData() was called for "${s2}", but its initializer does not define \`defaultData\`.`
        );
        return;
      }
      qm(t) && mt.transact(() => {
        Gm(s2, i, a2);
      });
    },
    onAwarenessChange: (a2) => {
      var _a4;
      if (G) {
        G.setLocalAwareness(
          e,
          s2,
          a2
        );
        return;
      }
      const l2 = Qs(), u = ((_a4 = l2.awareness.getLocalState()) == null ? void 0 : _a4[e]) || {};
      if (u[s2] === a2)
        return;
      const h = { ...u, [s2]: a2 };
      l2.awareness.setLocalStateField(e, h);
    },
    triggerAwarenessUpdate: () => {
      G || ho();
    }
  };
}
function Xm(t) {
  return uo(t).length === 0;
}
function uo(t) {
  if (t == null)
    return ["initializer"];
  const e = [], n = t.defaultData !== void 0, s2 = n && t.defaultData !== null && (typeof t.defaultData == "object" || typeof t.defaultData == "function"), r = typeof t.updateElement == "function", i = typeof t.view == "function", o2 = r || i, c2 = t.myDefaultAwareness !== void 0, a2 = typeof t.updateElementAwareness == "function", l2 = o2 || a2;
  return n && !s2 && e.push("defaultData must be an object or function"), n && !o2 ? e.push("defaultData requires updateElement or view") : !n && o2 && e.push("updateElement or view requires defaultData"), c2 && !a2 && e.push("myDefaultAwareness requires updateElementAwareness"), e.length === 0 && !l2 && e.push("updateElement, view, or updateElementAwareness"), e;
}
function Bc(t) {
  const e = t, n = {}, s2 = [
    "defaultData",
    "defaultLocalData",
    "myDefaultAwareness",
    "updateElement",
    "view",
    "updateElementAwareness",
    "onDrag",
    "onDragStart",
    "onClick",
    "onMount",
    "resetShortcut",
    "debounceMs",
    "isValidElementForTag"
  ];
  for (const r of s2)
    e[r] !== void 0 && (n[r] = e[r]);
  return n;
}
function Fu(t, e) {
  return t === Z.CanPlay || !e.hasAttribute(Z.CanPlay);
}
function Zm(t, e) {
  if (t === Z.CanPlay)
    return Bc(e);
  const n = Hn[t];
  if (!n) return;
  if (!Fu(t, e))
    return n;
  const s2 = Bc(e);
  return { ...n, ...s2 };
}
function ho() {
  const e = Qs().awareness.getStates(), n = cy(
    e
  );
  if (n === Dn)
    return;
  Dn = n;
  const s2 = /* @__PURE__ */ new Map();
  e.forEach((r, i) => {
    const o2 = yt(
      r,
      i
    );
    Object.keys(r).forEach((c2) => {
      if (c2.startsWith("__")) return;
      const a2 = r[c2];
      !a2 || typeof a2 != "object" || Object.keys(a2).forEach((l2) => {
        const u = a2[l2], h = `${c2}:${l2}`;
        s2.has(h) || s2.set(h, { array: [], byStableId: /* @__PURE__ */ new Map() });
        const d = s2.get(h);
        d.array.push(u), d.byStableId.set(o2, u);
      });
    });
  }), ju(s2);
}
function ju(t) {
  t.forEach(({ array: e, byStableId: n }, s2) => {
    ve(
      () => Vc(s2, e, n),
      "element awareness handler"
    );
  });
  for (const e of Tn)
    t.has(e) || ve(
      () => Vc(e, [], /* @__PURE__ */ new Map()),
      "element awareness handler"
    );
  Tn = new Set(t.keys());
}
function Vc(t, e, n) {
  const s2 = t.indexOf(":"), r = t.slice(0, s2), i = t.slice(s2 + 1), o2 = M.get(r);
  if (!o2) return;
  const c2 = o2.get(i);
  c2 && c2.updateAwareness(e, n);
}
function er() {
  if (q) {
    for (const [t, e] of bt) {
      const n = document.getElementById(t);
      n && Ae(n) && tr(n, e);
    }
    for (const t of wt()) {
      const e = Array.from(
        document.querySelectorAll(`[${t}]`)
      ).filter(Ae);
      e.length && Promise.all(
        e.map((n) => Kt(n, t))
      );
    }
    xn && (G ? G.refresh() : (io(), ho()), ut = Eg(async () => {
      await Bm();
    }), ds = _g(ut), xn = false);
  }
}
function fo() {
  return {
    ensureProxy: xu,
    getProxy: (t, e) => {
      var _a4;
      return (_a4 = Be.get(t)) == null ? void 0 : _a4.get(e);
    },
    // Getters so a handle held across a room change (which recreates store/doc)
    // reads the current ones, not stale references captured at creation.
    getDoc: () => mt,
    getStorePlay: () => ne.play,
    proxyByTagAndId: Be,
    yObserverByKey: Ge,
    channelRefCounts: Au,
    channelListeners: ku
  };
}
function Qm(t, e) {
  if (!q)
    throw new Error("playhtml.createPageData is not available before init()");
  return Hy(t, e, fo());
}
function e0(t) {
  if (!q)
    throw new Error("playhtml.createPresenceRoom is not available before init()");
  const e = An(window.location.host, t), n = wu() ? new mu({ host: fe, room: e }) : null;
  if (n) {
    const c2 = (Q == null ? void 0 : Q.onSelfChange(() => {
      try {
        n.join({ identity: Te(), page: ze() });
      } catch (u) {
        console.warn(
          "[playhtml] Failed to republish identity on change:",
          u
        );
      }
    })) ?? null, a2 = new vu({
      transport: n,
      getIdentity: Te,
      getPage: ze
    });
    let l2 = false;
    return {
      presence: a2,
      destroy: () => {
        if (!l2) {
          l2 = true;
          try {
            c2 == null ? void 0 : c2();
          } catch {
          }
          try {
            a2.destroy();
          } catch {
          }
          try {
            n.destroy();
          } catch {
          }
        }
      }
    };
  }
  const s2 = new me(), r = new $i(fe, e, s2), i = cu({
    getAwareness: () => r.awareness,
    getPlayerIdentity: Te,
    publishIdentity: true
  });
  let o2 = false;
  return {
    presence: i,
    destroy: () => {
      o2 || (o2 = true, r.destroy(), s2.destroy());
    }
  };
}
async function b0() {
  var _a4, _b;
  if (!(xn && !kt)) {
    if (Zi.clear(), ut && (ut.destroy(), ut = null), ds && (ds(), ds = null), It && (document.removeEventListener(
      "playhtml:configure-identity",
      It
    ), It = null), it && rt)
      try {
        it.awareness.off("change", rt);
      } catch {
      }
    it = null, rt = null;
    for (const [, t] of M) {
      for (const e of t.values())
        try {
          (_a4 = e.destroy) == null ? void 0 : _a4.call(e);
        } catch {
        }
      t.clear();
    }
    M.clear(), Au.clear(), ku.clear(), Ln.clear(), $u(), Pu(), so(), ro();
    try {
      Q == null ? void 0 : Q.destroy();
    } catch {
    }
    Q = null;
    for (const [, t] of jt) {
      try {
        (_b = t.selfChangeUnsub) == null ? void 0 : _b.call(t);
      } catch {
      }
      try {
        t.transport.destroy();
      } catch {
      }
    }
    jt.clear(), on = null;
    try {
      Qr == null ? void 0 : Qr.teardownDevUI();
    } catch {
    }
    document.head.querySelectorAll("link[href*='playhtml']").forEach((t) => t.remove()), document.querySelectorAll("#playhtml-cursor-styles").forEach((t) => t.remove()), delete window.playhtml, delete document.documentElement.dataset.playhtml, q = false, Dn = null, Tn.clear(), xn = true, Zs = true, kt = false, tt = Tu(), X = "", fe = "", kn = null, st.subscribers.clear(), fs = null, je = null, D = null, to = false;
  }
}
var Kc = {
  init: Km,
  configure: Vm,
  get isLoading() {
    return Zs;
  },
  get ready() {
    return tt;
  },
  handleNavigation: async function() {
    ut && await ut.trigger();
  },
  setupPlayElements: er,
  setupPlayElement: zu,
  removePlayElement: Bn,
  deleteElementData: l0,
  setupPlayElementForTag: Kt,
  register: o0,
  define: c0,
  getHandle: Ku,
  get syncedStore() {
    return Su;
  },
  elementHandlers: M,
  dispatchPlayEvent: u0,
  registerPlayEventListener: Wu,
  removePlayEventListener: h0,
  get cursorClient() {
    return se;
  },
  get presence() {
    if (!kn)
      throw new Error("playhtml.presence is not available before init()");
    return kn;
  },
  get users() {
    if (!Q)
      throw new Error("playhtml.users is not available before init()");
    return Q;
  },
  // Filled after init
  get roomId() {
    return X;
  },
  get host() {
    return fe;
  },
  createPageData: Qm,
  createPresenceRoom: e0,
  listSharedElements: Sg
};
function t0(t) {
  var _a4;
  if (t === J)
    throw new Error(`"${J}" is a reserved tag name for page-level data`);
  M.has(t) || q && (M.has(t) || M.set(t, /* @__PURE__ */ new Map()), (_a4 = ne.play)[t] ?? (_a4[t] = {}));
}
function n0(t, e) {
  var _a4, _b;
  const n = Fu(e, t) ? t.isValidElementForTag : void 0;
  return typeof n == "function" ? n(t) : ((_b = (_a4 = Hn[e]) == null ? void 0 : _a4.isValidElementForTag) == null ? void 0 : _b.call(_a4, t)) ?? true;
}
function Wc(t) {
  const e = t.tagName.toLowerCase(), n = t.id ? `#${t.id}` : "", s2 = Array.from(t.classList).map((r) => `.${r}`).join("");
  return `<${e}${n}${s2}>`;
}
function s0(t, e, n, s2) {
  console.error(
    `[playhtml] Duplicate element id "${e}" for ${t}. Element IDs must be unique per capability tag because playhtml stores shared data by tag and ID. Keeping ${Wc(n)} and ignoring ${Wc(s2)}.`,
    { existingElement: n, duplicateElement: s2 }
  );
}
function r0(t, e, n) {
  var _a4;
  const s2 = (_a4 = M.get(t)) == null ? void 0 : _a4.get(e);
  !s2 || s2.element === n || s2.element.isConnected || Bn(s2.element);
}
async function Kt(t, e) {
  var _a4;
  if (!n0(t, e) || !q)
    return;
  if (!t.id) {
    const c2 = t.getAttribute("selector-id");
    if (c2) {
      const a2 = zc.get(c2) ?? 0;
      t.id = btoa(`${e}-${c2}-${a2}`), zc.set(c2, a2 + 1);
    } else
      t.id = await oy(e, t);
  }
  const n = Fn(t);
  if (!n) {
    console.error(
      `Element ${t} does not have an acceptable ID. Please add an ID to the element to register it as a playhtml element.`
    );
    return;
  }
  t0(e);
  const s2 = M.get(e);
  r0(e, n, t);
  const r = Zm(
    e,
    t
  );
  if (!Xm(r)) {
    const c2 = uo(r);
    console.error(
      `Element ${n} does not have proper info to initialize a playhtml element. Missing or invalid initializer properties: ${c2.join(", ")}. Please refer to https://github.com/spencerc99/playhtml#can-play for troubleshooting help.`
    );
    return;
  }
  const i = Jm(
    t,
    e,
    r,
    n
  ), o2 = s2.get(n);
  if (o2) {
    if (o2.element !== t) {
      s0(
        e,
        n,
        o2.element,
        t
      );
      return;
    }
    o2.reinitializeElementData(i), Hu(e, n, o2), Yc(e, n);
    return;
  } else {
    const c2 = new iy(
      i,
      e === Z.CanMirror ? { scheduleSetupDataWrite: (a2) => Zi.queue(a2) } : void 0
    );
    s2.set(n, c2), r.view && (c2.onAfterRender = a0, c2.observeDescendants()), e === Z.CanMirror && Bu(t);
  }
  (_a4 = i.triggerAwarenessUpdate) == null ? void 0 : _a4.call(i), t.classList.add("__playhtml-element"), t.style.setProperty("--jiggle-delay", `${Math.random() * 1}s;}`), Yc(e, n);
}
function Hu(t, e, n) {
  var _a4;
  const s2 = (_a4 = ne.play[t]) == null ? void 0 : _a4[e];
  if (s2 === void 0) return false;
  const r = `${t}:${e}`;
  Hc.add(r);
  try {
    n.__data = We(s2), t === Z.CanMirror && Bu(n.element);
  } finally {
    Hc.delete(r);
  }
  return true;
}
function Yc(t, e) {
  var _a4;
  const n = `${t}:${e}`, s2 = M.get(t);
  if (!s2) return;
  const r = s2.get(e);
  if (!r) return;
  const i = ye((_a4 = ne.play[t]) == null ? void 0 : _a4[e]);
  if (!i || typeof i.observeDeep != "function") return;
  const o2 = Ge.get(n);
  o2 && i.unobserveDeep(o2);
  let c2 = false;
  const a2 = () => {
    c2 || (c2 = true, queueMicrotask(() => {
      c2 = false, Hu(t, e, r) && Is.add(n);
    }));
  };
  if (i.observeDeep(a2), Ge.set(n, a2), D == null ? void 0 : D.developmentMode) {
    const l2 = r.element;
    if (l2 && l2.hasAttribute && l2.hasAttribute("data-source") && !lt.has(n)) {
      const u = window.setTimeout(() => {
        Is.has(n) || console.warn(
          `[playhtml] Shared reference ${t}:${e} has not received data. Check data-source and source availability.`
        ), lt.delete(n);
      }, 3e3);
      lt.set(n, u);
    }
  }
}
function zu(t, { ignoreIfAlreadySetup: e } = {}) {
  var _a4, _b;
  if (((_a4 = t.hasAttribute) == null ? void 0 : _a4.call(t, "data-source")) && ((_b = t.hasAttribute) == null ? void 0 : _b.call(t, "shared"))) {
    const s2 = t.id || "<no-id>";
    console.error(
      `[playhtml] Element ${s2} has both 'data-source' and 'shared'. Ignoring. A single element cannot be both a consumer and a source.`
    );
    return;
  }
  if (e && Array.from(M.values()).some(
    (s2) => s2.has(t.id)
  ))
    return;
  if (!Ae(t)) {
    console.log(`Element ${t.id} not an HTML element. Ignoring.`);
    return;
  }
  t.id && bt.has(t.id) && tr(t, bt.get(t.id)), t.hasAttribute("data-source") && xm(t), t.hasAttribute("shared") && Dm(t), wt().some(
    (s2) => t.hasAttribute(s2)
  ) && (q ? Uu(t) : Nu(t)), Promise.all(
    wt().filter((s2) => t.hasAttribute(s2)).map((s2) => Kt(t, s2))
  );
}
function Bu(t) {
  var _a4;
  const e = /* @__PURE__ */ new Set(), n = /* @__PURE__ */ new Map();
  for (const r of wt())
    t.querySelectorAll(`[${r}]`).forEach((i) => {
      if (Ae(i)) {
        e.add(i);
        const o2 = Fn(i);
        o2 && n.set(`${r}:${o2}`, i);
      }
    });
  (_a4 = jc.get(t)) == null ? void 0 : _a4.forEach((r, i) => {
    n.get(i) !== r && Bn(r);
  }), e.forEach((r) => {
    zu(r);
  }), jc.set(t, n);
}
function Bn(t) {
  var _a4, _b;
  if (!t || !t.id)
    return;
  const e = Fn(t);
  if (e)
    for (const [n, s2] of M) {
      const r = s2.get(e);
      if (!r || r.element !== t)
        continue;
      const i = `${n}:${e}`, o2 = ye((_a4 = ne.play[n]) == null ? void 0 : _a4[e]), c2 = Ge.get(i);
      o2 && c2 && typeof o2.unobserveDeep == "function" && o2.unobserveDeep(c2), Ge.delete(i), Is.delete(i);
      const a2 = lt.get(i);
      a2 !== void 0 && (clearTimeout(a2), lt.delete(i)), (_b = r.destroy) == null ? void 0 : _b.call(r), G == null ? void 0 : G.removeLocalAwareness(n, e), s2.delete(e);
    }
}
var bt = /* @__PURE__ */ new Map();
function Vu(t, e) {
  if (e.view && e.updateElement)
    throw new Error(
      `[playhtml] "${t}" defines both \`view\` and \`updateElement\`. They are mutually exclusive — pick one.`
    );
  if (e.view && (e.onClick || e.onDrag || e.onDragStart))
    throw new Error(
      `[playhtml] "${t}" defines \`view\` alongside an element event handler (onClick/onDrag/onDragStart). In view mode, attach events inside the template (e.g. \`@click=\${...}\`) instead.`
    );
  if (e.defaultData !== void 0 && typeof e.defaultData != "function" && (typeof e.defaultData != "object" || e.defaultData === null))
    throw new Error(
      `[playhtml] "${t}" has a non-object \`defaultData\`. Use an object (e.g. \`{ count: 0 }\`) so the shape can grow without a data migration.`
    );
  const n = uo(e);
  if (n.length > 0)
    throw new Error(
      `[playhtml] "${t}" has an invalid initializer: ${n.join(", ")}.`
    );
}
function tr(t, e) {
  Object.assign(t, e), t.hasAttribute(Z.CanPlay) || t.setAttribute(Z.CanPlay, "");
}
function i0(t) {
  if (!q) return;
  const e = bt.get(t);
  if (!e) return;
  const n = document.getElementById(t);
  !n || !Ae(n) || (tr(n, e), Kt(n, Z.CanPlay));
}
function Zt(t, e) {
  var _a4, _b;
  if (e !== void 0)
    return (_a4 = M.get(e)) == null ? void 0 : _a4.get(t);
  const n = (_b = M.get(Z.CanPlay)) == null ? void 0 : _b.get(t);
  if (n) return n;
  for (const [, s2] of M) {
    const r = s2.get(t);
    if (r) return r;
  }
}
function is(t, e) {
  (D == null ? void 0 : D.developmentMode) && console.warn(
    `[playhtml] ${t}("${e}") — no bound element with that id yet; the write was dropped. Register/add the element first, or check the id.`
  );
}
function Ku(t, e) {
  return {
    id: t,
    getElement: () => document.getElementById(t),
    getData: () => {
      var _a4;
      return (_a4 = Zt(t, e)) == null ? void 0 : _a4.data;
    },
    setData: (n) => {
      const s2 = Zt(t, e);
      if (!s2) return is("setData", t);
      s2.setData(n);
    },
    setLocalData: (n) => {
      const s2 = Zt(t, e);
      if (!s2) return is("setLocalData", t);
      s2.setLocalData(n);
    },
    setMyAwareness: (n) => {
      const s2 = Zt(t, e);
      if (!s2) return is("setMyAwareness", t);
      s2.setMyAwareness(n);
    },
    requestUpdate: () => {
      const n = Zt(t, e);
      if (!n) return is("requestUpdate", t);
      n.requestUpdate();
    },
    unregister: () => {
      bt.delete(t);
      const n = document.getElementById(t);
      n && Bn(n);
    }
  };
}
function o0(t, e) {
  return Vu(t, e), bt.set(t, e), i0(t), (D == null ? void 0 : D.developmentMode) && q && !document.getElementById(t) && console.warn(
    `[playhtml] register("${t}") — no element with that id is in the DOM yet. It will bind automatically when the element appears.`
  ), Ku(
    t,
    Z.CanPlay
  );
}
function c0(t, e) {
  if (t === J)
    throw new Error(`"${J}" is a reserved tag name for page-level data`);
  if (t === Z.CanPlay)
    throw new Error(
      `[playhtml] "${Z.CanPlay}" is reserved — use register(id, init) for single elements.`
    );
  if (Object.prototype.hasOwnProperty.call(Il, t))
    throw new Error(
      `[playhtml] "${t}" is a built-in capability and cannot be redefined.`
    );
  if (Vu(t, e), Hn[t] = e, q) {
    const n = Array.from(
      document.querySelectorAll(`[${t}]`)
    ).filter(Ae);
    Promise.all(
      n.map((s2) => Kt(s2, t))
    );
  }
}
var Gc = /* @__PURE__ */ new WeakMap();
function a0(t) {
  var _a4;
  for (const [s2, r] of bt) {
    if (s2 === t.id) continue;
    const i = document.getElementById(s2);
    i && t.contains(i) && Ae(i) && tr(i, r);
  }
  const e = /* @__PURE__ */ new Map();
  for (const s2 of wt()) {
    const r = Array.from(t.querySelectorAll(`[${s2}]`)).filter(
      Ae
    );
    for (const i of r) {
      if (i === t) continue;
      if (!i.id) {
        (D == null ? void 0 : D.developmentMode) && console.warn(
          `[playhtml] a view rendered a "${s2}" element with no id; it won't bind. Give capability children a stable, unique id (key keyed lists by it).`
        );
        continue;
      }
      e.set(`${s2}:${i.id}`, i);
      const o2 = (_a4 = M.get(s2)) == null ? void 0 : _a4.get(i.id);
      o2 && o2.element === i || Kt(i, s2);
    }
  }
  const n = Gc.get(t);
  if (n)
    for (const [s2, r] of n)
      e.has(s2) || Bn(r);
  Gc.set(t, e);
}
function l0(t, e) {
  var _a4;
  if (!q) {
    console.warn(
      `[PLAYHTML] Cannot remove element data before sync: ${t}:${e}`
    );
    return;
  }
  const n = `${t}:${e}`, s2 = ye((_a4 = ne.play[t]) == null ? void 0 : _a4[e]);
  if (s2 && typeof s2.observeDeep == "function") {
    const a2 = Ge.get(n);
    if (a2) {
      try {
        s2.unobserveDeep(a2);
      } catch (l2) {
        console.warn(`[PLAYHTML] Failed to remove observer for ${n}:`, l2);
      }
      Ge.delete(n);
    }
  }
  const r = ne.play[t];
  if (r && e in r)
    try {
      mt.transact(() => {
        delete r[e];
      });
    } catch (a2) {
      console.warn(
        `[PLAYHTML] Failed to remove SyncedStore data for ${n}:`,
        a2
      );
    }
  const i = Be.get(t);
  i && (i.delete(e), i.size === 0 && Be.delete(t));
  const o2 = M.get(t);
  o2 && o2.delete(e), Is.delete(n);
  const c2 = lt.get(n);
  c2 !== void 0 && (clearTimeout(c2), lt.delete(n));
}
function u0(t) {
  const { type: e } = t;
  if (!Ft.has(e)) {
    console.error(`[playhtml] event "${e}" not registered.`);
    return;
  }
  Lm(t);
}
function Wu(t, e) {
  const n = String(Tm++);
  return Ft.set(t, [
    ...Ft.get(t) ?? [],
    { type: t, ...e, id: n }
  ]), n;
}
function h0(t, e) {
  const n = Ft.get(t);
  if (!n)
    return;
  const s2 = n.findIndex((r) => r.id === e);
  s2 !== -1 && (n.splice(s2, 1), n.length === 0 && Ft.delete(t));
}

export {
  o,
  l,
  c,
  y,
  yg,
  mg,
  wg,
  bg,
  Z,
  Fn,
  Il,
  Sg,
  f0,
  p0,
  F,
  y0,
  m0,
  w0,
  M,
  b0,
  Kc
};
//# sourceMappingURL=chunk-T4VLCKKZ.js.map
